---
phase: 26-execution-engine-flow-control
plan: 03
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/app/workflows/actions.ts
  - src/lib/execution/recursion.ts
  - src/lib/execution/recursion.test.ts
  - src/lib/triggers/create-run.ts
  - src/lib/triggers/matcher.ts
autonomous: true
requirements:
  - EXEC-01

must_haves:
  truths:
    - "User can toggle a workflow on/off via server action"
    - "Disabling a workflow cancels all waiting (delay-paused) runs with a clear error message"
    - "Disabled workflows do not fire even when trigger conditions are met"
    - "Recursion depth is tracked: runs created by workflow-triggered CRM events carry depth + 1"
    - "Runs at recursion depth >= 5 are immediately created with failed status and error message"
  artifacts:
    - path: "src/app/workflows/actions.ts"
      provides: "toggleWorkflow server action"
      exports: ["toggleWorkflow"]
    - path: "src/lib/execution/recursion.ts"
      provides: "AsyncLocalStorage-based execution depth tracking"
      exports: ["executionDepthStorage", "getCurrentExecutionDepth", "runWithExecutionDepth"]
    - path: "src/lib/triggers/create-run.ts"
      provides: "Updated createWorkflowRun with depth parameter and recursion check"
      exports: ["createWorkflowRun"]
  key_links:
    - from: "src/app/workflows/actions.ts"
      to: "src/db/schema/workflows.ts"
      via: "toggleWorkflow updates workflows.active and cancels waiting runs"
      pattern: "update.*workflows.*active"
    - from: "src/lib/execution/recursion.ts"
      to: "src/lib/triggers/create-run.ts"
      via: "createWorkflowRun reads current depth and rejects at >= 5"
      pattern: "getCurrentExecutionDepth"
    - from: "src/lib/triggers/matcher.ts"
      to: "src/lib/execution/recursion.ts"
      via: "matchAndFireTriggers checks workflow.active before creating runs"
      pattern: "workflow\\.active"
---

<objective>
Implement the workflow toggle server action (enable/disable with waiting-run cancellation) and recursion depth tracking to prevent infinite workflow loops.

Purpose: EXEC-01 requires users to toggle workflows on/off. The toggle must cancel delay-paused runs when disabling and block new triggers for inactive workflows. Recursion depth tracking (via AsyncLocalStorage) prevents infinite loops when workflow actions trigger CRM events that fire other workflows -- this is a safety guardrail needed from day one.

Output: Toggle server action, recursion depth module, updated create-run with depth enforcement.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-execution-engine-flow-control/26-RESEARCH.md
@.planning/phases/26-execution-engine-flow-control/26-01-SUMMARY.md

@src/app/workflows/actions.ts
@src/lib/triggers/create-run.ts
@src/lib/triggers/matcher.ts
@src/db/schema/workflows.ts

<interfaces>
<!-- Existing server action pattern -->
```typescript
// src/app/workflows/actions.ts - existing pattern
export async function updateWorkflow(id, data): Promise<{ success: true } | { success: false; error: string }>
```

<!-- Schema columns from Plan 01 -->
```typescript
// workflowRuns now has: depth (integer, default 0), context (jsonb), currentNodeId (text)
// workflows has: active (boolean)
```

<!-- Trigger matcher currently does NOT check workflow.active -->
```typescript
// src/lib/triggers/matcher.ts - matchAndFireTriggers queries:
const activeWorkflows = await db.select().from(workflows).where(eq(workflows.active, true))
// Note: already filters by active=true, so disabled workflows won't fire from CRM events
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recursion depth tracking with AsyncLocalStorage</name>
  <files>
    src/lib/execution/recursion.ts,
    src/lib/execution/recursion.test.ts,
    src/lib/triggers/create-run.ts
  </files>
  <behavior>
    - getCurrentExecutionDepth() returns 0 when not inside a workflow execution
    - getCurrentExecutionDepth() returns the depth set by runWithExecutionDepth
    - runWithExecutionDepth(3, fn) makes getCurrentExecutionDepth() return 3 inside fn
    - Nested runWithExecutionDepth calls use the inner depth
    - createWorkflowRun with depth < 5 creates the run successfully with the depth value
    - createWorkflowRun with depth >= 5 creates a run with status "failed" and error "Recursion limit reached (5 levels)"
  </behavior>
  <action>
    1. Write tests FIRST in `recursion.test.ts` covering the behaviors above. Run -- must fail (RED).

    2. Create `src/lib/execution/recursion.ts`:
       - `const MAX_RECURSION_DEPTH = 5`
       - `const executionDepthStorage = new AsyncLocalStorage<number>()` (import from `node:async_hooks`)
       - `getCurrentExecutionDepth(): number` -- returns `executionDepthStorage.getStore() ?? 0`
       - `runWithExecutionDepth<T>(depth: number, fn: () => T | Promise<T>): T | Promise<T>` -- runs fn within AsyncLocalStorage context with given depth

    3. Update `src/lib/triggers/create-run.ts`:
       - Import `getCurrentExecutionDepth` and `MAX_RECURSION_DEPTH` from recursion.ts
       - Add optional `depth` parameter to `createWorkflowRun(workflowId, triggerEnvelope, depth?)`
       - If depth is not provided, read it from `getCurrentExecutionDepth()`
       - If effective depth >= MAX_RECURSION_DEPTH, insert a run with status "failed", error "Recursion limit reached (5 levels)", and completedAt = now. Return the failed run.
       - Otherwise, insert the run with the depth value in the new `depth` column

    4. Run tests -- must pass (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/lib/execution/recursion.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - AsyncLocalStorage tracks execution depth across async boundaries
    - getCurrentExecutionDepth returns 0 outside workflow execution context
    - createWorkflowRun enforces depth limit of 5 and creates failed runs at the limit
    - Depth value persisted in workflow_runs.depth column
  </done>
</task>

<task type="auto">
  <name>Task 2: Toggle workflow server action with waiting-run cancellation</name>
  <files>src/app/workflows/actions.ts</files>
  <action>
    Add `toggleWorkflow` server action to `src/app/workflows/actions.ts`:

    1. Function signature: `toggleWorkflow(id: string, active: boolean): Promise<{ success: true; cancelledRuns: number } | { success: false; error: string }>`
    2. Auth check: `const session = await auth()` -- return error if not authenticated
    3. Verify workflow exists: query by id, return error if not found
    4. Update `workflows.active` and `updatedAt` via Drizzle update
    5. If disabling (`active === false`):
       - Cancel all waiting runs for this workflow: UPDATE workflow_runs SET status = 'failed', error = 'Workflow disabled while waiting', completed_at = NOW() WHERE workflow_id = id AND status = 'waiting'. Use Drizzle `.returning()` to count affected rows.
    6. `revalidatePath("/workflows")`
    7. Return `{ success: true, cancelledRuns }` (0 if enabling)

    Import `and`, `eq` from drizzle-orm. Import `workflowRuns` from schema. Follow the exact return shape pattern used by other actions in the file.

    Note: The matcher (matchAndFireTriggers) already filters `WHERE active = true`, so disabled workflows won't fire from CRM events -- no change needed there. Schedule processor also checks `active = true` in its WHERE clause. The toggle action just needs to update the active flag and cancel waiting runs.
  </action>
  <verify>
    <automated>npx vitest run src/lib/execution/ --reporter=verbose</automated>
  </verify>
  <done>
    - toggleWorkflow server action exported from workflows/actions.ts
    - Enabling sets active=true, returns cancelledRuns=0
    - Disabling sets active=false, cancels waiting runs with clear error message, returns count
    - Auth check prevents unauthenticated access
    - revalidatePath called for UI refresh
  </done>
</task>

</tasks>

<verification>
npx vitest run src/lib/execution/ --reporter=verbose
</verification>

<success_criteria>
- toggleWorkflow server action correctly enables/disables workflows
- Disabling cancels waiting runs with "Workflow disabled while waiting" error
- Recursion depth tracked via AsyncLocalStorage
- createWorkflowRun rejects runs at depth >= 5 with clear error
- Depth persisted in workflow_runs.depth column
- Existing trigger matching continues to filter by active=true
</success_criteria>

<output>
After completion, create `.planning/phases/26-execution-engine-flow-control/26-03-SUMMARY.md`
</output>
