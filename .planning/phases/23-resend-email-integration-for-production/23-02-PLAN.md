---
phase: 23-resend-email-integration-for-production
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/lib/email/templates/invite-user.ts
  - src/lib/email/templates/deal-assigned.ts
  - src/lib/email/templates/activity-reminder.ts
  - src/lib/email/templates/weekly-digest.ts
  - src/lib/email/templates/index.ts
  - src/lib/email/send.ts
  - src/lib/email/send.test.ts
  - src/lib/email/templates/__tests__/templates.test.ts
  - src/messages/en-US.json
  - src/messages/pt-BR.json
  - src/messages/es-ES.json
autonomous: true
requirements:
  - EMAIL-04
  - EMAIL-05
  - EMAIL-06
  - EMAIL-07

must_haves:
  truths:
    - "Invite user template renders registration link with token in recipient locale"
    - "Deal assigned template renders deal name and assigner info in recipient locale"
    - "Activity reminder template renders activity title, due time in recipient locale"
    - "Weekly digest template renders deals summary and activities due in recipient locale"
    - "All 4 new send functions use safeSend wrapper"
  artifacts:
    - path: "src/lib/email/templates/invite-user.ts"
      provides: "Invite user email template"
      exports: ["getInviteUserTemplate"]
    - path: "src/lib/email/templates/deal-assigned.ts"
      provides: "Deal assigned email template"
      exports: ["getDealAssignedTemplate"]
    - path: "src/lib/email/templates/activity-reminder.ts"
      provides: "Activity reminder email template"
      exports: ["getActivityReminderTemplate"]
    - path: "src/lib/email/templates/weekly-digest.ts"
      provides: "Weekly digest email template"
      exports: ["getWeeklyDigestTemplate"]
    - path: "src/lib/email/send.ts"
      provides: "4 new send functions"
      exports: ["sendInviteEmail", "sendDealAssignedEmail", "sendActivityReminderEmail", "sendWeeklyDigestEmail"]
  key_links:
    - from: "src/lib/email/templates/*.ts"
      to: "src/lib/email/i18n.ts"
      via: "getEmailTranslator for i18n"
      pattern: "getEmailTranslator"
    - from: "src/lib/email/send.ts"
      to: "src/lib/email/templates/index.ts"
      via: "template imports"
      pattern: "import.*templates"
---

<objective>
Create 4 new transactional email templates with full i18n support and their corresponding send functions.

Purpose: These templates are consumed by the invite flow (Plan 03), deal assignment trigger and cron processors (Plan 04).
Output: 4 template files, 4 send functions in send.ts, i18n keys in all 3 locale files, behavioral tests.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-resend-email-integration-for-production/23-CONTEXT.md
@.planning/phases/23-resend-email-integration-for-production/23-RESEARCH.md
@.planning/phases/23-resend-email-integration-for-production/23-01-SUMMARY.md

@src/lib/email/send.ts
@src/lib/email/i18n.ts
@src/lib/email/templates/index.ts
@src/lib/email/templates/verify-email.ts

<interfaces>
<!-- From Plan 01 outputs -->
From src/lib/email/i18n.ts (created in Plan 01):
```typescript
export async function getEmailTranslator(locale: string, namespace: string): Promise<ReturnType<typeof createTranslator>>
```

From src/lib/email/send.ts (updated in Plan 01):
```typescript
export async function safeSend(to: string, template: { subject: string; html: string; text: string }): Promise<void>
```

<!-- Existing template pattern (all follow this shape, now async with locale) -->
```typescript
export async function getVerifyEmailTemplate(verificationUrl: string, host: string, locale: string): Promise<{ subject: string; html: string; text: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 new email templates with i18n</name>
  <files>src/lib/email/templates/invite-user.ts, src/lib/email/templates/deal-assigned.ts, src/lib/email/templates/activity-reminder.ts, src/lib/email/templates/weekly-digest.ts, src/lib/email/templates/index.ts, src/messages/en-US.json, src/messages/pt-BR.json, src/messages/es-ES.json</files>
  <action>
Create 4 new template files following the established pattern (async, locale param, uses getEmailTranslator, returns { subject, html, text }). Use the same HTML styling as existing templates (white card on #f9f9f9 background, table-based layout, #346df1 accent color).

1. `invite-user.ts` -- `getInviteUserTemplate(inviteUrl: string, host: string, inviterName: string, locale: string)`:
   - Namespace: `emails.inviteUser`
   - Subject: "You've been invited to {host}"
   - Body: "{inviterName} has invited you to join {host}. Click the button to create your account."
   - Button: "Create Account" linking to inviteUrl
   - Note: "This invitation expires in 7 days."

2. `deal-assigned.ts` -- `getDealAssignedTemplate(dealName: string, dealUrl: string, assignerName: string, locale: string)`:
   - Namespace: `emails.dealAssigned`
   - Subject: "Deal assigned to you: {dealName}"
   - Body: "{assignerName} assigned the deal '{dealName}' to you."
   - Button: "View Deal" linking to dealUrl

3. `activity-reminder.ts` -- `getActivityReminderTemplate(activityTitle: string, activityUrl: string, dueDate: string, locale: string)`:
   - Namespace: `emails.activityReminder`
   - Subject: "Reminder: {activityTitle} due soon"
   - Body: "Your activity '{activityTitle}' is due at {dueDate}."
   - Button: "View Activity" linking to activityUrl

4. `weekly-digest.ts` -- `getWeeklyDigestTemplate(data: WeeklyDigestData, host: string, dashboardUrl: string, locale: string)`:
   - Define `WeeklyDigestData` type: `{ newDeals: number; dealsMovedStage: number; dealsWon: number; dealsLost: number; overdueActivities: number; upcomingActivities: number }`
   - Namespace: `emails.weeklyDigest`
   - Subject: "Weekly Summary - {host}"
   - Body: Two sections: "Deals This Week" (new, moved stages, won, lost as a list) and "Activities" (overdue count, upcoming this week count).
   - Button: "Go to Dashboard" linking to dashboardUrl

5. Update `src/lib/email/templates/index.ts` to re-export all 4 new templates.

6. Add i18n keys to all 3 locale files under `emails.inviteUser`, `emails.dealAssigned`, `emails.activityReminder`, `emails.weeklyDigest`. Provide proper translations for pt-BR and es-ES.

7. Update `src/lib/email/templates/__tests__/templates.test.ts`: replace the placeholder tests for the 4 NEW templates with actual tests. Each test should import the template, call it with 'en-US' and 'pt-BR', and verify: subject is non-empty, html contains expected interpolated values (e.g., deal name, invite URL), subject differs across locales.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite && npx tsc --noEmit 2>&1 | head -30 && npx vitest run src/lib/email/templates/__tests__/templates.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 4 template files exist, compile, export the correct function signatures. i18n keys present in all 3 locale files. Template tests pass for all 4 new templates.</done>
</task>

<task type="auto">
  <name>Task 2: Add send functions for all 4 new email types</name>
  <files>src/lib/email/send.ts, src/lib/email/send.test.ts</files>
  <action>
Add 4 new send functions to `src/lib/email/send.ts`, all using `safeSend`:

1. `sendInviteEmail(email: string, token: string, inviterName: string, locale?: string)`:
   - Build invite URL: `${appUrl}/register?invite=${token}`
   - Get template via `getInviteUserTemplate(inviteUrl, host, inviterName, locale || 'en-US')`
   - Call `safeSend(email, template)`

2. `sendDealAssignedEmail(email: string, dealId: string, dealName: string, assignerName: string, locale?: string)`:
   - Build deal URL: `${appUrl}/deals/${dealId}`
   - Get template via `getDealAssignedTemplate(dealName, dealUrl, assignerName, locale || 'en-US')`
   - Call `safeSend(email, template)`

3. `sendActivityReminderEmail(email: string, activityTitle: string, dueDate: Date, locale?: string)`:
   - Format dueDate to locale-appropriate string (use `dueDate.toLocaleString(locale || 'en-US')`)
   - Build activity URL: `${appUrl}/activities` (activities are listed, no individual page)
   - Get template via `getActivityReminderTemplate(activityTitle, activityUrl, formattedDueDate, locale || 'en-US')`
   - Call `safeSend(email, template)`

4. `sendWeeklyDigestEmail(email: string, data: WeeklyDigestData, locale?: string)`:
   - Import WeeklyDigestData type from template
   - Build dashboard URL: `${appUrl}/dashboard`
   - Get template via `getWeeklyDigestTemplate(data, host, dashboardUrl, locale || 'en-US')`
   - Call `safeSend(email, template)`

Import the 4 new template functions from `./templates`.

5. Update `src/lib/email/send.test.ts`: replace placeholder tests for the 4 new send functions with actual tests. Mock `safeSend` (or the transporter) and verify each function:
   - Builds the correct URL (invite URL, deal URL, etc.)
   - Calls safeSend with the correct `to` address
   - Passes locale through to the template function
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite && npx tsc --noEmit 2>&1 | head -30 && npx vitest run src/lib/email/send.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>4 new send functions exported from send.ts, all using safeSend wrapper. TypeScript compiles clean. Send function tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes clean
- `npx vitest run src/lib/email/send.test.ts src/lib/email/templates/__tests__/templates.test.ts` all pass
- All 4 template files exist and follow the established pattern
- All 4 send functions exist in send.ts
- i18n keys exist in all 3 locale files for 4 new email types
</verification>

<success_criteria>
- 4 new email templates with full i18n support (en-US, pt-BR, es-ES)
- 4 new send functions using safeSend (silent fail when no SMTP)
- All locale files have proper translations for new email types
- TypeScript compiles without errors
- All behavioral tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-resend-email-integration-for-production/23-02-SUMMARY.md`
</output>
