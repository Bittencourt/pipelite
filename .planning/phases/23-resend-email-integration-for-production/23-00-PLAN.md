---
phase: 23-resend-email-integration-for-production
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - src/lib/email/send.test.ts
  - src/lib/email/templates/__tests__/templates.test.ts
  - src/lib/email-processor.test.ts
  - src/app/settings/notifications/actions.test.ts
  - src/app/admin/users/actions.test.ts
  - src/app/deals/actions.test.ts
autonomous: true
requirements:
  - EMAIL-00

must_haves:
  truths:
    - "vitest runs successfully with path aliases matching tsconfig"
    - "Test skeleton files exist for all plan tasks with failing placeholder tests"
    - "npx vitest run completes without config errors"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with path aliases"
    - path: "src/lib/email/send.test.ts"
      provides: "Test stubs for safe send wrapper and all send functions"
    - path: "src/lib/email/templates/__tests__/templates.test.ts"
      provides: "Test stubs for all email template i18n rendering"
    - path: "src/lib/email-processor.test.ts"
      provides: "Test stubs for activity reminder and weekly digest processing"
    - path: "src/app/settings/notifications/actions.test.ts"
      provides: "Test stubs for notification preference CRUD"
    - path: "src/app/admin/users/actions.test.ts"
      provides: "Test stubs for invite user action"
    - path: "src/app/deals/actions.test.ts"
      provides: "Test stubs for deal assignment email trigger"
  key_links: []
---

<objective>
Set up vitest configuration and create test skeleton files for all phase 23 plans. Every task's automated verification depends on these test files existing.

Purpose: Nyquist compliance -- every code-producing task needs an automated behavioral test, not just `tsc --noEmit`.
Output: vitest.config.ts + 6 test skeleton files with describe/it blocks ready for implementation.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-resend-email-integration-for-production/23-CONTEXT.md
@.planning/phases/23-resend-email-integration-for-production/23-RESEARCH.md

@tsconfig.json
@src/lib/formula-engine.test.ts

<interfaces>
<!-- Existing vitest usage pattern -->
From src/lib/formula-engine.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
// Direct imports, no mocking framework needed for pure function tests
```

From tsconfig.json:
```json
{
  "paths": { "@/*": ["./src/*"] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vitest.config.ts and all test skeleton files</name>
  <files>vitest.config.ts, src/lib/email/send.test.ts, src/lib/email/templates/__tests__/templates.test.ts, src/lib/email-processor.test.ts, src/app/settings/notifications/actions.test.ts, src/app/admin/users/actions.test.ts, src/app/deals/actions.test.ts</files>
  <action>
1. Create `vitest.config.ts` at project root:
   ```typescript
   import { defineConfig } from 'vitest/config'
   import path from 'path'

   export default defineConfig({
     test: {
       globals: false,
       environment: 'node',
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   })
   ```

2. Create `src/lib/email/send.test.ts` with skeleton tests:
   - `describe('safeSend')`:
     - `it('logs warning and returns when SMTP_HOST is not configured')` -- placeholder `expect(true).toBe(false)`
     - `it('sends email via transporter when SMTP_HOST is configured')` -- placeholder
   - `describe('sendInviteEmail')`:
     - `it('builds invite URL and calls safeSend with correct template')` -- placeholder
   - `describe('sendDealAssignedEmail')`:
     - `it('builds deal URL and calls safeSend')` -- placeholder
   - `describe('sendActivityReminderEmail')`:
     - `it('formats due date and calls safeSend')` -- placeholder
   - `describe('sendWeeklyDigestEmail')`:
     - `it('calls safeSend with digest data')` -- placeholder

3. Create `src/lib/email/templates/__tests__/templates.test.ts` with skeleton tests:
   - `describe('existing templates i18n')`:
     - `it('getVerifyEmailTemplate returns subject/html/text for en-US')` -- placeholder
     - `it('getVerifyEmailTemplate returns translated content for pt-BR')` -- placeholder
     - `it('getApprovedTemplate returns i18n content')` -- placeholder
     - `it('getPasswordResetTemplate returns i18n content')` -- placeholder
   - `describe('new templates i18n')`:
     - `it('getInviteUserTemplate returns i18n content for each locale')` -- placeholder
     - `it('getDealAssignedTemplate returns i18n content')` -- placeholder
     - `it('getActivityReminderTemplate returns i18n content')` -- placeholder
     - `it('getWeeklyDigestTemplate returns i18n content with data')` -- placeholder

4. Create `src/lib/email-processor.test.ts` with skeleton tests:
   - `describe('processActivityReminders')`:
     - `it('sends reminders for activities due within 1 hour')` -- placeholder
     - `it('skips activities that already have reminderSentAt set')` -- placeholder
     - `it('skips activities where user disabled emailActivityReminder')` -- placeholder
   - `describe('processWeeklyDigest')`:
     - `it('sends digest on Monday morning for opted-in users')` -- placeholder
     - `it('skips digest on non-Monday days')` -- placeholder
     - `it('does not send duplicate digest on same Monday')` -- placeholder

5. Create `src/app/settings/notifications/actions.test.ts` with skeleton tests:
   - `describe('getNotificationPreferences')`:
     - `it('returns defaults when no preferences row exists')` -- placeholder
   - `describe('updateNotificationPreferences')`:
     - `it('upserts preferences for user')` -- placeholder

6. Create `src/app/admin/users/actions.test.ts` with skeleton tests:
   - `describe('inviteUser')`:
     - `it('creates invite record and sends email')` -- placeholder
     - `it('rejects if email already has active user')` -- placeholder
     - `it('rejects if pending invite exists')` -- placeholder

7. Create `src/app/deals/actions.test.ts` with skeleton tests:
   - `describe('deal assignment email')`:
     - `it('sends email to newly assigned users only')` -- placeholder
     - `it('respects notification preferences')` -- placeholder
     - `it('does not send email for re-saved existing assignees')` -- placeholder

All placeholder tests use `expect(true).toBe(false)` with a `// TODO: implement in plan XX-YY` comment referencing the plan that will flesh them out. This ensures they are RED until the corresponding plan implements them.
  </action>
  <verify>
    <automated>cd /home/pedro/programming/pipelite && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>vitest.config.ts exists with @/ alias. All 6 test files exist with describe/it skeleton blocks. `npx vitest run` executes without config errors (tests fail as expected -- they are RED placeholders).</done>
</task>

</tasks>

<verification>
- `npx vitest run` executes without config/resolution errors
- All test files importable (no path alias failures)
- Test count matches: at least 20 skeleton tests across all files
</verification>

<success_criteria>
- vitest.config.ts exists with path aliases matching tsconfig
- 6 test skeleton files exist with failing placeholder tests for every plan's tasks
- `npx vitest run` completes (tests fail intentionally, but no config errors)
</success_criteria>

<output>
After completion, create `.planning/phases/23-resend-email-integration-for-production/23-00-SUMMARY.md`
</output>
