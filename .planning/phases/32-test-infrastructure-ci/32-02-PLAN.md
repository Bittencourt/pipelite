---
phase: 32-test-infrastructure-ci
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/lib/formula-engine.ts
  - src/lib/mutations/workflows.test.ts
autonomous: true
requirements: [CI-03]

must_haves:
  truths:
    - "`LOGIC.isBlank({{Value}})` with `Value: null` returns `true`, because the source bug that discarded `usesNullSafe` at formula-engine.ts:164 is fixed at the source, not papered over in the test (D-04)"
    - "`{{Missing}} + 1` with `Missing: null` still returns `null` — the existing `propagates null values` regression guard survives the fix (D-04)"
    - "`deleteWorkflow > deletes existing workflow` passes because the stale test now stubs `mockDb.select`; the cascade source in workflows.ts is unchanged (D-05)"
    - "The `runs.length > 0` cascade branch is covered by a new test asserting exactly 3 delete calls — steps, then runs, then the workflow (D-06)"
    - "`npm test` exits 0 with 41 files and 454 passed / 4 skipped — the whole suite is green (CI-03)"
  artifacts:
    - path: "src/lib/formula-engine.ts"
      provides: "Null-safe carve-out actually gating the three null early-returns"
      contains: "usesNullSafe"
    - path: "src/lib/mutations/workflows.test.ts"
      provides: "Repaired deleteWorkflow test plus a cascade-branch test"
      contains: "mockDb.select.mockReturnValue"
  key_links:
    - from: "src/lib/formula-engine.ts usesNullSafeFunction(expression)"
      to: "the three `return { value: null, error: null }` early-returns in the deps loop"
      via: "boolean guard hoisted above the loop"
      pattern: "&& !usesNullSafe"
    - from: "src/lib/mutations/workflows.test.ts cascade test"
      to: "workflows.ts deleteWorkflow cascade branch"
      via: "mockDb.select returning two run rows"
      pattern: "toHaveBeenCalledTimes\\(3\\)"
---

<objective>
Make the suite green by fixing the two known failures at whichever layer is actually wrong, and close the cascade-delete coverage gap SC-3 names explicitly.

Purpose: CI-03. Both failures were reproduced and root-caused by the researcher, and both candidate fixes were **executed and proven green in a disposable worktree** — this plan applies known-good fixes, it does not investigate. One is a source bug (D-04), one is a stale test (D-05), and the cascade branch SC-3 calls out by name is currently untested even after the stale mock is repaired (D-06).
Output: a corrected `formula-engine.ts`, a repaired and extended `workflows.test.ts`, and `npm test` exiting 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-test-infrastructure-ci/32-RESEARCH.md
@.planning/phases/32-test-infrastructure-ci/32-VALIDATION.md
@.planning/phases/32-test-infrastructure-ci/32-01-SUMMARY.md

@src/lib/formula-engine.ts
@src/lib/formula-engine.test.ts
@src/lib/mutations/workflows.ts
@src/lib/mutations/workflows.test.ts

**The two decisions this plan implements are locked, and the diagnosis behind them is already done. Read `32-RESEARCH.md` § "Diagnosis of the Two Failures" in full before touching either file — it contains the exact stack traces, the exact line numbers, and the verified fixes with their measured green results.**

**D-04 (source bug):** `formula-engine.ts` defines `usesNullSafeFunction(expression)` with a docstring saying null-safe functions "should not trigger null propagation", computes it at line 164, and then never reads it. eslint flags it as unused. The dependency loop at lines 128-160 has already returned by then, so `LOGIC.isBlank({{Value}})` with `Value: null` can never reach QuickJS. Fix the source.

**D-05 (stale test):** `deleteWorkflow` grew a `db.select().from().where()` cascade lookup at `workflows.ts:201-204`. The test's `vi.mock("@/db")` factory supplies `select: vi.fn()` with no return value, so `.from` is called on `undefined`. The source cascade is correct behaviour and must not change. Fix the test.

**D-06:** the minimal D-05 fix returns `[]` runs, so `if (runs.length > 0)` is never entered and the cascade path stays uncovered. Add a second test.

**Environment note:** this shell has an `rtk` command-rewriting hook that can swallow tool output. If a verification command prints nothing unexpectedly, invoke the binary directly (`node ./node_modules/vitest/vitest.mjs ...`, `node ./node_modules/eslint/bin/eslint.js`).
</context>

<interfaces>
<!-- workflows.ts:191-215 — the source under test. DO NOT MODIFY THIS FILE. -->
```ts
export async function deleteWorkflow(id: string): Promise<DeleteResult> {
  const existing = await db.query.workflows.findFirst({ where: eq(workflows.id, id) })
  if (!existing) return { success: false, error: "Workflow not found" }

  // Cascade delete: steps -> runs -> workflow
  const runs = await db
    .select({ id: workflowRuns.id })
    .from(workflowRuns)
    .where(eq(workflowRuns.workflowId, id))

  if (runs.length > 0) {
    const runIds = runs.map((r) => r.id)
    await db.delete(workflowRunSteps).where(inArray(workflowRunSteps.runId, runIds))
    await db.delete(workflowRuns).where(eq(workflowRuns.workflowId, id))
  }

  await db.delete(workflows).where(eq(workflows.id, id))
  return { success: true }
}
```

<!-- workflows.test.ts:443-453 — the stale test. `mockDb.select` is never stubbed. -->
```ts
describe("deleteWorkflow", () => {
  it("deletes existing workflow", async () => {
    mockDb.query.workflows.findFirst.mockResolvedValue({ id: "wf-1" })
    mockDb.delete.mockReturnValue({ where: vi.fn().mockResolvedValue(undefined) })
    const result = await deleteWorkflow("wf-1")
    expect(result).toEqual({ success: true })
  })
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the null-safe carve-out in formula-engine (D-04)</name>
  <files>src/lib/formula-engine.ts</files>
  <behavior>
    - `evaluateFormula('LOGIC.isBlank({{Value}})', { Value: null })` resolves to `{ value: true, error: null }` (currently `{ value: null, ... }`) — `formula-engine.test.ts:107`
    - `evaluateFormula('{{Missing}} + 1', { Missing: null })` still resolves to `value: null` — the `propagates null values` regression guard at `formula-engine.test.ts:42` must keep passing, because `usesNullSafeFunction('{{Missing}} + 1')` is `false` (no `LOGIC.*`/`TEXT.*` call) so the early-return still fires
    - `handles missing field as null` at `formula-engine.test.ts:47` is unaffected — it exits on the `Unknown field` branch, which is NOT gated by the new flag
    - All 57 tests in `src/lib/formula-engine.test.ts` pass
  </behavior>
  <read_first>
    - `src/lib/formula-engine.ts` lines 85-215 — in particular `usesNullSafeFunction` (89-104), `containsArithmeticOperation` (106-114), and the whole body of `evaluateFormula` from line 117 through line 215
    - `src/lib/formula-engine.test.ts` lines 40-115 — the `propagates null values` guard, `handles missing field as null`, and the failing `handles LOGIC.isBlank function`
    - `.planning/phases/32-test-infrastructure-ci/32-RESEARCH.md` § "Failure 1 — formula-engine.test.ts > LOGIC.isBlank → the SOURCE is wrong" (contains the verified fix and the measured 57/57 result)
  </read_first>
  <action>
    Do not touch `formula-engine.test.ts` in this task — the test encodes the intended behaviour and is correct as written.

    In `evaluateFormula`:
    1. Hoist `const usesNullSafe = usesNullSafeFunction(expression)` from its current position (line 164, after the dependency loop) to **above** `const deps = extractDependencies(expression)` — it must be computed before the loop that returns early.
    2. Gate all three null early-returns inside the deps loop on `&& !usesNullSafe`. They are the three `return { value: null, error: null }` statements: the related-entity null branch (`entityData[field.trim()] === null`), the `fromRelated === null` branch, and the `else if (fields[dep] === null)` branch.
    3. Leave the two **error** returns ungated — `Unknown entity: ...`, `Field "..." not found on ...`, and `Unknown field: ...` must still fire regardless of null-safety, otherwise `handles missing field as null` and the unknown-entity tests break.
    4. Delete the now-provably-dead `const hasArithmetic = containsArithmeticOperation(expression)` at line 163. If `containsArithmeticOperation` has no remaining caller after that deletion, leave the function itself in place but confirm eslint does not newly flag it; do not delete the helper unless eslint demands it.
    5. Fix the `prefer-const` eslint **error** at `formula-engine.ts:210` — `let processedExpr` is never reassigned, so it becomes `const`. This is bundled here rather than in the lint plans because it is in this file and would otherwise be a cross-plan file conflict. `npx eslint --fix src/lib/formula-engine.ts` resolves it mechanically.

    Do not change the QuickJS `isBlank` implementation at line 47 (`v === null || v === undefined || v === ''`) — it is already correct; it was simply unreachable.
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/lib/formula-engine.test.ts` reports `Tests  57 passed (57)` and exits 0
    - `node ./node_modules/vitest/vitest.mjs run src/lib/formula-engine.test.ts -t "isBlank"` passes
    - `node ./node_modules/vitest/vitest.mjs run src/lib/formula-engine.test.ts -t "propagates null"` passes (regression guard, D-04 requires it)
    - `git diff --name-only` does NOT list `src/lib/formula-engine.test.ts` — the test file is untouched
    - `grep -c '&& !usesNullSafe' src/lib/formula-engine.ts` returns `3`
    - `grep -c 'hasArithmetic' src/lib/formula-engine.ts` returns `0`
    - `node ./node_modules/eslint/bin/eslint.js --format json src/lib/formula-engine.ts` reports zero messages with `severity: 2`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/formula-engine.test.ts</automated>
  </verify>
  <done>All 57 formula-engine tests pass including both `isBlank` and the `propagates null values` guard; `usesNullSafe` gates exactly the three null early-returns; `hasArithmetic` is gone; the file is eslint-error-free; the test file is unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair the stale deleteWorkflow mock and cover the cascade branch (D-05, D-06)</name>
  <files>src/lib/mutations/workflows.test.ts</files>
  <behavior>
    - `deleteWorkflow("wf-1")` with `findFirst` returning a workflow and `select` resolving to `[]` returns `{ success: true }` — the existing `deletes existing workflow` test at `:444`
    - `deleteWorkflow("wf-1")` with `select` resolving to `[{ id: "run-1" }, { id: "run-2" }]` returns `{ success: true }` AND calls `db.delete` exactly 3 times — steps, then runs, then the workflow (new test, D-06)
    - `deleteWorkflow("wf-nonexistent")` with `findFirst` returning `undefined` still returns `{ success: false }` with an `error` property — the existing test is untouched
    - The file goes from 22 tests to 23 tests, all passing
  </behavior>
  <read_first>
    - `src/lib/mutations/workflows.test.ts` lines 1-30 (the `vi.mock("@/db")` factory and the `mockDb` shape) and lines 440-465 (the `deleteWorkflow` describe block)
    - `src/lib/mutations/workflows.ts` lines 190-215 (`deleteWorkflow` — the source under test, which must NOT change)
    - `.planning/phases/32-test-infrastructure-ci/32-RESEARCH.md` § "Failure 2 — workflows.test.ts > deleteWorkflow → the TEST is stale" (verified fixes, measured 22/22 then 23/23) and § "Pitfall 7: `vi.clearAllMocks()` does not restore implementations"
  </read_first>
  <action>
    Modify only `src/lib/mutations/workflows.test.ts`. `src/lib/mutations/workflows.ts` is correct and must not be touched — its `steps -> runs -> workflow` cascade is the intended behaviour.

    In the existing `deletes existing workflow` test (around line 444), add a `mockDb.select.mockReturnValue({...})` stub alongside the existing `findFirst` and `delete` stubs. The chain the source calls is `db.select({...}).from(x).where(y)`, so the stub returns an object whose `from` is a `vi.fn()` returning an object whose `where` is a `vi.fn()` resolving to `[]`. Returning `[]` keeps this test on the no-runs path.

    Add a second test in the same `describe("deleteWorkflow")` block named so that `-t "cascades"` selects it — e.g. `cascades to run steps and runs before deleting the workflow`. It stubs `findFirst` the same way, stubs `mockDb.select` to resolve to two run rows (`[{ id: "run-1" }, { id: "run-2" }]`) so the `runs.length > 0` branch is entered, captures the `where` spy in a local `vi.fn()` so `mockDb.delete.mockReturnValue({ where: deleteWhere })` is reusable across all three delete calls, asserts `{ success: true }`, and asserts `mockDb.delete` was called exactly 3 times.

    **Stub `mockDb.select` inside each test, never in a `beforeEach`.** The file's `beforeEach(() => vi.clearAllMocks())` calls `mockClear()`, which resets call history but NOT implementations set via `mockReturnValue` — a hoisted stub would leak into every later test in the file and produce order-dependent failures.

    Do not remove or weaken the existing `returns error for non-existent workflow` test.
  </action>
  <acceptance_criteria>
    - `node ./node_modules/vitest/vitest.mjs run src/lib/mutations/workflows.test.ts` reports `Tests  23 passed (23)` and exits 0
    - `node ./node_modules/vitest/vitest.mjs run src/lib/mutations/workflows.test.ts -t "deletes existing workflow"` passes
    - `node ./node_modules/vitest/vitest.mjs run src/lib/mutations/workflows.test.ts -t "cascades"` passes and matches exactly 1 test
    - `grep -c 'toHaveBeenCalledTimes(3)' src/lib/mutations/workflows.test.ts` returns `1`
    - `git diff --name-only` does NOT list `src/lib/mutations/workflows.ts` — the source is untouched
    - `grep -v '^\s*//' src/lib/mutations/workflows.test.ts | grep -c 'mockDb.select.mockReturnValue'` returns `2` — stubbed per-test, not hoisted
    - `node ./node_modules/eslint/bin/eslint.js --format json src/lib/mutations/workflows.test.ts` reports zero messages with `severity: 2`
  </acceptance_criteria>
  <verify>
    <automated>node ./node_modules/vitest/vitest.mjs run src/lib/mutations/workflows.test.ts</automated>
  </verify>
  <done>`workflows.test.ts` has 23 passing tests, the cascade branch asserts 3 delete calls, `mockDb.select` is stubbed per-test, and `workflows.ts` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the whole suite is green end to end</name>
  <files>(no files modified — verification gate)</files>
  <read_first>
    - `.planning/phases/32-test-infrastructure-ci/32-VALIDATION.md` § "Per-Task Verification Map" rows CI-03a through CI-03d
    - `.planning/phases/32-test-infrastructure-ci/32-RESEARCH.md` § "Proven end state"
  </read_first>
  <action>
    Run the full suite via the `npm test` entry point created in Plan 32-01 and confirm the exact numbers the researcher measured on a clean worktree: 41 files, 454 passed, 4 skipped, exit 0.

    If any *other* test fails — one not named in CI-03 — do NOT skip it, delete it, or mark it `.todo`. Diagnose it the same way: determine whether the source or the test is wrong, fix the wrong one, and record the finding in the SUMMARY. A suite made green by deletion does not satisfy CI-03.

    If the counts differ from 41/454/4 but everything passes, record the actual numbers in the SUMMARY rather than forcing them to match — the researcher's numbers are from a specific commit and other phases may have landed tests since.
  </action>
  <acceptance_criteria>
    - `npm test; echo $?` prints `0`
    - `npm test` output contains `Test Files` with `41 passed (41)` and zero failed
    - `npm test 2>&1 | grep -c '\.next/'` returns `0`
    - `npm test 2>&1 | grep -c 'node_modules/'` returns `0`
    - No test in the repo is newly marked `.skip`, `.todo`, or `.only` — `git diff -U0 | grep -E '^\+.*\b(it|test|describe)\.(skip|todo|only)\b'` produces no output
    - `npm run typecheck; echo $?` prints `0`
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>`npm test` exits 0 with 41 files green, nothing collected from `.next/` or `node_modules/`, and no test was skipped or deleted to get there.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored formula expression → QuickJS sandbox | Formula strings originate from admin-configured custom fields and are evaluated in a sandboxed VM; changing when evaluation is reached changes what reaches the sandbox |
| test double → production cascade-delete path | The mock is the only thing standing between a regression in `deleteWorkflow` and orphaned `workflow_run_steps` rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Tampering | `evaluateFormula` null early-returns | mitigate | Gate the three null returns on `!usesNullSafe` only — leave the `Unknown entity` / `Field not found` / `Unknown field` **error** returns ungated, so a formula referencing a non-existent field still errors rather than silently reaching QuickJS. Verified by the acceptance criterion that all 57 formula-engine tests pass, including `handles missing field as null`. |
| T-32-06 | Elevation of Privilege | QuickJS sandbox reachability | accept | This change makes `LOGIC.*`/`TEXT.*` expressions with null inputs reach QuickJS where they previously short-circuited. The sandbox boundary, the function library, and the `{{Field}}` → `fields["Field"]` substitution are all unchanged, so no new expression syntax becomes evaluable — only expressions that were always intended to evaluate now do. Risk is bounded by the existing sandbox, which this plan does not modify. |
| T-32-07 | Tampering | `src/lib/mutations/workflows.ts` | mitigate | The source is explicitly out of scope (D-05: the test is stale, not the source). Enforced by the acceptance criterion `git diff --name-only` must not list `workflows.ts`. A "fix" that relaxed the cascade would leave orphaned `workflow_run_steps` rows referencing deleted runs. |
| T-32-08 | Repudiation | test suite integrity | mitigate | Task 3 forbids `.skip`/`.todo`/`.only` as a path to green and asserts it with a `git diff` grep. A gate that can be silenced by annotation is not a gate, which is the same principle D-03 applies to lint. |
| T-32-09 | Information Disclosure | test execution environment | accept | The researcher proved `env -u DATABASE_URL npx vitest run` produces byte-identical results — all 27 DB-touching test files `vi.mock("@/db")`, and `src/db/index.ts` throws at module load without `DATABASE_URL`. No task in this plan introduces a real database connection or a credential. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero packages; no `npm install` / `npm ci` task exists in it. Nothing for the Package Legitimacy Gate to audit. |
</threat_model>

<verification>
```bash
# CI-03a / D-04
node ./node_modules/vitest/vitest.mjs run src/lib/formula-engine.test.ts        # 57 passed (57)
node ./node_modules/vitest/vitest.mjs run src/lib/formula-engine.test.ts -t "propagates null"   # regression guard

# CI-03b / CI-03c / D-05 / D-06
node ./node_modules/vitest/vitest.mjs run src/lib/mutations/workflows.test.ts   # 23 passed (23)
node ./node_modules/vitest/vitest.mjs run src/lib/mutations/workflows.test.ts -t "cascades"

# CI-03d
npm test; echo $?         # -> 0, Test Files 41 passed (41)
npm run typecheck; echo $?  # -> 0

# Sources that must be untouched
git diff --name-only | grep -E 'formula-engine\.test\.ts|mutations/workflows\.ts'   # -> no output
```
</verification>

<success_criteria>
- `npm test` exits 0 with 41 files passing and nothing collected from `.next/` or `node_modules/`
- `LOGIC.isBlank` returns `true` for a null input and `{{Missing}} + 1` still returns `null`
- `formula-engine.test.ts` was not modified; `mutations/workflows.ts` was not modified
- `workflows.test.ts` has 23 tests, one of which asserts exactly 3 `db.delete` calls on the cascade branch
- No test was skipped, deleted, or marked `.only` to reach green
- `src/lib/formula-engine.ts` has zero eslint errors (including the `prefer-const` at line 210)
</success_criteria>

<output>
Create `.planning/phases/32-test-infrastructure-ci/32-02-SUMMARY.md` when done
</output>
