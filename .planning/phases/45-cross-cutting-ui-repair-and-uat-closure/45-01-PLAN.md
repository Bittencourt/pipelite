---
phase: 45-cross-cutting-ui-repair-and-uat-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/locale-parity.test.ts
  - src/messages/en-US.json
  - src/messages/pt-BR.json
  - src/messages/es-ES.json
autonomous: true
requirements: [SC-3, SC-4]
must_haves:
  truths:
    - "Every string this phase puts on screen exists in en-US, pt-BR and es-ES"
    - "A future developer who adds an admin.nav.* or theme.* string without translating it fails the suite"
    - "The ICU plural wrapper on the partial-selection hint cannot be dropped from one locale silently"
  artifacts:
    - path: "src/messages/en-US.json"
      provides: "admin.nav (12), theme (4), nav.workflows, nav.searchDescription, bulk.failures.retryHintPartial, bulk.failures.prunedHint, audit.field.movedToTrash, audit.field.restoredFromTrash"
      contains: "\"openMenu\""
    - path: "src/messages/pt-BR.json"
      provides: "the same 22 keys, translated"
      contains: "\"Painel de Administração\""
    - path: "src/messages/es-ES.json"
      provides: "the same 22 keys, translated"
      contains: "\"Panel de Administración\""
    - path: "src/messages/locale-parity.test.ts"
      provides: "REQUIRED_SHELL_KEYS contract, extended REQUIRED_BULK_KEYS and REQUIRED_AUDIT_KEYS, populated IDENTICAL_TRANSLATION_ALLOWED, explicit ICU plural assertion"
      contains: "REQUIRED_SHELL_KEYS"
  key_links:
    - from: "src/messages/locale-parity.test.ts"
      to: "src/messages/{en-US,pt-BR,es-ES}.json"
      via: "exact-set contract assertion"
      pattern: "REQUIRED_SHELL_KEYS"
    - from: "IDENTICAL_TRANSLATION_ALLOWED"
      to: "untranslatedInBoth()"
      via: "allowlist for the three product nouns"
      pattern: "admin\\.nav\\.pipelines"
---

<objective>
Land every message key this phase needs, in all three locales, together with the contract lists that
make an untranslated or missing key a red suite rather than a silent shipping defect.

Purpose: every other plan in this phase consumes these keys. next-intl resolves a missing key at
render time, so a UI plan that lands before its copy does produces a broken page that no source gate
catches. This plan is Wave 1 for that reason.

Output: 22 new keys × 3 locale files, and an extended `locale-parity.test.ts` that gates them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-CONTEXT.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-UI-SPEC.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-VALIDATION.md
</context>

<interfaces>
<!-- The exact copy manifest is 45-UI-SPEC.md § New message keys. It is authoritative: the -->
<!-- en-US / pt-BR / es-ES columns there are the values to write, verbatim, with no paraphrase. -->

Key count reconciliation (do this arithmetic once, then assert it):
  admin.nav.*        12  (title, dashboard, users, pipelines, customFields, webhooks,
                          auditLog, trash, exportData, pipedriveImport, backToApp, openMenu)
  theme.*             4  (label, light, dark, system)
  nav.*               2  (workflows, searchDescription)
  bulk.failures.*     2  (retryHintPartial, prunedHint)
  audit.field.*       2  (movedToTrash, restoredFromTrash)
  ----------------------
  TOTAL              22

45-UI-SPEC.md's section heading says "23 keys × 3 locales". That heading is one off against its own
five tables, which enumerate 22. The tables are authoritative. Assert 22.

Existing helpers in src/messages/locale-parity.test.ts (call them, never re-implement):
  missingIn(keys)              -> Record<Locale, string[]>
  blankIn(keys)                -> Record<Locale, string[]>
  untranslatedInBoth(keys)     -> Record<Locale, string[]>   // consults IDENTICAL_TRANSLATION_ALLOWED
  placeholderDrift(keys)       -> Record<Locale, string[]>
  keysMatching(predicate)      -> Record<Locale, string[]>
  inNamespace(ns)              -> (key: string) => boolean

Existing declarations this plan edits, by line:
  REQUIRED_AUDIT_KEYS          line 73
  REQUIRED_BULK_KEYS           line 302   (its "Failure report — 4" group is at 353-357)
  IDENTICAL_TRANSLATION_ALLOWED line 381  (currently `[]`)
  TRASH_EXTRA_KEYS             line 436   (the cross-namespace idiom to copy)
  keysMatching call sites      lines 454-462
  the four assertion it() blocks lines 535-604
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the locale contract lists and prove them RED</name>
  <files>src/messages/locale-parity.test.ts</files>
  <read_first>
    - src/messages/locale-parity.test.ts (read the WHOLE file — 600+ lines; you need the doc-block
      style of REQUIRED_BULK_KEYS at 291-310, the TRASH_EXTRA_KEYS cross-namespace idiom at 432-462,
      the five shared assertion bodies at 476-532, and all four it() blocks at 535-604)
    - src/messages/en-US.json (namespace shapes for admin, nav, bulk.failures, audit.field)
    - .planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-UI-SPEC.md § New message keys
  </read_first>
  <behavior>
    - Running this file BEFORE task 2 must fail, and each failure must name a specific missing key.
    - `missingIn(REQUIRED_SHELL_KEYS)` reports all 16 shell keys missing in all three locales.
    - `missingIn(REQUIRED_BULK_KEYS)` reports `bulk.failures.retryHintPartial` and
      `bulk.failures.prunedHint` missing in all three locales.
    - `missingIn(REQUIRED_AUDIT_KEYS)` reports `audit.field.movedToTrash` and
      `audit.field.restoredFromTrash` missing in all three locales.
    - The new ICU assertion reports `bulk.failures.retryHintPartial` as absent (not as
      structurally wrong) while the key does not exist yet.
    - After task 2 all of the above are green, and no existing assertion changed behaviour.
  </behavior>
  <action>
    Five edits to `src/messages/locale-parity.test.ts`, all following the file's own conventions
    (`export const REQUIRED_<NS>_KEYS: string[]`, a doc block naming the source contract document,
    inline `// Group — N.` count comments, full dot-path keys, every `expect` carrying a prose
    second argument).

    1. `REQUIRED_BULK_KEYS` (line 302): add `"bulk.failures.retryHintPartial"` and
       `"bulk.failures.prunedHint"` into the existing "Failure report — 4" group at lines 353-357,
       and change that group comment's count from 4 to 6. The exact-set assertion at ~line 572
       (`expect(bulkKeys[locale]).toEqual(bulkContract)`) is why this cannot be skipped.

    2. `REQUIRED_AUDIT_KEYS` (line 73): add `"audit.field.movedToTrash"` and
       `"audit.field.restoredFromTrash"`, appended to the `audit.field.*` group, and update that
       group's count comment. Same exact-set assertion at ~line 576.

    3. New `export const REQUIRED_SHELL_KEYS: string[]` declared immediately after
       `REQUIRED_BULK_KEYS`. 16 keys: the 12 `admin.nav.*`, the 4 `theme.*`. Doc block must name
       45-UI-SPEC.md § New message keys as the source and state that the `nav` half is carried as
       extras because the `nav` namespace already holds 12 pre-existing keys, so a whole-namespace
       exact-set contract would drag them all in.
       Alongside it declare `const SHELL_EXTRA_KEYS = ["nav.workflows", "nav.searchDescription"]`,
       copying the `TRASH_EXTRA_KEYS` shape at line 436.

    4. Wire the shell contract into the existing structure, mirroring `trashKeys` at lines 458-461:
       add `const SHELL_NAMESPACES = ["admin.nav", "theme"]` (or two `inNamespace` predicates
       OR-ed) and
       `const shellKeys = keysMatching((key) => inNamespace("admin.nav")(key) || inNamespace("theme")(key) || SHELL_EXTRA_KEYS.includes(key))`.
       Then add ONE new `expect(...)` line to EACH of the four existing `it()` blocks at 535-604 —
       `missingIn`, `blankIn`, `untranslatedInBoth`, `placeholderDrift` — passing
       `REQUIRED_SHELL_KEYS` concatenated with `SHELL_EXTRA_KEYS` as a separate argument, never
       merged into an existing call (the file's header at 469-474 states the rule: contracts are
       passed separately so a failure diff names which contract broke). Add the matching exact-set
       assertion `expect(shellKeys[locale]).toEqual(shellContract)` next to the bulk and audit ones.

    5. `IDENTICAL_TRANSLATION_ALLOWED` (line 381, currently `[]`): populate with exactly
       `"admin.nav.pipelines"`, `"admin.nav.webhooks"`, `"nav.workflows"`. Rewrite its doc comment
       to record WHY each is there — they are product nouns, `nav.pipelines` is already "Pipelines"
       in all three locales, and translating `nav.workflows` would send a pt-BR user to a workflows
       surface that has no `workflows` message namespace at all. Without this, step 4's
       `untranslatedInBoth` line fails on correct copy.

    6. NEW assertion closing Pitfall 5. `placeholders()` at line ~415 uses
       `/\{[a-zA-Z0-9_]+\}/g`, which cannot match `{count, plural, one {# …} other {# …}}` — the
       comma, the spaces and the `#` all fall outside the character class — so `placeholderDrift`
       hits `if (expected.length === 0) continue` and checks nothing. Add a dedicated `it()` block
       asserting that for every locale the value at `bulk.failures.retryHintPartial` contains both
       the substring `{count,` and the substring `plural,`. Its doc comment must state that the
       generic placeholder gate does not cover ICU plural syntax and that this is the only thing
       defending the wrapper. Do NOT claim `placeholderDrift` covers it.

    Do not touch REQUIRED_NOTE_KEYS or REQUIRED_TRASH_KEYS.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/messages/locale-parity.test.ts 2>&1 | tee /tmp/45-01-red.txt; grep -q "retryHintPartial" /tmp/45-01-red.txt &amp;&amp; grep -q "admin.nav.openMenu" /tmp/45-01-red.txt &amp;&amp; echo "RED CONFIRMED"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` exits NON-ZERO.
    - The failure output contains the literal `bulk.failures.retryHintPartial`.
    - The failure output contains the literal `admin.nav.openMenu`.
    - `grep -c "REQUIRED_SHELL_KEYS" src/messages/locale-parity.test.ts` is at least 2 after
      filtering comments: `grep -v '^\s*\*' src/messages/locale-parity.test.ts | grep -v '^\s*//' | grep -c "REQUIRED_SHELL_KEYS"` >= 2.
    - `grep -v '^\s*\*' src/messages/locale-parity.test.ts | grep -c '"admin.nav.pipelines"'` >= 2
      (once in REQUIRED_SHELL_KEYS, once in IDENTICAL_TRANSLATION_ALLOWED).
    - `./node_modules/.bin/tsc --noEmit` exits 0.
    - `./node_modules/.bin/eslint src/messages/locale-parity.test.ts` exits 0.
  </acceptance_criteria>
  <done>The contract lists name all 22 new keys and the suite fails, naming them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the 22 keys into all three locale files</name>
  <files>src/messages/en-US.json, src/messages/pt-BR.json, src/messages/es-ES.json</files>
  <read_first>
    - .planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-UI-SPEC.md § New message keys
      (all five tables — the en-US / pt-BR / es-ES columns are the values, verbatim)
    - src/messages/en-US.json (find the existing `admin`, `nav`, `bulk.failures`, `audit.field`
      objects and the top level to place `theme` at)
    - src/messages/pt-BR.json
    - src/messages/es-ES.json
    - src/messages/locale-parity.test.ts (as edited by task 1 — the contract you are satisfying)
  </read_first>
  <behavior>
    - After this task `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` is green.
    - All three files carry the identical whole-file key set (770 leaves each before, 792 after).
    - `audit.field.movedToTrash` and `audit.field.restoredFromTrash` are FLAT siblings of the
      existing `audit.field.*` leaves, not a nested object — `AUDIT_FIELD_LABELS` in
      `src/lib/audit/present.ts` maps a column name to a flat dot-path and a nested value breaks it.
    - `theme` is a NEW TOP-LEVEL namespace, not `nav.theme`.
  </behavior>
  <action>
    Add a new `admin.nav` object with the 12 keys, a new top-level `theme` object with the 4 keys,
    two keys into the existing `nav` object (`workflows`, `searchDescription`), two into
    `bulk.failures` (`retryHintPartial`, `prunedHint`) and two into `audit.field`
    (`movedToTrash`, `restoredFromTrash`) — in all three files.

    Values come verbatim from 45-UI-SPEC.md § New message keys. Three rules that constrain them:

    - Vocabulary rule V-1: eight of the `admin.nav.*` strings already have a translated twin under
      `admin.dashboard.*` or `admin.webhooks.title`. The UI-SPEC values are COPIED from those. Do
      not paraphrase — the sidebar and the dashboard tile for the same route must not disagree.
    - `admin.nav.dashboard` is deliberately "Visão geral" / "Resumen", NOT `admin.dashboard.title`'s
      "Painel de Administração", because `admin.nav.title` already says that 40px above it.
    - `bulk.failures.retryHintPartial` carries an ICU plural wrapper in all three locales, using
      only the `one` and `other` categories. `bulk.failures.retryHint` is UNCHANGED, verbatim, in
      all three — it is still correct in the branch that keeps it.

    Do not reformat, re-sort or re-indent any existing key. Match each file's existing indentation.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/messages/locale-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` exits 0.
    - `node -e "const l=['en-US','pt-BR','es-ES'].map(f=>require('./src/messages/'+f+'.json')); const f=o=>{const out=[];const w=(p,v)=>{for(const k in v){const q=p?p+'.'+k:k; typeof v[k]==='object'&&v[k]!==null?w(q,v[k]):out.push(q)}};w('',o);return out.sort()}; const s=l.map(f); if(JSON.stringify(s[0])!==JSON.stringify(s[1])||JSON.stringify(s[1])!==JSON.stringify(s[2])) throw new Error('key sets differ'); console.log('leaves', s[0].length)"`
      prints `leaves 792`.
    - `node -e "const m=require('./src/messages/pt-BR.json'); if(m.admin.nav.title!=='Painel de Administração') throw 1; if(!m.theme||m.theme.dark!=='Escuro') throw 2; if(m.nav.workflows!=='Workflows') throw 3; console.log('ok')"`
      prints `ok`.
    - `node -e "for (const f of ['en-US','pt-BR','es-ES']){const v=require('./src/messages/'+f+'.json').bulk.failures.retryHintPartial; if(!v.includes('{count,')||!v.includes('plural,')) throw new Error(f)} console.log('icu ok')"`
      prints `icu ok`.
    - `npm run test` exits 0 (both vitest projects).
    - `./node_modules/.bin/tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>All 22 keys exist in all three locales and the locale gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator/developer → shipped copy | An untranslated or missing string reaches a user's screen |
| message catalog → server error prose | A copy key must never become a channel for server-side text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | src/messages/*.json | mitigate | Exact-set contract lists (`REQUIRED_SHELL_KEYS`, extended `REQUIRED_BULK_KEYS` / `REQUIRED_AUDIT_KEYS`) turn a key added without a translation into a red suite; `IDENTICAL_TRANSLATION_ALLOWED` is an explicit three-entry allowlist, never a blanket exemption |
| T-45-02 | Information disclosure | bulk.failures.* | accept | The two new keys are static sentences with a single numeric `{count}` argument. No server prose, no id, no entity name is interpolated. The closed reason-code union is enforced separately by the existing `bulk-failure-report-wiring` gate |
| T-45-03 | Repudiation | ICU plural wrapper | mitigate | `placeholderDrift()`'s regex provably cannot match ICU plural syntax (verified: `/\{[a-zA-Z0-9_]+\}/g` against `{count, plural, …}`), so a dedicated assertion checks for `{count,` and `plural,` per locale rather than claiming coverage the gate does not provide |
| T-45-SC | Tampering | npm installs | mitigate | This plan installs nothing. No package-manager operation occurs |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` — green
- `npm run test` — green (both projects)
- `npm run typecheck` — 0 errors
- `npm run lint` — 0 errors
</verification>

<success_criteria>
- 22 new keys present in en-US.json, pt-BR.json and es-ES.json, values verbatim from 45-UI-SPEC.md
- All three files carry 792 identical leaf keys
- `REQUIRED_SHELL_KEYS` (16) + `SHELL_EXTRA_KEYS` (2) gated by all four shared assertion bodies
- `IDENTICAL_TRANSLATION_ALLOWED` holds exactly the three product nouns
- An explicit ICU-structure assertion defends `bulk.failures.retryHintPartial`
</success_criteria>

<output>
Create `.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-01-SUMMARY.md` when done
</output>
