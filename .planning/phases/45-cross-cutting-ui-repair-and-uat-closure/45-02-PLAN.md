---
phase: 45-cross-cutting-ui-repair-and-uat-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - .dockerignore
  - .env
  - playwright.config.ts
  - e2e/seed-admin.ts
  - e2e/auth.setup.ts
autonomous: true
requirements: [SC-1, SC-2, SC-5]
user_setup: []
must_haves:
  truths:
    - "A developer can run the e2e suite locally against the Docker app with one command"
    - "The suite authenticates as a dedicated admin through the real login form, so a broken login page fails the suite"
    - "The live session cookie the harness produces can never be committed"
    - "The seed refuses to run against anything but a local development database"
  artifacts:
    - path: "playwright.config.ts"
      provides: "testDir e2e, setup+chromium projects, 320x640 viewport, scrollbar flag, no webServer"
      contains: "ignoreDefaultArgs"
    - path: "e2e/seed-admin.ts"
      provides: "idempotent argon2id upsert of the pipelite-e2e@local admin"
      exports: ["seedE2eAdmin"]
    - path: "e2e/auth.setup.ts"
      provides: "one real-form login writing e2e/.auth/admin.json"
    - path: ".gitignore"
      provides: "/e2e/.auth/, /playwright-report/, /test-results/"
      contains: "/e2e/.auth/"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/auth.setup.ts"
      via: "setup project + dependencies"
      pattern: "dependencies:\\s*\\[\"setup\"\\]"
    - from: "e2e/auth.setup.ts"
      to: "e2e/.auth/admin.json"
      via: "storageState write"
      pattern: "storageState"
    - from: "e2e/seed-admin.ts"
      to: "src/lib/password.ts"
      via: "hashPassword import"
      pattern: "hashPassword"
---

<objective>
Stand up the minimal Playwright harness this phase needs: one devDependency, one config, one
authenticated session, and the ignore-file entries that keep a live admin session token out of git
and out of the Docker build context.

Purpose: SC-1 and SC-5 are layout and trusted-input claims. jsdom computes no layout and synthetic
event dispatch is refused as evidence, so a real browser is the only instrument. This plan builds
that instrument; no spec is written here.

Output: `@playwright/test` recorded in devDependencies, `playwright.config.ts`, an idempotent e2e
admin seed, an authenticated `storageState`, and four ignore-file entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-CONTEXT.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-VALIDATION.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-RESEARCH.md
</context>

<interfaces>
<!-- Contracts downstream specs (45-08) will consume. Define them here; do not change them later. -->

playwright.config.ts, exported shape:
  testDir            "./e2e"
  projects[0]        { name: "setup", testMatch: /.*\.setup\.ts/ }
  projects[1]        { name: "chromium", dependencies: ["setup"],
                       use: { storageState: "e2e/.auth/admin.json",
                              viewport: { width: 320, height: 640 } } }
  use.baseURL        process.env.E2E_BASE_URL ?? "http://localhost:3001"
  use.launchOptions  { ignoreDefaultArgs: ["--hide-scrollbars"] }
  NO webServer key.

e2e/seed-admin.ts:
  export async function seedE2eAdmin(): Promise<{ email: string; password: string }>

Environment contract (both REQUIRED; the seed throws a named error if either is unset):
  E2E_ADMIN_PASSWORD   the seeded account's plaintext password
  E2E_DATABASE_URL     host-mapped Postgres, e.g. postgresql://<user>:<pw>@localhost:5433/<db>
                       (the app's own DATABASE_URL points at postgres:5432 inside the Docker
                       network and is NOT reachable from the host)

Login form selectors, all hardcoded English and therefore locale-independent
(src/app/(auth)/login/page.tsx:105-160):
  #email, #password, submit button with accessible name "Sign In"
Submission goes through signIn("credentials", { redirect: false }) and the PAGE then navigates
itself — wait for the post-login navigation, not for a form response.

users row columns the seed must set (src/db/schema/users.ts:11-25):
  email, passwordHash, name, role: "admin", status: "approved", emailVerified: <a Date>
  role and status both DEFAULT to values that fail login and fail the /admin gate.

Password hashing — import it, never re-derive the parameters (src/lib/password.ts:7-14):
  hashPassword(password) -> argon2id, memoryCost 19456, timeCost 2, parallelism 1
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Record the dependency and close the credential-leak holes FIRST</name>
  <files>package.json, package-lock.json, .gitignore, .dockerignore, .env</files>
  <read_first>
    - .gitignore (read in full — note it already carries `.playwright-mcp/`, which is a DIFFERENT
      directory from `/playwright-report/`; do not assume the entry exists)
    - .dockerignore (read in full — it uses `# Title-Case Section` headers and UNANCHORED names)
    - package.json (scripts block, lines 5-16 — every script is a bare binary name resolved from
      node_modules/.bin; the string `npx` appears zero times in this file)
    - .planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-RESEARCH.md § Pitfall 11,
      § Pitfall 12, § Package Legitimacy Audit
  </read_first>
  <action>
    Order matters. The ignore entries must land BEFORE task 3 ever runs the setup project, because
    that run writes a live Auth.js JWT session cookie with a 7-day maxAge to disk (VALIDATION V-2).

    1. `.gitignore` — append, under a new `# Playwright e2e` section header, using the file's
       leading-slash anchored style: `/e2e/.auth/`, `/playwright-report/`, `/test-results/`.

    2. `.dockerignore` — append, under a new `# E2E` Title-Case section header, using that file's
       UNANCHORED style: `e2e`, `playwright.config.ts`, `playwright-report`, `test-results`.
       Reason: `COPY . .` in the Dockerfile would otherwise ship the specs into the build context
       and `next build` would typecheck them (next.config.ts sets no `typescript.ignoreBuildErrors`).

    3. `npm install -D @playwright/test@^1.62.1`. Note that `@playwright/test@1.62.1` is ALREADY
       physically present in `node_modules` (left there by a research tool) while being absent from
       `package.json` — so `./node_modules/.bin/playwright` resolving is NOT proof the dependency is
       recorded. Run the install anyway so `package.json` and `package-lock.json` record it. It must
       land in `devDependencies`, never `dependencies`. The package has no `postinstall` script, so
       this does not make CI download browser binaries.
       Do NOT use `slopcheck install` as a verification step anywhere: it runs `npm install` as a
       side effect and writes to `dependencies`. The legitimacy audit is already complete
       (37M weekly downloads, microsoft/playwright, verdict OK).

    4. `package.json` scripts — add `"test:e2e": "playwright test"`, a bare binary name matching the
       existing `"lint": "eslint"` / `"typecheck": "tsc --noEmit"` convention. Do NOT add it to
       `"test"`, and do NOT touch `.github/workflows/ci.yml` — CI has no Docker, no database and no
       app server, so wiring Playwright there produces a permanently red required check
       (VALIDATION V-3).

    5. `.env` (gitignored) — append two keys WITHOUT echoing their values to stdout, to any log, or
       to the task summary:
         - `E2E_ADMIN_PASSWORD=` set to the output of `openssl rand -base64 24`, generated and
           redirected straight into the file.
         - `E2E_DATABASE_URL=` built from the POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB values
           already in `.env`, pointing at `localhost:5433` (the host-mapped port from
           docker-compose.yml line 32), NOT at `postgres:5432`.
       Both keys are read back by `playwright.config.ts` in task 2 via Node's built-in
       `process.loadEnvFile()`; no `dotenv` dependency is added.
  </action>
  <verify>
    <automated>git check-ignore -q e2e/.auth/admin.json &amp;&amp; git check-ignore -q playwright-report/index.html &amp;&amp; git check-ignore -q test-results/x &amp;&amp; node -e "const p=require('./package.json'); if(!p.devDependencies['@playwright/test']) throw new Error('not a devDependency'); if(p.dependencies['@playwright/test']) throw new Error('leaked into dependencies'); if(p.scripts['test:e2e']!=='playwright test') throw new Error('script'); if(p.scripts.test.includes('playwright')) throw new Error('e2e must not be in npm test'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore -q e2e/.auth/admin.json` exits 0.
    - `git check-ignore -q playwright-report/index.html` exits 0.
    - `git check-ignore -q test-results/x` exits 0.
    - `package.json` `devDependencies["@playwright/test"]` is present; `dependencies["@playwright/test"]` is absent.
    - `package.json` `scripts["test:e2e"] === "playwright test"`.
    - `package.json` `scripts["test"]` does NOT contain the substring `playwright`.
    - `git diff --stat .github/workflows/ci.yml` produces no output (file untouched).
    - `grep -c '^e2e$' .dockerignore` is 1 and `grep -c '^playwright.config.ts$' .dockerignore` is 1.
    - `grep -c '^E2E_ADMIN_PASSWORD=' .env` is 1 and `grep -c '^E2E_DATABASE_URL=.*localhost:5433' .env` is 1.
    - No task output, commit message or SUMMARY line contains the generated password or the database password.
    - `npm run lint` exits 0 and `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The dependency is recorded, the session file and the report directories are gitignored, and the two e2e env vars exist locally.</done>
</task>

<task type="auto">
  <name>Task 2: playwright.config.ts</name>
  <files>playwright.config.ts</files>
  <read_first>
    - vitest.config.ts (the repo's config-file idiom: default export of `defineConfig({...})`, and
      an inline comment on every non-obvious option stating what breaks without it)
    - vitest.rsc.config.ts (25 comment lines above a 20-line config, including a MEASURED
      justification — this is the house style for a config decision that looks removable)
    - .planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-RESEARCH.md § Pitfall 8
      and § Code Examples → Example 4
    - .planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-VALIDATION.md rules V-1, V-3, V-7
  </read_first>
  <action>
    Create `playwright.config.ts` at the repo root with `export default defineConfig({...})` from
    `@playwright/test`, matching the shape pinned in this plan's `<interfaces>` block exactly.

    Two decisions MUST each carry a comment stating what breaks without them, or a future reader
    deletes them:

    - `launchOptions: { ignoreDefaultArgs: ["--hide-scrollbars"] }`. Measured: at a 320x640 viewport
      against a 416px-wide page, default headless Chromium reports `clientWidth 320`, with this flag
      it reports `clientWidth 305`, and headed reports `305`. The recorded UAT baseline is 305.
      Without the flag the harness is 15px more permissive than a real phone and green-lights
      layouts that still overflow.
    - The ABSENCE of a `webServer` block. The app runs in Docker (`docker compose up -d`, host 3001
      to container 3000). Never start a local dev server — `npm run dev` / `next dev` are forbidden
      in this project. The comment must say so, because "playwright has no webServer" reads like an
      omission rather than a decision.

    Also set `fullyParallel: false` (one app instance, one shared database), `forbidOnly: !!process.env.CI`,
    and `reporter: [["list"]]`.

    At the top of the file, call Node's built-in `process.loadEnvFile()` inside a try/catch so a
    missing `.env` degrades to "env vars must be exported by hand" rather than crashing the config.
    This is what makes `E2E_ADMIN_PASSWORD` and `E2E_DATABASE_URL` from task 1 visible to the setup
    project without adding a `dotenv` dependency.

    Do NOT set `isMobile: true` — it activates mobile viewport emulation and measured 980px on a
    probe page, which is a variable this phase does not need. Plain `viewport: { width: 320, height: 640 }`
    plus the scrollbar flag is the closest match to the recorded evidence.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit &amp;&amp; ./node_modules/.bin/eslint playwright.config.ts &amp;&amp; grep -v '^\s*//' playwright.config.ts | grep -v '^\s*\*' | grep -q 'ignoreDefaultArgs' &amp;&amp; ! (grep -v '^\s*//' playwright.config.ts | grep -v '^\s*\*' | grep -q 'webServer') &amp;&amp; echo "config ok"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exits 0.
    - `./node_modules/.bin/eslint playwright.config.ts` exits 0.
    - With comments stripped (`grep -v '^\s*//' | grep -v '^\s*\*'`), the file CONTAINS `ignoreDefaultArgs` and `"--hide-scrollbars"`.
    - With comments stripped, the file does NOT contain `webServer`.
    - With comments stripped, the file contains `viewport` with `320` and `640`, `storageState`, `"e2e/.auth/admin.json"`, `dependencies` with `"setup"`, and `process.loadEnvFile`.
    - `node -e "require('fs').readFileSync('playwright.config.ts','utf8')"` — the file contains an explicit comment mentioning the measured 320-vs-305 clientWidth difference and an explicit comment stating the app runs in Docker and no dev server may be started.
    - `npm run test` still exits 0 — vitest's include glob is anchored at `src/`, so nothing under `e2e/` or a root config can be collected by it.
  </acceptance_criteria>
  <done>The config typechecks, lints, carries both load-bearing comments, and defines no webServer.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the e2e admin and capture one real-form login</name>
  <files>e2e/seed-admin.ts, e2e/auth.setup.ts</files>
  <read_first>
    - drizzle/seed-activity-types.ts (the repo's only seed script: idempotency by find-then-insert,
      the ✓ / → / ✗ console vocabulary, top-level `seed().catch(console.error)`)
    - src/lib/password.ts (hashPassword — import it, do not re-derive argon2id parameters)
    - src/db/schema/users.ts (lines 11-25 — role and status both default to values that fail login)
    - src/db/index.ts (lines 5-7 — the DATABASE_URL guard idiom)
    - src/app/(auth)/login/page.tsx (lines 55-160 — the #email / #password / "Sign In" selectors and
      the signIn(..., { redirect: false }) then self-navigate flow)
    - .planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-PATTERNS.md
      § `e2e/auth.setup.ts` + the e2e admin seed
  </read_first>
  <action>
    `e2e/seed-admin.ts` — export `async function seedE2eAdmin()`:

    - Do NOT import `@/db`. That module is pinned to `process.env.DATABASE_URL`, which in this repo
      points at `postgres:5432` inside the Docker network and is unreachable from the host. Build a
      local client instead: `postgres` + `drizzle-orm/postgres-js` (both already app dependencies)
      against `process.env.E2E_DATABASE_URL`, importing only the `users` table from
      `@/db/schema/users`.
    - Throw a named error if `E2E_DATABASE_URL` is unset, and a second named error if
      `E2E_ADMIN_PASSWORD` is unset. Never fall back to a literal password.
    - DEV-DATABASE GUARD: parse `E2E_DATABASE_URL` and throw unless its hostname is exactly
      `localhost` or `127.0.0.1`. This is the concrete form of the locked "must never run against a
      non-dev database" constraint. State the reason in a comment at the throw.
    - Idempotent upsert for `pipelite-e2e@local`: select by email; if absent insert; if present
      update. Either way the row must end with `passwordHash` = `await hashPassword(process.env.E2E_ADMIN_PASSWORD)`,
      `role: "admin"`, `status: "approved"`, `emailVerified` set to a Date, and a `name`.
      Both defaults (`role: "member"`, `status: "pending_verification"`) would fail the login and the
      `/admin` gate, so setting them explicitly is not belt-and-braces.
    - Close the postgres client before returning. Return `{ email, password }`.
    - Log the ✓ / → vocabulary from the analog. Never log the password.

    `e2e/auth.setup.ts` — a Playwright `setup` test (`import { test as setup, expect } from "@playwright/test"`):

    - Call `seedE2eAdmin()` first. Running the seed from inside the setup project is deliberate:
      Playwright transpiles TypeScript itself, so this adds no `tsx` dependency — and `tsx` is NOT
      installed in this repo, which makes the existing `"db:seed-activities": "tsx ..."` script
      convention currently unrunnable and unsafe to copy.
    - Navigate to `/login`, fill `#email` and `#password`, click the submit button, and wait for the
      post-login navigation away from `/login` (the page self-navigates after
      `signIn(..., { redirect: false })`; there is no form response to await).
    - ANTI-VACUITY: before saving state, assert something that proves the session is real AND is an
      admin — navigate to `/admin/audit` and assert the level-1 heading is visible. A failed login
      redirects to `/login`, and a non-admin session redirects to `/?error=unauthorized`; both would
      otherwise produce a saved storageState that "works" and fails every later spec obscurely.
    - Save `storageState` to `e2e/.auth/admin.json`.
    - This is the only place the harness logs in. Every other spec inherits the state through the
      `chromium` project's `dependencies: ["setup"]`.

    Precondition for running this task's verification: the app must be answering on
    http://localhost:3001. Bring it up with `docker compose up -d` (no `--build` needed — this plan
    changes no application source, so the existing image is adequate; `docker` needs no sudo).
  </action>
  <verify>
    <automated>docker compose up -d &amp;&amp; timeout 120 bash -c 'until curl -sf -o /dev/null http://localhost:3001/login; do sleep 3; done' &amp;&amp; ./node_modules/.bin/playwright test --project=setup &amp;&amp; test -s e2e/.auth/admin.json &amp;&amp; test -z "$(git status --porcelain e2e/.auth)" &amp;&amp; echo "auth ok"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/playwright test --project=setup` exits 0.
    - `e2e/.auth/admin.json` exists and is non-empty.
    - `git status --porcelain e2e/.auth` produces NO output (the directory is ignored, not merely untracked-and-noisy).
    - `git status --porcelain e2e/.auth/admin.json` produces NO output.
    - Running the setup project a SECOND time also exits 0 (proves the seed is idempotent, not insert-only).
    - `psql "$E2E_DATABASE_URL" -tAc "select role,status from users where email='pipelite-e2e@local'"` returns exactly `admin|approved`.
    - With comments stripped, `e2e/seed-admin.ts` contains `hashPassword`, `E2E_ADMIN_PASSWORD`, `E2E_DATABASE_URL`, `"admin"`, `"approved"`, and a hostname check naming `localhost`.
    - With comments stripped, `e2e/seed-admin.ts` contains NO string literal that looks like a password and does NOT contain `@/db"` (the container-pinned db module).
    - With `E2E_ADMIN_PASSWORD` unset, `./node_modules/.bin/playwright test --project=setup` exits NON-ZERO and the message names `E2E_ADMIN_PASSWORD`. Run this negative proof; do not reason about it.
    - `./node_modules/.bin/tsc --noEmit` exits 0 and `./node_modules/.bin/eslint e2e playwright.config.ts` exits 0.
  </acceptance_criteria>
  <done>A dedicated admin exists in the dev database, one real-form login produced a gitignored storageState, and the negative proof for the missing-password guard was executed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → dev database | The seed writes a privileged user row |
| test harness → app | A real login produces a live JWT session cookie persisted to disk |
| repository → git remote | A committed session token or password is public forever |
| npm registry → repository | One new devDependency enters the build |
| CI pipeline → e2e suite | A test needing Docker+DB in a runner with neither is a permanently red gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Information disclosure | e2e/.auth/admin.json | mitigate | `/e2e/.auth/` added to `.gitignore` in task 1, BEFORE task 3 first writes the file; `git check-ignore` is an acceptance criterion, and `git status --porcelain e2e/.auth` must be empty |
| T-45-05 | Information disclosure | E2E_ADMIN_PASSWORD | mitigate | Generated by `openssl rand -base64 24` redirected straight into the gitignored `.env`; never printed, never in a spec, never in a config, never in a commit message or SUMMARY. The seed throws if the variable is unset rather than falling back |
| T-45-06 | Elevation of privilege | e2e admin row | mitigate | The row is `pipelite-e2e@local` — a dedicated account, not a borrowed human one. The guard rejects any `E2E_DATABASE_URL` whose hostname is not `localhost`/`127.0.0.1`, so the row cannot be created in a shared or production database |
| T-45-07 | Spoofing | Auth.js session | accept | The harness logs in through the real form rather than minting a JWT with `AUTH_SECRET`. Slower, but it exercises the code most worth defending — a broken login page fails the suite instead of being bypassed |
| T-45-08 | Denial of service | .github/workflows/ci.yml | mitigate | Playwright is deliberately NOT added to CI (V-3). `scripts.test` must not contain `playwright`, and `git diff --stat .github/workflows/ci.yml` must be empty — both are acceptance criteria |
| T-45-SC | Tampering | npm install @playwright/test | mitigate | Legitimacy audit complete and APPROVED: created 2020-09-24, 37,489,737 weekly downloads, `github.com/microsoft/playwright`, `npm view scripts` is `{}` so there is no postinstall. No `[ASSUMED]` or `[SUS]` package in this phase, so no blocking human checkpoint is required. `slopcheck install` is forbidden as a verification step — it runs `npm install` and writes to `dependencies` |
</threat_model>

<verification>
- `./node_modules/.bin/playwright test --project=setup` — green, twice in a row
- `git check-ignore` — all three new ignore paths honoured
- `npm run typecheck` / `npm run lint` / `npm run test` — unchanged and green
- `.github/workflows/ci.yml` — byte-identical to before this plan
</verification>

<success_criteria>
- `@playwright/test` is in devDependencies and recorded in package-lock.json
- `playwright.config.ts` carries `ignoreDefaultArgs: ["--hide-scrollbars"]` and no `webServer`
- `e2e/.auth/`, `/playwright-report/`, `/test-results/` are gitignored; `e2e`, `playwright.config.ts`, `playwright-report`, `test-results` are dockerignored
- A `pipelite-e2e@local` admin exists with role `admin` and status `approved`
- `e2e/.auth/admin.json` holds a working authenticated session and is invisible to git
- The missing-password negative proof was RUN and produced a named error
</success_criteria>

<output>
Create `.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-02-SUMMARY.md` when done
</output>
