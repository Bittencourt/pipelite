---
phase: 45-cross-cutting-ui-repair-and-uat-closure
plan: 09
type: execute
wave: 3
depends_on: ["45-01", "45-04"]
files_modified:
  - src/components/__tests__/admin-shell-wiring.test.ts
  - src/components/admin-sidebar.tsx
  - src/components/admin-mobile-bar.tsx
  - src/app/admin/layout.tsx
autonomous: true
requirements: [SC-1, SC-3]
must_haves:
  truths:
    - "The admin sidebar reads in the user's language on every entry"
    - "Below 768px the admin rail is replaced by a drawer, so /admin/audit fits a 320px viewport"
    - "The drawer closes when the user picks a destination"
    - "The /admin server authorization gate is unchanged and still authoritative"
  artifacts:
    - path: "src/components/admin-sidebar.tsx"
      provides: "one translated item array and one item renderer, consumed by both rail and drawer"
      contains: "useTranslations(\"admin.nav\")"
    - path: "src/components/admin-mobile-bar.tsx"
      provides: "md:hidden bar with the Sheet trigger and the admin.nav.title heading"
    - path: "src/app/admin/layout.tsx"
      provides: "hidden md:flex rail beside a min-w-0 flex-1 content column"
      contains: "min-w-0"
  key_links:
    - from: "src/components/admin-mobile-bar.tsx"
      to: "src/components/ui/sheet.tsx"
      via: "Sheet + SheetContent side=left w-64"
      pattern: "SheetContent"
    - from: "src/components/admin-sidebar.tsx"
      to: "admin.nav.*"
      via: "t(item.key) over the shared item array"
      pattern: "admin\\.nav"
---

<objective>
Collapse the admin shell below `md` and put its eleven hardcoded English strings into the catalog.

Purpose: `/admin/audit` measures `scrollWidth` 508 in pt-BR and 526 in es-ES against a 305px client
width, with `<main>` starting at x≈206px under an uncollapsed 256px rail. It degrades further as
translations get longer, which is why any fix that keeps the rail on screen keeps losing. And the
whole sidebar — "Admin Panel, Dashboard, User Management, Pipelines, Custom Fields, Webhooks, Audit
Log, Trash, Export Data, Pipedrive Import, Back to App" — is English in every locale.

Output: a translated, shared item array; a Sheet drawer behind a hamburger; a `min-w-0` content
column; and a source gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-CONTEXT.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-UI-SPEC.md
@.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-PATTERNS.md
</context>

<interfaces>
<!-- Target layout shape (UI-SPEC R-2). min-w-0 on the content column is load-bearing, not tidiness. -->

  <div className="flex min-h-[calc(100vh-3.5rem)]">
    <aside className="hidden md:flex w-64 border-r bg-background">  ...rail...
    <div className="flex min-w-0 flex-1 flex-col">
      <AdminMobileBar />                      // h-12 border-b px-4 flex items-center gap-2 md:hidden
      <main className="flex-1 p-6 bg-muted/30">{children}</main>

Without `min-w-0` on the content column its `min-width: auto` reproduces exactly the overflow this
plan removes. There is no in-repo precedent for this shape; `grep min-w-0` finds it only on inner
text nodes.

Fixed values (UI-SPEC § Spacing Scale, all resolved):
  breakpoint            md = 768px, matching the existing `hidden md:flex` on the main nav
  mobile bar height     h-12 (48px) — shorter than the 56px global header so the two stacked bars
                        cost 104px, not 112px, of a 640px-tall phone viewport
  drawer width          w-64 (256px), byte-identical to the desktop rail. This OVERRIDES the shadcn
                        block default `w-3/4 sm:max-w-sm`, which would render 240px at 320px and
                        575px at 767px — two different drawers at two viewports
  trigger               Menu lucide icon, ghost, size="icon-lg" (40px = size-10, button.tsx:31),
                        accessible name admin.nav.openMenu
  SheetContent          side="left" className="w-64 gap-0 p-0", plus aria-describedby={undefined}
  SheetTitle            renders admin.nav.title VISIBLY at text-lg font-semibold inside a
                        p-4 border-b header — the same heading the rail shows, not an sr-only duplicate

Message keys consumed (delivered by 45-01): the 12 `admin.nav.*` keys.

The 11 literals to remove, all in src/components/admin-sidebar.tsx:
  the `sidebarItems` array's 9 `title` values (9-61), the `<h2>Admin Panel</h2>` at 70, and the
  `Back to App` button label at 95.

Typography constraint: `leading-none` is forbidden on the SheetTitle. pt-BR renders
"Painel de Administração" — 22 characters at 256px minus 32px of padding — so it may wrap and must
not collide.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the admin-shell source gate (RED)</name>
  <files>src/components/__tests__/admin-shell-wiring.test.ts</files>
  <read_first>
    - src/components/admin-sidebar.tsx (in full — the array at 9-61 including the stale comments at
      35-38 and 44-45, the h2 at 70, the item renderer at 73-89, the footer button at 95)
    - src/app/admin/layout.tsx (in full — the auth gate at 10-18 and the render at 20-27)
    - src/components/nav-header.tsx (lines 39-48 — the `hidden md:flex` collapse idiom and the
      `t()`-on-every-link pattern this file must adopt)
    - src/components/bulk/__tests__/bulk-failure-report-wiring.test.ts (gate shape)
    - src/app/trash/__tests__/trash-client-wiring.test.ts (the multi-source variant)
    - src/components/custom-fields/__tests__/source-scan.ts (readStrippedSource)
  </read_first>
  <behavior>
    - Anti-vacuity 1: all three sources read non-empty (admin-sidebar.tsx, admin-mobile-bar.tsx,
      app/admin/layout.tsx), with the mobile bar's read guarded by `existsSync` so its absence
      reports as a named failure rather than aborting the file.
    - Anti-vacuity 2: positive markers — `sidebarItems` (or its renamed equivalent) in the sidebar
      source, `SheetContent` in the mobile bar source, `redirect` in the layout source.
    - RECOGNISED in admin-sidebar.tsx: `useTranslations("admin.nav")`, `hidden md:flex`.
    - LEFT_ALONE in admin-sidebar.tsx, every one of the 11 English literals: `>Admin Panel<`,
      `>Back to App<`, and the nine array values `"Dashboard"`, `"User Management"`, `"Pipelines"`,
      `"Custom Fields"`, `"Webhooks"`, `"Audit Log"`, `"Trash"`, `"Export Data"`, `"Pipedrive Import"`.
    - LEFT_ALONE in admin-sidebar.tsx and admin-mobile-bar.tsx: `useMediaQuery`, `window.innerWidth`,
      `useEffect` — the collapse is CSS only; a JS breakpoint in state either mismatches on hydration
      or trips `react-hooks/set-state-in-effect`, which is severity 2 here.
    - RECOGNISED in admin-mobile-bar.tsx: `SheetContent`, `side="left"`, `w-64`,
      `aria-describedby={undefined}`, `SheetTitle`, `admin.nav`, `openMenu`, `md:hidden`, `h-12`,
      `size="icon-lg"`.
    - LEFT_ALONE in admin-mobile-bar.tsx: `leading-none`.
    - RECOGNISED in app/admin/layout.tsx: `min-w-0`, `hidden md:flex`, `flex-1 p-6 bg-muted/30`,
      and — this is the security assertion — `session.user.role !== "admin"` and both `redirect(`
      calls. The message must say the drawer is presentation only and the server gate stays
      authoritative.
    - The shared-array assertion: the item list literal appears exactly ONCE across the two component
      files. Two copies of an 11-item list is how the next locale leak gets in.
    - All 12 `admin.nav.*` keys resolve to non-empty strings in the imported en-US catalog.
    - This file FAILS before task 2 and passes after task 3.
  </behavior>
  <action>
    Create `src/components/__tests__/admin-shell-wiring.test.ts` (a new directory) following the
    multi-source gate shape. Reuse the `FORBIDDEN_COLOURS` / `HEX_LITERAL` / `CLIENT_DIRECTIVE`
    regex constants that `bulk-failure-report-wiring.test.ts:49-73` and
    `trash-client-wiring.test.ts:26-41` both carry — duplicating them verbatim across gate files is
    this repo's accepted level of duplication for gate vocabulary.

    Two assertions need their reasoning in the message:

    - The `min-w-0` requirement on the content column. State that a flex item defaults to
      `min-width: auto` and refuses to shrink below its content, which is the mechanism behind the
      measured 508/526px scrollWidth, and that removing `min-w-0` reintroduces the exact defect.
    - The authorization assertions. State that making the sidebar a client-side drawer changes only
      presentation; `/admin/*` is gated twice — `middleware.ts`'s `authorized()` and this layout's
      `auth()` + role check — and this phase must not weaken either.

    Also assert the CLIENT_DIRECTIVE regex matches the first non-comment token of
    `admin-mobile-bar.tsx` (Sheet is a Radix Dialog and cannot render in a server component), and
    assert zero FORBIDDEN_COLOURS and zero HEX_LITERAL matches in both component files — the rule
    for this phase is zero hardcoded palette colours in the files it touches.
  </action>
  <verify>
    <automated>! ./node_modules/.bin/vitest run src/components/__tests__/admin-shell-wiring.test.ts &amp;&amp; echo "RED CONFIRMED"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/components/__tests__/admin-shell-wiring.test.ts` exits NON-ZERO.
    - The failure output names `admin-mobile-bar.tsx` and names at least one of the English literals (for example `Audit Log`).
    - The gate's LEFT_ALONE table for admin-sidebar.tsx contains all 11 literals.
    - The gate asserts `session.user.role !== "admin"` is present in `src/app/admin/layout.tsx`.
    - Every `expect(` carries a prose second argument.
    - `./node_modules/.bin/tsc --noEmit` exits 0 and `./node_modules/.bin/eslint src/components/__tests__` exits 0.
  </acceptance_criteria>
  <done>The gate exists, is red, and names both the missing drawer and the surviving English.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translate the sidebar and extract one shared item renderer</name>
  <files>src/components/admin-sidebar.tsx</files>
  <read_first>
    - src/components/admin-sidebar.tsx (in full)
    - src/components/nav-header.tsx (lines 40-48 — `t()` on every link is the pattern to adopt)
    - src/components/timeline/audit-entry.tsx (lines 226-229 — the key-stored-in-data then resolved
      through `t()` idiom, which is the shape for a translated data-driven array)
    - src/messages/en-US.json (the `admin.nav` object added by 45-01 — confirm all 12 keys)
    - src/components/__tests__/admin-shell-wiring.test.ts (the contract you are satisfying)
  </read_first>
  <behavior>
    - Every visible string in the admin sidebar comes from `admin.nav.*`.
    - The active-item highlight behaves exactly as before: `/admin` matches only on an exact pathname,
      every other item matches on `pathname.startsWith(item.href)`.
    - The item array and the item renderer each exist exactly once and are exported for the drawer
      to consume.
  </behavior>
  <action>
    Change each `sidebarItems` entry's `title` from an English string to a message KEY (for example
    `"dashboard"`, `"users"`, `"customFields"`), and render it through `t(item.<field>)` with
    `const t = useTranslations("admin.nav")`. Rename the field to something that reads as a key
    (`key` or `labelKey`) so a future reader does not mistake it for copy. Keep the `item.icon`
    capital-letter component reference — that is what makes the array data-driven and it must survive.

    Replace the `<h2>Admin Panel</h2>` at line 70 with `t("title")` and the `Back to App` label at
    line 95 with `t("backToApp")`.

    S-1: the comments at lines 35-38 and 44-45 justify the English literals with the argument
    "don't half-migrate a single entry". Migrating all eleven SATISFIES that argument rather than
    violating it — so REMOVE OR REWRITE those comments. The file must not ship a stale justification
    for a rule it no longer follows. Rewriting is preferred: a one-line note saying the array now
    holds keys and why is more useful than silence.

    EXTRACT, so the drawer can consume the same code:
    - export the item array (or a function returning it) from this file;
    - export a single item-renderer component (for example `AdminNavItems`) containing the existing
      `sidebarItems.map(...)` block at 73-89 verbatim — the `cn()` + `variant={isActive ? "secondary" : "ghost"}`
      idiom, the `w-full justify-start` classes, the `mr-2 h-4 w-4` icon size;
    - give the renderer an optional `onNavigate?: () => void` callback invoked on item click, so the
      drawer can close itself on navigation while the rail passes nothing.

    The rail's own `<aside>` gets `hidden md:flex` in task 3, when the layout changes shape. Do not
    add a Sheet to this file.

    Vocabulary constraint: the values are already written and locked in the catalog. Eight of the
    eleven have a translated twin under `admin.dashboard.*` or `admin.webhooks.title` and were copied
    verbatim from them, because the sidebar and the dashboard tile for the same route must not
    disagree. Do not "improve" any of them here.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; node -e "const s=require('fs').readFileSync('src/components/admin-sidebar.tsx','utf8'); for (const lit of ['>Admin Panel<','>Back to App<','\"Dashboard\"','\"User Management\"','\"Custom Fields\"','\"Audit Log\"','\"Export Data\"','\"Pipedrive Import\"']) if (s.includes(lit)) throw new Error('literal survives: '+lit); if(!s.includes('useTranslations(\"admin.nav\")')) throw new Error('no admin.nav hook'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/admin-sidebar.tsx` contains `useTranslations("admin.nav")`.
    - It contains none of the 11 English literals listed in this plan's `<interfaces>`.
    - `grep -c "half-migrating" src/components/admin-sidebar.tsx` is 0 — the stale justification is gone or rewritten.
    - The file exports both the item array and a reusable item renderer.
    - `npm run typecheck` exits 0 and `npm run lint` exits 0.
    - `npm run test` exits 0.
    - Visiting `/admin` still highlights only the Dashboard entry — assert by reading the preserved `item.href === "/admin" ? pathname === "/admin" : pathname.startsWith(item.href)` expression in the diff.
  </acceptance_criteria>
  <done>The sidebar speaks the user's language and its item list exists once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the mobile bar with the Sheet drawer and reshape the admin layout (GREEN)</name>
  <files>src/components/admin-mobile-bar.tsx, src/app/admin/layout.tsx</files>
  <read_first>
    - src/components/ui/sheet.tsx (created by 45-04 — its exports, its `side="left"` base classes,
      and the `closeLabel` prop already routed through `common.close`)
    - src/components/admin-sidebar.tsx (as edited in task 2 — the exported array and renderer)
    - src/app/admin/layout.tsx (in full — the auth gate at 10-18 MUST NOT MOVE)
    - src/components/ui/button.tsx (line 31 — confirm `size="icon-lg"` resolves to `size-10`)
    - src/components/__tests__/admin-shell-wiring.test.ts (the contract you are satisfying)
    - .planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-UI-SPEC.md § R-2
  </read_first>
  <behavior>
    - At 768px and above: the rail is visible, the mobile bar is not, and nothing about the current
      admin layout changed.
    - Below 768px: the rail is not rendered, a 48px bar carries a hamburger and the "Admin Panel"
      heading, and `document.documentElement.scrollWidth <= clientWidth` holds on `/admin/audit` in
      all three locales.
    - Tapping the hamburger opens a 256px drawer from the left showing the same eleven entries.
    - Selecting any entry navigates AND closes the drawer — a drawer still open over the page it just
      navigated to reads as a failed tap.
    - Escape and an overlay tap close it, and focus returns to the trigger (Radix defaults; do not
      override).
    - The `/admin` server authorization gate is byte-identical.
  </behavior>
  <action>
    Create `src/components/admin-mobile-bar.tsx` as a `"use client"` module with a named export
    `AdminMobileBar`. Sheet is a Radix Dialog and cannot render in a server component.

    Structure: a `div` with `h-12 border-b px-4 flex items-center gap-2 md:hidden` containing, in
    order, the `SheetTrigger` button and the `admin.nav.title` heading at `text-lg font-semibold`.
    The trigger is a ghost `Button size="icon-lg"` with the lucide `Menu` icon and an accessible name
    from `t("openMenu")`.

    The drawer: `<SheetContent side="left" className="w-64 gap-0 p-0" aria-describedby={undefined}>`.
    - `w-64` is 256px, byte-identical to the desktop rail — one sidebar design, not two. It overrides
      the block's `w-3/4` through tailwind-merge (same `w-` group). The block's `sm:max-w-sm` is a
      different group and survives but is inert: a 384px max-width never reduces a 256px element.
    - `aria-describedby={undefined}` is the sanctioned suppression for Radix Dialog's
      missing-description warning and costs no new copy.
    - Inside, a `p-4 border-b` header containing `SheetTitle` rendering `t("title")` VISIBLY at
      `text-lg font-semibold` — the same heading the rail shows, not an sr-only duplicate. Do not
      put `leading-none` on it: pt-BR renders "Painel de Administração" at 256px minus 32px of
      padding and may wrap.
    - Below it, the SHARED item renderer exported by `admin-sidebar.tsx`, with `onNavigate` wired to
      close the drawer. Do not re-declare the eleven items here.
    - Keep the "Back to App" outline button in the drawer footer, using the same shared source as the
      rail.

    Hold the open state in this component with `useState`. That is a user-interaction state, not a
    breakpoint state — the collapse itself is `md:hidden` / `hidden md:flex`, pure CSS, with no
    `useMediaQuery` and no `window.innerWidth` anywhere. A hook-based breakpoint returns false on the
    server and the real value after an effect, which is either a hydration mismatch or a
    `react-hooks/set-state-in-effect` lint error (severity 2 in this repo).

    `src/app/admin/layout.tsx` — reshape the render only:
    - the current `<div className="flex min-h-[calc(100vh-3.5rem)]"><AdminSidebar /><main className="flex-1 p-6 bg-muted/30">`
      becomes the shape in this plan's `<interfaces>`: the rail wrapped in
      `<aside className="hidden md:flex w-64 border-r bg-background">`, then a
      `<div className="flex min-w-0 flex-1 flex-col">` holding `<AdminMobileBar />` and the existing
      `<main className="flex-1 p-6 bg-muted/30">`.
    - the mobile bar sits ABOVE `<main>` and OUTSIDE its `p-6`, so the trigger aligns to the page
      edge like the global header above it. It does NOT go in `NavHeader` — that would couple the
      global shell to one route group.
    - `min-w-0` on the content column is mandatory. Add a comment saying a flex item's default
      `min-width: auto` refuses to shrink below its content and is the mechanism behind the measured
      508/526px scrollWidth.

    DO NOT TOUCH the auth block at lines 10-18: `auth()`, `redirect("/login?callbackUrl=/admin")`,
    `session.user.role !== "admin"` then `redirect("/?error=unauthorized")`. The drawer is
    presentation; this stays authoritative, and `middleware.ts`'s independent gate stays too.

    Behavioural proof at a real 320px viewport is `e2e/viewport-320.spec.ts`, run in 45-11 after the
    single Docker rebuild. Do not rebuild here.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/components/__tests__/admin-shell-wiring.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/components/__tests__/admin-shell-wiring.test.ts` exits 0.
    - `src/components/admin-mobile-bar.tsx` exists, begins with `"use client"`, and exports `AdminMobileBar` as a named export.
    - Comments stripped, it contains `SheetContent`, `side="left"`, `w-64`, `aria-describedby={undefined}`, `SheetTitle`, `md:hidden`, `h-12`, `size="icon-lg"` and `openMenu`.
    - Comments stripped, it contains none of `useMediaQuery`, `window.innerWidth`, `leading-none`.
    - Comments stripped, `src/app/admin/layout.tsx` contains `min-w-0`, `hidden md:flex` and `flex-1 p-6 bg-muted/30`.
    - `git diff src/app/admin/layout.tsx` shows ZERO changes to lines containing `auth()`, `redirect(` or `session.user.role`.
    - `git diff --stat middleware.ts` is empty.
    - The eleven item labels are declared in exactly one file: `grep -l '"pipedriveImport"' src/components/*.tsx` returns exactly one path.
    - `npm run typecheck` exits 0, `npm run lint` exits 0, `npm run test` exits 0.
  </acceptance_criteria>
  <done>The admin shell collapses below md, the drawer shares one item list with the rail, and the server gate is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /admin/* | Every admin route is authorization-gated; the sidebar is only a menu |
| server layout → client drawer | The rail moves from server-rendered chrome to a client-side portal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-32 | Elevation of privilege | /admin authorization | mitigate | `/admin/*` is gated TWICE — `middleware.ts`'s `authorized()` (`isAdminPath && !isAdmin` redirects) and `src/app/admin/layout.tsx`'s `auth()` plus `session.user.role !== "admin"`. Making the sidebar a client-side drawer changes only presentation. The gate asserts both layout checks are still present, and `git diff --stat middleware.ts` must be empty. Hiding a menu item is never an access control |
| T-45-33 | Information disclosure | drawer contents | accept | The drawer lists the same routes the rail already listed to the same already-authorized admin. It renders no data |
| T-45-34 | Denial of service | horizontal overflow on /admin/audit | mitigate | This IS the defect: 508px (pt-BR) and 526px (es-ES) against a 305px client width makes the page unusable on a phone. `hidden md:flex` on the rail plus `min-w-0` on the content column removes it; `e2e/viewport-320.spec.ts` measures it in 45-11 |
| T-45-35 | Tampering | Radix Dialog focus trap | accept | Focus trap, scroll lock, Escape, overlay click and focus return to the trigger all come from the Sheet primitive. Radix's defaults are not overridden — hand-rolling any of them is where this class of component goes wrong |
| T-45-36 | Information disclosure | untranslated admin shell | mitigate | All 11 literals move into `admin.nav.*`, gated by `REQUIRED_SHELL_KEYS` (45-01) for presence and translation and by this plan's LEFT_ALONE table for their absence from the source |
| T-45-SC | Tampering | npm installs | mitigate | This plan installs nothing. `sheet.tsx` was added by 45-04 through the local shadcn devDependency |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/components/__tests__/admin-shell-wiring.test.ts` — red after task 1, green after task 3
- `npm run test` / `npm run typecheck` / `npm run lint` — green
- `git diff --stat middleware.ts` — empty
- Real-viewport proof is `e2e/viewport-320.spec.ts` in 45-11, after the single rebuild
</verification>

<success_criteria>
- Zero English literals remain in `src/components/admin-sidebar.tsx`
- The eleven items are declared once and rendered by both the rail and the drawer
- The rail is `hidden md:flex`; the content column carries `min-w-0`
- The drawer is 256px, opens from the left, and closes on navigation
- `src/app/admin/layout.tsx`'s auth block and `middleware.ts` are unchanged
</success_criteria>

<output>
Create `.planning/phases/45-cross-cutting-ui-repair-and-uat-closure/45-09-SUMMARY.md` when done
</output>
