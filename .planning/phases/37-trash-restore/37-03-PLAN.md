---
phase: 37-trash-restore
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/en-US.json
  - src/messages/es-ES.json
  - src/messages/pt-BR.json
  - src/messages/locale-parity.test.ts
autonomous: true
requirements: [TRASH-01]
must_haves:
  truths:
    - "Every string the trash surface renders exists in all three locales"
    - "Adding a trash string without listing it in the contract fails the suite instead of shipping ungated"
  artifacts:
    - path: "src/messages/en-US.json"
      provides: "The trash namespace plus nav.trash and the two admin.dashboard keys"
      contains: "\"trash\""
    - path: "src/messages/locale-parity.test.ts"
      provides: "REQUIRED_TRASH_KEYS and the exact-contract assertion"
      contains: "REQUIRED_TRASH_KEYS"
  key_links:
    - from: "src/messages/locale-parity.test.ts"
      to: "src/messages/{en-US,es-ES,pt-BR}.json"
      via: "keysMatching(inNamespace('trash')) compared against REQUIRED_TRASH_KEYS"
      pattern: "REQUIRED_TRASH_KEYS"
---

<objective>
Add the 61 new message keys the trash surface needs to all three locale catalogues, and extend
`locale-parity.test.ts` with a `REQUIRED_TRASH_KEYS` contract so drift, blanks, untranslated copy and
placeholder loss all fail the suite.

Purpose: every later plan in this phase renders through these keys. Shipping them first means no UI
plan has to invent copy, and the exact-contract assertion means no UI plan can add an ungated string.
Output: 58 `trash.*` keys, 2 `admin.dashboard.*` keys and 1 `nav.trash` key in each of three files,
plus the five parity assertions extended to cover them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-trash-restore/37-CONTEXT.md
@.planning/phases/37-trash-restore/37-PATTERNS.md
@.planning/phases/37-trash-restore/37-UI-SPEC.md

<interfaces>
The machinery to extend, in `src/messages/locale-parity.test.ts`:

  export const REQUIRED_AUDIT_KEYS: string[] = [ ... ]              // line 73
  const AUDIT_DASHBOARD_KEYS = ["admin.dashboard.auditLog", "admin.dashboard.auditLogDescription"]  // line 228
  const auditKeys = keysMatching(
    (key) => inNamespace(AUDIT_NAMESPACE)(key) || AUDIT_DASHBOARD_KEYS.includes(key),
  )                                                                  // line 243
  function expectIdenticalKeySets(scoped: Record<Locale, string[]>, label: string): void  // line 310

  Five `it` blocks at lines 318, 324, 340, 345, 352 each assert over both
  REQUIRED_NOTE_KEYS and REQUIRED_AUDIT_KEYS. The block at 324 additionally asserts that the SHIPPED
  audit key set equals `[...REQUIRED_AUDIT_KEYS].sort()` exactly.

Keys that must be REUSED, never duplicated under `trash.*` (UI-SPEC § Reused keys):
  nav.deals, nav.people, nav.organizations, nav.activities
  audit.actorKind.workflowRun, audit.actorKind.apiKey, audit.actorKind.import, audit.actorKind.system
  audit.unknownActor
  common.actions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 61 trash keys to all three locale catalogues</name>
  <files>src/messages/en-US.json, src/messages/es-ES.json, src/messages/pt-BR.json</files>
  <read_first>
    - .planning/phases/37-trash-restore/37-UI-SPEC.md § "New key inventory (en-US) — 61 keys" (the authoritative en-US copy for every key, verbatim)
    - src/messages/en-US.json (the existing `audit` namespace, for placement and nesting style; and the existing `nav` and `admin.dashboard` objects)
    - src/messages/es-ES.json (the existing `audit` namespace, for the es-ES register and how ICU plurals are written there)
    - src/messages/pt-BR.json (same, for pt-BR)
  </read_first>
  <action>
Add a new top-level `trash` namespace to each of the three catalogues, plus one key in the existing
`nav` object and two in the existing `admin.dashboard` object. The en-US strings are fixed by
`37-UI-SPEC.md § New key inventory` and must be copied verbatim — do not paraphrase them.

The 61 keys, by group (58 under `trash.*`, 2 under `admin.dashboard.*`, 1 under `nav.*`):

  - `nav.trash` — 1.
  - `trash.title`, `trash.description` — 2.
  - `trash.column.{deal,person,organization,activity,deletedAt,deletedBy,email,website,dueDate}` — 9.
    Four singular entity nouns rather than one "Record" header, because es-ES and pt-BR inflect
    articles and adjectives with the noun's gender and these four strings are reused inside the purge
    dialog description.
  - `trash.actor.notRecorded`, `trash.actor.notRecordedTitle` — 2.
  - `trash.linkedInTrash`, `trash.linkedInTrashTitle` — 2. The second carries a `{names}` placeholder.
  - `trash.restore`, `trash.restoring`, `trash.restoreWithLinked`, `trash.deletePermanently`, `trash.deleting` — 5.
  - `trash.restored` (`{name}`, `{list}`), `trash.restoredWithLinked` (ICU plural on `{count}`), `trash.openRecord`, `trash.purged` (`{name}`) — 4.
  - `trash.error.{restoreFailed,alreadyPurged,purgeFailed,purgeNotPermitted,unavailable}` — 5.
  - `trash.empty.{deals,people,organizations,activities,body,bodyNoRetention}` — 6. `body` carries `{days}`.
  - `trash.purgeDialog.{title,description,cancel,confirm}` — 4. `description` carries `{name}`.
  - `trash.loadMore` — 1.
  - `trash.retention.{title,description,windowTitle,windowLabel,windowHelp,notSet,save,saving,saved,saveFailed,costTitle,recordsLabel,oldestLabel,oldestNone}` plus
    `trash.retention.shortenDialog.{title,description,cancel,confirm}` — 18. `shortenDialog.description` carries `{days}`.
  - `admin.dashboard.trash`, `admin.dashboard.trashDescription` — 2.

Two copy constraints that are not negotiable:

  - `trash.retention.windowHelp` states the range "between 1 and 365" in every locale, matching
    `RETENTION_MIN`/`RETENTION_MAX` in `src/lib/trash/settings.ts` and the `min`/`max` on the number
    `Input`. If those three ever disagree the UI lies about its own configuration.
  - `trash.empty.bodyNoRetention` exists precisely so the empty state never promises a `{days}` window
    when the retention setting is unset or unparseable. It must not be translated as if a default
    applied.

Copy rules inherited from the two prior UI specs and binding here: every button names its object (no
bare "Save", "Cancel", "Confirm", "OK", "Yes", "Apply" anywhere in these strings, including inside both
confirmation dialogs); no exclamation marks; every error names the failed action and the next step;
the copy is factual, never editorial.

For es-ES and pt-BR, write real translations. Match the register the existing `audit` namespace uses in
each file. Preserve every interpolation placeholder exactly — `{name}`, `{list}`, `{days}`, `{names}`,
`{count}` — and keep the ICU plural structure of `trash.restoredWithLinked` valid in each locale
(es-ES and pt-BR both use `one`/`other`).

Keep the JSON key ordering and indentation style of each file as it already is; place the `trash`
namespace so the file stays readable (alphabetical if the file is alphabetical, otherwise beside
`audit`).
  </action>
  <verify>
    <automated>node -e "const l=['en-US','es-ES','pt-BR'].map(n=>require('./src/messages/'+n+'.json')); const flat=o=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v!==null?flat(v).map(s=>k+'.'+s):[k]); const sets=l.map(m=>flat(m).filter(k=>k.startsWith('trash.')||k==='nav.trash'||k==='admin.dashboard.trash'||k==='admin.dashboard.trashDescription').sort()); if(sets[0].length!==61) throw new Error('expected 61 keys, got '+sets[0].length); if(JSON.stringify(sets[0])!==JSON.stringify(sets[1])||JSON.stringify(sets[1])!==JSON.stringify(sets[2])) throw new Error('locale key sets diverge'); console.log('61 keys, identical across 3 locales')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `61 keys, identical across 3 locales`.
    - `rtk proxy grep -c 'actorKind' src/messages/en-US.json` is unchanged from before this task — the four `audit.actorKind.*` keys are reused, not duplicated under `trash.*`.
    - `node -e "const m=require('./src/messages/en-US.json'); if(!/between 1 and 365/.test(m.trash.retention.windowHelp)) throw new Error('windowHelp must state the 1-365 range')"` exits 0.
    - Every one of the three files parses as JSON (`node -e "['en-US','es-ES','pt-BR'].forEach(n=>require('./src/messages/'+n+'.json'))"` exits 0).
    - No `trash.*` value is the empty string in any locale.
  </acceptance_criteria>
  <done>All 61 keys exist in all three catalogues with real translations and intact placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Extend locale-parity.test.ts with REQUIRED_TRASH_KEYS</name>
  <files>src/messages/locale-parity.test.ts</files>
  <read_first>
    - src/messages/locale-parity.test.ts (read in full — the `REQUIRED_AUDIT_KEYS` list at 73-113, the `AUDIT_DASHBOARD_KEYS` / `auditKeys` scoper at 228-244, `expectIdenticalKeySets` at 310, and all five `it` blocks at 318-357)
    - .planning/phases/37-trash-restore/37-PATTERNS.md § `src/messages/locale-parity.test.ts`
  </read_first>
  <action>
Extend the existing machinery three times, mirroring `REQUIRED_AUDIT_KEYS` exactly. Do not create a
new test file and do not copy whole `it` blocks — add a third expectation to each existing one, which
is what keeps the failure diff naming which contract broke.

  1. `export const REQUIRED_TRASH_KEYS: string[]` beside `REQUIRED_AUDIT_KEYS`, listing all 61 dot
     paths from Task 1, grouped with a comment per group carrying that group's count (shell 2,
     columns 9, actor 2, linked 2, actions 5, results 4, errors 5, empty 6, purge dialog 4,
     pagination 1, retention 18, dashboard 2, nav 1 — total 61). The comments are load-bearing: they
     are how a future reader sees at a glance that a group lost a key.
  2. A `TRASH_NAMESPACE = "trash"` const, a `TRASH_EXTRA_KEYS = ["admin.dashboard.trash",
     "admin.dashboard.trashDescription", "nav.trash"]` const, and
     `const trashKeys = keysMatching((key) => inNamespace(TRASH_NAMESPACE)(key) || TRASH_EXTRA_KEYS.includes(key))`
     — the same shape as the `auditKeys` scoper at line 243.
  3. In each of the five `it` blocks, add the trash expectation as a third statement, and rename the
     `it` titles to include "trash" so the reporter names all three contracts:
       - existence: `expect(missingIn(REQUIRED_TRASH_KEYS)).toEqual(emptyPerLocale)`
       - key-set identity: `expectIdenticalKeySets(trashKeys, TRASH_NAMESPACE)` plus the exact-contract
         loop comparing each locale's `trashKeys` to `[...REQUIRED_TRASH_KEYS].sort()`. That
         exact-contract check is the point: a string added to the `trash` namespace without its dot
         path going into `REQUIRED_TRASH_KEYS` must fail here rather than ship ungated.
       - non-empty: `expect(blankIn(REQUIRED_TRASH_KEYS)).toEqual(emptyPerLocale)`
       - not-untranslated-in-both: `expect(untranslatedInBoth(REQUIRED_TRASH_KEYS)).toEqual([])`
       - placeholder survival: `expect(placeholderDrift(REQUIRED_TRASH_KEYS)).toEqual({})`

Pass `REQUIRED_TRASH_KEYS` as its own argument in each assertion — never concatenated with the note or
audit lists — so a failure diff names which contract broke, which is the reason the existing file
keeps them separate.
  </action>
  <verify>
    <automated>npx vitest run src/messages/locale-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/messages/locale-parity.test.ts` passes with every existing test still green.
    - `node -e "const {REQUIRED_TRASH_KEYS}=require('ts-node')" ` is NOT required; instead assert with `rtk proxy grep -c '"trash\.' src/messages/locale-parity.test.ts` returning at least 58.
    - Deliberately breaking the contract fails: temporarily add `"trash.zzz": "x"` to `src/messages/en-US.json`, run `npx vitest run src/messages/locale-parity.test.ts`, observe a failure naming the trash key set, then remove it and confirm green again. Record both outcomes in the summary.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>The 61 trash strings are gated by the same five assertions that already gate the notes and audit namespaces, and the gate is proven to fire.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translation catalogue → rendered page | Copy is what tells a user what a destructive action will do; wrong copy is a safety failure, not a cosmetic one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-12 | Tampering | `trash.retention.windowHelp` vs `RETENTION_MIN`/`RETENTION_MAX` | mitigate | The copy states "between 1 and 365" and an acceptance check asserts that literal; the same two numbers appear in `settings.ts` and on the `Input`. A range the UI advertises but the validator rejects trains operators to distrust the form |
| T-37-13 | Tampering | `trash.empty.bodyNoRetention` | mitigate | A separate key exists for the unset-retention case so the empty state never promises an automatic purge the system will not perform when the setting is missing or unparseable |
| T-37-14 | Repudiation | `trash.purgeDialog.description` | mitigate | The confirmation states what survives ("Its change history is kept") as well as what dies, so an admin is not led to believe a purge erases the evidence of the purge |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | This phase installs nothing (`37-RESEARCH.md § Package Legitimacy Audit`) |
</threat_model>

<verification>
- `npx vitest run src/messages/locale-parity.test.ts` green
- All three catalogues parse and hold identical trash key sets
- `npm run typecheck` exits 0 and `npm run lint` reports 0 errors
</verification>

<success_criteria>
Every string the trash surface will render exists, translated, in all three locales, and the suite
fails if a later plan adds a trash string without registering it in the contract.
</success_criteria>

<output>
Create `.planning/phases/37-trash-restore/37-03-SUMMARY.md` when done
</output>
