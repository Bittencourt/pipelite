---
phase: 33-database-indexes-for-the-crm-core
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified:
  - drizzle/0012_*.sql
  - drizzle/meta/0012_snapshot.json
  - drizzle/meta/_journal.json
  - .planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md
autonomous: true
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "Exactly one new generated migration file exists (expected drizzle/0012_*.sql) and it contains only CREATE INDEX statements — no ALTER, no DROP, no CREATE TABLE (SC-3, and RESEARCH.md Pitfall 8 verified schema and snapshot 0011 are in sync so no drift DDL should appear)"
    - "The generated SQL contains no CONCURRENTLY (D-03), no WHERE clause on any CREATE INDEX (D-02), and no statement naming both stage_id and position (D-04)"
    - "The migration was generated by drizzle-kit from the schema, never hand-authored, and drizzle/meta/0012_snapshot.json plus the _journal.json entry are committed alongside it (D-06 — omitting the meta files is what produced the 0009 to 0010 index loss)"
    - "npm run db:migrate exits 0 and applies only 0012 — drizzle.__drizzle_migrations goes from 4 rows to 5 (RESEARCH.md Pitfall 9: the migrator compares against max created_at only, so the 8 unrecorded db:push-era migrations are never re-run and must not be 'fixed')"
    - "The pg_indexes CTE assertion returns index_backed = t for all eleven target columns — this catalog query is the ONLY proof path for deals.owner_id (D-05, n_distinct = 1 means the planner correctly ignores that index forever)"
    - "The kanban AFTER plan shows Bitmap Heap Scan on deals fed by Bitmap Index Scan on deals_stage_id_idx, which SATISFIES SC-1 (D-01) — a literal Index Scan node is physically unachievable for a ~3,753-row scattered fetch and its absence is NOT a failure"
    - "The reminder-cron AFTER plan shows a literal Index Scan using activities_due_date_idx with cost dropping from 5071.99 to ~12.21 and buffers from ~3294 to ~5 (SC-2)"
    - "The Closer pipeline keeping a Seq Scan at 61% selectivity, and deals.owner_id not using its index, are recorded in the artifact as correct planner behaviour, not gaps"
    - "random_page_cost remains 4 — no server configuration was changed (D-08)"
    - "npm test, npm run typecheck and npm run lint all pass with zero *.test.ts files modified anywhere in the phase (SC-4)"
    - "No row in deals, activities, organizations or people was inserted, updated, deleted or truncated at any point"
  artifacts:
    - path: "drizzle/0012_*.sql"
      provides: "The single generated migration carrying all eleven CREATE INDEX statements (SC-3)"
      contains: "CREATE INDEX"
    - path: "drizzle/meta/0012_snapshot.json"
      provides: "The snapshot that keeps schema and migration history in agreement, preventing a future generate from DROPping these indexes"
      contains: "deals_stage_id_idx"
    - path: ".planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md"
      provides: "Completed BEFORE/AFTER evidence artifact with the SC-1..SC-4 verdicts and the D-01/D-05/D-08 caveats"
      contains: "Bitmap Index Scan on deals_stage_id_idx"
  key_links:
    - from: "src/db/schema/{deals,activities,people,organizations}.ts"
      to: "drizzle/0012_*.sql"
      via: "npm run db:generate (drizzle-kit diff against drizzle/meta/0011_snapshot.json)"
      pattern: "CREATE INDEX \"deals_stage_id_idx\""
    - from: "drizzle/0012_*.sql"
      to: "PostgreSQL pg_index catalog"
      via: "npm run db:migrate (session.transaction wrapping all 11 statements)"
      pattern: "index_backed"
    - from: "PostgreSQL pg_index catalog"
      to: "planner plan choice"
      via: "EXPLAIN (ANALYZE, BUFFERS, COSTS) via verify-plans.sql"
      pattern: "(Bitmap Index Scan on deals_stage_id_idx|Index Scan using activities_due_date_idx)"
---

<objective>
Generate the single index migration, hard-gate its contents before it touches the database, apply it, then re-run the plan-33-01 capture script unchanged to produce the AFTER half of the evidence artifact and close SC-1 through SC-4.

Purpose: this is the plan where the phase becomes real and irreversible-ish. Types come from the schema files, not the live database, so `tsc` passing proves nothing about whether the migration ran — every claim here is settled by querying the live catalog and the live planner. The generated SQL is asserted index-only BEFORE `db:migrate` is allowed to run, because that assertion is the only thing standing between a schema diff and DDL executed against a database holding real imported customer CRM data.

Output: `drizzle/0012_*.sql` plus its meta snapshot and journal entry, all eleven indexes live in `pg_index`, and `33-PLANS.md` completed with side-by-side BEFORE/AFTER plans and explicit SC verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-database-indexes-for-the-crm-core/33-CONTEXT.md
@.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md
@.planning/phases/33-database-indexes-for-the-crm-core/33-VALIDATION.md
@.planning/phases/33-database-indexes-for-the-crm-core/33-01-SUMMARY.md
@.planning/phases/33-database-indexes-for-the-crm-core/33-02-SUMMARY.md
@.planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql
@.planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md

<expected_measurements>
Every number below was measured on 2026-08-14 against this exact database (RESEARCH.md
`## Measured AFTER Plans`, lines 257-277), by creating the full 11-index set inside a transaction and
rolling it back. Costs and buffer counts are reproducible; execution TIMES fluctuate and are NOT graded.

| Statement | BEFORE | AFTER | Node to look for |
|-----------|--------|-------|------------------|
| Q1 kanban, BDR default (14.9%) | `Seq Scan on deals`, cost ~2729, ~2414 buffers | cost ~2614, ~426 buffers on the deals node | `Bitmap Heap Scan on deals` ← `Bitmap Index Scan on deals_stage_id_idx` |
| Q2 kanban, single stage | `Seq Scan on deals` | cost ~2623 | `Bitmap Index Scan` (or `Index Scan`) on `deals_stage_id_idx` |
| Q3 reminder cron | `Seq Scan on activities`, cost **5071.99**, **3294** buffers, Rows Removed 79023 | cost **12.21**, **5** buffers | literal `Index Scan using activities_due_date_idx` |
| Q4 catalog | eleven `f` rows | eleven `t` rows | — |

Index footprint added: 7328 kB total across 11 indexes. Total `CREATE INDEX` wall time: ~1.08 s.
Migration ledger: `drizzle.__drizzle_migrations` holds 4 rows before, 5 after.
</expected_measurements>

<environment_notes>
- Postgres is in Docker; `psql` is NOT on the host. Cache sudo once with `echo "883112" | sudo -S -v`,
  then use `sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite ...` from the repo root.
  `sudo -S` collides with stdin redirection, which is why credentials are cached first.
- `npm run db:migrate` connects to `localhost:5433` via `drizzle.config.ts`. It does NOT need Docker exec.
- Do NOT start a dev server. Do NOT run `npm run db:push` — it writes no migration file and SC-3 requires one.
- The `rtk` hook swallows `npm`/`npx` output and mangles `grep -c` / `wc -l`. Run every npm command as
  `rtk proxy "<cmd>" > /tmp/<file>.txt 2>&1` and assert on the file with `node -e`.
- Whole-phase scope anchor: the commit immediately preceding this phase is `5a88626`. If that SHA is not
  an ancestor of HEAD (e.g. after a rebase), substitute the equivalent pre-phase commit rather than
  skipping the check.
</environment_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the migration and hard-gate its contents (no DB writes)</name>

  <read_first>
    - `src/db/schema/deals.ts`, `src/db/schema/activities.ts`, `src/db/schema/people.ts`,
      `src/db/schema/organizations.ts` — the eleven declarations from plan 33-02, to cross-check the
      generated SQL against.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `### Confirming the
      generated migration is index-only` (lines 560-570), which shows the exact expected output shape,
      and `### Pitfall 8` (lines 484-487), which records that a scratch generate against the real
      `0011_snapshot.json` emitted `CREATE INDEX` statements ONLY — schema and snapshot are in sync and
      there is no drift, so any extra DDL is a red flag rather than an expected artefact.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `### Pitfall 6`
      (lines 474-477) on why `drizzle/meta/` must be committed with the SQL.
    - `drizzle/meta/_journal.json` — currently ends at `idx: 11, tag: 0011_simple_darwin,
      when: 1774729567507`. The new entry must be `idx: 12` with a larger `when`.
  </read_first>

  <files>drizzle/0012_*.sql, drizzle/meta/0012_snapshot.json, drizzle/meta/_journal.json</files>

  <action>
    Run `rtk proxy "npm run db:generate" > /tmp/33-gen.txt 2>&1` from the repo root. This only reads the
    schema and writes files under `drizzle/` — it makes no database connection and no DDL is executed.

    If drizzle-kit prompts interactively for anything (a rename or ambiguity resolution), STOP: that means
    unexpected schema drift, which contradicts the verified state of `0011_snapshot.json`. Do not answer
    the prompt, do not proceed to plan-33-03 Task 2, and report the prompt text.

    Locate the emitted file by reading `drizzle/meta/_journal.json`'s last entry tag, or by listing
    `drizzle/` for a filename matching `0012_*.sql`. Read the file in full.

    Then gate it. The gate is BLOCKING: if any assertion below fails, do not run `db:migrate` in Task 2,
    and do not hand-edit the generated SQL or the snapshot to make it pass (hand-editing a snapshot is
    exactly what produced the `0009` to `0010` index loss). Fix the schema declaration and regenerate.

    The gate asserts: exactly eleven `CREATE INDEX` statements; every statement in the file, once comments
    and `--> statement-breakpoint` separators are stripped, begins with `CREATE INDEX`; all eleven expected
    index names are present (`deals_stage_id_idx`, `deals_organization_id_idx`, `deals_person_id_idx`,
    `deals_owner_id_idx`, `deals_deleted_at_idx`, `activities_due_date_idx`, `activities_deal_id_idx`,
    `activities_deleted_at_idx`, `people_organization_id_idx`, `people_deleted_at_idx`,
    `organizations_deleted_at_idx`); the word `CONCURRENTLY` never appears (D-03); the word `WHERE` never
    appears, which is what proves all eleven are plain rather than partial (D-02); no single statement
    mentions both `stage_id` and `position` (D-04); and none of `DROP`, `ALTER TABLE`, `CREATE TABLE` or
    `ADD COLUMN` appears anywhere.

    Also assert exactly one new `.sql` file landed in `drizzle/` — no existing migration was modified —
    and that `drizzle/meta/0012_snapshot.json` was created and `drizzle/meta/_journal.json` gained an
    `idx: 12` entry. Commit the `.sql`, the snapshot and the journal together in one commit; separating
    them is how the snapshot drifts from history.
  </action>

  <acceptance_criteria>
    - `/tmp/33-gen.txt` contains no `error` and no unanswered prompt text.
    - Exactly one file matching `^0012_.*\.sql$` exists in `drizzle/`, and `git status --short drizzle/`
      shows no modification to any of `0000_*.sql` through `0011_*.sql`.
    - That file contains exactly 11 occurrences of `CREATE INDEX`.
    - Every non-empty line of that file, after stripping `--` comment lines and `--> statement-breakpoint`
      markers, starts with `CREATE INDEX`.
    - All eleven expected index-name string literals are present in the file.
    - The file contains 0 case-insensitive occurrences of each of: `CONCURRENTLY`, `WHERE`, `DROP`,
      `ALTER TABLE`, `CREATE TABLE`, `ADD COLUMN`.
    - No single `CREATE INDEX` statement contains both `stage_id` and `position`.
    - `drizzle/meta/0012_snapshot.json` exists and contains `deals_stage_id_idx`.
    - `drizzle/meta/_journal.json` last entry has `idx` 12 and a `when` value greater than 1774729567507.
    - No database connection was made and no DDL executed: the non-pkey index count across
      deals/activities/people/organizations is still 0, and `drizzle.__drizzle_migrations` still holds 4 rows.
  </acceptance_criteria>

  <verify>
    <automated>echo "883112" | sudo -S -v && sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite -tAc "SELECT 'PRE='||(SELECT count(*) FROM pg_indexes WHERE tablename IN ('deals','activities','people','organizations') AND indexname NOT LIKE '%_pkey')||'|'||(SELECT count(*) FROM drizzle.__drizzle_migrations)" > /tmp/33-pre.txt 2>&1; node -e "const fs=require('fs'),cp=require('child_process');const fail=[];const f=fs.readdirSync('drizzle').filter(x=>/^0012_.*\.sql\$/.test(x));if(f.length!==1){fail.push('expected 1 0012_*.sql, found '+f.length+': '+f.join(','))}else{const p='drizzle/'+f[0];const s=fs.readFileSync(p,'utf8');const ci=(s.match(/CREATE INDEX/g)||[]).length;if(ci!==11)fail.push('CREATE INDEX count '+ci+' != 11');['deals_stage_id_idx','deals_organization_id_idx','deals_person_id_idx','deals_owner_id_idx','deals_deleted_at_idx','activities_due_date_idx','activities_deal_id_idx','activities_deleted_at_idx','people_organization_id_idx','people_deleted_at_idx','organizations_deleted_at_idx'].forEach(n=>{if(!s.includes(n))fail.push('missing index '+n)});['CONCURRENTLY','WHERE','DROP','ALTER TABLE','CREATE TABLE','ADD COLUMN'].forEach(t=>{if(s.toUpperCase().includes(t))fail.push('FORBIDDEN in generated SQL: '+t)});s.split('--> statement-breakpoint').map(x=>x.split('\n').filter(l=>l.trim()&&!l.trim().startsWith('--')).join(' ').trim()).filter(Boolean).forEach(st=>{if(!st.startsWith('CREATE INDEX'))fail.push('non-CREATE-INDEX statement: '+st.slice(0,80));if(st.includes('stage_id')&&st.includes('position'))fail.push('FORBIDDEN composite (stage_id, position) - D-04')})}const snap='drizzle/meta/0012_snapshot.json';if(!fs.existsSync(snap))fail.push('missing '+snap);else if(!fs.readFileSync(snap,'utf8').includes('deals_stage_id_idx'))fail.push('snapshot lacks deals_stage_id_idx');const j=JSON.parse(fs.readFileSync('drizzle/meta/_journal.json','utf8')).entries.slice(-1)[0];if(j.idx!==12)fail.push('journal last idx '+j.idx+' != 12');if(!(j.when>1774729567507))fail.push('journal when not newer than 0011');cp.execSync('git status --short drizzle/').toString().split('\n').filter(Boolean).forEach(l=>{if(/^ ?M.*drizzle\/00(0[0-9]|1[01])_/.test(l))fail.push('existing migration modified: '+l)});const pre=fs.readFileSync('/tmp/33-pre.txt','utf8').trim();if(!pre.includes('PRE=0|4'))fail.push('DB already mutated before apply (want PRE=0|4, got '+pre+')');if(/error/i.test(fs.readFileSync('/tmp/33-gen.txt','utf8')))fail.push('db:generate reported an error');console.log(fail.length?'FAIL '+fail.join(' | '):'PASS');process.exit(fail.length?1:0)"</automated>
  </verify>

  <done>Exactly one `drizzle/0012_*.sql` exists containing exactly eleven `CREATE INDEX` statements and nothing else — no `CONCURRENTLY`, no `WHERE`, no composite, no `DROP`/`ALTER`/`CREATE TABLE` — with its snapshot and journal entry present, no existing migration modified, and the database still untouched (0 non-pkey indexes, 4 ledger rows).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the migration and assert the catalog</name>

  <read_first>
    - `drizzle/0012_*.sql` — the gated migration from Task 1. Do not run this task if Task 1's gate failed.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `### Pitfall 9`
      (lines 489-491): `drizzle.__drizzle_migrations` holds only 4 rows for 12 migration files because
      0000-0007 were `db:push`ed. This is safe and must NOT be "fixed" — the migrator compares
      `folderMillis` against the single most recent record (max `created_at` = 1774729567507 = 0011), so
      only 0012 will run.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `### Pitfall 7`
      (lines 479-482): the migration runs inside `session.transaction()`, so all eleven statements commit
      or none do, and `ShareLock` per table blocks writes but not reads for ~1.08 s total.
    - `.planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql` — statement Q4, the catalog
      assertion, which is the only admissible proof for `deals.owner_id` (D-05).
  </read_first>

  <files>drizzle/meta/_journal.json</files>

  <action>
    Run `rtk proxy "npm run db:migrate" > /tmp/33-migrate.txt 2>&1` from the repo root. It connects to
    `localhost:5433` per `drizzle.config.ts`, wraps all eleven `CREATE INDEX` statements in one
    transaction, and inserts one row into `drizzle.__drizzle_migrations`. Expected wall time for the index
    builds is about 1.08 seconds; each build takes a `ShareLock` on its table, which blocks writes but not
    reads. Do not pass `--force`, do not use `db:push`, and do not run it twice.

    Then prove the result against the live catalog rather than against the schema. Cache sudo credentials,
    copy `verify-plans.sql` into the container as in plan 33-01, and run statement Q4 (the eleven-row
    `pg_indexes` CTE) — all eleven rows must now read `t`. This is the ONLY proof available for
    `deals.owner_id` (D-05: `n_distinct = 1`, so no EXPLAIN will ever show that index being chosen; do not
    attempt one).

    Additionally assert the exact set of eleven non-pkey index names now present across the four tables,
    that `drizzle.__drizzle_migrations` went from 4 rows to exactly 5 (only 0012 applied, nothing
    re-applied), that `random_page_cost` is still `4` (D-08 — no server configuration was changed), and
    that row counts are byte-for-byte unchanged: deals 25206, activities 79023, organizations 46055,
    people 38345.

    If `db:migrate` fails, the transaction rolls back and no index exists. Do not retry blindly: read the
    error, and if it mentions `CONCURRENTLY` then a `.concurrently()` call survived plan 33-02's gate and
    the schema must be fixed and regenerated.
  </action>

  <acceptance_criteria>
    - `/tmp/33-migrate.txt` shows no error and the command exited 0.
    - `psql -tAc` guard output is exactly `GUARD=25206|79023|46055|38345|11|5|4` — meaning: deals 25206 rows,
      activities 79023, organizations 46055, people 38345 (all unchanged), 11 non-pkey indexes across the
      four tables, 5 rows in `drizzle.__drizzle_migrations` (was 4, so only 0012 applied), and
      `random_page_cost` still 4 (D-08).
    - The sorted non-pkey index name list across the four tables is exactly:
      `activities_deal_id_idx,activities_deleted_at_idx,activities_due_date_idx,deals_deleted_at_idx,deals_organization_id_idx,deals_owner_id_idx,deals_person_id_idx,deals_stage_id_idx,organizations_deleted_at_idx,people_deleted_at_idx,people_organization_id_idx`
      — eleven names, no extras, no missing.
    - Statement Q4 of `verify-plans.sql` returns eleven rows with `index_backed = t` for every one,
      including the `deals` / `owner_id` row (D-05's only proof).
    - No `*.test.ts` file and no file under `src/` was modified by this task.
  </acceptance_criteria>

  <verify>
    <automated>rtk proxy "npm run db:migrate" > /tmp/33-migrate.txt 2>&1; echo "883112" | sudo -S -v && sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite -tAc "SELECT 'GUARD='||(SELECT count(*) FROM deals)||'|'||(SELECT count(*) FROM activities)||'|'||(SELECT count(*) FROM organizations)||'|'||(SELECT count(*) FROM people)||'|'||(SELECT count(*) FROM pg_indexes WHERE tablename IN ('deals','activities','people','organizations') AND indexname NOT LIKE '%_pkey')||'|'||(SELECT count(*) FROM drizzle.__drizzle_migrations)||'|'||current_setting('random_page_cost'); SELECT 'IDX='||string_agg(indexname,',' ORDER BY indexname) FROM pg_indexes WHERE tablename IN ('deals','activities','people','organizations') AND indexname NOT LIKE '%_pkey';" > /tmp/33-post.txt 2>&1; node -e "const fs=require('fs');const o=fs.readFileSync('/tmp/33-post.txt','utf8');const m=fs.readFileSync('/tmp/33-migrate.txt','utf8');const fail=[];if(!o.includes('GUARD=25206|79023|46055|38345|11|5|4'))fail.push('guard mismatch, want GUARD=25206|79023|46055|38345|11|5|4 got: '+o.replace(/\n/g,' '));const want='IDX=activities_deal_id_idx,activities_deleted_at_idx,activities_due_date_idx,deals_deleted_at_idx,deals_organization_id_idx,deals_owner_id_idx,deals_person_id_idx,deals_stage_id_idx,organizations_deleted_at_idx,people_deleted_at_idx,people_organization_id_idx';if(!o.includes(want))fail.push('index name set mismatch; want '+want);if(/error|ERROR|CONCURRENTLY/.test(m))fail.push('db:migrate error: '+m.slice(-400));console.log(fail.length?'FAIL '+fail.join(' | '):'PASS');process.exit(fail.length?1:0)"</automated>
  </verify>

  <done>`npm run db:migrate` exited 0 having applied only 0012; all eleven expected indexes exist in `pg_index` under exactly their expected names; the Q4 catalog assertion returns eleven `t` rows including `deals.owner_id` (D-05 satisfied); `drizzle.__drizzle_migrations` is at 5 rows; `random_page_cost` is still 4 (D-08); and all four tables' row counts are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Capture AFTER plans, complete the evidence artifact, and close SC-1..SC-4</name>

  <read_first>
    - `.planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql` — re-run UNCHANGED. Do not
      edit it; identity with the BEFORE run is what makes the comparison sound.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md` — the BEFORE half and the
      pre-stated acceptance block from plan 33-01, plus the `## AFTER (11 indexes)` placeholder line to
      replace.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `## Measured AFTER Plans`
      (lines 257-277) for the expected nodes, costs and buffer counts; `### Pitfall 1` (lines 449-453) for
      the `Closer` pipeline caveat; `### Pitfall 2` (lines 455-458) for the bitmap node-name trap;
      `### Pitfall 10` (lines 493-495) for the `deals.owner_id` caveat.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-VALIDATION.md` — the Per-Task Verification
      Map, for the exact SC-to-command mapping the artifact's verdict table must mirror.
    - `.planning/ROADMAP.md` — Phase 33's four success criteria, verbatim, so the verdict table quotes
      them rather than paraphrasing.
  </read_first>

  <files>.planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md</files>

  <action>
    Cache sudo credentials, copy `verify-plans.sql` into the container unchanged, and run all four
    statements exactly as plan 33-01 did, capturing stdout and stderr to a scratch file.

    Replace the `## AFTER (11 indexes)` placeholder line in `33-PLANS.md` with the verbatim output of all
    four statements under `### Q1` .. `### Q4` subheadings, mirroring the BEFORE structure so the two
    halves read side by side.

    Then append two sections.

    First, a `## Deltas` table with one row per statement giving BEFORE node, AFTER node, BEFORE cost,
    AFTER cost, BEFORE buffers, AFTER buffers, taken from the two captures actually in this file — not from
    RESEARCH.md. Note in a sentence beneath it that execution times are reported for interest only and are
    not graded, because they fluctuate run to run.

    Second, a `## Verdicts` table with one row per ROADMAP success criterion, quoting each criterion
    verbatim and giving the verdict plus the specific evidence line that establishes it:
    - SC-1: satisfied by Q1's `Bitmap Heap Scan on deals` fed by `Bitmap Index Scan on deals_stage_id_idx`,
      where the BEFORE half shows `Seq Scan on deals` for the identical statement. State again, in this
      table, that per D-01 the bitmap node form IS an index scan for grading purposes and that a literal
      `Index Scan using deals_stage_id_idx` is physically unachievable here — it must not be required.
      Reference Q2 as the wide-margin corroboration at lower selectivity.
    - SC-2: satisfied by Q3's literal `Index Scan using activities_due_date_idx`, with the cost and buffer
      deltas quoted.
    - SC-3: satisfied by Q4's eleven `t` rows plus the single generated migration `drizzle/0012_*.sql`.
      Name `deals.owner_id` explicitly as catalog-proven only (D-05).
    - SC-4: satisfied by the three Phase 32 gates passing and by the whole-phase scope check showing zero
      `*.test.ts` files touched.

    Close the artifact with a short `## Not gaps` list restating, one line each: the `Closer` pipeline
    (61% selectivity) correctly keeps a `Seq Scan`; `deals.owner_id` correctly never uses its index
    (`n_distinct = 1`); the `deleted_at IS NULL` read path is correctly NOT faster (those indexes serve the
    `IS NOT NULL` direction, which Phase 37 Trash & Restore needs); and `random_page_cost` stays at 4 by
    decision (D-08), which is the reason the selectivity crossover sits at 15-19% and is deferred to a
    later milestone. Also record the measured cost side: 7328 kB of added index storage and a ~1.08 s
    write-blocking `ShareLock` window per deploy.

    Finally run the three Phase 32 gates and the whole-phase scope check. Do not modify any test file to
    make anything pass — SC-4 forbids it and the scope check will catch it.
  </action>

  <acceptance_criteria>
    - `psql -f /tmp/33-verify-plans.sql` exits 0 and `verify-plans.sql` is byte-identical to its
      plan-33-01 version (`git diff --name-only HEAD` does not list it).
    - Q1 AFTER output contains `Bitmap Index Scan on deals_stage_id_idx`. It is NOT required to contain
      `Index Scan using deals_stage_id_idx`, and the absence of that literal is explicitly NOT a failure
      (D-01). A `Parallel Bitmap Heap Scan on deals` parent node is equally acceptable to
      `Bitmap Heap Scan on deals`.
    - Q1 AFTER output contains no `Hash Cond: (deals.stage_id = stages.id)`.
    - Q2 AFTER output contains either `Bitmap Index Scan` or `Index Scan` naming `deals_stage_id_idx`.
    - Q3 AFTER output contains the literal `Index Scan using activities_due_date_idx`, an estimated cost in
      the 8-25 range (measured 12.21, down from 5071.99), and a buffer count of 50 or fewer (measured 5,
      down from 3294).
    - Q4 AFTER output has exactly eleven data rows, every `index_backed` = `t`.
    - `33-PLANS.md` contains `## BEFORE (no indexes)`, `## AFTER (11 indexes)`, `## Deltas`, `## Verdicts`
      and `## Not gaps`; contains `Bitmap Index Scan on deals_stage_id_idx` and
      `Index Scan using activities_due_date_idx`; contains `SC-1`, `SC-2`, `SC-3`, `SC-4`, `D-01`, `D-05`,
      `D-08` and `Closer`; and the `## AFTER` section no longer contains the word `Pending`.
    - `33-PLANS.md` contains neither `883112` nor `DATABASE_URL`.
    - `rtk proxy "npm test" > /tmp/33-test-final.txt 2>&1` — zero failures reported.
    - `rtk proxy "npm run typecheck" > /tmp/33-tc-final.txt 2>&1` — no `error TS`.
    - `rtk proxy "npm run lint" > /tmp/33-lint-final.txt 2>&1` — clean.
    - Whole-phase scope check on `git diff --name-only 5a88626..HEAD`: no path ends in `.test.ts`, and every
      path not under `.planning/` is one of `src/db/schema/deals.ts`, `src/db/schema/activities.ts`,
      `src/db/schema/people.ts`, `src/db/schema/organizations.ts`, `drizzle/meta/_journal.json`,
      `drizzle/meta/0012_snapshot.json`, or a file matching `drizzle/0012_.*\.sql`.
    - Row counts still 25206 / 79023 / 46055 / 38345 and the non-pkey index count is still exactly 11.
  </acceptance_criteria>

  <verify>
    <automated>rtk proxy "npm test" > /tmp/33-test-final.txt 2>&1; rtk proxy "npm run typecheck" > /tmp/33-tc-final.txt 2>&1; rtk proxy "npm run lint" > /tmp/33-lint-final.txt 2>&1; echo "883112" | sudo -S -v && sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite -tAc "SELECT 'GUARD='||(SELECT count(*) FROM deals)||'|'||(SELECT count(*) FROM activities)||'|'||(SELECT count(*) FROM organizations)||'|'||(SELECT count(*) FROM people)||'|'||(SELECT count(*) FROM pg_indexes WHERE tablename IN ('deals','activities','people','organizations') AND indexname NOT LIKE '%_pkey')" > /tmp/33-final.txt 2>&1; node -e "const fs=require('fs'),cp=require('child_process');const fail=[];const p=fs.readFileSync('.planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md','utf8');['## BEFORE','## AFTER','## Deltas','## Verdicts','## Not gaps','Bitmap Index Scan on deals_stage_id_idx','Index Scan using activities_due_date_idx','SC-1','SC-2','SC-3','SC-4','D-01','D-05','D-08','Closer'].forEach(t=>{if(!p.includes(t))fail.push('33-PLANS.md missing: '+t)});['883112','DATABASE_URL'].forEach(t=>{if(p.includes(t))fail.push('33-PLANS.md LEAKS: '+t)});const after=p.split('## AFTER')[1]||'';if(after.includes('Pending'))fail.push('AFTER section still says Pending');if(!/Seq Scan on deals/.test(p.split('## AFTER')[0]||''))fail.push('BEFORE half lost its Seq Scan on deals evidence');if(!fs.readFileSync('/tmp/33-final.txt','utf8').includes('GUARD=25206|79023|46055|38345|11'))fail.push('final DB guard failed: '+fs.readFileSync('/tmp/33-final.txt','utf8').trim());if(/error TS/.test(fs.readFileSync('/tmp/33-tc-final.txt','utf8')))fail.push('typecheck errors');const t=fs.readFileSync('/tmp/33-test-final.txt','utf8');if(/[1-9][0-9]* failed/.test(t)||/FAIL \([1-9]/.test(t))fail.push('test failures: '+t.slice(-400));const allow=['src/db/schema/deals.ts','src/db/schema/activities.ts','src/db/schema/people.ts','src/db/schema/organizations.ts','drizzle/meta/_journal.json','drizzle/meta/0012_snapshot.json'];cp.execSync('git diff --name-only 5a88626..HEAD').toString().split('\n').filter(Boolean).forEach(f=>{if(f.endsWith('.test.ts'))fail.push('TEST FILE TOUCHED IN PHASE (SC-4): '+f);if(f.startsWith('.planning/'))return;if(/^drizzle\/0012_.*\.sql\$/.test(f))return;if(!allow.includes(f))fail.push('out-of-scope phase change: '+f)});console.log(fail.length?'FAIL '+fail.join(' | '):'PASS');process.exit(fail.length?1:0)"</automated>
  </verify>

  <done>`33-PLANS.md` is complete with verbatim BEFORE and AFTER captures of the identical four statements, a Deltas table, a Verdicts table quoting all four ROADMAP success criteria with their evidence, and a Not-gaps list; Q1 shows `Bitmap Index Scan on deals_stage_id_idx` (accepted as SC-1 per D-01) where BEFORE showed `Seq Scan on deals`; Q3 shows a literal `Index Scan using activities_due_date_idx`; Q4 shows eleven `t` rows; all three Phase 32 gates pass; and the whole-phase diff touches zero test files and nothing outside the four schema files, the 0012 migration, its snapshot and the journal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated SQL → live PostgreSQL | This is the only place in the phase where DDL crosses into a database holding real imported customer CRM data (25,206 deals / 79,023 activities / 46,055 orgs / 38,345 people). `drizzle-kit migrate` executes whatever the file contains, unreviewed, in one transaction. |
| `drizzle/` migration chain → any other deployment | The committed migration is replayed verbatim on every other environment at deploy time. A defect here propagates. |
| schema files → `drizzle/meta/0012_snapshot.json` | The snapshot becomes the diff basis for every future migration. Divergence silently DROPs indexes. |
| index leaves → `postgres_data` volume at rest | 7328 kB of new on-disk structures containing copies of indexed column values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-14 | Tampering | `drizzle/0012_*.sql` executed against real CRM data | mitigate | Task 1 is a BLOCKING content gate that runs with zero database connections: it rejects the file unless every statement begins with `CREATE INDEX`, and fails on any case-insensitive `DROP`, `ALTER TABLE`, `CREATE TABLE` or `ADD COLUMN`. `db:migrate` in Task 2 is only permitted after that gate passes. Task 1's verify also asserts the DB is still pristine (0 non-pkey indexes, 4 ledger rows), proving generate touched nothing. |
| T-33-15 | Denial of service | write availability during the index build | accept | `CREATE INDEX` without `CONCURRENTLY` takes a `ShareLock` per table: reads continue, writes block for the build duration. Measured total for all eleven indexes on these exact table sizes (deals 19 MB, activities 26 MB, organizations 13 MB, people 8824 kB) is **≈1.08 s**, and the whole set runs in one transaction. For a single-instance self-hosted deployment applied at deploy time, a sub-2-second write pause is acceptable — this is why D-03's ban on `.concurrently()` costs nothing. Recorded in `33-PLANS.md` under `## Not gaps` so any future multi-instance or high-write deployment can re-evaluate rather than rediscover it. |
| T-33-16 | Tampering | irreversibility / absence of a down path | mitigate | drizzle-kit has no down migrations, so there is no automated rollback. Two things make this acceptable and both are recorded in `33-PLANS.md`: the migration runs inside `session.transaction()`, so a failure leaves zero indexes rather than a partial set; and an index carries no data, so the manual reversal is a lossless `DROP INDEX` of the eleven named indexes plus deleting the declarations from the four schema files (the schema must be reverted too, or the next generate would recreate them). No row is altered, moved, or reinterpreted, so there is no data-loss path to roll back from. |
| T-33-17 | Tampering | silent index loss on a future `generate` | mitigate | `drizzle/0012_*.sql`, `drizzle/meta/0012_snapshot.json` and the `_journal.json` entry are committed together in one commit, and Task 1 asserts the snapshot exists and contains `deals_stage_id_idx`. This is the direct countermeasure to the `0009` → `0010` regression that deleted `workflows_next_run_at_idx`. Hand-editing the generated SQL or the snapshot to satisfy the gate is explicitly forbidden in the task action. |
| T-33-18 | Tampering | accidental re-application of historical migrations | accept | `drizzle.__drizzle_migrations` holds 4 rows for 12 files because 0000-0007 were `db:push`ed. RESEARCH.md Pitfall 9 verified the migrator compares only against the max `created_at` (1774729567507 = 0011), so only 0012 runs. Task 2 asserts the ledger goes 4 → 5 exactly, which detects any re-application. The task action explicitly instructs not to "fix" the missing rows. |
| T-33-19 | Tampering | rows in the live dev database | mitigate | Every DB-touching verify in this plan re-asserts exact row counts (25206 / 79023 / 46055 / 38345). No `INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, seed script or `ANALYZE` is run anywhere in the phase; `verify-plans.sql` was asserted DML-free in plan 33-01 and is re-run byte-identically here. |
| T-33-20 | Information disclosure | index leaves on disk (V12 Data Protection) | accept | btree leaves store copies of the indexed values — FK identifiers, `due_date`, `deleted_at` timestamps. They live in the same `postgres_data` volume, under the same OS permissions, as the heap tuples those values are already stored in. No new reader, no new network surface, no new trust boundary. Backup/restore procedures (POLISH-02, Phase 43) rebuild indexes from the schema rather than shipping leaf data separately. |
| T-33-21 | Information disclosure | enumeration or row-visibility change | accept | An index alters only the physical access path. No query, endpoint, parameter or authorization check is modified (SC-4), so the set of rows any caller can retrieve is byte-identical. Soft-delete remains enforced solely by the application's `isNull(deletedAt)` predicates, which are untouched — D-02's choice of plain over partial indexes deliberately avoids coupling index structure to soft-delete semantics. |
| T-33-22 | Information disclosure | credentials in the committed artifact | mitigate | `33-PLANS.md` is asserted to contain neither `883112` nor `DATABASE_URL`. `psql -U pipelite` inside the container uses local trust auth, so no connection string is needed; the sudo password appears only in the transient shell command and never in a tracked file. |
| T-33-23 | Elevation of privilege | `sudo docker exec` usage | accept | Required and unavoidable: `psql` is not installed on the host, so container exec is the only DB access path. Credentials are cached via `sudo -S -v` for the session rather than embedded in scripts, and every statement sent across that boundary is read-only `EXPLAIN`/`SELECT` (the write path is `npm run db:migrate` over TCP to `localhost:5433`, which needs no sudo). |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — zero packages installed. RESEARCH.md `## Package Legitimacy Audit` records that the whole phase adds no dependency; `drizzle-kit@0.31.9`, `drizzle-orm@0.45.1`, `postgres@3.4.8` and `vitest@4.0.18` are already pinned in `package.json`. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
1. `rtk proxy "npm run db:generate"` produces exactly one `drizzle/0012_*.sql`, its snapshot, and journal entry `idx: 12`.
2. Generated SQL gate: exactly 11 `CREATE INDEX`; every statement starts with `CREATE INDEX`; zero `CONCURRENTLY` (D-03), zero `WHERE` (D-02), zero statements naming both `stage_id` and `position` (D-04), zero `DROP`/`ALTER TABLE`/`CREATE TABLE`/`ADD COLUMN`; no existing migration modified.
3. Database still pristine after generate: 0 non-pkey indexes, 4 ledger rows.
4. `rtk proxy "npm run db:migrate"` exits 0 with no error and no `CONCURRENTLY` complaint.
5. Post-apply guard is exactly `GUARD=25206|79023|46055|38345|11|5|4` — row counts unchanged, 11 indexes present, ledger 4 → 5, `random_page_cost` still 4 (D-08).
6. The eleven non-pkey index names match the expected sorted set exactly.
7. Q4 returns eleven `index_backed = t` rows, including `deals` / `owner_id` — D-05's only proof path.
8. Q1 shows `Bitmap Index Scan on deals_stage_id_idx` under a (Parallel) `Bitmap Heap Scan on deals`, where the BEFORE half of the same file shows `Seq Scan on deals` for the identical statement. Per D-01 this SATISFIES SC-1; a literal `Index Scan using deals_stage_id_idx` is NOT required and its absence is NOT a failure.
9. Q2 corroborates at lower selectivity with an index-driven plan on `deals_stage_id_idx`.
10. Q3 shows a literal `Index Scan using activities_due_date_idx`, cost in the 8-25 range (from 5071.99), buffers ≤50 (from 3294).
11. `33-PLANS.md` contains BEFORE, AFTER, Deltas, Verdicts and Not-gaps sections, no `Pending` in the AFTER half, and no leaked credential.
12. `rtk proxy "npm test"` zero failures; `rtk proxy "npm run typecheck"` no `error TS`; `rtk proxy "npm run lint"` clean.
13. Whole-phase scope check on `5a88626..HEAD`: zero `*.test.ts` paths; every non-`.planning/` path is one of the four schema files, `drizzle/0012_*.sql`, `drizzle/meta/0012_snapshot.json`, or `drizzle/meta/_journal.json`.
</verification>

<success_criteria>
- SC-1 closed: the kanban board query, pinned to the app's real default pipeline, moved from `Seq Scan on deals` to `Bitmap Index Scan on deals_stage_id_idx` — with the D-01 acceptance stated in the artifact ahead of the evidence so no verifier can false-fail on node naming.
- SC-2 closed: the activity-reminder cron query moved from `Seq Scan on activities` (cost 5071.99, 3294 buffers) to a literal `Index Scan using activities_due_date_idx` (cost ~12.21, ~5 buffers).
- SC-3 closed: all eleven PERF-01 columns are index-backed, proven by catalog query rather than by reading the migration file, delivered via exactly one generated migration — `deals.owner_id` included and explicitly catalog-only (D-05).
- SC-4 closed: `npm test`, `npm run typecheck` and `npm run lint` all pass, and the whole-phase diff proves zero test files and zero application source files were touched.
- The three "correct behaviour that is not a gap" facts (the `Closer` pipeline's Seq Scan, `deals.owner_id` ignoring its index, the `deleted_at IS NULL` read path not getting faster) and the two costs (7328 kB storage, ~1.08 s write-blocking lock per deploy) are on the record in `33-PLANS.md`.
- Zero rows mutated across the entire phase; `random_page_cost` untouched (D-08).
</success_criteria>

<output>
Create `.planning/phases/33-database-indexes-for-the-crm-core/33-03-SUMMARY.md` when done.
</output>
