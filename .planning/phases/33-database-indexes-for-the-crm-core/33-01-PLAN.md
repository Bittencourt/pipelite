---
phase: 33-database-indexes-for-the-crm-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql
  - .planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "A single reusable SQL capture script exists that can be run identically before and after the migration, so BEFORE/AFTER plans are comparable by construction"
    - "The BEFORE EXPLAIN plan for the kanban board query is captured and committed BEFORE any index DDL exists (D-07 — the ordering is non-negotiable; once indexes exist the 'previously showed a sequential scan' clause of SC-1/SC-2 is unprovable)"
    - "The kanban BEFORE plan is pinned to the BDR - Base Fria stage IDs (the pipeline the app actually loads), not an arbitrary pipeline, and shows Seq Scan on deals"
    - "The reminder-cron BEFORE plan shows Seq Scan on activities at cost ~5071.99 with ~3294 buffers"
    - "The pg_indexes CTE assertion runs BEFORE the migration and returns index_backed = f for all eleven target columns, establishing the catalog path that is the ONLY proof available for deals.owner_id (D-05)"
    - "SET enable_seqscan = off appears nowhere in the capture script — the planner's genuine preference is what is being recorded"
    - "Zero rows in the live dev database are inserted, updated, deleted, truncated, or seeded; no ANALYZE is run, keeping the captured costs directly comparable to the RESEARCH.md baseline"
  artifacts:
    - path: ".planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql"
      provides: "The four reusable verification statements (kanban SC-1, kanban single-stage corroboration, reminder cron SC-2, pg_indexes SC-3 assertion)"
      contains: "ad4d9fb5-92c7-4170-8e93-2163153a99d9"
    - path: ".planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md"
      provides: "Committed BEFORE plan capture with the D-01 acceptance statement and the D-04/D-05/D-08 caveats pre-recorded"
      contains: "## BEFORE"
  key_links:
    - from: ".planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql"
      to: "pipelite-postgres-1 psql"
      via: "sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite -f /tmp/33-verify-plans.sql"
      pattern: "docker compose exec -T postgres psql"
    - from: ".planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md"
      to: "SC-1 / SC-2 verification"
      via: "verbatim EXPLAIN output pasted under ## BEFORE"
      pattern: "Seq Scan on"
---

<objective>
Capture the BEFORE `EXPLAIN (ANALYZE, BUFFERS, COSTS)` evidence for both named queries, plus the empty-state catalog assertion, into one committed artifact — and do it before a single line of index DDL exists anywhere.

Purpose: D-07 makes this non-negotiably the first work in the phase. SC-1 and SC-2 are both worded as *transitions* ("where it previously showed a sequential scan"). If the migration lands first, that clause becomes unprovable, and re-proving it would require dropping the indexes — a second schema mutation against a database holding real imported customer CRM data.

Output: `verify-plans.sql` (a reusable, idempotent, read-only capture script) and `33-PLANS.md` with its `## BEFORE` half filled in verbatim. Plan 33-03 re-runs the identical script to fill in `## AFTER`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-database-indexes-for-the-crm-core/33-CONTEXT.md
@.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md
@.planning/phases/33-database-indexes-for-the-crm-core/33-VALIDATION.md

<environment_notes>
- Postgres runs in Docker. `psql` is NOT installed on the host. Every DB access goes through
  `sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite ...` from the repo root.
- All `docker` commands need `sudo`. `sudo -S` reads the password from stdin and therefore COLLIDES
  with stdin redirection (`< file`, `-f -`). Cache credentials once with `echo "883112" | sudo -S -v`,
  then use `sudo -n` for every subsequent invocation. This is the pattern RESEARCH.md used successfully.
- Do NOT start a dev server. This phase needs none.
- The `rtk` shell hook rewrites commands and swallows output for `npx`/`npm` and mangles `wc -l` and
  `grep -c` (they return match summaries, not counts). Direct `sudo docker ... psql` invocations were
  proven to return full plan output in RESEARCH.md and are safe. For any file assertion or count in
  this plan, use `node -e` — never `grep -c` or `wc -l`.
</environment_notes>

<verified_facts>
Load-bearing constants, all measured against this exact database on 2026-08-14 (RESEARCH.md):

- Default pipeline the kanban page loads: `BDR - Base Fria` (no pipeline has `is_default = true`, so
  `allPipelines[0]` wins). Its two stage IDs — the SC-1 pin:
  `ad4d9fb5-92c7-4170-8e93-2163153a99d9` and `01374f39-b838-4977-a48e-8fd126aa83f5`
  (3,753 live deals = 14.9% of the table, inside the index-winning region with a 4% cost margin).
- Row counts: deals 25,206 / activities 79,023 / organizations 46,055 / people 38,345. Real imported
  CRM data. Read-only inputs to this phase.
- Existing indexes: exactly four, all primary keys. Zero of the eleven target columns is covered.
</verified_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the reusable read-only verification script</name>

  <read_first>
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — section `## Code Examples`
      (lines 499-548). It holds the exact SQL for all four statements: the SC-1 kanban EXPLAIN with the
      literal stage-ID list, the SC-2 reminder-cron EXPLAIN with the fully-qualified column predicates,
      and the SC-3 `pg_indexes` CTE assertion. Copy the SQL from there verbatim rather than retyping it.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `### Pitfall 4`
      (lines 465-468): why the literal `IN ('a','b')` form is mandatory and the
      `IN (SELECT id FROM stages WHERE pipeline_id = ...)` subquery form must NOT be used.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `### Pitfall 2`
      (lines 455-458): the bitmap-vs-plain node-naming trap.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-VALIDATION.md` — the Per-Task
      Verification Map, for the expected numeric deltas.
  </read_first>

  <files>.planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql</files>

  <action>
    Create `verify-plans.sql` in the phase directory as a read-only, idempotent, four-statement script
    that will be executed unchanged both now (BEFORE) and again in plan 33-03 (AFTER). It contains no
    DDL, no DML, no `SET`, no `ANALYZE`, and no transaction control.

    Head the file with a `--` comment block stating: (a) that it is read-only and safe to run against a
    database holding real customer CRM data; (b) that `SET enable_seqscan = off` is deliberately absent
    because it only applies a cost penalty and would prove usability rather than the planner's genuine
    preference; (c) that `ANALYZE` is deliberately absent so captured costs stay directly comparable to
    the RESEARCH.md baseline (statistics were verified present and accurate via `pg_class.reltuples` and
    `pg_stats`).

    Statement Q1 — SC-1, the kanban board query. `EXPLAIN (ANALYZE, BUFFERS, COSTS)` selecting
    `id, title, position` from `deals` where `stage_id IN ('ad4d9fb5-92c7-4170-8e93-2163153a99d9','01374f39-b838-4977-a48e-8fd126aa83f5')`
    and `deleted_at IS NULL`, ordered by `position ASC`. The stage-ID list MUST be a literal value list —
    per Pitfall 4, the subquery form produces a Hash Join with a Seq Scan on deals and would
    misrepresent the app's actual plan. Precede it with a comment naming it
    `Q1 / SC-1 — kanban, BDR - Base Fria default pipeline, 14.9% selectivity`.

    Statement Q2 — SC-1 wide-margin corroboration. The same query narrowed to the single stage
    `ad4d9fb5-92c7-4170-8e93-2163153a99d9`. Comment it as a corroboration at strictly lower selectivity
    than Q1, and state that no specific row count is asserted for it.

    Statement Q3 — SC-2, the activity-reminder cron query. `EXPLAIN (ANALYZE, BUFFERS, COSTS)` of the
    exact SQL Drizzle emits from `src/app/api/internal/email/process/route.ts:32-49`: select
    `id, title, due_date, assignee_id, owner_id` from `activities` where `completed_at IS NULL` and
    `deleted_at IS NULL` and `reminder_sent_at IS NULL` and `due_date >= now()` and
    `due_date <= now() + interval '1 hour'`. Comment that `due_date` is stored at `00:00:00` for every
    row, so a mid-day window legitimately matches zero rows and a midnight-boundary run matches one —
    both are valid and both yield the same plan shape.

    Statement Q4 — SC-3, the catalog assertion. The `WITH required(tbl, col) AS (VALUES ...)` CTE from
    RESEARCH.md lines 533-547, listing all eleven pairs: (deals, stage_id), (deals, deleted_at),
    (deals, organization_id), (deals, person_id), (deals, owner_id), (activities, due_date),
    (activities, deal_id), (activities, deleted_at), (people, organization_id), (people, deleted_at),
    (organizations, deleted_at); joined to `pg_index` / `pg_class` / `pg_attribute` on
    `a.attnum = i.indkey[0]` to test the LEADING index column, projecting `index_backed`. Comment that
    this is the ONLY admissible proof for `deals.owner_id` (D-05: `n_distinct = 1`, so the planner
    correctly ignores that index forever and no EXPLAIN can ever demonstrate it), and that it must
    return eleven `f` rows now and eleven `t` rows after the migration.

    Do not embed a connection string, `DATABASE_URL`, or the sudo password anywhere in the file — the
    in-container `psql -U pipelite` uses local trust auth and needs no credential.
  </action>

  <acceptance_criteria>
    - The file exists at `.planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql`.
    - A `node -e` assertion over the file contents passes every one of the following and exits 0
      (printing FAIL and exiting non-zero on any miss):
      - The literal `EXPLAIN (ANALYZE, BUFFERS, COSTS)` occurs exactly three times — one each for Q1, Q2
        and Q3. Q4 is a plain `SELECT` and contributes none.
      - Both stage IDs are present: `ad4d9fb5-92c7-4170-8e93-2163153a99d9` and
        `01374f39-b838-4977-a48e-8fd126aa83f5`.
      - The string `now() + interval '1 hour'` is present.
      - The string `indkey[0]` is present (proves the CTE tests the LEADING index column).
      - All eleven `('<table>','<column>')` CTE pairs are present.
      - Case-insensitively, NONE of these appear: `enable_seqscan`, `CREATE INDEX`, `DROP `, `INSERT `,
        `UPDATE `, `DELETE `, `TRUNCATE`, `BEGIN`, `COMMIT`, `ROLLBACK`, `IN (SELECT`, `DATABASE_URL`,
        `883112`. (Bare `ANALYZE` is NOT forbidden — `EXPLAIN (ANALYZE` legitimately contains it; the
        standalone-statement forms above are what is rejected.)
    - The script parses: the `psql -f /tmp/33-verify-plans.sql` invocation in Task 2 exits 0 (a syntax
      error would exit non-zero).
  </acceptance_criteria>

  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql','utf8');const fail=[];const n=(s.match(/EXPLAIN \(ANALYZE, BUFFERS, COSTS\)/g)||[]).length;if(n!==3)fail.push('EXPLAIN count '+n+' != 3');['ad4d9fb5-92c7-4170-8e93-2163153a99d9','01374f39-b838-4977-a48e-8fd126aa83f5',\"now() + interval '1 hour'\",'indkey[0]'].forEach(t=>{if(!s.includes(t))fail.push('missing '+t)});[['deals','stage_id'],['deals','deleted_at'],['deals','organization_id'],['deals','person_id'],['deals','owner_id'],['activities','due_date'],['activities','deal_id'],['activities','deleted_at'],['people','organization_id'],['people','deleted_at'],['organizations','deleted_at']].forEach(p=>{const a=\"'\"+p[0]+\"','\"+p[1]+\"'\";const b=\"'\"+p[0]+\"', '\"+p[1]+\"'\";if(!s.includes(a)&&!s.includes(b))fail.push('missing pair '+p[0]+'.'+p[1])});['enable_seqscan','CREATE INDEX','DROP ','INSERT ','UPDATE ','DELETE ','TRUNCATE','BEGIN','COMMIT','ROLLBACK','IN (SELECT','DATABASE_URL','883112'].forEach(t=>{if(s.toUpperCase().includes(t.toUpperCase()))fail.push('FORBIDDEN '+t)});console.log(fail.length?'FAIL '+fail.join(' | '):'PASS');process.exit(fail.length?1:0)"</automated>
  </verify>

  <done>`verify-plans.sql` exists, the node assertion above prints PASS and exits 0, and the file contains no DDL, no DML, no `enable_seqscan`, no subquery stage filter, and no credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Run the script against the live DB and commit the BEFORE capture</name>

  <read_first>
    - `.planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql` — the script written in Task 1.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `## Measured BEFORE Plans`
      (lines 241-255) for the expected node names, costs, execution times and buffer counts; and
      `### SC-1: capture the kanban plan` / `### SC-2: capture the reminder-cron plan` (lines 499-528)
      for the exact invocation and the `sudo -S` / stdin collision workaround.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `### Pitfall 1`
      (lines 449-453) and `### Pitfall 10` (lines 493-495), the two caveats that must be pre-recorded in
      the artifact so `/gsd:verify-work` cannot mistake correct planner behaviour for a phase failure.
    - `.planning/phases/33-database-indexes-for-the-crm-core/33-RESEARCH.md` — `## Partial vs Plain`
      (lines 314-347), for the honest disclosure about what the `deleted_at` indexes do and do not buy.
  </read_first>

  <files>.planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md</files>

  <action>
    Cache sudo credentials once with `echo "883112" | sudo -S -v`. Copy the script into the container
    with `sudo -n docker compose exec -T postgres bash -c 'cat > /tmp/33-verify-plans.sql' < .planning/phases/33-database-indexes-for-the-crm-core/verify-plans.sql`
    (the stdin redirect is why credentials must be cached first — `sudo -S` would consume it). Execute it
    with `sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite -f /tmp/33-verify-plans.sql`,
    redirecting both streams to a scratch file outside the repo, then read that file back.

    Create `33-PLANS.md` in the phase directory. Structure it as: a short preamble; an
    `## Acceptance Criteria (pre-stated, per D-01/D-05/D-08)` section; a `## BEFORE (no indexes)` section
    holding the verbatim output; and an `## AFTER (11 indexes)` section containing only the single line
    `_Pending — filled in by plan 33-03 after the migration is applied._` so plan 33-03 has a defined slot.

    The pre-stated acceptance section must record, in plain declarative sentences, before any output is
    shown:
    - D-01: for Q1, `Bitmap Heap Scan on deals` fed by `Bitmap Index Scan on deals_stage_id_idx`
      SATISFIES SC-1. A literal `Index Scan using deals_stage_id_idx` node is physically unachievable for
      a ~3,753-row scattered fetch at any selectivity where the index beats a sequential scan; its
      absence is NOT a failure and must not be graded as one. SC-2 is unaffected and does yield a literal
      `Index Scan`.
    - Pitfall 1: the `Closer` pipeline (61% of the deals table) correctly keeps its sequential scan even
      with a perfect index present. That is the cost model being right. It is deliberately not measured
      here and is not a gap.
    - D-05: `deals.owner_id` has `n_distinct = 1` in this dataset, so the planner correctly ignores its
      index. Q4's catalog row is the only proof, and no EXPLAIN proof will be attempted.
    - D-02/D-04: all eleven indexes are plain single-column btrees. A partial `(stage_id) WHERE deleted_at IS NULL`
      is byte-identical in size and cost here and would break the stage-delete guard at
      `src/app/admin/pipelines/actions.ts:483-489`; a composite `(stage_id, position)` was measured to
      grow the index 200 kB to 1696 kB and push the planner back to `Seq Scan`, actively failing SC-1.
    - D-08: `random_page_cost` stays at 4. It is why the selectivity crossover sits at 15-19%, but it is
      server configuration, not an index, and is out of scope.
    - Honest disclosure: the plain `deleted_at` indexes will NOT speed up the `deleted_at IS NULL` read
      path (measured: `organizations WHERE deleted_at IS NULL ORDER BY name LIMIT 50` still correctly
      chooses a Seq Scan with the index present). They serve the `IS NOT NULL` direction, which is what
      Phase 37 Trash & Restore needs. SC-3 asks only that the columns be index-backed.

    Paste all four statements' output verbatim under `## BEFORE`, each under its own `### Q1..Q4`
    subheading with the statement's purpose restated. Do not paraphrase, trim, or reformat plan output.
    Under Q4, state explicitly that eleven `f` rows is the expected and required BEFORE result.

    Do not run `ANALYZE`. Do not create, drop, or alter anything. Do not insert, update, delete or
    truncate a single row. Before committing, confirm the artifact does not contain the sudo password or
    a connection string.
  </action>

  <acceptance_criteria>
    - `psql -f /tmp/33-verify-plans.sql` exits 0.
    - Q1 output contains `Seq Scan on deals` (a `Parallel Seq Scan on deals` variant also satisfies this)
      and contains neither `Bitmap Index Scan` nor `Index Scan using deals_stage_id_idx`. Estimated cost
      on the deals node is in the 2600-2800 range and `Buffers` on that node totals roughly 2414
      (accept ±15%; execution timings fluctuate freely and are NOT graded).
    - Q1 output does NOT contain `Hash Cond: (deals.stage_id = stages.id)` — that string would mean the
      subquery form leaked in (Pitfall 4).
    - Q3 output contains `Seq Scan on activities`, `Rows Removed by Filter: 79023` (79022 also accepted),
      estimated cost `5071.99` (accept 5000-5150) and roughly `3294` buffers (accept ±15%).
    - Q4 output has exactly eleven data rows and every `index_backed` value is `f`.
    - `33-PLANS.md` exists, contains the headings `## BEFORE (no indexes)` and `## AFTER (11 indexes)`,
      contains a sentence carrying both `SATISFIES SC-1` and `Bitmap Index Scan`, and contains the
      strings `D-01`, `D-02`, `D-04`, `D-05`, `D-08`, and `Closer`.
    - `33-PLANS.md` contains neither `883112` nor `DATABASE_URL`.
    - The text preceding the `## AFTER` heading contains no `Index Scan using deals_stage_id_idx` — if it
      did, the BEFORE capture happened after the DDL and D-07 was violated.
    - `git status --short` shows no modification to any file under `src/`, `drizzle/`, or any `*.test.ts`.
    - Row counts and index counts are unchanged: `count(*)` on deals is 25206, on activities is 79023,
      and the non-pkey index count across deals/activities/people/organizations is 0.
  </acceptance_criteria>

  <verify>
    <automated>echo "883112" | sudo -S -v && sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite -tAc "SELECT (SELECT count(*) FROM deals)||'/'||(SELECT count(*) FROM activities)||'/'||(SELECT count(*) FROM pg_indexes WHERE tablename IN ('deals','activities','people','organizations') AND indexname NOT LIKE '%_pkey')" > /tmp/33-guard.txt 2>&1; node -e "const fs=require('fs');const g=fs.readFileSync('/tmp/33-guard.txt','utf8').trim();const p=fs.readFileSync('.planning/phases/33-database-indexes-for-the-crm-core/33-PLANS.md','utf8');const fail=[];if(!g.includes('25206/79023/0'))fail.push('DB guard failed (want 25206/79023/0, got '+g+')');['## BEFORE','## AFTER','SATISFIES SC-1','Bitmap Index Scan','Seq Scan on deals','Seq Scan on activities','D-01','D-02','D-04','D-05','D-08','Closer'].forEach(t=>{if(!p.includes(t))fail.push('33-PLANS.md missing: '+t)});['883112','DATABASE_URL'].forEach(t=>{if(p.includes(t))fail.push('33-PLANS.md LEAKS: '+t)});if(/Index Scan using deals_stage_id_idx/.test(p.split('## AFTER')[0]))fail.push('BEFORE half already shows an index scan - D-07 violated');console.log(fail.length?'FAIL '+fail.join(' | '):'PASS');process.exit(fail.length?1:0)"</automated>
  </verify>

  <done>`33-PLANS.md` is committed with the verbatim BEFORE output for all four statements, both named queries demonstrably sequential-scanning, Q4 returning eleven `f` rows, the D-01 bitmap acceptance and the Pitfall-1/D-05/D-08 caveats pre-stated ahead of the evidence, zero non-pkey indexes still present on the four tables, and row counts untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → `pipelite-postgres-1` | A privileged `sudo docker exec` session reaches a database holding real imported customer CRM data (25,206 deals / 79,023 activities / 46,055 orgs / 38,345 people). Everything this plan sends across this boundary must be read-only. |
| captured plan output → committed git artifact | `EXPLAIN` output is copied verbatim into a tracked file. Anything present in that output becomes permanently public in repo history. |
| shell history → developer workstation | The sudo password is typed into a cached-credential command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `verify-plans.sql` executed against the live dev DB | mitigate | The script is asserted DDL-free and DML-free before it is ever run: the Task 1 node assertion rejects `CREATE INDEX`, `DROP`, `INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, `BEGIN`, `COMMIT`, `ROLLBACK` case-insensitively and exits non-zero. Task 2's verify re-checks `count(*)` on deals and activities against the exact expected 25206/79023 afterwards, so any accidental mutation hard-fails the task. |
| T-33-02 | Tampering | statistics state of the four tables | mitigate | `ANALYZE` as a standalone statement is deliberately excluded and asserted absent. Running it would silently shift the recorded costs and break comparability with both the RESEARCH.md baseline and the plan-33-03 AFTER capture. |
| T-33-03 | Information disclosure | `33-PLANS.md` committed to git | mitigate | The artifact carries only plan output and prose. Task 2's verify greps the committed file for `883112` and `DATABASE_URL` and fails on either. `psql -U pipelite` inside the container uses local trust auth, so no connection string is needed in the script at all. |
| T-33-04 | Information disclosure | `EXPLAIN ANALYZE` output content | accept | `EXPLAIN (ANALYZE, BUFFERS, COSTS)` on these statements emits node names, costs, row counts and buffer counts — no column values and no customer PII. The only literals committed are stage UUIDs, which are already recorded in RESEARCH.md. |
| T-33-05 | Repudiation | proof that BEFORE preceded the DDL (D-07) | mitigate | The BEFORE capture lands in its own commit in wave 1, strictly before the schema-declaration commit (wave 2) and the migration commit (wave 3). Commit ordering, not convention, is the evidence. Task 2's verify additionally asserts that zero non-pkey indexes exist on the four tables at capture time and that the BEFORE half contains no `Index Scan using deals_stage_id_idx`. |
| T-33-06 | Denial of service | the running app during capture | accept | All four statements are read-only `EXPLAIN`/`SELECT`. The heaviest is a full sequential scan of a 19 MB table taking under 700 ms and acquiring only `AccessShareLock`. No writer is blocked. |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | Not applicable — this plan installs zero packages. RESEARCH.md `## Package Legitimacy Audit` records that the whole phase adds no dependency; every tool (`drizzle-orm@0.45.1`, `drizzle-kit@0.31.9`, `postgres@3.4.8`, `vitest@4.0.18`) is already pinned in `package.json`. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package exists to gate. |
</threat_model>

<verification>
1. `node -e` assertion on `verify-plans.sql` prints PASS and exits 0 (no DDL/DML/`enable_seqscan`/subquery-form/credentials; three `EXPLAIN` statements; both stage IDs; all eleven CTE pairs).
2. `sudo -n docker compose exec -T postgres psql -U pipelite -d pipelite -f /tmp/33-verify-plans.sql` exits 0.
3. Q1 shows a sequential scan on `deals`, no `Bitmap Index Scan`, no `Hash Cond: (deals.stage_id = stages.id)`.
4. Q3 shows `Seq Scan on activities` with cost ≈5071.99 and ≈3294 buffers.
5. Q4 returns eleven rows, all `index_backed = f`.
6. `33-PLANS.md` carries the verbatim output plus the pre-stated D-01/D-02/D-04/D-05/D-08 acceptance and caveat block, and leaks no credential.
7. Database integrity guard: deals `count(*)` = 25206, activities `count(*)` = 79023, non-pkey index count on the four tables = 0.
8. `git status --short` shows changes only under `.planning/phases/33-database-indexes-for-the-crm-core/`.
</verification>

<success_criteria>
- D-07 satisfied: both named queries are on the record as sequential scans, captured and committed before any index exists anywhere in the repo or the database.
- A single reusable read-only script exists that plan 33-03 will re-run byte-identically, making the BEFORE/AFTER comparison sound by construction rather than by narration.
- The catalog proof path for `deals.owner_id` (D-05) is exercised and shown returning `f` for all eleven columns.
- SC-1's bitmap acceptance (D-01) and the three "correct planner behaviour that is not a failure" caveats are written down ahead of the evidence, so downstream verification cannot false-fail on node naming, on the `Closer` pipeline, or on `deals.owner_id`.
- Zero rows mutated, zero DDL executed, zero schema files touched, zero test files touched.
</success_criteria>

<output>
Create `.planning/phases/33-database-indexes-for-the-crm-core/33-01-SUMMARY.md` when done.
</output>
