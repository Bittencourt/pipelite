---
phase: 38-bulk-operations
plan: 16
type: execute
wave: 4
depends_on: [38-07, 38-09, 38-10, 38-12]
files_modified:
  - src/app/people/page.tsx
  - src/app/people/data-table.tsx
autonomous: true
requirements: [BULK-01, BULK-02, BULK-03, BULK-04]

must_haves:
  truths:
    - "A user checks individual person rows and a header checkbox that selects every loaded row"
    - "Selecting three rows, pressing Load More, still reads three selected"
    - "Changing the search clears the selection"
    - "The bar never covers the last row or the Load More button"
    - "After a partial failure the succeeded rows are deselected and the failed rows stay selected"
  artifacts:
    - path: "src/app/people/page.tsx"
      provides: "retentionDays and bulkOwners passed to DataTable as plain serializable props"
      contains: "readTrashRetentionDays"
    - path: "src/app/people/data-table.tsx"
      provides: "rowSelection wiring, the prepended select column, and the bar/spacer/report mount"
      contains: "getRowId"
  key_links:
    - from: "src/app/people/data-table.tsx"
      to: "src/components/bulk/select-column.tsx"
      via: "useSelectColumn prepended into a derived columnsWithSelect array"
      pattern: "useSelectColumn"
    - from: "src/app/people/data-table.tsx"
      to: "src/app/people/actions.ts"
      via: "the three bulk actions passed to BulkActionBar as onDelete / onReassign / onExport"
      pattern: "bulkDeletePeople|bulkReassignPersonOwner|exportSelectedPeople"
---

<objective>
Wire selection into the People list: enable TanStack `rowSelection`, prepend the shared checkbox column,
thread the two new server props, and mount the bar, its spacer and the failure report.

Purpose: `src/app/people/data-table.tsx` is BYTE-IDENTICAL to `src/app/organizations/data-table.tsx`
modulo the Organization -> Person rename (verified by
`diff <(sed 's/Organization/Person/;s/organizations/people/' organizations/data-table.tsx) people/data-table.tsx`
returning identical). Whatever was done to one is done to the other verbatim, and keeping them identical
is itself the maintenance contract — a divergence here is a bug on one of the two surfaces that nothing
else will catch.
Output: two edited files; `people/columns.tsx` is deliberately NOT touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-bulk-operations/38-UI-SPEC.md
@.planning/phases/38-bulk-operations/38-PATTERNS.md

<interfaces>
Consumed contracts, all already landed:

  useSelectColumn<T>(getLabel: (row: T) => string): ColumnDef<T, unknown>        // plan 38-07
  BulkActionBar(props: BulkActionBarProps)                                       // plan 38-10
  BulkFailureReport(props: BulkFailureReportProps)                               // plan 38-09
  bulkDeletePeople(ids): Promise<BulkWriteResult>                                // plan 38-12
  bulkReassignPersonOwner(ids, ownerId): Promise<BulkWriteResult>                // plan 38-12
  exportSelectedPeople(ids): Promise<ExportResult>                               // plan 38-12
  readTrashRetentionDays(): Promise<number | null>                               // src/lib/trash/settings.ts:71
  BulkOutcome = { kind, succeeded, failed, labelById }                           // src/lib/bulk/types.ts

TanStack wiring to add to the existing `useReactTable` call at people/data-table.tsx:125-134:
  columns: columnsWithSelect
  getRowId: (row) => row.id
  state: { rowSelection }
  onRowSelectionChange: setRowSelection
  enableRowSelection: true
  (keep the existing `getCoreRowModel` and `meta` exactly as they are)

There is NO in-repo analog for this configuration: zero occurrences of `rowSelection`,
`enableRowSelection`, `getIsAllPageRowsSelected`, `getIsSomePageRowsSelected` or
`toggleAllPageRowsSelected` anywhere under `src/` (verified). The only existing asset is
`data-state={row.getIsSelected() && "selected"}`, already present and INERT at
people/data-table.tsx:180, which `src/components/ui/table.tsx:60` already styles
`data-[state=selected]:bg-muted`. Zero new CSS.

Defensive id derivation (required, not optional):
  const loadedIds = useMemo(() => new Set(data.map((r) => r.id)), [data])
  const selectedIds = useMemo(
    () => Object.keys(rowSelection).filter((id) => rowSelection[id] && loadedIds.has(id)),
    [rowSelection, loadedIds],
  )
TanStack does NOT prune `rowSelection` when a row leaves `data`, so after a bulk delete the keys of the
deleted rows linger as phantoms inflating the count. Never derive from `table.getSelectedRowModel()`.

The two selection affordances that already exist and must not be conflated (38-UI-SPEC § Color):
  data-selected           = the KEYBOARD CURSOR (`globals.css:172-174` primary ring plus `bg-muted/50`)
  data-state="selected"   = BULK selection (`table.tsx:60` -> `bg-muted`)
A bulk-selected row must NEVER get a primary ring or a primary border.

`getLabel` for this surface: a person's display name. Read `people/columns.tsx` to find how the name is
composed today (`firstName` / `lastName` are separate columns on the `people` table) and reuse the same
composition rather than inventing one — the failure report must name the record the way the table does.

Owners query shape to copy (src/app/activities/page.tsx:102-114 and :167-171), with the status
predicate ADDED: `db.query.users.findMany({ where: and(isNull(users.deletedAt),
eq(users.status, "approved")), columns: { id: true, name: true, email: true }, orderBy: [users.name] })`
mapped to `{ id, name: u.name || "Unknown" }`. `people/page.tsx` fetches no users at all today, so this
query is new outright.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — retentionDays and bulkOwners props</name>
  <files>src/app/people/page.tsx</files>
  <read_first>
    - src/app/people/page.tsx — read the WHOLE file: `PAGE_SIZE`, the cumulative `limit = PAGE_SIZE * pageNum + 1` and `hasMore` slice, the `leftJoin` that supplies `ownerName`, and the `<DataTable ... />` call site
    - src/app/trash/page.tsx lines 75-125 — the `readTrashRetentionDays()` read inside a `Promise.all` and the `retentionDays={retentionDays}` prop pass, including the comment explaining that `null` selects the no-retention copy
    - src/lib/trash/settings.ts lines 55-80 — `readTrashRetentionDays`'s contract and its header stating there is deliberately NO code-level fallback
    - src/app/activities/page.tsx lines 95-120 and 160-175 — the owners query shape and the `u.name || "Unknown"` mapping. Read it ONLY as a shape source: its `where` has no `status` predicate and it feeds `ActivityFilters` plus `ActivityDialog`, so it must not be copied verbatim and must not be modified
    - src/db/schema/users.ts lines 1-25 — the `userStatusEnum` values
  </read_first>
  <action>
    Extend `src/app/people/page.tsx` with two new props for `DataTable`, both plain serializable values.

    1. Read the retention window alongside the existing data fetch — add `readTrashRetentionDays()` to
       the page's `Promise.all` (or introduce one in the shape `trash/page.tsx` uses) and pass
       `retentionDays={retentionDays}`, type `number | null`. Add NO `?? 30`, no `|| 30` and no other
       numeric default anywhere in this file: `readTrashRetentionDays()` fails closed to `null`, nothing
       is purged automatically in that state, and the delete dialog's `descriptionNoRetention` branch is
       what keeps the UI from promising a window the pruner is not enforcing.
    2. Add a NEW, SEPARATE owners query — do not fold it into the existing `leftJoin`:
       `db.query.users.findMany({ where: and(isNull(users.deletedAt), eq(users.status, "approved")),
       columns: { id: true, name: true, email: true }, orderBy: [users.name] })`, mapped to
       `{ id: u.id, name: u.name || "Unknown" }`, passed as `bulkOwners={bulkOwners}`. BOTH predicates
       are required: assigning 100 records to an unverified or rejected user is a data defect no
       per-record failure would report, because the write succeeds. Name the prop `bulkOwners`, not
       `owners`, so it is unmistakably the bulk picker's list.

    Do not change the existing `leftJoin`, the `PAGE_SIZE` constant, the cumulative `limit` arithmetic or
    the `hasMore` slice — the cumulative rows array is exactly what makes "selection persists across Load
    More" true. Do not import any component from `src/components/bulk/` into this server file; the bar and
    the report mount inside `data-table.tsx`, which is the `'use client'` module.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ./node_modules/.bin/vitest run "src/app/admin/fields/[entityType]/__tests__/rsc-boundary.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'readTrashRetentionDays' src/app/people/page.tsx` is at least 1
    - `grep -c 'retentionDays={' src/app/people/page.tsx` is 1 and `grep -c 'bulkOwners={' ...` is 1
    - `grep -c 'eq(users.status, "approved")' src/app/people/page.tsx` is 1
    - `grep -vE '^\s*[*/]' src/app/people/page.tsx | grep -cE '\?\? *[0-9]|\|\| *[0-9]'` is 0
    - `git diff src/app/people/page.tsx` shows no change to the `limit`/`hasMore` arithmetic or the `PAGE_SIZE` constant
    - `grep -c 'components/bulk' src/app/people/page.tsx` is 0
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
  </acceptance_criteria>
  <done>`people/page.tsx` passes `retentionDays: number | null` with no numeric default and a separate `bulkOwners` list filtered on both `deletedAt IS NULL` and `status = 'approved'`, with the pagination arithmetic untouched.</done>
</task>

<task type="auto">
  <name>Task 2: data-table.tsx — rowSelection, the select column, and the colSpan fix</name>
  <files>src/app/people/data-table.tsx</files>
  <read_first>
    - src/app/people/data-table.tsx — read the WHOLE file: the props interface including `search = ""` (~:38), the `useDataTableKeyboard` call (~:116-123), the `useReactTable` call (~:125-134), the root `<div className="space-y-4">` (~:137), the row rendering with its already-inert `data-state={row.getIsSelected() && "selected"}` (~:174-205), the empty-state `colSpan={columns.length}` (~:199), the `Load More` block (~:216), and the `handleRecordSaved` comment recording the measured `revalidatePath` behaviour
    - src/app/people/columns.tsx — read it to learn how the person display name is composed (the `people` table has separate `firstName` and `lastName` columns), so `getLabel` in Task 3 names records the way the table does. Do NOT edit this file
    - src/components/bulk/select-column.tsx — `useSelectColumn`'s signature, landed by plan 38-07
    - src/components/ui/table.tsx line 60 — `data-[state=selected]:bg-muted`
    - src/components/keyboard/data-table-keyboard.tsx lines 55-150 — `rowProps(index)`, whose `onClick` moves the keyboard cursor, and the hotkey list, which does NOT include `Escape`
    - src/app/globals.css lines 165-180 — the global `[data-selected="true"]` primary ring
    - .planning/phases/38-bulk-operations/38-UI-SPEC.md § Surface 1
  </read_first>
  <action>
    Edit `src/app/people/data-table.tsx`, applying exactly the same six changes made to
    `src/app/organizations/data-table.tsx` with the Person rename:

    1. `const [rowSelection, setRowSelection] = useState<RowSelectionState>({})`, importing
       `type RowSelectionState` from `@tanstack/react-table`.
    2. `const selectColumn = useSelectColumn<Person>(<the same name composition columns.tsx uses>)` and
       `const columnsWithSelect = useMemo(() => [selectColumn, ...columns], [selectColumn, columns])`.
       The select column is PREPENDED — first column — never appended.
    3. Extend the existing `useReactTable` call with `columns: columnsWithSelect`,
       `getRowId: (row) => row.id`, `state: { rowSelection }`, `onRowSelectionChange: setRowSelection`
       and `enableRowSelection: true`. Keep `getCoreRowModel` and the existing `meta` object
       byte-identical. `getRowId` is mandatory: TanStack's default row id is the row INDEX, and the rows
       array is cumulative across Load More, so index keys would silently retarget the selection onto
       different records after any reorder or removal — and the next action would be a bulk delete of
       records the user never picked.
    4. Add the defensive derivation exactly as given in `<interfaces>`: `loadedIds` as a `Set` over
       `data.map((r) => r.id)`, then `selectedIds` as the truthy `rowSelection` keys present in
       `loadedIds`. Do NOT use `table.getSelectedRowModel()`.
    5. Fix the empty-state colSpan: change `colSpan={columns.length}` to
       `colSpan={table.getAllLeafColumns().length}`. `columns.length` reads the PROP, which no longer
       matches the rendered column count.
    6. Add `useEffect(() => { setRowSelection({}) }, [search])` — keyed on the SEARCH STRING, never on
       `[data]`. This file's own `handleRecordSaved` comment records the measurement: an action that calls
       `revalidatePath` at all re-renders the CURRENT client tree a few milliseconds after the action
       resolves, whichever path it names. A `[data]`-keyed effect would fire mid-action and wipe the
       failed-record selection SC-3 requires to survive.

    Do NOT touch `src/app/people/columns.tsx`. Both `page.tsx` files import the STATIC `columns` array, so
    a checkbox column added there could not call `useTranslations`; and leaving the file alone removes the
    Phase 43 / POLISH-01 retype collision entirely.
    Do NOT add any ring, border or `data-selected` value to a bulk-selected row. The existing
    `data-state={row.getIsSelected() && "selected"}` plus `table.tsx:60` is the entire bulk row treatment;
    add zero CSS.
    Do NOT add `getPaginationRowModel`. Do NOT render the checkbox column conditionally or on hover.

    After finishing, run the byte-parity check that documents this file's relationship to its twin:
    `diff <(sed 's/Organization/Person/g; s/organizations/people/g' src/app/organizations/data-table.tsx) src/app/people/data-table.tsx`.
    It is expected to be non-empty ONLY where the two surfaces genuinely differ (the `getLabel`
    composition, since organizations have a single `name` column and people have `firstName`/`lastName`).
    Record the full diff output in the SUMMARY and justify every remaining line. If a line differs for any
    other reason, that is a drift bug — fix it here rather than documenting it.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ./node_modules/.bin/vitest run src/components/bulk/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getRowId: (row) => row.id' src/app/people/data-table.tsx` is 1
    - `grep -c 'enableRowSelection: true' src/app/people/data-table.tsx` is 1
    - `grep -c 'onRowSelectionChange: setRowSelection' src/app/people/data-table.tsx` is 1
    - `grep -c 'useSelectColumn' src/app/people/data-table.tsx` is 1 and `grep -c 'columnsWithSelect' ...` is at least 2
    - `grep -c 'colSpan={columns.length}' src/app/people/data-table.tsx` is 0 and `grep -c 'getAllLeafColumns' ...` is 1
    - `grep -c 'getSelectedRowModel' src/app/people/data-table.tsx` is 0 and `grep -c 'getPaginationRowModel' ...` is 0
    - The clear-on-filter effect's dependency array is `[search]`: read the effect and record its exact dependency array in the SUMMARY. It must not contain `data`
    - The rename-normalised diff against `organizations/data-table.tsx` is recorded in full in the SUMMARY, and every remaining differing line is justified as a genuine surface difference
    - `git status --porcelain src/app/people/columns.tsx` is empty
    - `git status --porcelain src/app/globals.css src/components/ui/table.tsx` is empty (zero new CSS)
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
  </acceptance_criteria>
  <done>The People table has `rowSelection` keyed on the record id, a prepended shared checkbox column, a colSpan read from the table, a defensively derived `selectedIds`, and a clear-on-filter effect keyed on the search string — and the rename-normalised diff against its Organizations twin is recorded and justified line by line.</done>
</task>

<task type="auto">
  <name>Task 3: data-table.tsx — mount the bar, the spacer and the failure report</name>
  <files>src/app/people/data-table.tsx</files>
  <read_first>
    - src/app/people/data-table.tsx — the file you just edited, especially the root `space-y-4` stack and the `Load More` block the bar must render after
    - src/components/bulk/bulk-action-bar.tsx — `BulkActionBarProps`, landed by plan 38-10, including `getLabel`, `onOutcome` and `onClear`
    - src/components/bulk/bulk-failure-report.tsx — `BulkFailureReportProps`, landed by plan 38-09
    - src/app/people/actions.ts — the three bulk actions, landed by plan 38-12, and their exact signatures
    - src/lib/bulk/types.ts — `BulkOutcome` and `BulkFailure`
    - .planning/phases/38-bulk-operations/38-UI-SPEC.md § Surface 3 and § Surface 7
  </read_first>
  <action>
    Add the bulk mounts to `src/app/people/data-table.tsx`.

    Local state: `const [outcome, setOutcome] = useState<BulkOutcome | null>(null)`.

    Mount `<BulkFailureReport>` ABOVE the table — at the top of the root `space-y-4` stack, after the
    search/filter row and before the `Card`/table — passing `kind={outcome.kind}`,
    `failures={outcome.failed}`, `labelById={outcome.labelById}` and
    `onDismiss={() => setOutcome(null)}`, rendered only when `outcome !== null && outcome.failed.length > 0`.
    It must NOT go inside the fixed bar, which stays a single compact control cluster at every viewport.

    Mount `<BulkActionBar>` as the LAST element of the root stack, AFTER the `Load More` block, so nothing
    above the fold moves when it appears and its `h-20` spacer sits below everything. Props:
      entityType="person"
      selectedIds={selectedIds}
      getLabel={(id) => <the same name composition used for the select column's aria-label, falling back to the raw id>}
      retentionDays={retentionDays}
      owners={bulkOwners}
      onDelete={bulkDeletePeople}
      onReassign={bulkReassignPersonOwner}
      onExport={exportSelectedPeople}
      onOutcome={handleOutcome}
      onClear={() => { setRowSelection({}); setOutcome(null) }}

    `entityType="person"` — the singular `EntityType` literal, matching
    `src/db/schema/custom-fields.ts:19`. The bar uses it for `ENTITY_TO_TRASH_TAB`, which maps
    `person -> "people"`, so the Trash deep link lands on the right tab.

    `handleOutcome(next: BulkOutcome)` must, in this order:
      1. `setRowSelection((prev) => ...)` removing every id in `next.succeeded` and PRESERVING every id in
         `next.failed`. Do this EXPLICITLY here, in the handler — never via an effect. Failed records
         staying selected is what makes the retry one click, and it is a locked decision.
      2. `setOutcome(next.failed.length > 0 ? next : null)` so a fully successful action clears any
         previous report.
      3. Call `refresh?.()` following this file's existing `handleRecordSaved` convention — read that
         handler and match it. Do NOT reintroduce a prop named `onSuccess`; the Phase 35 rename to
         `onRecordSaved` is enforced by seven call sites and must not be undone.

    Add `retentionDays: number | null` and `bulkOwners: { id: string; name: string }[]` to this
    component's props interface, matching what Task 1 passes from `page.tsx`.

    Do NOT wrap the server actions in local closures that reshape their arguments — pass the imported
    functions directly, so a mismatch is a type error rather than a runtime surprise.
    Do NOT add any keyboard binding here; `Escape` is owned by the bar.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ./node_modules/.bin/vitest run src/components/bulk/ src/app/people/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'BulkActionBar' src/app/people/data-table.tsx` is at least 2 (import and usage)
    - `grep -c 'BulkFailureReport' src/app/people/data-table.tsx` is at least 2
    - `grep -c 'bulkDeletePeople\|bulkReassignPersonOwner\|exportSelectedPeople' src/app/people/data-table.tsx` is at least 6
    - `grep -c 'entityType="person"' src/app/people/data-table.tsx` is 1 (singular, not `"people"`)
    - `grep -c 'onSuccess' src/app/people/data-table.tsx` is 0
    - Read the JSX and confirm the `BulkActionBar` element appears AFTER the `Load More` block and the `BulkFailureReport` element appears BEFORE the table; record the two line numbers in the SUMMARY
    - `handleOutcome` removes succeeded ids and preserves failed ids in one explicit `setRowSelection` call; record the handler body's control flow in the SUMMARY
    - Read every `useEffect` in the file and confirm none of them calls `setRowSelection({})` with a dependency array containing `data` or `outcome`; record each effect's dependency array in the SUMMARY
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
    - `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` passes
  </acceptance_criteria>
  <done>The failure report mounts above the table and the bar plus its spacer mount after Load More, both wired to the three People bulk actions with the singular `entityType="person"`, and succeeded ids are deselected while failed ids are preserved explicitly in the outcome handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server page -> client table | Only plain serializable values cross (`retentionDays: number \| null`, `bulkOwners: {id,name}[]`) |
| selection state -> destructive action | The id set the bar submits is derived here; a phantom or mis-keyed id is a delete of a record the user never picked |
| owners list -> ownership transfer | An unapproved or soft-deleted user in that list becomes the owner of up to 100 records, and the write succeeds so no per-record failure reports it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-36 | Tampering (acting on the wrong records) | `getRowId` | mitigate | `getRowId: (row) => row.id` is mandatory because TanStack's default row id is the row INDEX and the rows array is cumulative across Load More. Asserted by a source check |
| T-38-37 | Tampering (phantom ids in a destructive submit) | `selectedIds` derivation | mitigate | The intersection of `rowSelection`'s truthy keys with `loadedIds`, because TanStack does not prune `rowSelection` when a row leaves `data`. `table.getSelectedRowModel()` is asserted absent |
| T-38-33 | Tampering (failed-id selection wiped mid-action) | the clear-on-filter effect | mitigate | Keys on `[search]`, never `[data]`. Phase 35 measured that `revalidatePath` re-renders the current client tree regardless of the path argument, and every bulk action calls it. Succeeded ids are removed explicitly in `handleOutcome` |
| T-38-06 | Elevation of Privilege (ownership transfer to an inactive principal) | `bulkOwners` | mitigate | Filters on `isNull(users.deletedAt)` AND `eq(users.status, "approved")`, as a NEW separate query so no existing dropdown's predicate changes. The server action re-validates the target once before the loop |
| T-38-10 | Repudiation (UI lies about configuration) | `retentionDays` | mitigate | Passed straight through from `readTrashRetentionDays()` with no numeric default anywhere in either file, asserted by a regex source check |
| T-38-26 | Information Disclosure (RSC boundary) | the page -> table props | mitigate | `page.tsx` imports nothing from `src/components/bulk/` and passes only plain values; both dialogs live inside the `'use client'` bar. The repo-wide `rsc-boundary.test.tsx` gate is run in this plan's verification |
| T-38-38 | Denial of Service (bar covering the primary affordance) | the bar mount point | mitigate | The bar and its `h-20` spacer are the LAST elements of the root stack, after the `Load More` block, verified by reading the JSX order and recorded by line number |
| T-38-39 | Tampering (silent divergence from the Organizations twin) | `people/data-table.tsx` | mitigate | The two files are byte-identical modulo the rename. A rename-normalised `diff` against the Organizations file is run and every remaining line justified, so a wiring change applied to one surface and not the other cannot ship unnoticed |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (38-RESEARCH § Package Legitimacy Audit: empty input set) |
</threat_model>

<verification>
- `npm run typecheck` — 0 errors, no new `@ts-expect-error`; `npm run lint` — 0 errors
- `./node_modules/.bin/vitest run "src/app/admin/fields/[entityType]/__tests__/rsc-boundary.test.tsx"` — green
- `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts src/components/bulk/` — green
- `git status --porcelain src/app/people/columns.tsx src/app/globals.css src/components/ui/table.tsx` — empty
- The rename-normalised diff against `organizations/data-table.tsx` recorded and every line justified
</verification>

<success_criteria>
- `people/page.tsx` passes `retentionDays: number | null` (no numeric default) and a separate `bulkOwners` list filtered on `deletedAt IS NULL` and `status = 'approved'`
- `data-table.tsx` enables `rowSelection` with `getRowId: (row) => row.id`, prepends the shared select column, and reads its empty-state colSpan from the table
- `selectedIds` is derived by intersecting `rowSelection` keys with `loadedIds`
- The clear-on-filter effect keys on `[search]`; no effect clears the selection off a server-supplied array
- The failure report mounts above the table; the bar and spacer mount after `Load More`; `entityType="person"`
- `handleOutcome` deselects succeeded ids and preserves failed ids explicitly
- `columns.tsx` is untouched, zero CSS is added, no `getPaginationRowModel` is introduced
- The rename-normalised diff against the Organizations twin is recorded and fully justified
</success_criteria>

<output>
Create `.planning/phases/38-bulk-operations/38-16-SUMMARY.md` when done
</output>
