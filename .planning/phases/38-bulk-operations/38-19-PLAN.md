---
phase: 38-bulk-operations
plan: 19
type: execute
wave: 5
depends_on: [38-15, 38-16, 38-17, 38-18]
files_modified:
  - src/components/bulk/__tests__/select-wiring.test.ts
autonomous: true
requirements: [BULK-01, BULK-02, BULK-03, BULK-04]

must_haves:
  truths:
    - "All three list tables key their selection on the record id, not the row index"
    - "No surface clears its selection from an effect keyed on a server-supplied array"
    - "No surface derives its submitted ids from getSelectedRowModel"
    - "The deal card stops all three propagation channels the dnd-kit sensors listen on"
    - "The bar and its spacer mount after the Load More button on every surface that has one"
    - "The whole suite, typecheck and lint are green with zero new type suppressions"
  artifacts:
    - path: "src/components/bulk/__tests__/select-wiring.test.ts"
      provides: "one comment-stripped gate covering all four surfaces at once"
      contains: "readStrippedSource"
  key_links:
    - from: "src/components/bulk/__tests__/select-wiring.test.ts"
      to: "the four surface modules"
      via: "readStrippedSource over each, with a positive marker asserted before every negative"
      pattern: "readStrippedSource"
---

<objective>
Add the one cross-surface source gate that no individual surface plan could own, then prove the whole
phase green.

Purpose: the four surface wirings (plans 38-15 through 38-18) all landed in the same wave, so a gate
asserting a rule across all of them could not live inside any one of them without going red until the
others merged. This plan owns that gate. It exists because the phase's most dangerous wiring mistakes are
each invisible in a single file's own diff: an index-keyed `getRowId` on one of three tables, a
`[data]`-keyed clear effect on one surface, a missing propagation stop on the deal card, or a bar mounted
above `Load More`. Every one of those ships green under `npm test` today.
Output: one test file plus the phase-wide gate run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-bulk-operations/38-PATTERNS.md
@.planning/phases/38-bulk-operations/38-VALIDATION.md

<interfaces>
The seven surface modules this gate reads, all via `readStrippedSource` (repo-relative paths):
  src/app/organizations/data-table.tsx
  src/app/people/data-table.tsx
  src/app/activities/activity-list.tsx
  src/app/activities/activities-client.tsx
  src/app/deals/deal-card.tsx
  src/app/deals/kanban-column.tsx
  src/app/deals/kanban-board.tsx

Source-gate helpers, exported from src/components/custom-fields/__tests__/source-scan.ts:
  export function stripComments(source: string): string          // string-aware: tracks " ' and backticks
  export function readStrippedSource(path: string): string
  export function callArguments(source: string, callee: string): string[]

Consumer template with reusable vocabulary tables:
  src/app/trash/__tests__/trash-client-wiring.test.ts:19-50
    CLIENT_DIRECTIVE  = /^\s*(['"])use client\1/
    FORBIDDEN_COLOURS = ["text-red-", "text-green-", "bg-red-", "bg-green-", "bg-white", "text-black"]
    HEX_LITERAL       = /#[0-9a-fA-F]{3,6}/
    BARE_LABELS       = [">Save<", ">Cancel<", ">Confirm<", ">OK<", ">Yes<", ">Apply<"]

Three anti-vacuity requirements, from src/lib/audit/no-mutation-coupling.test.ts:38-50:
  1. Prove the files were found and read — a glob that silently matches zero files passes every
     "no file contains X" test perfectly.
  2. Prove they are the RIGHT files, by asserting a known POSITIVE marker before asserting any negative.
  3. A gate for the gate: vocabulary tables pinning what the detector recognises and what it must leave
     alone.

Known pre-existing token debt that this gate must NOT flag, and must therefore scope around:
  - src/app/activities/activity-list.tsx ~:400-452 contains `border-red-200 bg-red-50 text-red-600
    text-red-700` and `bg-white` in its overdue banner. Pre-existing, in a block this phase does not
    touch, explicitly out of scope. A blanket FORBIDDEN_COLOURS scan over that whole file would go red on
    day one, which is worse than no gate: it would be disabled. Scope the colour assertions to the files
    this phase actually authored (`src/components/bulk/**`) and to the specific ADDED regions elsewhere,
    or exclude `activity-list.tsx` from the colour table with the exclusion stated in a named message.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The cross-surface select-wiring gate</name>
  <files>src/components/bulk/__tests__/select-wiring.test.ts</files>
  <read_first>
    - src/app/trash/__tests__/trash-client-wiring.test.ts — read in full: the module-scope `readStrippedSource` constants, the vocabulary tables, the `CLIENT_DIRECTIVE` regex, and the named-message `expect` style
    - src/components/custom-fields/__tests__/source-scan.ts — `stripComments`, `readStrippedSource`, `callArguments`
    - src/lib/audit/no-mutation-coupling.test.ts lines 38-60 and 190-210 — the three anti-vacuity requirements, the tombstone-comment assertion that PROVES the stripping ran, and `sliceDeclaration`'s WR-13 discipline
    - src/app/organizations/data-table.tsx and src/app/people/data-table.tsx — the wiring landed by plans 38-15 and 38-16
    - src/app/activities/activity-list.tsx and src/app/activities/activities-client.tsx — the lifted-state wiring landed by plan 38-17, including the overdue banner this gate must not flag
    - src/app/deals/deal-card.tsx, src/app/deals/kanban-column.tsx, src/app/deals/kanban-board.tsx — the kanban wiring landed by plan 38-18
    - .planning/STATE.md — the Phase 37 entry recording that a raw-text grep gate collided with an explanatory comment NINE times in one phase, including once with the plan's own suggested wording
  </read_first>
  <action>
    Create `src/components/bulk/__tests__/select-wiring.test.ts`. Read all seven surface modules through
    `readStrippedSource` at module scope; never `readFileSync`.

    Header comment, stating three things: (a) every assertion here is comment-blind by construction,
    because Phase 37 lost nine gate runs to a raw-text grep colliding with an explanatory comment —
    including once with the plan's own suggested wording — and the correct response to a collision is to
    reword the comment, never to weaken the gate; (b) this gate lives here rather than in any surface plan
    because it asserts rules ACROSS four surfaces that all landed in one wave; (c) `activity-list.tsx`'s
    overdue banner carries pre-existing red tokens that are deliberately excluded from the colour
    assertions, with the exclusion named rather than silent.

    ANTI-VACUITY, all three requirements:
      1. Assert every one of the seven stripped sources has non-zero length, in a single table-driven test
         that names each path. A silently-empty read would pass every negative assertion below.
      2. POSITIVE MARKERS FIRST, before any negative assertion in the file:
         - the three table modules each contain `useReactTable`
         - `deal-card.tsx` contains `useSortable`
         - `kanban-column.tsx` contains the header row's `justify-between`
         - `kanban-board.tsx` contains `useSensors`
         If any of those moves, this gate must go red and be reconsidered rather than keep passing over
         files that no longer do what it thinks they do.
      3. PROVE THE STRIPPING RAN. Assert that at least one of the seven RAW sources contains a comment
         mentioning a token this gate forbids, while the STRIPPED source does not — the same technique
         `no-mutation-coupling.test.ts` uses when it asserts four files carry a tombstone comment saying
         "audit" out loud. If no such comment exists naturally, assert instead that
         `stripComments("// getSelectedRowModel\n")` yields no match, so the stripper itself is exercised
         by the gate rather than assumed.
      4. A vocabulary table pinning what the detector RECOGNISES and a second pinning what it must LEAVE
         ALONE, each iterated by its own test.

    SUBSTANTIVE ASSERTIONS.

    A. Record-id keying — all three tables plus the kanban.
       For `organizations/data-table.tsx`, `people/data-table.tsx` and `activities/activity-list.tsx`:
         - contains `getRowId` and the `getRowId` argument resolves to the row's `id` (use
           `callArguments` or a targeted regex on `getRowId:` — record which you used)
         - contains `enableRowSelection: true`
         - contains `onRowSelectionChange`
       Named message: TanStack's default row id is the row INDEX, and the rows arrays are CUMULATIVE
       across Load More, so index keys would silently retarget the selection onto different records after
       any reorder or removal — and the next action would be a bulk delete of records the user never
       picked.

    B. No `getSelectedRowModel`, anywhere.
       Assert zero occurrences across all seven stripped sources. Named message: TanStack does not prune
       `rowSelection` when a row leaves `data`, so a model-derived list can carry phantom ids; every
       surface must intersect with its loaded ids instead.

    C. No effect clearing the selection off a server-supplied array.
       For each of the four state-owning modules (`organizations/data-table.tsx`,
       `people/data-table.tsx`, `activities/activities-client.tsx`, `deals/kanban-board.tsx`), locate every
       `useEffect` whose body calls `setRowSelection` or `setSelectedDealIds` and assert its dependency
       array does NOT contain the module's server-supplied collection identifier (`data` for the two
       tables, `activities` for the Activities client, `dealsByStage` / `initialDealsByStage` for the
       board). Implement this by extracting each effect's dependency-array text with a regex over the
       stripped source; if the regex proves brittle, fall back to asserting the ABSENCE of the exact
       forbidden dependency-array literals (`, [data])`, `, [activities])`, `, [dealsByStage])`,
       `, [initialDealsByStage])`) in the same statement as the setter — and say in the message which
       approach was used.
       Named message: Phase 35 measured that `revalidatePath` re-renders the current client tree
       regardless of the path argument (Next 16.1.6), and every bulk action calls it, so such an effect
       would fire mid-action and wipe the failed-record selection SC-3 requires to survive.

    D. Empty-state colSpan reads the table, not the prop.
       For the three table modules: zero occurrences of `colSpan={columns.length}`, at least one
       occurrence of `getAllLeafColumns`.

    E. The select column is PREPENDED, not appended.
       For the three table modules: assert the `columnsWithSelect` construction places the select column
       first — assert the presence of a spread whose select entry precedes `...columns` and the absence of
       one where `...columns` precedes it.

    F. The deal card's three propagation stops.
       For `deal-card.tsx`: assert it contains `onPointerDown`, `onKeyDown`, and at least three
       `stopPropagation` occurrences. Named message per handler, naming what each prevents: the expand
       toggle, `PointerSensor`'s `activationConstraint: { distance: 5 }` being crossed by a 6px wobble, and
       `KeyboardSensor` consuming Space so keyboard selection becomes impossible.
       Also assert `deal-card.tsx` contains `bg-primary/5` and that its count of `ring-2 ring-primary` is
       exactly 2 (the expanded state and the keyboard cursor) — no fourth ring treatment was added.

    G. Bar and spacer mount position.
       For `organizations/data-table.tsx`, `people/data-table.tsx` and `activities/activities-client.tsx`:
       assert the index of `BulkActionBar` in the stripped source is GREATER than the index of the Load
       More affordance (find its marker by reading each file — it is a `Button` with a load-more handler;
       resolve the exact token per file and record the three tokens in the SUMMARY). Assert the index of
       `BulkFailureReport` is LESS than the index of the table/list element.
       Named message: the bar is `fixed`, so its `h-20` spacer must sit below everything; a spacer above
       the `Load More` button means the bar covers the button, which is the exact defect the spacer exists
       to prevent.

    H. Kanban capping and boundary.
       For `kanban-column.tsx`: contains `selectAllInStageCapped`, contains `BULK_MAX_IDS`, contains
       `indeterminate`, contains `disabled={deals.length === 0}`.
       For `kanban-board.tsx`: contains `BULK_MAX_IDS`, contains `useState<Set<string>>`, contains
       `entityType="deal"`.

    I. Deferred ideas are absent from every surface.
       Assert zero occurrences, across all seven stripped sources, of tokens that would indicate a
       deferred idea shipped: `bulkStage`, `moveToStage` (as a bulk control), `assigneeId` in any
       Activities bulk path, `selectAllMatching`, `selectAllFiltered`, and `getPaginationRowModel`. Put
       these in the RECOGNISED vocabulary table so the list is explicit and iterated.

    J. Colour and copy hygiene, SCOPED.
       Apply `FORBIDDEN_COLOURS` and `HEX_LITERAL` to every file under `src/components/bulk/` (walk the
       directory) and to `deal-card.tsx` and `kanban-column.tsx`. EXCLUDE `activity-list.tsx` from the
       colour assertions with the exclusion stated in the test's message and justified by its pre-existing
       overdue banner. Assert `BARE_LABELS` and `tCommon` are absent from every file under
       `src/components/bulk/`.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/components/bulk/__tests__/select-wiring.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/components/bulk/__tests__/select-wiring.test.ts` passes with at least 20 tests
    - `grep -c 'readStrippedSource' src/components/bulk/__tests__/select-wiring.test.ts` is at least 7 and `grep -c 'readFileSync' ...` is 0
    - All seven module paths appear in the file; record the list in the SUMMARY
    - The positive-marker test appears BEFORE every negative-assertion test in file order; record the line numbers in the SUMMARY
    - A stripping-proof assertion exists (either a real raw-vs-stripped comparison or an explicit `stripComments` exercise); record which form was used and why
    - Both a RECOGNISED and a LEFT-ALONE vocabulary table exist and each is iterated
    - The `activity-list.tsx` colour exclusion is stated in a named `expect` message, not silent
    - Negative proof 1: temporarily change `getRowId: (row) => row.id` to `getRowId: (_row, index) => String(index)` in `people/data-table.tsx`, run the gate, observe FAILURE naming the index-key hazard, revert. Record the message
    - Negative proof 2: temporarily change the Activities clear effect's dependency array to `[activities]`, run the gate, observe FAILURE, revert. Record the message
    - Negative proof 3: temporarily remove `onKeyDown` from the deal-card checkbox wrapper, run the gate, observe FAILURE naming `KeyboardSensor`, revert. Record the message
    - Negative proof 4: temporarily move `<BulkActionBar>` above the `Load More` block in `organizations/data-table.tsx`, run the gate, observe FAILURE, revert. Record the message
    - `npm run typecheck` reports 0 errors
  </acceptance_criteria>
  <done>One comment-blind gate asserts record-id keying, the absence of `getSelectedRowModel`, safe clear-effect dependencies, the table-derived colSpan, column prepending, the deal card's three stops and unchanged ring count, the bar/spacer mount order, the kanban cap, the absence of every deferred idea, and scoped colour hygiene — with four negative directions demonstrated.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide gate run</name>
  <files>src/components/bulk/__tests__/select-wiring.test.ts</files>
  <read_first>
    - .planning/phases/38-bulk-operations/38-VALIDATION.md — the sampling contract, the full-suite command, and the current baseline of 1703 passing
    - package.json — the `test`, `typecheck` and `lint` script definitions, so the commands you run are the project's own
    - .planning/STATE.md — the Phase 37 note that `src/lib/execution/condition-evaluator.test.ts` T-34-20 is a wall-clock ratio assertion (`large/small < 10`, measured 11.9-15.6) that fails intermittently under vitest's OWN parallel workers. It is a Phase 34 file and a pre-existing live flake on master; if it fails, re-run it in isolation and record the result rather than attributing it to this phase or "fixing" it here
  </read_first>
  <action>
    Run the phase's full gate set and record the results. This task writes no production code; if a gate
    fails, fix the cause in the file that owns it and record what changed.

    1. `npm test` — both vitest projects. Record the total passing count and compare it against the
       Phase 37 close-out baseline of 1703. The count must be strictly greater; record the delta and
       attribute it to this phase's new test files.
    2. `npm run typecheck` — must report 0 errors.
    3. `npm run lint` — must report 0 errors.
    4. `git diff --stat` plus a repo-wide search for `@ts-expect-error` and `eslint-disable` added by this
       phase: `git diff master --stat` and
       `git diff master -- 'src/**' | grep -c '^+.*@ts-expect-error'` must be 0, and the same for
       `^+.*eslint-disable`. Phase 43 (POLISH-01) exists to clear the 14 existing type suppressions and
       must not inherit a fifteenth from this phase.
    5. `git diff master --stat -- package.json package-lock.json` must be empty — this phase installs zero
       packages (38-RESEARCH § Package Legitimacy Audit: empty input set), so any dependency change is a
       finding, not a detail.
    6. Confirm the `src/lib/export/formatters-live.test.ts` suite from plan 38-04 reports as SKIPPED under
       `npm test` (no `DATABASE_URL` in the hermetic run) and PASSES when run with
       `DATABASE_URL="postgresql://pipelite:pipelite@localhost:5433/pipelite"`. Record both outcomes.
    7. Run the three pre-existing repo-wide gates explicitly and record each result:
       `./node_modules/.bin/vitest run src/lib/audit/no-mutation-coupling.test.ts`
       `./node_modules/.bin/vitest run "src/app/admin/fields/[entityType]/__tests__/rsc-boundary.test.tsx"`
       `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts`

    If `src/lib/execution/condition-evaluator.test.ts` fails, re-run that file alone
    (`./node_modules/.bin/vitest run src/lib/execution/condition-evaluator.test.ts`) and record both
    results. It is a known pre-existing wall-clock flake in a Phase 34 file and is out of this phase's
    scope — do not adjust its threshold here, and do not report it as a Phase 38 regression.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` passes; the total passing count is recorded and is strictly greater than 1703, with the delta attributed to this phase's new files
    - `npm run typecheck` reports 0 errors; `npm run lint` reports 0 errors
    - `git diff master -- 'src/**' | grep -c '^+.*@ts-expect-error'` is 0 and the same count for `^+.*eslint-disable` is 0
    - `git diff master --stat -- package.json package-lock.json` is empty
    - `src/lib/export/formatters-live.test.ts` is recorded as SKIPPED under `npm test` and PASSING with an explicit `DATABASE_URL`
    - All three pre-existing repo-wide gates recorded green individually
    - If `condition-evaluator.test.ts` failed, its isolated re-run result is recorded and it is explicitly attributed to the known Phase 34 flake rather than to this phase
    - Every one of the four requirement IDs (BULK-01..BULK-04) is mapped in the SUMMARY to at least one green automated command from 38-VALIDATION.md's per-task table
  </acceptance_criteria>
  <done>The full suite, typecheck and lint are green with a recorded passing-count delta, zero new type or lint suppressions, zero dependency changes, and all four pre-existing repo-wide gates individually confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-file diff -> cross-surface invariant | Every rule in this plan is invisible in any single file's own diff, which is why it needs its own gate |
| gate text -> explanatory comments | A raw-text gate is trivially invalidated by a comment that mentions the code it forbids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-36 | Tampering (acting on the wrong records) | `getRowId` on three tables | mitigate | Asserted across all three table modules at once, with the argument proven to resolve to the row's `id`. An index-keyed `getRowId` on any one surface would otherwise ship green, because no per-file test compares the three |
| T-38-33 | Tampering (failed-id selection wiped mid-action) | the four clear effects | mitigate | Every effect that clears a selection is checked for a forbidden server-supplied dependency across all four state-owning modules, with the Phase 35 `revalidatePath` measurement named in the failure message |
| T-38-37 | Tampering (phantom ids in a destructive submit) | `getSelectedRowModel` | mitigate | Asserted absent across all seven modules, so no surface can regress to a model-derived list that carries un-pruned keys |
| T-38-41 | Denial of Service (keyboard user cannot select) | `deal-card.tsx` `onKeyDown` | mitigate | Asserted present with a message naming `KeyboardSensor`, and the negative direction demonstrated by removing it. This is the single wiring omission that would make the kanban unusable by keyboard while looking correct in a screenshot |
| T-38-38 | Denial of Service (bar covering the primary affordance) | bar/spacer mount order | mitigate | Asserted by comparing source indices of `BulkActionBar` against each surface's Load More marker, with the negative direction demonstrated by moving the bar |
| T-38-44 | Tampering (deferred idea shipped) | all seven surfaces | mitigate | A RECOGNISED vocabulary table asserts the absence of `bulkStage`, a bulk `moveToStage`, an Activities `assigneeId` bulk path, `selectAllMatching`, `selectAllFiltered` and `getPaginationRowModel` — every one of which corresponds to a specific deferred idea in 38-CONTEXT.md |
| T-38-19 | Repudiation (self-invalidating gate) | this gate | mitigate | All reads go through the shared string-aware `readStrippedSource`, a positive marker precedes every negative, the file-found assertion rules out a vacuous pass, and the stripping itself is proven to run rather than assumed |
| T-38-45 | Repudiation (gate disabled by an unavoidable failure) | colour assertions over `activity-list.tsx` | accept | The overdue banner's pre-existing red tokens are explicitly excluded, with the exclusion named in the failure message. A blanket scan would go red on day one and would then be deleted, which is strictly worse than a scoped gate that stays enabled |
| T-38-SC | Tampering | npm/pip/cargo installs | mitigate | Task 2 asserts `git diff master --stat -- package.json package-lock.json` is empty. 38-RESEARCH § Package Legitimacy Audit declares an empty input set for the whole phase, so any dependency change here is a finding rather than a detail |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/components/bulk/__tests__/select-wiring.test.ts` — green, with four negative directions demonstrated and reverted
- `npm test` — green, passing count strictly greater than 1703
- `npm run typecheck` — 0 errors; `npm run lint` — 0 errors
- Zero added `@ts-expect-error`, zero added `eslint-disable`, zero dependency changes
- `no-mutation-coupling.test.ts`, `rsc-boundary.test.tsx` and `locale-parity.test.ts` each green individually
</verification>

<success_criteria>
- One comment-blind cross-surface gate exists, satisfying all three anti-vacuity requirements including a proof that the stripping ran
- All three tables are proven to key selection on the record id and to read their colSpan from the table
- No surface uses `getSelectedRowModel` or clears its selection off a server-supplied array
- The deal card's three propagation stops are proven present and its ring count unchanged
- The bar mounts after Load More and the report before the table on every surface that has both
- Every deferred idea is proven absent from all seven surface modules
- The full suite, typecheck and lint are green with the passing-count delta recorded and no new suppressions or dependencies
- All four requirement IDs are mapped to a green automated command
</success_criteria>

<output>
Create `.planning/phases/38-bulk-operations/38-19-SUMMARY.md` when done
</output>
