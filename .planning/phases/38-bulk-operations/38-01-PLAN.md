---
phase: 38-bulk-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/en-US.json
  - src/messages/es-ES.json
  - src/messages/pt-BR.json
  - src/messages/locale-parity.test.ts
autonomous: true
requirements: [BULK-01, BULK-02, BULK-03, BULK-04]

must_haves:
  truths:
    - "Every string the bulk surfaces render exists in en-US, es-ES and pt-BR"
    - "Shipping a bulk.* key that is not in the checked-in contract fails the suite"
    - "Removing a bulk.* key from one locale fails the suite"
  artifacts:
    - path: "src/messages/en-US.json"
      provides: "44 bulk.* keys under a new top-level bulk namespace"
      contains: "selectAllInStageCapped"
    - path: "src/messages/es-ES.json"
      provides: "the same 44 keys, translated"
      contains: "selectAllInStageCapped"
    - path: "src/messages/pt-BR.json"
      provides: "the same 44 keys, translated"
      contains: "selectAllInStageCapped"
    - path: "src/messages/locale-parity.test.ts"
      provides: "REQUIRED_BULK_KEYS contract + BULK_NAMESPACE + bulkKeys selector"
      contains: "REQUIRED_BULK_KEYS"
  key_links:
    - from: "src/messages/locale-parity.test.ts"
      to: "REQUIRED_BULK_KEYS"
      via: "the five assertion bodies, each passed the list separately"
      pattern: "(missingIn|blankIn|untranslatedInBoth|placeholderDrift)\\(REQUIRED_BULK_KEYS\\)"
    - from: "src/messages/locale-parity.test.ts"
      to: "bulkKeys"
      via: "exact-contract key-set assertion per locale"
      pattern: "bulkKeys\\[locale\\]"
---

<objective>
Land the phase's entire copy contract — 44 `bulk.*` keys in all three locale files — and extend
`locale-parity.test.ts` so the contract is mechanically enforced before any component consumes it.

Purpose: every other plan in this phase calls `useTranslations("bulk")`. If the keys land
piecemeal alongside the components, `locale-parity.test.ts`'s whole-file parity gate goes red on
every intermediate commit and its exact-contract assertion goes red for every key shipped without
a contract entry. Landing the copy first makes every downstream plan additively green.
Output: 44 keys × 3 locales, plus `BULK_NAMESPACE`, `bulkKeys` and `REQUIRED_BULK_KEYS` wired into
the five existing assertion bodies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-bulk-operations/38-UI-SPEC.md
@.planning/phases/38-bulk-operations/38-PATTERNS.md

<interfaces>
The 44-key contract. Copy these dot-paths and en-US values EXACTLY — they are the contract that
`REQUIRED_BULK_KEYS` pins and that every downstream plan calls by name.

Selection (3 + 1 capped variant = 4):
  bulk.selectRow                          Select {name}
  bulk.selectAllLoaded                    {count, plural, one {Select the 1 loaded record} other {Select all # loaded records}}
  bulk.selectAllInStage                   {count, plural, one {Select the 1 deal in {stage}} other {Select all # deals in {stage}}}
  bulk.selectAllInStageCapped             Select the first {max} of {total} deals in {stage}

Action bar (7):
  bulk.selected                           {count, plural, one {# selected} other {# selected}}
  bulk.actionBarLabel                     {count, plural, one {Actions for # selected record} other {Actions for # selected records}}
  bulk.reassignOwner                      Reassign owner
  bulk.exportCsv                          Export CSV
  bulk.delete                             Delete
  bulk.clearSelection                     Clear selection
  bulk.exporting                          Preparing CSV…

Delete dialog (6):
  bulk.deleteDialog.title                 {count, plural, one {Delete 1 record?} other {Delete # records?}}
  bulk.deleteDialog.description           {count, plural, one {# record moves to Trash. You can restore it for {days} days.} other {# records move to Trash. You can restore them for {days} days.}}
  bulk.deleteDialog.descriptionNoRetention {count, plural, one {# record moves to Trash. You can restore it until an admin deletes it permanently.} other {# records move to Trash. You can restore them until an admin deletes them permanently.}}
  bulk.deleteDialog.cancel                Keep records
  bulk.deleteDialog.confirm               {count, plural, one {Delete 1 record} other {Delete # records}}
  bulk.deleteDialog.deleting              Deleting…

Reassign dialog (8):
  bulk.reassignDialog.title               Reassign owner
  bulk.reassignDialog.description         {count, plural, one {Choose the new owner of # selected record.} other {Choose the new owner of # selected records.}}
  bulk.reassignDialog.ownerLabel          New owner
  bulk.reassignDialog.ownerPlaceholder    Choose a user
  bulk.reassignDialog.noEmailNotice       The new owner isn't emailed about this change.
  bulk.reassignDialog.cancel              Keep current owners
  bulk.reassignDialog.confirm             {count, plural, one {Reassign 1 record} other {Reassign # records}}
  bulk.reassignDialog.reassigning         Reassigning…

Results (6):
  bulk.deleted                            {count, plural, one {# record moved to Trash.} other {# records moved to Trash.}}
  bulk.reassigned                         {count, plural, one {# record reassigned to {owner}.} other {# records reassigned to {owner}.}}
  bulk.exported                           {count, plural, one {# record exported.} other {# records exported.}}
  bulk.partial                            {succeeded} of {total} succeeded. {failed} failed.
  bulk.openTrash                          Open Trash
  bulk.working                            Working…

Failure report (4):
  bulk.failures.deleteTitle               {count, plural, one {# record wasn't deleted} other {# records weren't deleted}}
  bulk.failures.reassignTitle             {count, plural, one {# record wasn't reassigned} other {# records weren't reassigned}}
  bulk.failures.retryHint                 These records are still selected. Fix the problem and try again.
  bulk.failures.dismiss                   Dismiss

Errors (5):
  bulk.error.tooMany                      You can act on at most {max} records at once. {count} are selected.
  bulk.error.deleteFailed                 No records were deleted. Refresh the page and try again.
  bulk.error.reassignFailed               No records were reassigned. Refresh the page and try again.
  bulk.error.exportFailed                 The CSV couldn't be created. Refresh the page and try again.
  bulk.error.notPermitted                 You can only change records you own.

Per-record failure reasons (4) — a closed set; the server returns a code, never a message:
  bulk.reason.notFound                    No longer exists
  bulk.reason.notPermitted                You don't have access
  bulk.reason.alreadyDeleted              Already in Trash
  bulk.reason.unknown                     Couldn't be saved

Total: 4 + 7 + 6 + 8 + 6 + 4 + 5 + 4 = 44.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 44 bulk.* keys to all three locale files</name>
  <files>src/messages/en-US.json, src/messages/es-ES.json, src/messages/pt-BR.json</files>
  <read_first>
    - src/messages/en-US.json — locate the existing top-level `trash` namespace to copy its nesting shape and placement convention
    - src/messages/es-ES.json — the es-ES `trash` namespace, for translation register and tone
    - src/messages/pt-BR.json — the pt-BR `trash` namespace, same
    - src/messages/locale-parity.test.ts — read the `placeholders()` helper (~line 329) before writing any string, so you know exactly which tokens the drift gate compares
    - .planning/phases/38-bulk-operations/38-UI-SPEC.md § Copywriting Contract — the copy rules that bind these strings
  </read_first>
  <action>
    Add a new top-level `bulk` object to each of `src/messages/en-US.json`, `src/messages/es-ES.json`
    and `src/messages/pt-BR.json`, containing all 44 dot-paths listed verbatim in the
    `<interfaces>` block above. Nest them as real JSON objects (`bulk.deleteDialog.title` is
    `{"bulk": {"deleteDialog": {"title": ...}}}`), matching how the existing `trash` namespace nests.
    All three files must receive all 44 keys in this one commit — `locale-parity.test.ts`'s
    whole-file parity gate is unscoped and compares the three key sets directly, so a partial
    landing is red.

    Use the en-US values EXACTLY as given. Translate the es-ES and pt-BR values, subject to four
    hard rules from 38-UI-SPEC § Copywriting Contract:
    1. Preserve every simple placeholder token character-for-character: `{name}`, `{stage}`,
       `{days}`, `{owner}`, `{max}`, `{total}`, `{count}`, `{succeeded}`, `{failed}`.
       `placeholderDrift` in `locale-parity.test.ts` matches `/\{[a-zA-Z0-9_]+\}/g`, so these are
       compared literally across locales while the ICU plural skeleton itself is not.
    2. Preserve the ICU plural skeleton (`{count, plural, one {...} other {...}}`) and the `#`
       count marker. Add or drop plural categories only where the target language's CLDR rules
       require it; es-ES and pt-BR both use `one`/`other`, so keep both branches.
    3. No exclamation marks, no "Oops", no editorial tone. `bulk.reason.notFound` is
       "No longer exists" — a fact, not "mysteriously vanished".
    4. `bulk.exportCsv` keeps the literal token `CSV` in all three locales.

    Do NOT add any key outside the `bulk` namespace and do NOT change any existing key —
    38-UI-SPEC states zero keys are added elsewhere and zero existing keys change, and Task 2's
    exact-contract assertion depends on that.

    `bulk.selectAllInStageCapped` is the 44th key and is a deliberate, justified addition beyond
    38-UI-SPEC's 43 (per D-07 and 38-RESEARCH Open Question 1): `/deals` has no pagination,
    25,195 live deals, largest single stage 10,495, so a per-stage select-all is over-cap in the
    normal case. It is a plain-placeholder string, not an ICU plural, because `{max}` is always
    100 and therefore never singular.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/messages/locale-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const m=require('./src/messages/en-US.json');const f=(o,p='')=>Object.entries(o).flatMap(([k,v])=>typeof v==='object'&&v!==null?f(v,p+k+'.'):[p+k]);console.log(f(m.bulk).length)"` prints `44`
    - The same command against `es-ES.json` prints `44`, and against `pt-BR.json` prints `44`
    - `node -e "const a=k=>{const m=require('./src/messages/'+k+'.json');const f=(o,p='')=>Object.entries(o).flatMap(([x,v])=>typeof v==='object'&&v!==null?f(v,p+x+'.'):[p+x]);return f(m.bulk).sort().join(',')};console.log(a('en-US')===a('es-ES')&&a('en-US')===a('pt-BR'))"` prints `true`
    - `grep -c 'selectAllInStageCapped' src/messages/en-US.json src/messages/es-ES.json src/messages/pt-BR.json` reports `1` for each file
    - `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` passes (the pre-existing whole-file parity gate now covers the 44 new keys)
    - `git diff --stat src/messages/` lists exactly the three JSON files
  </acceptance_criteria>
  <done>All 44 `bulk.*` keys exist in all three locale files with identical key sets, identical placeholder tokens, and no key added outside the `bulk` namespace.</done>
</task>

<task type="auto">
  <name>Task 2: Wire REQUIRED_BULK_KEYS into locale-parity.test.ts</name>
  <files>src/messages/locale-parity.test.ts</files>
  <read_first>
    - src/messages/locale-parity.test.ts — read the whole file. The four edit sites are: the contract lists (~lines 23-28 for `REQUIRED_NOTE_KEYS`, ~73 for `REQUIRED_AUDIT_KEYS`, ~183 for `REQUIRED_TRASH_KEYS`), the namespace constants and key selectors (~lines 342-374), the five assertion bodies (~lines 446-500), and the per-namespace exact-contract assertion (~lines 460-477)
    - .planning/phases/38-bulk-operations/38-PATTERNS.md § 15 — the four edit sites with verbatim excerpts of each
  </read_first>
  <action>
    Extend `src/messages/locale-parity.test.ts` with a fourth copy contract, mirroring the
    checked-in `REQUIRED_TRASH_KEYS` treatment exactly:

    1. Export `REQUIRED_BULK_KEYS: string[]` as a sibling of `REQUIRED_NOTE_KEYS`,
       `REQUIRED_AUDIT_KEYS` and `REQUIRED_TRASH_KEYS`, listing all 44 dot-paths from this plan's
       `<interfaces>` block. Give it the same style of doc comment those three carry, citing
       `38-UI-SPEC.md § Copywriting Contract` and stating that adding a `bulk.*` string to the UI
       means adding its dot-path here first.
    2. Add `const BULK_NAMESPACE = "bulk"` beside `NOTES_NAMESPACE` / `AUDIT_NAMESPACE` /
       `TRASH_NAMESPACE`.
    3. Add `const bulkKeys = keysMatching(inNamespace(BULK_NAMESPACE))`. It needs NO
       `*_EXTRA_KEYS` sibling — 38-UI-SPEC states zero bulk strings live outside the namespace,
       unlike `trash`, which needed `TRASH_EXTRA_KEYS` for its three out-of-namespace strings.
    4. Add `REQUIRED_BULK_KEYS` to all five assertion bodies as a SEPARATE call — one extra
       `expect(missingIn(REQUIRED_BULK_KEYS)).toEqual(emptyPerLocale)` line, one extra
       `expect(blankIn(REQUIRED_BULK_KEYS)).toEqual(emptyPerLocale)`, one extra
       `expect(untranslatedInBoth(REQUIRED_BULK_KEYS)).toEqual([])`, one extra
       `expect(placeholderDrift(REQUIRED_BULK_KEYS)).toEqual({})`, and one extra
       `expectIdenticalKeySets(bulkKeys, BULK_NAMESPACE)`. Never concatenate the list into an
       existing call — the file's own comment at ~line 384 states the reason: a failure diff must
       name which contract broke and list only its keys.
    5. Add the exact-contract assertion, modelled on the `trashContract` block: sort a copy of
       `REQUIRED_BULK_KEYS` and assert `bulkKeys[locale]` equals it for every locale, with a
       message naming `BULK_NAMESPACE`. This is the assertion that makes an ungated 45th key fail,
       not just a missing 44th.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/messages/locale-parity.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*[*/]' src/messages/locale-parity.test.ts | grep -c 'REQUIRED_BULK_KEYS'` is at least 6 (the declaration plus five assertion sites)
    - `grep -v '^\s*[*/]' src/messages/locale-parity.test.ts | grep -c 'BULK_NAMESPACE'` is at least 3 (declaration, selector, exact-contract message)
    - `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` passes
    - Negative proof of the contract gate: temporarily add `"bulk": {"zzUngated": "x"}` merged into all three locale files, re-run the test, observe it FAILS naming the bulk namespace, then revert. Record the observed failure message in the SUMMARY.
    - Negative proof of the missing gate: temporarily delete `bulk.reason.unknown` from `pt-BR.json`, re-run, observe FAIL, then revert.
    - `npm run typecheck` reports 0 errors
  </acceptance_criteria>
  <done>`REQUIRED_BULK_KEYS` is checked in and passed separately to all five assertion bodies plus the exact-contract assertion, both directions proven red-then-green by a temporary mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator → rendered copy | A translated string is user-facing text that may promise behaviour the system does not implement |
| copy contract → downstream plans | An ungated key is a string no locale gate covers, so it can ship untranslated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-07 | Information Disclosure | `bulk.reason.*` | mitigate | The four reason strings are a CLOSED set, keyed by code. No server error prose ever reaches the failure report. This plan is where the closed set is defined; plans 38-11..38-14 return only these four codes |
| T-38-10 | Repudiation (copy lies about system state) | `bulk.deleteDialog.description` vs `descriptionNoRetention` | mitigate | Two separate strings, never a `{days}` default. `readTrashRetentionDays()` fails closed to `null` (Phase 37 T-37-05); a `?? 30` in any consumer would make the dialog promise a window the pruner is not enforcing |
| T-38-11 | Repudiation (copy promises an unbuilt notification) | `bulk.reassignDialog.noEmailNotice` | mitigate | Required copy, not a nicety: the single-record reassign path DOES email a new assignee, so silence would be an implied promise. D-13 locks no email on bulk reassign |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | 38-RESEARCH § Package Legitimacy Audit: this phase installs zero packages, so the slopcheck gate has an empty input set. No `npm install`, no `shadcn add`, no registry fetch in this plan |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` — green
- `npm run typecheck` — 0 errors
- `npm run lint` — 0 errors
- All three locale files have identical `bulk.*` key sets of size 44
</verification>

<success_criteria>
- 44 `bulk.*` keys exist in `en-US.json`, `es-ES.json` and `pt-BR.json` with identical key sets
- Every simple placeholder token is byte-identical across the three locales
- `REQUIRED_BULK_KEYS` is checked in and enforced by all five assertion bodies plus the exact-contract assertion
- Zero keys added outside the `bulk` namespace; zero existing keys changed
- Both gate directions (missing key, ungated extra key) proven red by a temporary mutation and reverted
</success_criteria>

<output>
Create `.planning/phases/38-bulk-operations/38-01-SUMMARY.md` when done
</output>
