---
phase: 38-bulk-operations
plan: 17
type: execute
wave: 4
depends_on: [38-07, 38-09, 38-10, 38-14]
files_modified:
  - src/app/activities/page.tsx
  - src/app/activities/activity-list.tsx
  - src/app/activities/activities-client.tsx
autonomous: true
requirements: [BULK-01, BULK-02, BULK-03, BULK-04]

must_haves:
  truths:
    - "A user checks individual activity rows and a header checkbox that selects every loaded row"
    - "The bar and its spacer sit below the Load More button, so the button is never covered"
    - "Changing any filter or the search clears the selection"
    - "After a partial failure the succeeded rows are deselected and the failed rows stay selected"
  artifacts:
    - path: "src/app/activities/page.tsx"
      provides: "retentionDays and a separate bulkOwners list passed to ActivitiesClient"
      contains: "readTrashRetentionDays"
    - path: "src/app/activities/activities-client.tsx"
      provides: "the lifted rowSelection state plus the bar, spacer and failure report mounts"
      contains: "rowSelection"
    - path: "src/app/activities/activity-list.tsx"
      provides: "the prepended select column wired to the lifted state, and the colSpan fix"
      contains: "getRowId"
  key_links:
    - from: "src/app/activities/activities-client.tsx"
      to: "src/app/activities/activity-list.tsx"
      via: "rowSelection and onRowSelectionChange passed down as props into useReactTable"
      pattern: "onRowSelectionChange"
    - from: "src/app/activities/activities-client.tsx"
      to: "src/app/activities/actions.ts"
      via: "the three bulk actions passed to BulkActionBar as onDelete / onReassign / onExport"
      pattern: "bulkDeleteActivities|bulkReassignActivityOwner|exportSelectedActivities"
---

<objective>
Wire selection into the Activities list — with `rowSelection` LIFTED to the parent, which is the one
structural difference from the other two tables.

Purpose: `ActivityList` owns `useReactTable` but `ActivitiesClient` owns the `Load More` button
(`activities-client.tsx:170-176`), the `ActivityFilters` row, and the `search` / `activeFilters` props.
`ActivityList`'s props are only `{ activities, activityTypes, onEdit, onRefresh }` — there is no `search`
prop in that file at all. Keeping the bar inside `ActivityList` would put the `h-20` spacer ABOVE the
`Load More` button, so the fixed bar would cover the button — the exact defect the spacer exists to
prevent — and there would be no filter key to clear the selection on. Lifting `rowSelection` to
`ActivitiesClient` fixes both at once.
Output: three edited files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-bulk-operations/38-UI-SPEC.md
@.planning/phases/38-bulk-operations/38-PATTERNS.md

<interfaces>
Consumed contracts, all already landed:

  useSelectColumn<T>(getLabel: (row: T) => string): ColumnDef<T, unknown>        // plan 38-07
  BulkActionBar(props: BulkActionBarProps)                                       // plan 38-10
  BulkFailureReport(props: BulkFailureReportProps)                               // plan 38-09
  bulkDeleteActivities(ids): Promise<BulkWriteResult>                            // plan 38-14
  bulkReassignActivityOwner(ids, ownerId): Promise<BulkWriteResult>              // plan 38-14
  exportSelectedActivities(ids): Promise<ExportResult>                           // plan 38-14
  readTrashRetentionDays(): Promise<number | null>                               // src/lib/trash/settings.ts:71
  BulkOutcome = { kind, succeeded, failed, labelById }                           // src/lib/bulk/types.ts

New props to add to `ActivityListProps` (currently `{ activities, activityTypes, onEdit, onRefresh }`
at activity-list.tsx:83-88):
  rowSelection: RowSelectionState
  onRowSelectionChange: OnChangeFn<RowSelectionState>
Both types come from `@tanstack/react-table`.

Three divergences from `organizations/data-table.tsx`, all real:
  1. `activity-list.tsx:391-396` configures `getFilteredRowModel()` but it is INERT — verified zero
     occurrences of `columnFilters`, `globalFilter`, `setColumnFilters`, `setGlobalFilter`, `filterFn` or
     a `state:` key in that file. All Activities filtering is server-side via URL params
     (`activities/page.tsx:70-155`) plus post-query `.filter()` calls. So the filtered row model EQUALS
     the core row model. Keep using the `...PageRows...` variants — they are correct and future-proof —
     but justify the `loadedIds` intersection on the PHANTOM-KEY ground (TanStack does not prune
     `rowSelection` when a row leaves `data`), not on the filter ground.
  2. The header reads `getSize()` (`<TableHead key={header.id} style={{ width: header.getSize() }}` at
     ~:461), which is why the select column's `size: 44` is honoured HERE and auto-sized on the other two.
  3. The empty-state `colSpan={columns.length}` is at ~:499.

`activities-client.tsx:35-46` already receives `search` and `activeFilters`, so the available filter
signature is `JSON.stringify({ search, ...activeFilters })`.

Owners query: `activities/page.tsx:99-107` already fetches `users` with `isNull(users.deletedAt)` ONLY —
no `status` predicate — and feeds ONE array to BOTH `ActivityFilters` and `ActivityDialog`
(`activities-client.tsx:159-164, 205-213`). That existing query MUST NOT be modified; changing its
`where` would silently change two existing dropdowns. Add a SEPARATE `bulkOwners` query with both
predicates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — retentionDays and a separate bulkOwners list</name>
  <files>src/app/activities/page.tsx</files>
  <read_first>
    - src/app/activities/page.tsx — read the WHOLE file: the `Promise.all` at ~:99-114 including the existing `db.query.users.findMany({ where: isNull(users.deletedAt), ... })`, the `owners` mapping at ~:167-171 with its `u.name || "Unknown"`, the URL-param filter parsing at ~:70-155, the post-query `.filter()` calls, and the `<ActivitiesClient ... />` call site
    - src/app/activities/activities-client.tsx lines 30-60 — the props interface, so you know exactly what to add
    - src/app/trash/page.tsx lines 75-125 — the `readTrashRetentionDays()` read and the `retentionDays` prop pass with its explanatory comment
    - src/lib/trash/settings.ts lines 55-80 — `readTrashRetentionDays`'s contract and its no-fallback header
    - src/db/schema/users.ts lines 1-25 — the `userStatusEnum` values
  </read_first>
  <action>
    Extend `src/app/activities/page.tsx` with two new props for `ActivitiesClient`.

    1. Add `readTrashRetentionDays()` to the existing `Promise.all` and pass
       `retentionDays={retentionDays}`, type `number | null`. Add NO `?? 30`, no `|| 30` and no other
       numeric default anywhere in this file: `readTrashRetentionDays()` fails closed to `null`, nothing
       is purged automatically in that state, and the delete dialog's `descriptionNoRetention` branch is
       what keeps the UI from promising a window the pruner is not enforcing.
    2. Add a SECOND, SEPARATE users query to the same `Promise.all`:
       `db.query.users.findMany({ where: and(isNull(users.deletedAt), eq(users.status, "approved")),
       columns: { id: true, name: true, email: true }, orderBy: [users.name] })`, mapped to
       `{ id: u.id, name: u.name || "Unknown" }`, and passed as `bulkOwners={bulkOwners}`.

    DO NOT modify the existing `ownersResult` query's `where` clause. That array is fed to
    `ActivityFilters` AND `ActivityDialog`, so adding a `status` predicate to it would silently remove
    options from two existing dropdowns — a behaviour change entirely outside this phase's scope. Two
    queries against the same table in one `Promise.all` is the correct cost of not breaking them.

    Do not change the URL-param filter parsing, the post-query `.filter()` calls, or any existing prop.
    Do not import any component from `src/components/bulk/` into this server file.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ./node_modules/.bin/vitest run "src/app/admin/fields/[entityType]/__tests__/rsc-boundary.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'readTrashRetentionDays' src/app/activities/page.tsx` is at least 1
    - `grep -c 'retentionDays={' src/app/activities/page.tsx` is 1 and `grep -c 'bulkOwners={' ...` is 1
    - `grep -c 'eq(users.status, "approved")' src/app/activities/page.tsx` is 1
    - `grep -c 'users.findMany' src/app/activities/page.tsx` is 2 (the pre-existing one plus the new one)
    - `git diff src/app/activities/page.tsx` shows the pre-existing `users.findMany` `where` clause and the `owners` mapping UNCHANGED — record the diff hunk for that query in the SUMMARY to prove it
    - `grep -vE '^\s*[*/]' src/app/activities/page.tsx | grep -cE '\?\? *[0-9]|\|\| *[0-9]'` is 0
    - `grep -c 'components/bulk' src/app/activities/page.tsx` is 0
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
  </acceptance_criteria>
  <done>`activities/page.tsx` passes `retentionDays: number | null` with no numeric default and a SECOND, separate `bulkOwners` query filtered on both predicates, with the pre-existing users query provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: activity-list.tsx — accept the lifted selection state and prepend the select column</name>
  <files>src/app/activities/activity-list.tsx</files>
  <read_first>
    - src/app/activities/activity-list.tsx — read the WHOLE file: `ActivityListProps` (~:83-88), the inline column definitions, the `useReactTable` call with its `getFilteredRowModel()` (~:391-396), the `<TableHead ... style={{ width: header.getSize() }}` (~:461), the row rendering with its already-inert `data-state={row.getIsSelected() && "selected"}` (~:481), the empty-state `colSpan={columns.length}` (~:499), and the overdue banner at ~:400-452
    - src/components/bulk/select-column.tsx — `useSelectColumn`'s signature, landed by plan 38-07
    - src/components/ui/table.tsx line 60 — `data-[state=selected]:bg-muted`
    - src/components/keyboard/data-table-keyboard.tsx lines 55-150 — `rowProps(index)` and the hotkey list, which does NOT include `Escape`
    - .planning/phases/38-bulk-operations/38-UI-SPEC.md § Surface 1 and 38-PATTERNS.md § 9b — the three declared divergences
  </read_first>
  <action>
    Edit `src/app/activities/activity-list.tsx`.

    1. Extend `ActivityListProps` with `rowSelection: RowSelectionState` and
       `onRowSelectionChange: OnChangeFn<RowSelectionState>`, importing both types from
       `@tanstack/react-table`. The state itself is NOT declared here — it is lifted to
       `ActivitiesClient` (Task 3), because that component owns the `Load More` button and the filter
       props, and putting the bar's spacer inside this component would place it ABOVE the `Load More`
       button so the fixed bar would cover the button.
    2. `const selectColumn = useSelectColumn<Activity>((a) => a.title)` — use whatever field this file
       already renders as the activity's primary label; read the inline column definitions and reuse that
       expression so the checkbox's accessible name matches the visible name. Then
       `const columnsWithSelect = useMemo(() => [selectColumn, ...columns], [selectColumn, columns])`,
       prepended — first column, never appended.
    3. Extend the existing `useReactTable` call with `columns: columnsWithSelect`,
       `getRowId: (row) => row.id`, `state: { rowSelection }`, `onRowSelectionChange` and
       `enableRowSelection: true`. Keep `getCoreRowModel` and `getFilteredRowModel` exactly as they are —
       do NOT remove `getFilteredRowModel()` even though it is inert; removing it is an unrelated change
       and the `...PageRows...` variants the select column uses are correct against either row model.
       `getRowId` is mandatory: TanStack's default row id is the row INDEX.
    4. Fix the empty-state colSpan: change `colSpan={columns.length}` to
       `colSpan={table.getAllLeafColumns().length}`. `columns.length` reads the PROP, which no longer
       matches the rendered column count.

    Do NOT add the bar, the spacer or the failure report to this file — all three mount in
    `ActivitiesClient`.
    Do NOT declare `useState` for the selection here.
    Do NOT touch the overdue banner at ~:400-452. It contains `border-red-200 bg-red-50 text-red-600
    text-red-700` and `bg-white`, which are pre-existing token debt in a block this phase does not touch.
    Do not copy those classes and do not fix them here — that is unrelated scope, and it is named here
    only so it is not mistaken for this phase's work.
    Do NOT add any ring, border or `data-selected` value to a bulk-selected row; the existing
    `data-state={row.getIsSelected() && "selected"}` plus `table.tsx:60` is the whole treatment. Add zero CSS.
    Do NOT add `getPaginationRowModel`. Do NOT render the checkbox column conditionally or on hover.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ./node_modules/.bin/vitest run src/components/bulk/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'getRowId: (row) => row.id' src/app/activities/activity-list.tsx` is 1
    - `grep -c 'enableRowSelection: true' src/app/activities/activity-list.tsx` is 1
    - `grep -c 'onRowSelectionChange' src/app/activities/activity-list.tsx` is at least 2 (the prop declaration and the table option)
    - `grep -c 'useState' src/app/activities/activity-list.tsx` is unchanged from its pre-task count — no selection state is declared here (record both counts in the SUMMARY)
    - `grep -c 'useSelectColumn' src/app/activities/activity-list.tsx` is 1 and `grep -c 'columnsWithSelect' ...` is at least 2
    - `grep -c 'colSpan={columns.length}' src/app/activities/activity-list.tsx` is 0 and `grep -c 'getAllLeafColumns' ...` is 1
    - `grep -c 'getFilteredRowModel' src/app/activities/activity-list.tsx` is unchanged from its pre-task count
    - `grep -c 'BulkActionBar\|BulkFailureReport\|h-20' src/app/activities/activity-list.tsx` is 0
    - `grep -c 'getPaginationRowModel\|getSelectedRowModel' src/app/activities/activity-list.tsx` is 0
    - `git diff src/app/activities/activity-list.tsx` shows no change inside the overdue banner block; `grep -c 'bg-red-50' src/app/activities/activity-list.tsx` is unchanged
    - `git status --porcelain src/app/globals.css src/components/ui/table.tsx` is empty
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
  </acceptance_criteria>
  <done>`ActivityList` accepts the lifted `rowSelection` and its change handler, prepends the shared select column with `getRowId` keyed on the record id, reads its empty-state colSpan from the table, and hosts neither the bar nor the spacer — with the inert `getFilteredRowModel` and the pre-existing overdue banner both untouched.</done>
</task>

<task type="auto">
  <name>Task 3: activities-client.tsx — own the selection and mount the bar, spacer and report</name>
  <files>src/app/activities/activities-client.tsx</files>
  <read_first>
    - src/app/activities/activities-client.tsx — read the WHOLE file: the props interface (~:35-46) including `search` and `activeFilters`, the `TabsContent value="list"` block, the `<ActivityFilters ... />` row (~:159-164), the `<ActivityList ... />` call site, the `Load More` button block (~:170-176), and the `<ActivityDialog ... />` at ~:205-213
    - src/app/activities/activity-list.tsx — the props interface you just extended
    - src/components/bulk/bulk-action-bar.tsx — `BulkActionBarProps`, landed by plan 38-10
    - src/components/bulk/bulk-failure-report.tsx — `BulkFailureReportProps`, landed by plan 38-09
    - src/app/activities/actions.ts — the three bulk actions, landed by plan 38-14
    - src/lib/bulk/types.ts — `BulkOutcome` and `BulkFailure`
    - .planning/phases/38-bulk-operations/38-PATTERNS.md § 9b — the parent-owns-Load-More structure and why the bar belongs here
  </read_first>
  <action>
    Edit `src/app/activities/activities-client.tsx`.

    1. Add `retentionDays: number | null` and `bulkOwners: { id: string; name: string }[]` to the props
       interface, matching what Task 1 passes from `page.tsx`.
    2. Declare `const [rowSelection, setRowSelection] = useState<RowSelectionState>({})` HERE, and pass
       `rowSelection={rowSelection}` and `onRowSelectionChange={setRowSelection}` down into
       `<ActivityList />`.
    3. Derive the id list defensively, over the SAME array passed to `ActivityList`:
       `const loadedIds = useMemo(() => new Set(activities.map((a) => a.id)), [activities])` and
       `const selectedIds = useMemo(() => Object.keys(rowSelection).filter((id) => rowSelection[id] &&
       loadedIds.has(id)), [rowSelection, loadedIds])`. The justification here is PHANTOM KEYS, not
       filtering: TanStack does not prune `rowSelection` when a row leaves `data`, so after a bulk delete
       the deleted rows' keys linger and would inflate the count. Note in a comment that
       `activity-list.tsx`'s `getFilteredRowModel()` is inert (no filter state is ever set; all Activities
       filtering is server-side via URL params), so the filtered row model equals the core row model and
       the filter-based justification does not apply on this surface.
    4. Add `const filterSignature = useMemo(() => JSON.stringify({ search, ...activeFilters }),
       [search, activeFilters])` and `useEffect(() => { setRowSelection({}) }, [filterSignature])`.
       Key on the SIGNATURE STRING, never on the `activities` array: Phase 35 measured that
       `revalidatePath` re-renders the current client tree regardless of the path argument, and every bulk
       action calls it, so an `[activities]`-keyed effect would fire mid-action and wipe the failed-record
       selection SC-3 requires to survive.
    5. Declare `const [outcome, setOutcome] = useState<BulkOutcome | null>(null)` and mount
       `<BulkFailureReport>` ABOVE `<ActivityList />` — after the `<ActivityFilters />` row — passing
       `kind`, `failures={outcome.failed}`, `labelById={outcome.labelById}` and
       `onDismiss={() => setOutcome(null)}`, rendered only when `outcome !== null && outcome.failed.length > 0`.
    6. Mount `<BulkActionBar>` AFTER the `Load More` button block — this is the whole reason the state was
       lifted, and it is what keeps the `h-20` spacer below the button rather than above it. Props:
         entityType="activity"
         selectedIds={selectedIds}
         getLabel={(id) => <the same primary-label expression ActivityList uses, falling back to the raw id>}
         retentionDays={retentionDays}
         owners={bulkOwners}
         onDelete={bulkDeleteActivities}
         onReassign={bulkReassignActivityOwner}
         onExport={exportSelectedActivities}
         onOutcome={handleOutcome}
         onClear={() => { setRowSelection({}); setOutcome(null) }}
    7. `handleOutcome(next: BulkOutcome)` must, in this order:
         a. `setRowSelection((prev) => ...)` removing every id in `next.succeeded` and PRESERVING every id
            in `next.failed`. Do this EXPLICITLY here, never via an effect.
         b. `setOutcome(next.failed.length > 0 ? next : null)`.
         c. Call this file's existing refresh handler (read it — the `onRefresh` / `handleRefresh` this
            component already passes to `ActivityList`) and match its convention. Do NOT introduce a prop
            named `onSuccess`; the Phase 35 rename to `onRecordSaved` must not be undone.

    `entityType="activity"` — the singular `EntityType` literal from
    `src/db/schema/custom-fields.ts:19`. The bar uses it for `ENTITY_TO_TRASH_TAB`, which maps
    `activity -> "activities"`.

    Do NOT pass `bulkOwners` to `ActivityFilters` or `ActivityDialog` — they keep receiving the
    pre-existing `owners` array, unchanged. Do NOT add any keyboard binding here; `Escape` is owned by
    the bar. Do NOT wrap the server actions in local closures that reshape their arguments.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && ./node_modules/.bin/vitest run src/components/bulk/ src/app/activities/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'BulkActionBar' src/app/activities/activities-client.tsx` is at least 2 (import and usage)
    - `grep -c 'BulkFailureReport' src/app/activities/activities-client.tsx` is at least 2
    - `grep -c 'bulkDeleteActivities\|bulkReassignActivityOwner\|exportSelectedActivities' src/app/activities/activities-client.tsx` is at least 6
    - `grep -c 'entityType="activity"' src/app/activities/activities-client.tsx` is 1 (singular)
    - `grep -c 'rowSelection' src/app/activities/activities-client.tsx` is at least 4 (state, pass-down, derivation, handler)
    - Read the JSX and confirm the `BulkActionBar` element appears AFTER the `Load More` button block and the `BulkFailureReport` element appears BEFORE `<ActivityList`; record the three line numbers (report, ActivityList, Load More, bar) in the SUMMARY
    - The clear-on-filter effect's dependency array is `[filterSignature]` and does NOT contain `activities`; record its exact dependency array in the SUMMARY
    - `handleOutcome` removes succeeded ids and preserves failed ids in one explicit `setRowSelection` call; record the handler body's control flow in the SUMMARY
    - `grep -c 'onSuccess' src/app/activities/activities-client.tsx` is 0
    - `git diff src/app/activities/activities-client.tsx` shows the `<ActivityFilters` and `<ActivityDialog` prop lists unchanged apart from nothing — record both hunks in the SUMMARY to prove `bulkOwners` was not threaded into either
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
    - `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` passes
  </acceptance_criteria>
  <done>`ActivitiesClient` owns `rowSelection`, derives `selectedIds` defensively, clears on a filter-signature string, mounts the failure report above the list and the bar plus spacer after `Load More`, and wires the three Activities bulk actions with `entityType="activity"` — without touching the existing owners array's two consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server page -> client tree | Only plain serializable values cross (`retentionDays: number \| null`, `bulkOwners: {id,name}[]`) |
| parent -> child selection state | The child owns the table but the parent owns the id set that is submitted, so the two must not drift |
| owners list -> two existing dropdowns | The pre-existing users array feeds `ActivityFilters` and `ActivityDialog`; changing its predicate changes them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-36 | Tampering (acting on the wrong records) | `getRowId` | mitigate | `getRowId: (row) => row.id` is mandatory because TanStack's default row id is the row INDEX. Asserted by a source check |
| T-38-37 | Tampering (phantom ids in a destructive submit) | `selectedIds` derivation | mitigate | The intersection of `rowSelection`'s truthy keys with `loadedIds` over the same array passed to `ActivityList`. Justified on the phantom-key ground, because `getFilteredRowModel()` is inert here — a plan or verifier reasoning from the filter ground would be chasing an untestable scenario |
| T-38-33 | Tampering (failed-id selection wiped mid-action) | the clear-on-filter effect | mitigate | Keys on `JSON.stringify({ search, ...activeFilters })`, never on the `activities` array. Phase 35 measured that `revalidatePath` re-renders the current client tree regardless of the path argument. Succeeded ids are removed explicitly in `handleOutcome` |
| T-38-06 | Elevation of Privilege (ownership transfer to an inactive principal) | `bulkOwners` | mitigate | A SECOND, separate query filtered on `isNull(users.deletedAt)` AND `eq(users.status, "approved")`. The pre-existing query keeps its `deletedAt`-only predicate so `ActivityFilters` and `ActivityDialog` are unchanged, proven by recording both diff hunks |
| T-38-10 | Repudiation (UI lies about configuration) | `retentionDays` | mitigate | Passed straight through from `readTrashRetentionDays()` with no numeric default anywhere in the three files, asserted by a regex source check |
| T-38-38 | Denial of Service (bar covering the primary affordance) | the bar mount point | mitigate | This is the concrete reason the state is lifted: with the bar inside `ActivityList`, the `h-20` spacer would sit ABOVE the `Load More` button that lives in the parent, so the fixed bar would cover it. The bar and spacer mount after the button, verified by recording the JSX line order |
| T-38-13 | Tampering (out-of-scope column) | `assigneeId` | mitigate | The bar calls `bulkReassignActivityOwner(ids, ownerId)`, which writes `ownerId` only (gated in plan 38-14). No assignee control is added to any Activities surface; `ActivityDialog`'s assignee field is untouched |
| T-38-26 | Information Disclosure (RSC boundary) | the page -> client props | mitigate | `page.tsx` imports nothing from `src/components/bulk/` and passes only plain values; both dialogs live inside the `'use client'` bar. The repo-wide `rsc-boundary.test.tsx` gate is run in this plan's verification |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (38-RESEARCH § Package Legitimacy Audit: empty input set) |
</threat_model>

<verification>
- `npm run typecheck` — 0 errors, no new `@ts-expect-error`; `npm run lint` — 0 errors
- `./node_modules/.bin/vitest run "src/app/admin/fields/[entityType]/__tests__/rsc-boundary.test.tsx"` — green
- `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts src/components/bulk/` — green
- `git status --porcelain src/app/globals.css src/components/ui/table.tsx` — empty
- The pre-existing `users.findMany` hunk and the `ActivityFilters` / `ActivityDialog` prop lists recorded as unchanged
</verification>

<success_criteria>
- `activities/page.tsx` passes `retentionDays: number | null` (no numeric default) and a SECOND, separate `bulkOwners` query; the pre-existing users query is provably untouched
- `ActivityList` accepts `rowSelection` / `onRowSelectionChange` as props, declares no selection state, prepends the shared select column with `getRowId`, and reads its colSpan from the table
- `getFilteredRowModel()` and the pre-existing overdue banner in `activity-list.tsx` are unchanged
- `ActivitiesClient` owns `rowSelection`, derives `selectedIds` by intersecting with `loadedIds`, and clears on a filter-signature string rather than on the data array
- The failure report mounts above `ActivityList`; the bar and its spacer mount AFTER the `Load More` button
- `handleOutcome` deselects succeeded ids and preserves failed ids explicitly; `entityType="activity"`
- `bulkOwners` is not threaded into `ActivityFilters` or `ActivityDialog`; zero CSS is added
</success_criteria>

<output>
Create `.planning/phases/38-bulk-operations/38-17-SUMMARY.md` when done
</output>
