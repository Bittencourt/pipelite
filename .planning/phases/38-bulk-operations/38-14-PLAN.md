---
phase: 38-bulk-operations
plan: 14
type: execute
wave: 3
depends_on: [38-04, 38-06]
files_modified:
  - src/app/activities/actions.ts
  - src/app/activities/bulk-actions.test.ts
autonomous: true
requirements: [BULK-02, BULK-03, BULK-04]

must_haves:
  truths:
    - "An unauthenticated bulk call is refused before any actor scope opens and before any mutation runs"
    - "A call carrying more than 100 ids is refused with the cap and the count, and no mutation runs"
    - "An activity the caller does not own is refused per record, even for an admin caller, and the mutation is never called for it"
    - "The bulk reassign changes ownerId only and never assigneeId"
    - "The actor scope opens once for the whole loop and revalidatePath runs once after it"
    - "The scoped export accepts a list of ids and nothing else"
  artifacts:
    - path: "src/app/activities/actions.ts"
      provides: "bulkDeleteActivities, bulkReassignActivityOwner, exportSelectedActivities"
      contains: "bulkDeleteActivities"
    - path: "src/app/activities/bulk-actions.test.ts"
      provides: "session-swapping unit coverage plus the export-signature and assigneeId source gates"
      contains: "not.toHaveBeenCalled"
  key_links:
    - from: "src/app/activities/actions.ts"
      to: "src/lib/bulk/dispatch.ts"
      via: "deleteRecordByType(\"activity\", id, userId) and updateRecordOwnerByType(\"activity\", id, ownerId, userId) inside the loop"
      pattern: "(deleteRecordByType|updateRecordOwnerByType)\\(\\s*\"activity\""
    - from: "src/app/activities/actions.ts"
      to: "src/lib/audit/actor-context.ts"
      via: "one runWithActor wrapping the whole loop, opened after the session check"
      pattern: "runWithActor"
---

<objective>
Add the three Activities bulk server actions — delete, reassign owner, scoped CSV export — and the
session-swapping unit suite that proves their refusals happen BEFORE any write.

Purpose: Activities carry the same predicate as Organizations and People —
`activity.ownerId !== session.user.id`, with NO admin bypass (`src/app/activities/actions.ts:84, 131, 177`).
Only deals carries the admin clause. Activities also carry a SECOND user-valued column, `assigneeId`,
which is explicitly out of scope for this phase (D-11): the bulk reassign changes `ownerId` only, and
that boundary has to be gated rather than merely stated, because `activitySchema` declares `assigneeId`
and it is the more obvious field to reach for on this entity.
Output: three server actions and one test file whose most important assertions are ABSENCES.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-bulk-operations/38-PATTERNS.md

<interfaces>
Emit exactly these three exports — plan 38-17 wires them into the bar:

  export async function bulkDeleteActivities(ids: string[]): Promise<BulkWriteResult>
  export async function bulkReassignActivityOwner(ids: string[], ownerId: string): Promise<BulkWriteResult>
  export async function exportSelectedActivities(ids: string[]): Promise<ExportResult>

From src/lib/bulk/types.ts (plan 38-06):
  BulkFailureReason = "notFound" | "notPermitted" | "alreadyDeleted" | "unknown"
  BulkFailure = { id: string; reason: BulkFailureReason }
  BulkErrorCode = "not_authenticated" | "too_many" | "invalid_owner" | "no_selection"
  BulkWriteResult =
    | { success: true; succeeded: string[]; failed: BulkFailure[] }
    | { success: false; error: BulkErrorCode; max?: number }

From src/lib/bulk/limits.ts: BULK_MAX_IDS = 100
From src/lib/bulk/dispatch.ts: deleteRecordByType(entityType, id, userId) and
  updateRecordOwnerByType(entityType, id, ownerId, userId)
From src/lib/export/types.ts: ExportResult
From src/lib/export/formatters.ts: fetchFilteredData(options: ExportOptions): Promise<ExportResult>
From src/lib/audit/actor-context.ts: runWithActor(actor, fn) — returns `T | Promise<T>`, so an async
callback needs `await runWithActor(...)`.

ACTIVITIES' AUTHORIZATION PREDICATE, verbatim (src/app/activities/actions.ts:84, 131 and :177):
  if (activity.ownerId !== session.user.id) {
Activities have NO admin bypass. Only deals carries `&& session.user.role !== "admin"`.

Export entity slug facts (src/lib/export/formatters.ts:417-426): `entityType === "activity"` maps to the
English plural `"activities"`, which is the slug the scoped filename uses. Note also that `fetchActivities`
(~:322-336) annotates its `conditions` array explicitly, which is the typecheck risk plan 38-04 resolved.

`assigneeId` is OUT OF SCOPE (D-11). `activitySchema` declares `assigneeId` but not `ownerId`, so
`assigneeId` is the field a careless implementation would reach for. The bulk reassign writes `ownerId`
only, via `updateActivityOwnerMutation` whose `set()` object is exactly `{ ownerId, updatedAt }`.

`alreadyDeleted` is deliberately UNREACHABLE on the delete path: the per-record read already carries
`isNull(activities.deletedAt)`, so a miss maps to `notFound` (38-RESEARCH A6).

Fan-out, stated because nobody has: a 100-record bulk delete enqueues 100 webhook deliveries, runs 100
workflow-trigger evaluations and issues 100 fire-and-forget `audit_log` inserts, against a `postgres.js`
client created with no `max`. This is CORRECT semantics and is why the loop stays sequential.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bulkDeleteActivities and bulkReassignActivityOwner</name>
  <files>src/app/activities/actions.ts</files>
  <read_first>
    - src/app/activities/actions.ts — read the WHOLE file. You need `deleteActivity` (~:109-150) as the skeleton, its `auth()` check, the verbatim ownership predicate at :131, the `runWithActor` placement, and its `revalidatePath` call. Note this file also exports `getActivities`, `getActivityById` and `getActivityTypes`, so declaration slicing must anchor precisely
    - src/lib/mutations/activities.ts — `updateActivityOwnerMutation` (added by plan 38-03) and `activitySchema`, so you can see that `assigneeId` is declared and `ownerId` is not
    - src/lib/bulk/limits.ts, src/lib/bulk/types.ts, src/lib/bulk/dispatch.ts — the contract landed by plan 38-06
    - src/lib/audit/actor-context.ts — `runWithActor`'s real signature and `AsyncLocalStorage` semantics
    - src/app/api/v1/organizations/batch/route.ts lines 1-75 — the sequential loop shape to REUSE and the failure-swallowing to REJECT
    - src/app/trash/actions.ts lines 100-120 — `parseRecordId`'s runtime-narrowing reasoning
    - .planning/phases/38-bulk-operations/38-PATTERNS.md § 5 — the verbatim skeleton and the four-predicate table
  </read_first>
  <behavior>
    - No session / no `session.user.id` -> `{ success: false, error: "not_authenticated" }`. `runWithActor`
      NOT called and no dispatch function called
    - More than `BULK_MAX_IDS` distinct ids -> `{ success: false, error: "too_many", max: BULK_MAX_IDS }`.
      No dispatch, no `runWithActor`, no `revalidatePath`
    - Empty ids -> `{ success: false, error: "no_selection" }`. No dispatch
    - Duplicate ids collapse before the loop
    - Missing or soft-deleted row -> `failed` gains `{ id, reason: "notFound" }`, dispatch NOT called for it
    - Row owned by someone else -> `failed` gains `{ id, reason: "notPermitted" }`, dispatch NOT called.
      This holds even when the session user's `role` is `"admin"` — activities have no bypass
    - Dispatch returning `{ success: false }` -> `failed` gains `{ id, reason: "unknown" }` and the loop
      CONTINUES
    - 12 ids, 9 owned / 3 not -> `succeeded.length === 9`, `failed.length === 3`, all `notPermitted`
    - `runWithActor` called exactly ONCE with `{ kind: "user", userId: session.user.id }`
    - `revalidatePath` called exactly ONCE, after the loop, only when at least one id succeeded
    - Reassign only: `ownerId` not an approved, non-deleted user -> `{ success: false, error:
      "invalid_owner" }` with NO dispatch and NO actor scope. Validated ONCE before the loop
    - Reassign only: `updateRecordOwnerByType` is called with `("activity", id, ownerId, sessionUserId)`
      and there is no code path in either bulk action that reads or writes `assigneeId`
  </behavior>
  <action>
    Write the failing tests first (Task 3 creates the file; write the delete and reassign describes there
    before implementing here). Then add the two actions to `src/app/activities/actions.ts`, placed after
    `deleteActivity` so each bulk predicate sits adjacent to the single-record predicate it mirrors.

    `bulkDeleteActivities(ids: string[])`, in this exact order:
      1. `const session = await auth()`; if `!session?.user?.id` return
         `{ success: false, error: "not_authenticated" }`. The actor scope opens AFTER the session check
         so an unauthenticated call establishes no actor at all (T-36-02).
      2. Runtime-narrow the argument: reject anything that is not an array of non-empty strings, mapping
         a malformed argument to `{ success: false, error: "no_selection" }`. A server action is a POST
         endpoint; copy `parseRecordId`'s reasoning.
      3. `const uniqueIds = Array.from(new Set(ids))`; if empty return `{ success: false, error: "no_selection" }`.
      4. If `uniqueIds.length > BULK_MAX_IDS` return `{ success: false, error: "too_many", max: BULK_MAX_IDS }`.
      5. `const outcome = await runWithActor({ kind: "user", userId: session.user.id }, async () => { ... })`
         — ONE scope around the WHOLE loop. Inside, declare `succeeded: string[]` and `failed: BulkFailure[]`,
         then `for (const id of uniqueIds)`:
           - `const row = await db.query.activities.findFirst({ where: and(eq(activities.id, id),
             isNull(activities.deletedAt)) })`. If falsy: `failed.push({ id, reason: "notFound" })`, `continue`.
           - Copy this file's predicate VERBATIM: `if (row.ownerId !== session.user.id)` then
             `failed.push({ id, reason: "notPermitted" })`, `continue`. Do NOT add
             `&& session.user.role !== "admin"`, and do NOT substitute `assigneeId` for `ownerId` in the
             predicate — the predicate is about who OWNS the activity, and `assigneeId` is a different
             concept that this phase does not touch.
           - `const result = await deleteRecordByType("activity", id, session.user.id)`;
             `result.success ? succeeded.push(id) : failed.push({ id, reason: "unknown" })`.
           - Never `break` and never `throw` out of the loop.
         Return `{ succeeded, failed }`.
      6. `if (outcome.succeeded.length > 0) revalidatePath("/activities")` — ONCE, after the loop. Use the
         same path string `deleteActivity` already revalidates; read it and copy it rather than guessing.
      7. `return { success: true, ...outcome }`.

    `bulkReassignActivityOwner(ids: string[], ownerId: string)` is the same seven steps with two additions:
      - Between steps 4 and 5, validate the TARGET USER exactly once:
        `db.query.users.findFirst({ where: and(eq(users.id, ownerId), isNull(users.deletedAt),
        eq(users.status, "approved")) })`. If falsy return `{ success: false, error: "invalid_owner" }`
        before opening any actor scope. BOTH predicates are required. Do NOT copy
        `activities/page.tsx:99-107`, which filters on `deletedAt` alone and feeds `ActivityFilters` and
        `ActivityDialog` — that query is NOT modified by this plan.
      - In the loop, call `updateRecordOwnerByType("activity", id, ownerId, session.user.id)`. Verify for
        yourself that activities' update predicate (:84) is the same string as its delete predicate (:131)
        before reusing it.

    Neither bulk action may contain any reference to `assigneeId`, `assignee`, or any activity-assignment
    concept. Reassigning Activity `assigneeId` alongside `ownerId` is a DEFERRED idea (D-11) and must not
    appear in this phase in any form, including as an optional parameter.

    Import `BULK_MAX_IDS` from `@/lib/bulk/limits`, the types from `@/lib/bulk/types`, and the two
    dispatch functions from `@/lib/bulk/dispatch`. Do NOT import or call `updateActivityMutation` — and
    specifically never with `{ ownerId }`: `ownerId` is absent from `activitySchema` and Zod strips
    unknown keys, so that call writes only `updatedAt`, emits an empty diff, and `subscribers/audit.ts`
    drops the row — a silent success no-op that fails SC-3 and SC-5 while the whole suite stays green. Do
    NOT use `Promise.all`. Do NOT wrap the loop in a transaction. Do NOT send email. Do NOT write an
    `audit_log` row here. Do NOT touch `getActivities`, `getActivityById`, `getActivityTypes` or
    `toggleActivityCompletion`.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/activities/bulk-actions.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function bulkDeleteActivities\|export async function bulkReassignActivityOwner' src/app/activities/actions.ts` is 2
    - `grep -c 'runWithActor' src/app/activities/actions.ts` increased by exactly 2 from its pre-task count (record both counts in the SUMMARY)
    - Source assertion on the `bulkDeleteActivities` declaration slice: exactly one `runWithActor`, exactly one `revalidatePath`, and the `revalidatePath` appears AFTER the `runWithActor` call closes
    - Source assertion on both bulk write slices: zero occurrences of `assigneeId`, `assignee`, `Promise.all`, `db.transaction`, `session.user.role`, `updateActivityMutation`, `auditLog`
    - `grep -c 'eq(users.status, "approved")' src/app/activities/actions.ts` is 1, inside the reassign action before its `runWithActor`
    - The verbatim predicate appears in both bulk actions; record the exact two lines in the SUMMARY
    - The `revalidatePath` argument string in both bulk actions matches the one `deleteActivity` already uses; record that string in the SUMMARY
    - `grep -c 'BULK_MAX_IDS' src/app/activities/actions.ts` is at least 2
    - `git diff src/app/activities/actions.ts` shows NO modification to `createActivity`, `updateActivity`, `deleteActivity`, `toggleActivityCompletion`, `getActivities`, `getActivityById` or `getActivityTypes`
    - `git status --porcelain src/app/activities/page.tsx` is empty
    - `./node_modules/.bin/vitest run src/app/activities/bulk-actions.test.ts` passes
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors, no `@ts-expect-error` added
  </acceptance_criteria>
  <done>Both bulk write actions refuse unauthenticated and over-cap calls before any write, validate the reassign target once against approved-and-not-deleted, check activities' own ownership predicate per record with no admin bypass, never mention `assigneeId`, continue past failures, open one actor scope and call `revalidatePath` once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: exportSelectedActivities</name>
  <files>src/app/activities/actions.ts</files>
  <read_first>
    - src/app/activities/actions.ts — the file you just extended
    - src/lib/export/formatters.ts — `fetchFilteredData`'s signature (~:354), `fetchActivities` (~:322-336) whose `conditions` array is explicitly annotated, and the filename construction (~:417-426) whose `entityPlural` mapping yields `"activities"`
    - src/lib/export/types.ts — `ExportOptions`, `ExportFilters` (now carrying `ids`), `ExportResult`
    - src/app/admin/export/actions.ts — read `getExportData` ONLY to understand why it cannot be reused: it is gated on `role !== "admin"` and takes a full `ExportOptions`
    - .planning/phases/38-bulk-operations/38-RESEARCH.md § Pitfall 4 and § Security Domain — the admin-gate bypass this signature exists to prevent
  </read_first>
  <behavior>
    - No session -> `{ success: false, error: <non-empty string> }`, `fetchFilteredData` NOT called
    - Empty ids -> failure, not called
    - More than `BULK_MAX_IDS` ids -> failure, not called
    - A valid call passes `fetchFilteredData` exactly `{ entityType: "activity", format: "csv",
      includeCustomFields: true, filters: { ids: <deduped ids> } }` — every field constructed
      server-side, none from a parameter, and no `dateFrom`/`dateTo` key even though `ExportFilters` has
      them and activities are date-scoped everywhere else in the app
    - On success the returned `filename` is `activities-selected-<count>-<YYYY-MM-DD>.csv`, where
      `<count>` is `result.count`
    - `data` and `count` pass through unchanged
    - The parameter list is exactly one `ids: string[]`
  </behavior>
  <action>
    Add `export async function exportSelectedActivities(ids: string[]): Promise<ExportResult>` to
    `src/app/activities/actions.ts`.

    THE SIGNATURE IS `(ids: string[])` AND NOTHING ELSE. The only existing export action,
    `getExportData`, is admin-gated, and a non-admin action that accepted an `ExportFilters` or
    `ExportOptions` object and received `{}` would return every activity in the table — an admin-gate
    bypass. Do not add a `format`, `filters`, `entityType`, `includeCustomFields`, `dateFrom`, `dateTo`
    or options parameter, and do not accept an object argument.

    Body:
      1. `const session = await auth()`; if `!session?.user?.id` return
         `{ success: false, error: "Not authenticated" }`. Any authenticated user may export their own
         selection; there is no admin gate on the SCOPED export.
      2. Runtime-narrow `ids` to an array of non-empty strings, then dedupe with `new Set`.
      3. If empty return `{ success: false, error: "No records selected" }`.
      4. If longer than `BULK_MAX_IDS` return `{ success: false, error: "Too many records" }`.
      5. `const result = await fetchFilteredData({ entityType: "activity", format: "csv",
         includeCustomFields: true, filters: { ids: uniqueIds } })` — every field a literal constructed
         here, and no `dateFrom`/`dateTo` keys at all.
      6. `if (!result.success) return result`.
      7. `const date = new Date().toISOString().split("T")[0]` and return
         `{ ...result, filename: \`activities-selected-${result.count}-${date}.csv\` }`.
         The slug is `activities`, the English plural from `formatters.ts`'s own `entityPlural` mapping,
         and it is NEVER translated. The count comes from `result.count`, not from `uniqueIds.length`.

    Do NOT call `getExportData`. Do NOT add a `/api/export` route. Do NOT offer the Pipedrive variant.
    Do NOT modify `fetchFilteredData`'s own filename block.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/activities/bulk-actions.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function exportSelectedActivities' src/app/activities/actions.ts` is 1
    - Source assertion on the `exportSelectedActivities` declaration slice: zero occurrences of `ExportFilters`, `ExportOptions`, `ExportFormat`, `pipedrive`, `getExportData`, `role`, `dateFrom`, `dateTo`
    - Source assertion: the declaration's parameter list matches a single `ids` parameter — record the exact signature line in the SUMMARY
    - `grep -c 'activities-selected-' src/app/activities/actions.ts` is 1
    - A unit test asserts the exact object passed to `fetchFilteredData` deep-equals `{ entityType: "activity", format: "csv", includeCustomFields: true, filters: { ids: [...] } }` — the deep-equal is what proves no `dateFrom`/`dateTo` key leaked in
    - A unit test asserts the filename matches `/^activities-selected-\d+-\d{4}-\d{2}-\d{2}\.csv$/` and that its count segment equals the mocked `result.count`, using a mock where `count` differs from the input id count
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
  </acceptance_criteria>
  <done>`exportSelectedActivities` takes ids and nothing else, constructs the export options server-side with `entityType: "activity"` and no date filters, and returns an untranslated `activities-selected-<count>-<date>.csv` filename whose count comes from the fetch result.</done>
</task>

<task type="auto">
  <name>Task 3: bulk-actions.test.ts — session-swapping coverage and the assigneeId boundary gate</name>
  <files>src/app/activities/bulk-actions.test.ts</files>
  <read_first>
    - src/app/trash/actions.test.ts lines 1-60 — the WHOLE mock scaffold and its header note: the point is to swap the SESSION per test (absent / member-owner / member-non-owner / admin), which the `vi.mock("@/lib/api/auth")` auto-approve bypass used by the `/api/v1` route tests cannot do. Note its rule: "THE ASSERTION THAT MATTERS MOST IS AN ABSENCE"
    - src/app/trash/actions.test.ts lines 20-30 — the deliberate NON-mock: the authorization predicate is never stubbed
    - src/components/custom-fields/__tests__/source-scan.ts — `readStrippedSource`
    - src/app/activities/actions.ts — the three actions you just wrote, and the other exports in the file that declaration slicing must not swallow
    - src/lib/audit/no-mutation-coupling.test.ts lines 196-205 — `sliceDeclaration` and the WR-13 `indexOf`-anchor discipline
    - .planning/phases/38-bulk-operations/38-PATTERNS.md § 16 — the verbatim scaffold including the `runWithActor` recording mock
  </read_first>
  <action>
    Create `src/app/activities/bulk-actions.test.ts` by copying `src/app/trash/actions.test.ts`'s scaffold.
    Carry over its header discipline, and add one activities-specific note: this entity has a SECOND
    user-valued column, `assigneeId`, which `activitySchema` declares while `ownerId` is absent from it,
    so `assigneeId` is the field a careless implementation reaches for. Reassigning it is a deferred idea
    (D-11) and this file gates its absence.

    Mocks:
      - `vi.mock("@/auth", () => ({ auth: vi.fn() }))` — bare; every test drives `mockResolvedValue`.
      - `vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))`.
      - `vi.mock("@/lib/bulk/dispatch", () => ({ deleteRecordByType: vi.fn(), updateRecordOwnerByType: vi.fn() }))`.
      - `vi.mock("@/lib/export/formatters", () => ({ fetchFilteredData: vi.fn() }))`.
      - `vi.mock("@/lib/audit/actor-context", () => ({ runWithActor: vi.fn((_actor: unknown, fn: () => unknown) => fn()) }))`
        — records its actor and still invokes the callback.
      - `vi.mock("@/db", ...)` with a shaped `{ query: { activities: { findFirst: vi.fn() },
        users: { findFirst: vi.fn() } } }`.
      - Do NOT mock the ownership predicate.

    Describes and cases:

    A. `bulkDeleteActivities`
      1. no session -> `not_authenticated`; `runWithActor` NOT called, `deleteRecordByType` NOT called.
      2. 101 distinct ids -> `{ success: false, error: "too_many", max: 100 }`; dispatch NOT called,
         `revalidatePath` NOT called.
      3. empty ids -> `no_selection`; no dispatch.
      4. duplicate ids -> exactly one dispatch call for the repeated id.
      5. `findFirst` undefined -> `failed` contains `{ id, reason: "notFound" }`, dispatch NOT called for it.
      6. row owned by someone else -> `failed` contains `{ id, reason: "notPermitted" }`, dispatch NOT
         called. THIS IS THE CENTRAL ABSENCE.
      7. AUTHORIZATION ASYMMETRY: session user is an ADMIN (`role: "admin"`) and the row is owned by
         someone else -> still `notPermitted`, dispatch NOT called. Activities have NO admin bypass; this
         test is what fails if someone unifies the four predicates.
      8. A row whose `assigneeId` equals the session user but whose `ownerId` does not -> still
         `notPermitted`, dispatch NOT called. This pins that the predicate is about ownership, not
         assignment, and it is the case a careless implementation gets wrong on this entity.
      9. dispatch returns `{ success: false }` -> `failed` contains `{ id, reason: "unknown" }` AND the
         loop continued: with three ids where the middle one fails, all three were read and the third
         still dispatched.
     10. 12 ids, 9 owned / 3 not -> `succeeded.length === 9`, `failed.length === 3`, all `notPermitted`.
     11. `expect(runWithActor).toHaveBeenCalledTimes(1)` and the recorded actor deep-equals
         `{ kind: "user", userId: <session user id> }`.
     12. `expect(revalidatePath).toHaveBeenCalledTimes(1)` for a partially successful call and
         `toHaveBeenCalledTimes(0)` when zero ids succeeded.

    B. `bulkReassignActivityOwner`
      1. no session -> `not_authenticated`; no `runWithActor`, no dispatch.
      2. target user missing -> `invalid_owner`; `updateRecordOwnerByType` NOT called AND `runWithActor`
         NOT called.
      3. target user exists but `status: "pending_verification"` -> `invalid_owner`, no dispatch. Drive
         via the mocked `users.findFirst` returning `undefined` for the two-predicate query, and assert
         the recorded query argument carried BOTH predicates if the mock records it.
      4. target user exists but `deletedAt` set -> `invalid_owner`, no dispatch.
      5. row not owned -> `notPermitted`, no dispatch for that id.
      6. 12 ids, 9 owned / 3 not -> 9 succeeded / 3 `notPermitted`.
      7. `runWithActor` once; `revalidatePath` once after the loop.
      8. `updateRecordOwnerByType` receives `("activity", id, ownerId, sessionUserId)` in that exact
         argument order — the order types cannot catch, since all four are strings.
      9. ASSIGNEE BOUNDARY: after a fully successful 12-id reassign, assert every recorded
         `updateRecordOwnerByType` call has exactly 4 arguments and none of them is an object — proving
         no `assigneeId` payload was smuggled through as a fifth argument or an options bag.

    C. `exportSelectedActivities`
      1. no session -> failure, `fetchFilteredData` NOT called.
      2. empty ids -> failure, not called.
      3. 101 ids -> failure, not called.
      4. valid call -> the object passed to `fetchFilteredData` deep-equals
         `{ entityType: "activity", format: "csv", includeCustomFields: true, filters: { ids } }`.
      5. filename shape and count-from-result, using a mock whose `count` differs from the input length.
      6. `fetchFilteredData` failure passes through unchanged.

    D. SOURCE GATE (comment-stripped)
      Read `readStrippedSource("src/app/activities/actions.ts")` and slice out each of the three bulk
      declarations (from `export async function <name>` to the next top-level `\nexport `), asserting the
      anchor index is `> -1` with a named message BEFORE slicing — WR-13: `indexOf(x, -1)` silently
      behaves as `indexOf(x, 0)`, so a helper handed a missing anchor widens to the enclosing block and
      stops detecting. This file has seven other exports, so a widened slice would silently pull in
      `getActivities` and make every negative assertion meaningless — assert each slice's length is
      strictly less than the whole file's as an additional guard. Then assert:
        - Positive markers first: each slice non-empty; both write slices contain `runWithActor`.
        - The export slice contains none of `ExportFilters`, `ExportOptions`, `ExportFormat`,
          `pipedrive`, `getExportData`, `role`, `dateFrom`, `dateTo`.
        - Neither write slice contains `assigneeId`, `assignee`, `Promise.all`, `db.transaction`,
          `session.user.role`, `updateActivityMutation`, or `auditLog`.
        - Each write slice contains exactly one `runWithActor` and exactly one `revalidatePath`.
      Header-comment the gate as comment-blind, naming Phase 37's nine-collision lesson.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/activities/bulk-actions.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/app/activities/bulk-actions.test.ts` passes with at least 30 tests
    - `grep -c 'not.toHaveBeenCalled' src/app/activities/bulk-actions.test.ts` is at least 11
    - The admin-no-bypass test (A.7) and the assignee-is-not-owner test (A.8) both exist and pass; record their names in the SUMMARY
    - The assignee-boundary argument-shape test (B.9) exists and passes
    - `grep -c 'readStrippedSource' src/app/activities/bulk-actions.test.ts` is at least 1 and `grep -c 'readFileSync' ...` is 0
    - The declaration-slicing helper asserts its anchor index is `> -1` with a named message before slicing, AND asserts each slice is strictly shorter than the whole file
    - Negative proof 1: temporarily change the predicate to compare `row.assigneeId` instead of `row.ownerId`, run the suite, observe cases A.6/A.8 FAIL, revert. Record the messages
    - Negative proof 2: temporarily add `&& session.user.role !== "admin"` to `bulkDeleteActivities`' predicate, run the suite, observe case A.7 FAIL, revert. Record the message
    - Negative proof 3: temporarily move `revalidatePath` inside the loop, run the suite, observe the call-count assertion FAIL, revert. Record the message
    - `npm run typecheck` reports 0 errors
  </acceptance_criteria>
  <done>A session-swapping suite proves every refusal happens before any dispatch call, that activities have no admin bypass, that the predicate is about ownership rather than assignment, that no `assigneeId` write is smuggled through, that the loop continues past failures, and that the export signature admits nothing but ids — with three negative directions demonstrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> server action | A server action is a POST endpoint; `ids: string[]` is an annotation, not a control |
| server action -> mutation layer | The mutation checks existence only, so authorization must be complete before the dispatch call |
| ownerId vs assigneeId | Activities carry two user-valued columns; conflating them changes both who is authorized and what is written |
| non-admin caller -> admin-gated full export | The scoped export must not be able to express "no filter" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure / Elevation of Privilege | `exportSelectedActivities` | mitigate | Signature is `(ids: string[])` and nothing else; `ExportOptions` is constructed server-side with literal `entityType: "activity"`, `format: "csv"`, `includeCustomFields: true` and NO `dateFrom`/`dateTo`. Gated by a deep-equal unit assertion and a comment-stripped source gate |
| T-38-02 | Tampering / Elevation of Privilege | the per-record loop | mitigate | Ownership is checked INSIDE the loop before each dispatch call, using activities' own verbatim predicate against `ownerId`. A dedicated test proves that a row assigned to the caller but owned by someone else is still refused, so `assigneeId` cannot become an accidental authorization path |
| T-38-03 | Denial of Service | unbounded id list | mitigate | `BULK_MAX_IDS = 100` enforced server-side after dedupe and before the actor scope opens, asserted by a unit test that also proves no dispatch and no revalidation occurred |
| T-38-04 | Repudiation | actor attribution | mitigate | `runWithActor({ kind: "user", userId: session.user.id }, ...)` built only from the session, opened AFTER the session check, exactly once around the whole loop |
| T-38-06 | Elevation of Privilege (ownership transfer to an inactive principal) | `bulkReassignActivityOwner` | mitigate | The target is validated ONCE before the loop against `isNull(deletedAt)` AND `eq(status, "approved")`. `activities/page.tsx:99-107` filters on `deletedAt` alone and feeds `ActivityFilters` plus `ActivityDialog`; it is deliberately NOT copied and NOT modified |
| T-38-07 | Information Disclosure | per-record failure reporting | mitigate | Failures carry only a `BulkFailureReason` code; a dispatch failure collapses to `"unknown"` |
| T-38-09 | Repudiation | reassign path | mitigate | Routes through `updateRecordOwnerByType`, never `updateActivityMutation({ ownerId })`, which would strip the key, emit an empty diff and produce no audit row. A source gate asserts the write slices do not mention `updateActivityMutation` |
| T-38-13 | Tampering (out-of-scope column written) | `assigneeId` | mitigate | Neither bulk action mentions `assigneeId` or `assignee`, asserted by a comment-stripped source gate over precisely-anchored declaration slices, plus a runtime assertion that every dispatch call has exactly four scalar arguments. D-11 defers `assigneeId` reassignment |
| T-38-08 | Denial of Service (outbound fan-out) | 100 webhook deliveries per click | accept | Bounded by `BULK_MAX_IDS`. The delivery queue and its retry/backoff already exist and are unchanged; N deletes genuinely are N events and the loop stays sequential |
| T-38-34 | Tampering (aborting transaction hides per-record failures) | loop control flow | mitigate | No `db.transaction`, no `Promise.all`, asserted absent by the source gate; a unit test proves the loop continues past a mid-list failure |
| T-38-35 | Repudiation (self-invalidating gate via a widened slice) | the declaration slicer | mitigate | This file has seven other exports, so a missing anchor would widen the slice to the enclosing block and make every negative assertion vacuous. The helper asserts its anchor index is `> -1` with a named message AND that each slice is strictly shorter than the whole file |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (38-RESEARCH § Package Legitimacy Audit: empty input set) |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/app/activities/bulk-actions.test.ts` — green
- `./node_modules/.bin/vitest run src/lib/audit/no-mutation-coupling.test.ts` — green (no audit write added)
- `npm run typecheck` — 0 errors, no new `@ts-expect-error`; `npm run lint` — 0 errors
- `git status --porcelain src/app/activities/page.tsx` — empty
- All three negative proofs demonstrated and reverted, messages recorded
</verification>

<success_criteria>
- `bulkDeleteActivities`, `bulkReassignActivityOwner` and `exportSelectedActivities` exist with the documented signatures
- Every refusal (unauthenticated, over-cap, empty, invalid owner) happens before any dispatch call, proven by absence assertions
- Activities' predicate is copied verbatim against `ownerId` with no admin bypass, proven by an admin-non-owner test and an assignee-is-not-owner test
- Neither bulk write action references `assigneeId` or `assignee` in any form
- The loop is sequential, best-effort, and continues past failures; one `runWithActor`; one `revalidatePath` only when something succeeded
- The reassign target is validated once against approved-and-not-deleted; `activities/page.tsx` is untouched
- The export action admits only ids, passes no date filters, and returns `activities-selected-<count>-<date>.csv`
- The other seven exports in `actions.ts` are unmodified, and the gate's slices are proven not to have widened over them
</success_criteria>

<output>
Create `.planning/phases/38-bulk-operations/38-14-SUMMARY.md` when done
</output>
