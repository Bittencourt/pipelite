---
phase: 38-bulk-operations
plan: 13
type: execute
wave: 3
depends_on: [38-04, 38-06]
files_modified:
  - src/app/deals/actions.ts
  - src/app/deals/bulk-actions.test.ts
autonomous: true
requirements: [BULK-02, BULK-03, BULK-04]

must_haves:
  truths:
    - "An unauthenticated bulk call is refused before any actor scope opens and before any mutation runs"
    - "A call carrying more than 100 ids is refused with the cap and the count, and no mutation runs"
    - "A member who does not own a deal is refused per record, but an admin is allowed — deals alone carry that bypass"
    - "Nine successes and three failures produce nine succeeded ids and three named failures, and the loop does not stop at the first failure"
    - "The actor scope opens once for the whole loop and revalidatePath runs once after it"
    - "No email is sent by any deals bulk action"
    - "The scoped export accepts a list of ids and nothing else"
  artifacts:
    - path: "src/app/deals/actions.ts"
      provides: "bulkDeleteDeals, bulkReassignDealOwner, exportSelectedDeals"
      contains: "bulkDeleteDeals"
    - path: "src/app/deals/bulk-actions.test.ts"
      provides: "session-swapping coverage including the admin-bypass asymmetry and the no-email gate"
      contains: "not.toHaveBeenCalled"
  key_links:
    - from: "src/app/deals/actions.ts"
      to: "src/lib/bulk/dispatch.ts"
      via: "deleteRecordByType(\"deal\", id, userId) and updateRecordOwnerByType(\"deal\", id, ownerId, userId) inside the loop"
      pattern: "(deleteRecordByType|updateRecordOwnerByType)\\(\\s*\"deal\""
    - from: "src/app/deals/actions.ts"
      to: "session.user.role"
      via: "the admin bypass clause, present on deals and only on deals"
      pattern: "session.user.role !== \"admin\""
---

<objective>
Add the three Deals bulk server actions — delete, reassign owner, scoped CSV export — and the
session-swapping unit suite, including the ONE authorization asymmetry in the phase.

Purpose: deals is the single entity whose ownership predicate carries `&& session.user.role !== "admin"`
(`src/app/deals/actions.ts:83, 155, 191, 228`). Organizations, people and activities do not. Copying a
"uniform" predicate here would remove deals' bypass — a regression an admin will hit immediately, since
in the live data the admin owns everything — while copying deals' predicate to the other three would be
a privilege escalation shipped as a bulk feature. Deals is also where the no-email guarantee has to be
proven: `sendDealAssignedEmail` fires in THIS file (`:96-127`) off `result.newAssigneeUserIds`, and the
bulk reassign must simply not contain that loop.
Output: three server actions and one test file whose most important assertions are ABSENCES.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-bulk-operations/38-PATTERNS.md

<interfaces>
Emit exactly these three exports — plan 38-18 wires them into the bar:

  export async function bulkDeleteDeals(ids: string[]): Promise<BulkWriteResult>
  export async function bulkReassignDealOwner(ids: string[], ownerId: string): Promise<BulkWriteResult>
  export async function exportSelectedDeals(ids: string[]): Promise<ExportResult>

From src/lib/bulk/types.ts (plan 38-06):
  BulkFailureReason = "notFound" | "notPermitted" | "alreadyDeleted" | "unknown"
  BulkFailure = { id: string; reason: BulkFailureReason }
  BulkErrorCode = "not_authenticated" | "too_many" | "invalid_owner" | "no_selection"
  BulkWriteResult =
    | { success: true; succeeded: string[]; failed: BulkFailure[] }
    | { success: false; error: BulkErrorCode; max?: number }

From src/lib/bulk/limits.ts: BULK_MAX_IDS = 100
From src/lib/bulk/dispatch.ts: deleteRecordByType(entityType, id, userId) and
  updateRecordOwnerByType(entityType, id, ownerId, userId)
From src/lib/export/types.ts: ExportResult
From src/lib/export/formatters.ts: fetchFilteredData(options: ExportOptions): Promise<ExportResult>
From src/lib/audit/actor-context.ts: runWithActor(actor, fn) — returns `T | Promise<T>`, so an async
callback needs `await runWithActor(...)`.

DEALS' AUTHORIZATION PREDICATE, verbatim (src/app/deals/actions.ts:83, 155, 191, 228) — this one and
ONLY this one carries the admin bypass:
  if (deal.ownerId !== session.user.id && session.user.role !== "admin") {

Email side effect, resolved: the only email on any owner/assignee path is `sendDealAssignedEmail`, and
it fires in THIS FILE at ~:96-127 off `result.newAssigneeUserIds` — i.e. off deal ASSIGNEES, never off
`ownerId`. `updateDealOwnerMutation` (plan 38-03) returns no `newAssigneeUserIds`, so D-13's "no email
on bulk reassign" costs nothing: no suppression flag, no conditional email. The bulk action simply does
not contain the loop.

Latent bug this action routes around, NOT through: `updateDealMutation(id, { ownerId }, userId)`
unconditionally deletes every `deal_assignees` row (`src/lib/mutations/deals.ts:406`), because
`updateDealSchema = dealSchema.partial()` preserves `assigneeIds`' `.default([])`. The loss is unaudited
because assignees live in a join table. The bulk reassign must go through `updateRecordOwnerByType`,
which reaches `updateDealOwnerMutation`, which never touches `dealAssignees`.

`alreadyDeleted` is deliberately UNREACHABLE on the delete path: the per-record read already carries
`isNull(deals.deletedAt)`, so a miss maps to `notFound` (38-RESEARCH A6).

Fan-out, stated because nobody has: a 100-record bulk delete enqueues 100 webhook deliveries, runs 100
workflow-trigger evaluations and issues 100 fire-and-forget `audit_log` inserts, against a `postgres.js`
client created with no `max`. Live blast radius: 1 active webhook, 3 workflows of which 0 active. Note
`deal.deleted` is a workflow trigger type, so deals is the entity where this fan-out is most real.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bulkDeleteDeals and bulkReassignDealOwner</name>
  <files>src/app/deals/actions.ts</files>
  <read_first>
    - src/app/deals/actions.ts — read the WHOLE file. You need `deleteDeal` (~:138-168) as the skeleton, its `auth()` check, the verbatim admin-bypass predicate at :155, the `runWithActor` placement, `revalidatePath("/deals")` at ~:163-165, and the `sendDealAssignedEmail` call site at ~:96-127 so you can see exactly what the bulk reassign must NOT contain
    - src/lib/mutations/deals.ts — `updateDealOwnerMutation` (added by plan 38-03) and the `db.delete(dealAssignees)` at ~:406 inside `updateDealMutation`, so you understand which one you must NOT reach
    - src/lib/bulk/limits.ts, src/lib/bulk/types.ts, src/lib/bulk/dispatch.ts — the contract landed by plan 38-06
    - src/lib/audit/actor-context.ts — `runWithActor`'s real signature and `AsyncLocalStorage` semantics
    - src/app/api/v1/organizations/batch/route.ts lines 1-75 — the sequential loop shape to REUSE and the failure-swallowing to REJECT
    - src/app/trash/actions.ts lines 100-120 — `parseRecordId`'s runtime-narrowing reasoning
    - .planning/phases/38-bulk-operations/38-PATTERNS.md § 5 — the verbatim skeleton and the four-predicate table
  </read_first>
  <behavior>
    - No session / no `session.user.id` -> `{ success: false, error: "not_authenticated" }`. `runWithActor`
      NOT called and no dispatch function called
    - More than `BULK_MAX_IDS` distinct ids -> `{ success: false, error: "too_many", max: BULK_MAX_IDS }`.
      No dispatch, no `runWithActor`, no `revalidatePath`
    - Empty ids -> `{ success: false, error: "no_selection" }`. No dispatch
    - Duplicate ids collapse before the loop
    - Missing or soft-deleted row -> `failed` gains `{ id, reason: "notFound" }`, dispatch NOT called for it
    - ADMIN BYPASS PRESENT: session user role `"admin"` and the row owned by someone else -> the id
      SUCCEEDS and the dispatch IS called. This is the opposite of the organizations, people and
      activities behaviour, and both directions must be asserted
    - Non-admin (`role: "member"` or any non-admin value) who does not own the row -> `failed` gains
      `{ id, reason: "notPermitted" }`, dispatch NOT called
    - Dispatch returning `{ success: false }` -> `failed` gains `{ id, reason: "unknown" }` and the loop
      CONTINUES
    - 12 ids, 9 owned / 3 not, non-admin session -> `succeeded.length === 9`, `failed.length === 3`
    - `runWithActor` called exactly ONCE with `{ kind: "user", userId: session.user.id }`
    - `revalidatePath` called exactly ONCE, after the loop, only when at least one id succeeded
    - Reassign only: `ownerId` not an approved, non-deleted user -> `{ success: false, error:
      "invalid_owner" }` with NO dispatch and NO actor scope. Validated ONCE before the loop
    - NO EMAIL: `sendDealAssignedEmail` is never called by either bulk action, on any path
  </behavior>
  <action>
    Write the failing tests first (Task 3 creates the file; write the delete and reassign describes there
    before implementing here). Then add the two actions to `src/app/deals/actions.ts`, placed after
    `deleteDeal` so each bulk predicate sits adjacent to the single-record predicate it mirrors.

    `bulkDeleteDeals(ids: string[])`, in this exact order:
      1. `const session = await auth()`; if `!session?.user?.id` return
         `{ success: false, error: "not_authenticated" }`. The actor scope opens AFTER the session check
         so an unauthenticated call establishes no actor at all (T-36-02).
      2. Runtime-narrow the argument: reject anything that is not an array of non-empty strings, mapping
         a malformed argument to `{ success: false, error: "no_selection" }`. A server action is a POST
         endpoint; copy `parseRecordId`'s reasoning.
      3. `const uniqueIds = Array.from(new Set(ids))`; if empty return `{ success: false, error: "no_selection" }`.
      4. If `uniqueIds.length > BULK_MAX_IDS` return `{ success: false, error: "too_many", max: BULK_MAX_IDS }`.
      5. `const outcome = await runWithActor({ kind: "user", userId: session.user.id }, async () => { ... })`
         — ONE scope around the WHOLE loop. Inside, declare `succeeded: string[]` and `failed: BulkFailure[]`,
         then `for (const id of uniqueIds)`:
           - `const row = await db.query.deals.findFirst({ where: and(eq(deals.id, id),
             isNull(deals.deletedAt)) })`. If falsy: `failed.push({ id, reason: "notFound" })`, `continue`.
           - Copy this file's predicate VERBATIM, INCLUDING the admin clause:
             `if (row.ownerId !== session.user.id && session.user.role !== "admin")` then
             `failed.push({ id, reason: "notPermitted" })`, `continue`.
             Do NOT drop the `&& session.user.role !== "admin"` clause. Deals is the one entity that has
             it, and removing it is a regression an admin hits immediately because in the live data the
             admin owns everything.
           - `const result = await deleteRecordByType("deal", id, session.user.id)`;
             `result.success ? succeeded.push(id) : failed.push({ id, reason: "unknown" })`.
           - Never `break` and never `throw` out of the loop.
         Return `{ succeeded, failed }`.
      6. `if (outcome.succeeded.length > 0) revalidatePath("/deals")` — ONCE, after the loop.
      7. `return { success: true, ...outcome }`.

    `bulkReassignDealOwner(ids: string[], ownerId: string)` is the same seven steps with two additions:
      - Between steps 4 and 5, validate the TARGET USER exactly once:
        `db.query.users.findFirst({ where: and(eq(users.id, ownerId), isNull(users.deletedAt),
        eq(users.status, "approved")) })`. If falsy return `{ success: false, error: "invalid_owner" }`
        before opening any actor scope. BOTH predicates are required. Do NOT copy
        `deals/page.tsx:159-163`, which filters on `deletedAt` alone and can therefore offer an
        unapproved user — that file is NOT touched by this plan.
      - In the loop, call `updateRecordOwnerByType("deal", id, ownerId, session.user.id)`. Verify for
        yourself that deals' update predicate (:83) is the same string as its delete predicate (:155),
        including the admin clause, before reusing it.

    NO EMAIL, structurally: neither bulk action may contain any reference to `sendDealAssignedEmail`,
    `newAssigneeUserIds`, `dealTitle` or `assigneeIds`. `updateDealOwnerMutation` returns no
    `newAssigneeUserIds`, so there is nothing to loop over and no suppression flag is needed. A
    per-record notification would emit up to 100 emails from one click; a digest email is deferred, not
    built.

    Import `BULK_MAX_IDS` from `@/lib/bulk/limits`, the types from `@/lib/bulk/types`, and the two
    dispatch functions from `@/lib/bulk/dispatch`. Do NOT import or call `updateDealMutation` — and
    specifically never with `{ ownerId }`: `updateDealSchema` is `dealSchema.partial()` which preserves
    `assigneeIds`' `.default([])`, so that call unconditionally deletes every `deal_assignees` row for
    the deal, unaudited. Do NOT use `Promise.all`. Do NOT wrap the loop in a transaction. Do NOT write an
    `audit_log` row here. Do NOT touch `deals/page.tsx`.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/deals/bulk-actions.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function bulkDeleteDeals\|export async function bulkReassignDealOwner' src/app/deals/actions.ts` is 2
    - `grep -c 'session.user.role !== "admin"' src/app/deals/actions.ts` increased by exactly 2 from its pre-task count (record both counts in the SUMMARY)
    - Source assertion on each bulk write declaration slice: it contains exactly one `session.user.role !== "admin"`
    - Source assertion on both bulk write slices: zero occurrences of `sendDealAssignedEmail`, `newAssigneeUserIds`, `dealTitle`, `assigneeIds`, `updateDealMutation`, `dealAssignees`, `Promise.all`, `db.transaction`, `auditLog`
    - Source assertion on the `bulkDeleteDeals` slice: exactly one `runWithActor`, exactly one `revalidatePath`, and the `revalidatePath` appears AFTER the `runWithActor` call closes
    - `grep -c 'eq(users.status, "approved")' src/app/deals/actions.ts` is 1, inside the reassign action before its `runWithActor`
    - `grep -c 'BULK_MAX_IDS' src/app/deals/actions.ts` is at least 2
    - `git diff src/app/deals/actions.ts` shows NO modification to `createDeal`, `updateDeal`, `deleteDeal`, `updateDealStage` or `reorderDeals`
    - `git status --porcelain src/app/deals/page.tsx` is empty
    - `./node_modules/.bin/vitest run src/app/deals/bulk-actions.test.ts` passes
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors, no `@ts-expect-error` added
  </acceptance_criteria>
  <done>Both bulk write actions refuse unauthenticated and over-cap calls before any write, validate the reassign target once, carry deals' verbatim predicate INCLUDING the admin bypass, contain no reference to any email or assignee identifier, continue past failures, open one actor scope and call `revalidatePath` once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: exportSelectedDeals</name>
  <files>src/app/deals/actions.ts</files>
  <read_first>
    - src/app/deals/actions.ts — the file you just extended
    - src/lib/export/formatters.ts — `fetchFilteredData`'s signature (~:354), `fetchDeals` (~:290-307) whose `conditions` array is explicitly annotated, and the filename construction (~:417-426) whose `entityPlural` mapping yields `"deals"`
    - src/lib/export/types.ts — `ExportOptions`, `ExportFilters` (now carrying `ids`), `ExportResult`
    - src/app/admin/export/actions.ts — read `getExportData` ONLY to understand why it cannot be reused: it is gated on `role !== "admin"` and takes a full `ExportOptions`
    - .planning/phases/38-bulk-operations/38-RESEARCH.md § Pitfall 4 and § Security Domain — the admin-gate bypass this signature exists to prevent
  </read_first>
  <behavior>
    - No session -> `{ success: false, error: <non-empty string> }`, `fetchFilteredData` NOT called
    - Empty ids -> failure, not called
    - More than `BULK_MAX_IDS` ids -> failure, not called
    - A valid call passes `fetchFilteredData` exactly `{ entityType: "deal", format: "csv",
      includeCustomFields: true, filters: { ids: <deduped ids> } }` — every field constructed
      server-side, none from a parameter. In particular NO `stage` filter is passed, even though
      `ExportFilters` has one and `/deals` is a kanban organised by stage: the selection already
      determines the rows
    - On success the returned `filename` is `deals-selected-<count>-<YYYY-MM-DD>.csv`, where `<count>` is
      `result.count`
    - `data` and `count` pass through unchanged
    - The parameter list is exactly one `ids: string[]`
  </behavior>
  <action>
    Add `export async function exportSelectedDeals(ids: string[]): Promise<ExportResult>` to
    `src/app/deals/actions.ts`.

    THE SIGNATURE IS `(ids: string[])` AND NOTHING ELSE. The only existing export action,
    `getExportData`, is admin-gated, and a non-admin action that accepted an `ExportFilters` or
    `ExportOptions` object and received `{}` would return all 25,195 live deals — an admin-gate bypass.
    Do not add a `format`, `filters`, `entityType`, `includeCustomFields`, `stage` or options parameter,
    and do not accept an object argument.

    Body:
      1. `const session = await auth()`; if `!session?.user?.id` return
         `{ success: false, error: "Not authenticated" }`. Any authenticated user may export their own
         selection; there is no admin gate on the SCOPED export.
      2. Runtime-narrow `ids` to an array of non-empty strings, then dedupe with `new Set`.
      3. If empty return `{ success: false, error: "No records selected" }`.
      4. If longer than `BULK_MAX_IDS` return `{ success: false, error: "Too many records" }`.
      5. `const result = await fetchFilteredData({ entityType: "deal", format: "csv",
         includeCustomFields: true, filters: { ids: uniqueIds } })` — every field a literal constructed
         here, and no `stage` key at all.
      6. `if (!result.success) return result`.
      7. `const date = new Date().toISOString().split("T")[0]` and return
         `{ ...result, filename: \`deals-selected-${result.count}-${date}.csv\` }`.
         The slug is `deals`, the English plural from `formatters.ts`'s own `entityPlural` mapping, and it
         is NEVER translated. The count comes from `result.count`, not from `uniqueIds.length`.

    Do NOT call `getExportData`. Do NOT add a `/api/export` route. Do NOT offer the Pipedrive variant
    (`src/lib/export/pipedrive.ts`) — CSV only, and no format `Select` exists in the bar. Do NOT modify
    `fetchFilteredData`'s own filename block.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/deals/bulk-actions.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function exportSelectedDeals' src/app/deals/actions.ts` is 1
    - Source assertion on the `exportSelectedDeals` declaration slice: zero occurrences of `ExportFilters`, `ExportOptions`, `ExportFormat`, `pipedrive`, `getExportData`, `role`, `stage`
    - Source assertion: the declaration's parameter list matches a single `ids` parameter — record the exact signature line in the SUMMARY
    - `grep -c 'deals-selected-' src/app/deals/actions.ts` is 1
    - A unit test asserts the exact object passed to `fetchFilteredData` deep-equals `{ entityType: "deal", format: "csv", includeCustomFields: true, filters: { ids: [...] } }` — the deep-equal is what proves no extra `stage` key leaked in
    - A unit test asserts the filename matches `/^deals-selected-\d+-\d{4}-\d{2}-\d{2}\.csv$/` and that its count segment equals the mocked `result.count`, using a mock where `count` differs from the input id count
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
  </acceptance_criteria>
  <done>`exportSelectedDeals` takes ids and nothing else, constructs the export options server-side with `entityType: "deal"` and no stage filter, and returns an untranslated `deals-selected-<count>-<date>.csv` filename whose count comes from the fetch result.</done>
</task>

<task type="auto">
  <name>Task 3: bulk-actions.test.ts — the admin-bypass asymmetry and the no-email gate</name>
  <files>src/app/deals/bulk-actions.test.ts</files>
  <read_first>
    - src/app/trash/actions.test.ts lines 1-60 — the WHOLE mock scaffold and its header note: the point is to swap the SESSION per test (absent / member-owner / member-non-owner / admin), which the `vi.mock("@/lib/api/auth")` auto-approve bypass used by the `/api/v1` route tests cannot do. Note its rule: "THE ASSERTION THAT MATTERS MOST IS AN ABSENCE"
    - src/app/deals/actions.ts — the three actions you just wrote, plus `sendDealAssignedEmail`'s import and call site so you know what to mock and assert against
    - src/components/custom-fields/__tests__/source-scan.ts — `readStrippedSource`
    - src/lib/audit/no-mutation-coupling.test.ts lines 196-205 — `sliceDeclaration` and the WR-13 `indexOf`-anchor discipline
    - .planning/phases/38-bulk-operations/38-PATTERNS.md § 16 — the verbatim scaffold including the `runWithActor` recording mock
  </read_first>
  <action>
    Create `src/app/deals/bulk-actions.test.ts` by copying `src/app/trash/actions.test.ts`'s scaffold.
    Carry over its header discipline, and add one deals-specific note: this is the ONE entity in the
    phase with an admin bypass, so this file asserts BOTH directions of the asymmetry, and the sibling
    suites for organizations, people and activities assert the opposite.

    Mocks:
      - `vi.mock("@/auth", () => ({ auth: vi.fn() }))` — bare; every test drives `mockResolvedValue`.
      - `vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))`.
      - `vi.mock("@/lib/bulk/dispatch", () => ({ deleteRecordByType: vi.fn(), updateRecordOwnerByType: vi.fn() }))`.
      - `vi.mock("@/lib/export/formatters", () => ({ fetchFilteredData: vi.fn() }))`.
      - `vi.mock("@/lib/audit/actor-context", () => ({ runWithActor: vi.fn((_actor: unknown, fn: () => unknown) => fn()) }))`
        — records its actor and still invokes the callback.
      - Mock whatever module exports `sendDealAssignedEmail` with a `vi.fn()`, resolving the real module
        path by reading the import in `src/app/deals/actions.ts`. This spy exists so the no-email
        guarantee is a runtime assertion and not only a source assertion.
      - `vi.mock("@/db", ...)` with a shaped `{ query: { deals: { findFirst: vi.fn() },
        users: { findFirst: vi.fn() } } }`.
      - Do NOT mock the ownership predicate.

    Describes and cases:

    A. `bulkDeleteDeals`
      1. no session -> `not_authenticated`; `runWithActor` NOT called, `deleteRecordByType` NOT called.
      2. 101 distinct ids -> `{ success: false, error: "too_many", max: 100 }`; dispatch NOT called,
         `revalidatePath` NOT called.
      3. empty ids -> `no_selection`; no dispatch.
      4. duplicate ids -> exactly one dispatch call for the repeated id.
      5. `findFirst` undefined -> `failed` contains `{ id, reason: "notFound" }`, dispatch NOT called for it.
      6. NON-ADMIN who does not own the row -> `failed` contains `{ id, reason: "notPermitted" }`,
         dispatch NOT called. Drive the session with an explicit non-admin `role`.
      7. ADMIN BYPASS PRESENT: session `role: "admin"` and the row owned by someone else -> the id lands
         in `succeeded` and `deleteRecordByType` WAS called with that id. This is the assertion that
         fails if someone "unifies" the predicate by dropping the admin clause, and it is the mirror
         image of case A.7 in the organizations, people and activities suites.
      8. dispatch returns `{ success: false }` -> `failed` contains `{ id, reason: "unknown" }` AND the
         loop continued: with three ids where the middle one fails, all three were read and the third
         still dispatched.
      9. 12 ids, non-admin session, 9 owned / 3 not -> `succeeded.length === 9`, `failed.length === 3`.
     10. the SAME 12 ids with an ADMIN session -> `succeeded.length === 12`, `failed.length === 0`.
         Assert this alongside case 9 in the same describe, so the asymmetry is visible in one place.
     11. `expect(runWithActor).toHaveBeenCalledTimes(1)` and the recorded actor deep-equals
         `{ kind: "user", userId: <session user id> }`.
     12. `expect(revalidatePath).toHaveBeenCalledTimes(1)` for a partially successful call and
         `toHaveBeenCalledTimes(0)` when zero ids succeeded.
     13. NO EMAIL: `expect(sendDealAssignedEmail).not.toHaveBeenCalled()` after a fully successful
         12-id delete.

    B. `bulkReassignDealOwner`
      1. no session -> `not_authenticated`; no `runWithActor`, no dispatch.
      2. target user missing -> `invalid_owner`; `updateRecordOwnerByType` NOT called AND `runWithActor`
         NOT called.
      3. target user exists but `status: "pending_verification"` -> `invalid_owner`, no dispatch. Drive
         this via the mocked `users.findFirst` returning `undefined` for the two-predicate query, and
         assert the recorded query argument carried BOTH predicates if the mock records it.
      4. target user exists but `deletedAt` set -> `invalid_owner`, no dispatch.
      5. NON-ADMIN, row not owned -> `notPermitted`, no dispatch for that id.
      6. ADMIN, row not owned -> succeeds, dispatch called.
      7. 12 ids, non-admin, 9 owned / 3 not -> 9 succeeded / 3 `notPermitted`.
      8. `runWithActor` once; `revalidatePath` once after the loop.
      9. `updateRecordOwnerByType` receives `("deal", id, ownerId, sessionUserId)` in that exact
         argument order — the order types cannot catch, since all four are strings.
     10. NO EMAIL: `expect(sendDealAssignedEmail).not.toHaveBeenCalled()` after a fully successful
         12-id reassign. This is the runtime half of D-13.

    C. `exportSelectedDeals`
      1. no session -> failure, `fetchFilteredData` NOT called.
      2. empty ids -> failure, not called.
      3. 101 ids -> failure, not called.
      4. valid call -> the object passed to `fetchFilteredData` deep-equals
         `{ entityType: "deal", format: "csv", includeCustomFields: true, filters: { ids } }`.
      5. filename shape and count-from-result, using a mock whose `count` differs from the input length.
      6. `fetchFilteredData` failure passes through unchanged.

    D. SOURCE GATE (comment-stripped)
      Read `readStrippedSource("src/app/deals/actions.ts")` and slice out each of the three bulk
      declarations (from `export async function <name>` to the next top-level `\nexport `), asserting the
      anchor index is `> -1` with a named message BEFORE slicing — WR-13: `indexOf(x, -1)` silently
      behaves as `indexOf(x, 0)`, so a helper handed a missing anchor widens to the enclosing block and
      stops detecting. Then assert:
        - Positive markers first: each slice non-empty; both write slices contain `runWithActor`; both
          write slices contain `session.user.role !== "admin"` EXACTLY ONCE. The positive assertion on
          the admin clause is what makes this gate deals-specific rather than a copy of the others.
        - The export slice contains none of `ExportFilters`, `ExportOptions`, `ExportFormat`,
          `pipedrive`, `getExportData`, `role`, `stage`.
        - Neither write slice contains `Promise.all`, `db.transaction`, `updateDealMutation`,
          `dealAssignees`, `assigneeIds`, `newAssigneeUserIds`, `dealTitle`, `sendDealAssignedEmail`,
          or `auditLog`.
        - Each write slice contains exactly one `runWithActor` and exactly one `revalidatePath`.
      Header-comment the gate as comment-blind, naming Phase 37's nine-collision lesson — and note that
      this file is unusually exposed to it, because its most important rule is a NEGATIVE about a
      function name (`updateDealMutation` must never be called with `{ ownerId }`) that will inevitably
      be explained in a comment right beside the call site.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/deals/bulk-actions.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/app/deals/bulk-actions.test.ts` passes with at least 32 tests
    - `grep -c 'not.toHaveBeenCalled' src/app/deals/bulk-actions.test.ts` is at least 12
    - The admin-bypass-PRESENT tests exist and pass (cases A.7, A.10, B.6); record their names in the SUMMARY
    - Two `sendDealAssignedEmail` absence assertions exist (cases A.13 and B.10)
    - `grep -c 'readStrippedSource' src/app/deals/bulk-actions.test.ts` is at least 1 and `grep -c 'readFileSync' ...` is 0
    - The declaration-slicing helper asserts its anchor index is `> -1` with a named message before slicing
    - Negative proof 1: temporarily REMOVE `&& session.user.role !== "admin"` from `bulkDeleteDeals`' predicate, run the suite, observe cases A.7 and A.10 FAIL, revert. Record the messages
    - Negative proof 2: temporarily change the reassign dispatch to `updateDealMutation(id, { ownerId }, session.user.id)`, run the suite, observe the source gate FAIL naming `updateDealMutation`, revert. Record the message
    - Negative proof 3: temporarily move `revalidatePath` inside the loop, run the suite, observe the call-count assertion FAIL, revert. Record the message
    - `npm run typecheck` reports 0 errors
  </acceptance_criteria>
  <done>A session-swapping suite proves both directions of deals' admin bypass, that every refusal precedes any dispatch call, that no email is sent on either bulk path, that the loop continues past failures, and that the export signature admits nothing but ids — with three negative directions demonstrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> server action | A server action is a POST endpoint; `ids: string[]` is an annotation, not a control |
| member vs admin | Deals is the one entity where role changes the outcome, so the role read must come from the session and nothing else |
| server action -> mutation layer | The mutation checks existence only, so authorization must be complete before the dispatch call |
| deal row -> deal_assignees join table | A partial write through `updateDealMutation` destroys join rows that never appear in the audited diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure / Elevation of Privilege | `exportSelectedDeals` | mitigate | Signature is `(ids: string[])` and nothing else; `ExportOptions` is constructed server-side with literal `entityType: "deal"`, `format: "csv"`, `includeCustomFields: true` and NO `stage` key. A non-admin passing `{}` to a filter-accepting action would receive all 25,195 live deals. Gated by a deep-equal unit assertion and a comment-stripped source gate |
| T-38-03 | Elevation of Privilege (privilege escalation by unifying predicates) | the admin clause | mitigate | Deals' predicate is copied verbatim INCLUDING `&& session.user.role !== "admin"`, and the source gate asserts the clause is present exactly once in each write slice. Both runtime directions are asserted: admin succeeds on a non-owned deal, non-admin does not. Removing the clause is proven red |
| T-38-02 | Tampering / Elevation of Privilege | the per-record loop | mitigate | Ownership is checked INSIDE the loop before each dispatch call. Never a batch `UPDATE ... WHERE id IN (...)`, which would authorize once for many rows. `role` is read from `session`, never from a parameter |
| T-38-05 | Tampering (silent data destruction) | `deal_assignees` | mitigate | The reassign routes through `updateRecordOwnerByType` to `updateDealOwnerMutation`, which never touches `dealAssignees`. The source gate asserts the write slices mention neither `updateDealMutation` nor `dealAssignees` nor `assigneeIds`, and the negative proof demonstrates the gate catches a swap to `updateDealMutation` |
| T-38-14 | Repudiation (unwanted notification) | `sendDealAssignedEmail` | mitigate | Two runtime absence assertions plus a source assertion. A per-record notification would emit up to 100 emails from one click; a digest is deferred, not built. No suppression flag is needed because `updateDealOwnerMutation` returns no `newAssigneeUserIds` |
| T-38-04 | Repudiation | actor attribution | mitigate | `runWithActor({ kind: "user", userId: session.user.id }, ...)` built only from the session, opened AFTER the session check, exactly once around the whole loop |
| T-38-06 | Elevation of Privilege (ownership transfer to an inactive principal) | `bulkReassignDealOwner` | mitigate | The target is validated ONCE before the loop against `isNull(deletedAt)` AND `eq(status, "approved")`. `deals/page.tsx:159-163` filters on `deletedAt` alone and is deliberately NOT copied and NOT modified |
| T-38-07 | Information Disclosure | per-record failure reporting | mitigate | Failures carry only a `BulkFailureReason` code; a dispatch failure collapses to `"unknown"` |
| T-38-08 | Denial of Service (outbound fan-out) | 100 webhook deliveries and 100 trigger evaluations per click | accept | Bounded by `BULK_MAX_IDS`. `deal.deleted` is a real workflow trigger type, so deals is where the fan-out is most live: 1 active webhook, 0 active workflows today. N deletes genuinely are N events; the loop stays sequential |
| T-38-34 | Tampering (aborting transaction hides per-record failures) | loop control flow | mitigate | No `db.transaction`, no `Promise.all`, asserted absent by the source gate; a unit test proves the loop continues past a mid-list failure |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (38-RESEARCH § Package Legitimacy Audit: empty input set) |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/app/deals/bulk-actions.test.ts` — green
- `./node_modules/.bin/vitest run src/lib/audit/no-mutation-coupling.test.ts` — green (no audit write added)
- `npm run typecheck` — 0 errors, no new `@ts-expect-error`; `npm run lint` — 0 errors
- `git status --porcelain src/app/deals/page.tsx` — empty
- All three negative proofs demonstrated and reverted, messages recorded
</verification>

<success_criteria>
- `bulkDeleteDeals`, `bulkReassignDealOwner` and `exportSelectedDeals` exist with the documented signatures
- Deals' predicate is copied verbatim INCLUDING the admin bypass, with both runtime directions asserted
- Every refusal (unauthenticated, over-cap, empty, invalid owner) happens before any dispatch call
- Neither bulk write action references `sendDealAssignedEmail`, `newAssigneeUserIds`, `dealTitle`, `assigneeIds`, `updateDealMutation` or `dealAssignees`
- The loop is sequential, best-effort, and continues past failures; one `runWithActor`; one `revalidatePath` only when something succeeded
- The reassign target is validated once against approved-and-not-deleted; `deals/page.tsx` is untouched
- The export action admits only ids, passes no `stage` filter, and returns `deals-selected-<count>-<date>.csv`
- `createDeal`, `updateDeal`, `deleteDeal`, `updateDealStage` and `reorderDeals` are unmodified
</success_criteria>

<output>
Create `.planning/phases/38-bulk-operations/38-13-SUMMARY.md` when done
</output>
