---
phase: 38-bulk-operations
plan: 11
type: execute
wave: 3
depends_on: [38-04, 38-06]
files_modified:
  - src/app/organizations/actions.ts
  - src/app/organizations/bulk-actions.test.ts
autonomous: true
requirements: [BULK-02, BULK-03, BULK-04]

must_haves:
  truths:
    - "An unauthenticated bulk call is refused before any actor scope opens and before any mutation runs"
    - "A call carrying more than 100 ids is refused with the cap and the count, and no mutation runs"
    - "An organization the caller does not own is refused per record, and the mutation is never called for it"
    - "Nine successes and three failures produce nine succeeded ids and three named failures, and the loop does not stop at the first failure"
    - "The actor scope opens once for the whole loop and revalidatePath runs once after it"
    - "The scoped export accepts a list of ids and nothing else"
  artifacts:
    - path: "src/app/organizations/actions.ts"
      provides: "bulkDeleteOrganizations, bulkReassignOrganizationOwner, exportSelectedOrganizations"
      contains: "bulkDeleteOrganizations"
    - path: "src/app/organizations/bulk-actions.test.ts"
      provides: "session-swapping unit coverage plus the export-signature source gate"
      contains: "not.toHaveBeenCalled"
  key_links:
    - from: "src/app/organizations/actions.ts"
      to: "src/lib/bulk/dispatch.ts"
      via: "deleteRecordByType(\"organization\", id, userId) and updateRecordOwnerByType(\"organization\", id, ownerId, userId) inside the loop"
      pattern: "(deleteRecordByType|updateRecordOwnerByType)\\(\\s*\"organization\""
    - from: "src/app/organizations/actions.ts"
      to: "src/lib/audit/actor-context.ts"
      via: "one runWithActor wrapping the whole loop, opened after the session check"
      pattern: "runWithActor"
---

<objective>
Add the three Organizations bulk server actions — delete, reassign owner, scoped CSV export — and the
session-swapping unit suite that proves their refusals happen BEFORE any write.

Purpose: this is where the phase's access control lives. STATE.md's standing decision is that
ownership checks remain in server actions while mutations check entity existence only, and
organizations' predicate is `organization.ownerId !== session.user.id` with NO admin bypass — deals
carries one, the other three do not. Unifying the four predicates would either grant organizations,
people and activities an escalation they do not have today or remove deals' bypass. The predicate is
therefore copied verbatim from `deleteOrganization` in this same file, adjacent to the single-record
check it must match.
Output: three server actions and one test file whose most important assertions are ABSENCES.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-bulk-operations/38-PATTERNS.md

<interfaces>
Emit exactly these three exports — plan 38-15 wires them into the bar:

  export async function bulkDeleteOrganizations(ids: string[]): Promise<BulkWriteResult>
  export async function bulkReassignOrganizationOwner(ids: string[], ownerId: string): Promise<BulkWriteResult>
  export async function exportSelectedOrganizations(ids: string[]): Promise<ExportResult>

From src/lib/bulk/types.ts (plan 38-06):
  BulkFailureReason = "notFound" | "notPermitted" | "alreadyDeleted" | "unknown"
  BulkFailure = { id: string; reason: BulkFailureReason }
  BulkErrorCode = "not_authenticated" | "too_many" | "invalid_owner" | "no_selection"
  BulkWriteResult =
    | { success: true; succeeded: string[]; failed: BulkFailure[] }
    | { success: false; error: BulkErrorCode; max?: number }

From src/lib/bulk/limits.ts: BULK_MAX_IDS = 100
From src/lib/bulk/dispatch.ts:
  deleteRecordByType(entityType, id, userId)
  updateRecordOwnerByType(entityType, id, ownerId, userId)
From src/lib/export/types.ts: ExportResult = { success: true; data; filename; count } | { success: false; error }
From src/lib/export/formatters.ts: fetchFilteredData(options: ExportOptions): Promise<ExportResult>
From src/lib/audit/actor-context.ts: runWithActor(actor, fn) — returns `T | Promise<T>`, so an async
callback needs `await runWithActor(...)`; every existing single-record action does exactly this.

ORGANIZATIONS' AUTHORIZATION PREDICATE, verbatim (src/app/organizations/actions.ts:83 and :130):
  if (organization.ownerId !== session.user.id) {
Organizations have NO admin bypass. Only deals carries `&& session.user.role !== "admin"`
(src/app/deals/actions.ts:83, 155, 191, 228). Copy this file's own predicate; do not unify.

`alreadyDeleted` is deliberately UNREACHABLE on the delete path: the per-record read already carries
`isNull(organizations.deletedAt)`, so a miss maps to `notFound`. Distinguishing the two would need a
second read without the predicate (38-RESEARCH A6). Keep the key in the union — mapping a permission
failure onto "Already in Trash" would be strictly worse than collapsing a soft-deleted row into
"No longer exists".

Fan-out, stated because nobody has: a 100-record bulk delete enqueues 100 webhook deliveries and runs
100 workflow-trigger evaluations plus 100 fire-and-forget `audit_log` inserts, against a `postgres.js`
client created with no `max` (library default 10). Live blast radius: 1 active webhook, 3 workflows of
which 0 active. This is CORRECT semantics — a bulk delete genuinely is N deletes — but it is why the
loop stays sequential and why a 100-record delete takes seconds, not milliseconds.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bulkDeleteOrganizations and bulkReassignOrganizationOwner</name>
  <files>src/app/organizations/actions.ts</files>
  <read_first>
    - src/app/organizations/actions.ts — read the WHOLE file. You need `deleteOrganization` (~:108-145) as the skeleton, its `auth()` check, its verbatim ownership predicate at :130, the `runWithActor` placement comment at ~:34-36, and its `revalidatePath("/organizations")` call
    - src/lib/bulk/limits.ts, src/lib/bulk/types.ts, src/lib/bulk/dispatch.ts — the contract landed by plan 38-06
    - src/lib/audit/actor-context.ts — `runWithActor`'s real signature and its `AsyncLocalStorage` semantics
    - src/app/api/v1/organizations/batch/route.ts lines 1-75 — the sequential per-record loop shape to REUSE and the failure-swallowing to REJECT: it pushes only successes and sets `meta.total = created.length`, so it can never report a failure
    - src/app/trash/actions.ts lines 100-120 — `parseRecordId`, which narrows an `id: string` argument at RUNTIME on the stated ground that a server action is a POST endpoint and the type annotation is not a control
    - .planning/phases/38-bulk-operations/38-PATTERNS.md § 5 — the verbatim skeleton, the four predicates table, and the anti-analog notes
  </read_first>
  <behavior>
    - No session / no `session.user.id` -> `{ success: false, error: "not_authenticated" }`. `runWithActor`
      was NOT called and no dispatch function was called
    - `ids` with more than `BULK_MAX_IDS` distinct entries -> `{ success: false, error: "too_many",
      max: BULK_MAX_IDS }`. No dispatch call, no `runWithActor`, no `revalidatePath`
    - Empty `ids` -> `{ success: false, error: "no_selection" }`. No dispatch call
    - Duplicate ids are collapsed before the loop, so 3 copies of one id produce one dispatch call
    - A row that does not exist (or is soft-deleted) -> that id appears in `failed` with reason
      `"notFound"`, and `deleteRecordByType` was not called for it
    - A row owned by someone else -> `failed` with reason `"notPermitted"`, and `deleteRecordByType`
      was NOT called for it. This assertion is an ABSENCE and it is the most important one in the suite
    - A dispatch call returning `{ success: false }` -> `failed` with reason `"unknown"`, and the loop
      CONTINUES to the remaining ids
    - 12 ids where 9 are owned and 3 are not -> `succeeded.length === 9`, `failed.length === 3`, all
      three failures reason `"notPermitted"`
    - `runWithActor` is called exactly ONCE per action invocation, with `{ kind: "user", userId:
      session.user.id }` built from the session and from nothing else
    - `revalidatePath` is called exactly ONCE, after the loop, and only when at least one id succeeded
    - Reassign only: an `ownerId` that is not an approved, non-deleted user -> `{ success: false,
      error: "invalid_owner" }` with NO dispatch call at all. The target is validated ONCE before the
      loop, never per record
  </behavior>
  <action>
    Write the failing tests first (Task 3 creates the file; write the delete and reassign describes
    there before implementing here). Then add the two actions to `src/app/organizations/actions.ts`,
    placed after `deleteOrganization` so each bulk predicate sits adjacent to the single-record
    predicate it mirrors.

    `bulkDeleteOrganizations(ids: string[])`, in this exact order:
      1. `const session = await auth()`; if `!session?.user?.id` return
         `{ success: false, error: "not_authenticated" }`. Nothing else happens first — the actor scope
         must open AFTER the session check so an unauthenticated call establishes no actor at all
         (T-36-02).
      2. Runtime-narrow the argument: reject anything that is not an array of non-empty strings.
         A server action is a POST endpoint and the `string[]` annotation is not a control — copy
         `parseRecordId`'s reasoning from `src/app/trash/actions.ts`. Map a malformed argument to
         `{ success: false, error: "no_selection" }`.
      3. `const uniqueIds = Array.from(new Set(ids))`. If `uniqueIds.length === 0` return
         `{ success: false, error: "no_selection" }`.
      4. If `uniqueIds.length > BULK_MAX_IDS` return `{ success: false, error: "too_many", max: BULK_MAX_IDS }`.
      5. `const outcome = await runWithActor({ kind: "user", userId: session.user.id }, async () => { ... })`
         — ONE scope around the WHOLE loop, not per record. `AsyncLocalStorage` propagates across every
         `await` inside, and `crmBus.emit` is synchronous so the audit subscriber reads the actor inline
         on the mutation's own stack.
         Inside the callback, declare `succeeded: string[]` and `failed: BulkFailure[]`, then
         `for (const id of uniqueIds)`:
           - `const row = await db.query.organizations.findFirst({ where: and(eq(organizations.id, id),
             isNull(organizations.deletedAt)) })`. If falsy: `failed.push({ id, reason: "notFound" })`
             and `continue`.
           - Copy this file's predicate VERBATIM: `if (row.ownerId !== session.user.id)` then
             `failed.push({ id, reason: "notPermitted" })` and `continue`. Do NOT add
             `&& session.user.role !== "admin"` — organizations have no admin bypass, and adding one
             would be a privilege escalation shipped as a bulk feature.
           - `const result = await deleteRecordByType("organization", id, session.user.id)`.
             `result.success ? succeeded.push(id) : failed.push({ id, reason: "unknown" })`.
           - Never `break` and never `throw` out of the loop: best-effort per record is what makes SC-3
             satisfiable, because a single aborting transaction structurally cannot name a per-record
             failure.
         Return `{ succeeded, failed }`.
      6. `if (outcome.succeeded.length > 0) revalidatePath("/organizations")` — ONCE, after the loop.
         Never inside it: 100 records would otherwise mean 100 revalidations for one user action.
      7. `return { success: true, ...outcome }`.

    `bulkReassignOrganizationOwner(ids: string[], ownerId: string)` is the same seven steps with two
    additions:
      - Between steps 4 and 5, validate the TARGET USER exactly once:
        `db.query.users.findFirst({ where: and(eq(users.id, ownerId), isNull(users.deletedAt),
        eq(users.status, "approved")) })`. If falsy return `{ success: false, error: "invalid_owner" }`
        before opening any actor scope. Both predicates are required: handing 100 records to a
        `rejected` or `pending_verification` user is a data defect that NO per-record failure would
        report, because the write succeeds. Do NOT copy `deals/page.tsx:159-163`, which filters on
        `deletedAt` alone.
      - In the loop, call `updateRecordOwnerByType("organization", id, ownerId, session.user.id)`
        instead of the delete dispatch. The per-record ownership predicate is IDENTICAL to the delete
        one (organizations' update predicate at :83 is the same string as its delete predicate at :130
        — verify that for yourself before copying).

    Import `BULK_MAX_IDS` from `@/lib/bulk/limits`, the types from `@/lib/bulk/types`, and the two
    dispatch functions from `@/lib/bulk/dispatch`. Do NOT import any `update{Entity}Mutation` here and
    do NOT call `updateOrganizationMutation` with `{ ownerId }` — `ownerId` is absent from
    `organizationSchema` and Zod strips unknown keys, so that call writes only `updatedAt`, emits an
    empty diff, and the audit subscriber drops the row: a silent success no-op that also fails SC-3 and
    SC-5. Do NOT use `Promise.all` over the ids; the loop is sequential by decision and because the
    connection pool has no configured `max`. Do NOT wrap the loop in a transaction. Do NOT send any
    email. Do NOT write an `audit_log` row here — the existing `crmBus` subscriber does it, and a
    bulk-specific audit row would fail `no-mutation-coupling.test.ts`.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/organizations/bulk-actions.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function bulkDeleteOrganizations\|export async function bulkReassignOrganizationOwner' src/app/organizations/actions.ts` is 2
    - `grep -c 'runWithActor' src/app/organizations/actions.ts` increased by exactly 2 from its pre-task count (record both counts in the SUMMARY)
    - Source assertion on the `bulkDeleteOrganizations` declaration slice: it contains exactly one `runWithActor`, exactly one `revalidatePath`, and the `revalidatePath` appears AFTER the closing of the `runWithActor` call
    - Source assertion on both bulk declaration slices: zero occurrences of `Promise.all`, `db.transaction`, `session.user.role`, `updateOrganizationMutation`, `sendDeal`, `db.insert(auditLog` and `auditLog`
    - `grep -c 'eq(users.status, "approved")' src/app/organizations/actions.ts` is 1, and it appears inside the reassign action before its `runWithActor`
    - The verbatim predicate `organization.ownerId !== session.user.id` (or the same expression against the loop's row variable) appears in both bulk actions; record the exact two lines in the SUMMARY
    - `grep -c 'BULK_MAX_IDS' src/app/organizations/actions.ts` is at least 2
    - `git diff src/app/organizations/actions.ts` shows NO modification to `createOrganization`, `updateOrganization` or `deleteOrganization`
    - `./node_modules/.bin/vitest run src/app/organizations/bulk-actions.test.ts` passes
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors, no `@ts-expect-error` added
  </acceptance_criteria>
  <done>Both bulk write actions refuse unauthenticated and over-cap calls before any write, validate the reassign target once against approved-and-not-deleted, check organizations' own ownership predicate per record with no admin bypass, continue past failures, open one actor scope and call `revalidatePath` once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: exportSelectedOrganizations</name>
  <files>src/app/organizations/actions.ts</files>
  <read_first>
    - src/app/organizations/actions.ts — the file you just extended
    - src/lib/export/formatters.ts — `fetchFilteredData`'s signature (~:354) and its filename construction (~:417-426), including the `entityPlural` mapping that is the English-slug source
    - src/lib/export/types.ts — `ExportOptions`, `ExportFilters` (now carrying `ids`), `ExportResult`
    - src/app/admin/export/actions.ts — read `getExportData` ONLY to understand why it cannot be reused: it is gated on `role !== "admin"` and takes a full `ExportOptions`
    - .planning/phases/38-bulk-operations/38-RESEARCH.md § Pitfall 4 and § Security Domain — the admin-gate bypass this signature exists to prevent
  </read_first>
  <behavior>
    - No session -> `{ success: false, error: <a non-empty string> }`, and `fetchFilteredData` was NOT called
    - Empty `ids` -> failure, and `fetchFilteredData` was NOT called
    - More than `BULK_MAX_IDS` ids -> failure, and `fetchFilteredData` was NOT called
    - A valid call passes `fetchFilteredData` exactly `{ entityType: "organization", format: "csv",
      includeCustomFields: true, filters: { ids: <the deduped ids> } }` — every field constructed
      server-side, none taken from a parameter
    - On success the returned `filename` is `organizations-selected-<count>-<YYYY-MM-DD>.csv`, where
      `<count>` is `result.count` from `fetchFilteredData`, so the name and the row count cannot disagree
    - The returned `data` and `count` pass through from `fetchFilteredData` unchanged
    - The function's parameter list is exactly one `ids: string[]` — no second parameter of any kind
  </behavior>
  <action>
    Add `export async function exportSelectedOrganizations(ids: string[]): Promise<ExportResult>` to
    `src/app/organizations/actions.ts`.

    THE SIGNATURE IS `(ids: string[])` AND NOTHING ELSE. This is not a style preference: the only
    existing export action, `getExportData`, is admin-gated, and a non-admin action that accepted an
    `ExportFilters` or `ExportOptions` object and received `{}` would return all 46,054 organizations —
    an admin-gate bypass. Do not add a `format`, `filters`, `entityType`, `includeCustomFields` or
    options parameter, and do not accept an object argument.

    Body:
      1. `const session = await auth()`; if `!session?.user?.id` return
         `{ success: false, error: "Not authenticated" }`. Any authenticated user may export their
         own selection; there is no admin gate on the SCOPED export.
      2. Runtime-narrow `ids` to an array of non-empty strings, then dedupe with `new Set`.
      3. If empty return `{ success: false, error: "No records selected" }`.
      4. If longer than `BULK_MAX_IDS` return `{ success: false, error: "Too many records" }`.
      5. `const result = await fetchFilteredData({ entityType: "organization", format: "csv",
         includeCustomFields: true, filters: { ids: uniqueIds } })`. Every field is a literal
         constructed here; nothing comes from the caller except the ids.
      6. `if (!result.success) return result`.
      7. Compute `const date = new Date().toISOString().split("T")[0]` and return
         `{ ...result, filename: \`organizations-selected-${result.count}-${date}.csv\` }`.
         The slug `organizations` is the English plural from `formatters.ts`'s own `entityPlural`
         mapping and is NEVER translated — a locale-dependent filename on disk is unsupportable, and
         es-ES / pt-BR names carry diacritics that survive round trips poorly. The count comes from
         `result.count`, not from `uniqueIds.length`, so the name cannot disagree with the file.

    Do NOT call `getExportData`. Do NOT add a `/api/export` route. Do NOT offer the Pipedrive variant
    (`src/lib/export/pipedrive.ts`) — CSV only. Do NOT modify `fetchFilteredData`'s own filename block;
    rewriting `result.filename` here keeps a widely shared function untouched while still satisfying
    "generated server-side inside the ExportResult".
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/organizations/bulk-actions.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export async function exportSelectedOrganizations' src/app/organizations/actions.ts` is 1
    - Source assertion on the `exportSelectedOrganizations` declaration slice: it contains zero occurrences of `ExportFilters`, `ExportOptions`, `ExportFormat`, `pipedrive`, `getExportData`, and `role`
    - Source assertion: the declaration's parameter list matches a single `ids` parameter — record the exact signature line in the SUMMARY
    - `grep -c 'organizations-selected-' src/app/organizations/actions.ts` is 1
    - A unit test asserts the exact object passed to `fetchFilteredData` deep-equals `{ entityType: "organization", format: "csv", includeCustomFields: true, filters: { ids: [...] } }`
    - A unit test asserts the returned filename matches `/^organizations-selected-\d+-\d{4}-\d{2}-\d{2}\.csv$/` and that its count segment equals the mocked `result.count`, not the input id count (use a mock where the two differ)
    - `npm run typecheck` 0 errors, `npm run lint` 0 errors
  </acceptance_criteria>
  <done>`exportSelectedOrganizations` takes ids and nothing else, constructs the export options server-side, and returns a server-generated untranslated filename whose count comes from the fetch result.</done>
</task>

<task type="auto">
  <name>Task 3: bulk-actions.test.ts — session-swapping coverage and the signature gate</name>
  <files>src/app/organizations/bulk-actions.test.ts</files>
  <read_first>
    - src/app/trash/actions.test.ts lines 1-60 — the WHOLE mock scaffold, and its header note explaining that the point is to swap the SESSION per test (absent / member-owner / member-non-owner / admin), which the `vi.mock("@/lib/api/auth")` auto-approve bypass used by the `/api/v1` route tests cannot do. Also note its stated rule: "THE ASSERTION THAT MATTERS MOST IS AN ABSENCE"
    - src/app/trash/actions.test.ts lines 20-30 — the deliberate NON-mock: the authorization predicate itself is never stubbed, because it lives inline in the action and it is the subject of the file
    - src/components/custom-fields/__tests__/source-scan.ts — `readStrippedSource`, for the export-signature gate
    - src/app/organizations/actions.ts — the three actions you just wrote
    - .planning/phases/38-bulk-operations/38-PATTERNS.md § 16 — the verbatim scaffold including the `runWithActor` recording mock
  </read_first>
  <action>
    Create `src/app/organizations/bulk-actions.test.ts` by copying `src/app/trash/actions.test.ts`'s
    scaffold. Carry over its header discipline: state that the file exists to swap the SESSION per test
    and that the most important assertions are ABSENCES, because a refusal returned AFTER the write was
    issued would satisfy any test that only inspects the return value.

    Mocks:
      - `vi.mock("@/auth", () => ({ auth: vi.fn() }))` — a bare `vi.fn()`; every test drives
        `mockResolvedValue` itself.
      - `vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }))`.
      - `vi.mock("@/lib/bulk/dispatch", () => ({ deleteRecordByType: vi.fn(), updateRecordOwnerByType: vi.fn() }))`.
      - `vi.mock("@/lib/export/formatters", () => ({ fetchFilteredData: vi.fn() }))`.
      - `vi.mock("@/lib/audit/actor-context", () => ({ runWithActor: vi.fn((_actor: unknown, fn: () => unknown) => fn()) }))`
        — a spy that RECORDS its actor and still invokes the callback, so both the wrapping and the
        identity inside it are assertable.
      - `vi.mock("@/db", ...)` with a shaped `{ query: { organizations: { findFirst: vi.fn() },
        users: { findFirst: vi.fn() } } }`.
      - Do NOT mock the ownership predicate. It lives inline in the action and it is the subject of this file.

    Write these describes and cases:

    A. `bulkDeleteOrganizations`
      1. no session -> `{ success: false, error: "not_authenticated" }`; assert
         `runWithActor` NOT called and `deleteRecordByType` NOT called.
      2. 101 distinct ids -> `{ success: false, error: "too_many", max: 100 }`; assert
         `deleteRecordByType` NOT called and `revalidatePath` NOT called.
      3. empty ids -> `{ success: false, error: "no_selection" }`; assert no dispatch call.
      4. duplicate ids -> exactly one `deleteRecordByType` call for the repeated id.
      5. `findFirst` returns undefined -> `failed` contains `{ id, reason: "notFound" }` and
         `deleteRecordByType` NOT called for it.
      6. `findFirst` returns a row whose `ownerId` differs from the session user -> `failed` contains
         `{ id, reason: "notPermitted" }` and `deleteRecordByType` NOT called for it. THIS IS THE
         CENTRAL ABSENCE.
      7. AUTHORIZATION ASYMMETRY: session user is an ADMIN (`role: "admin"`) and the row is owned by
         someone else -> still `notPermitted`, and `deleteRecordByType` NOT called. Organizations have
         NO admin bypass; this test is what would fail if someone unified the four predicates.
      8. dispatch returns `{ success: false }` -> `failed` contains `{ id, reason: "unknown" }` AND the
         loop continued: with three ids where the middle one fails, all three were read and the third
         still dispatched.
      9. 12 ids, 9 owned / 3 not -> `succeeded.length === 9`, `failed.length === 3`, every failure
         reason `"notPermitted"`.
     10. `expect(runWithActor).toHaveBeenCalledTimes(1)` for a 12-id call, and the recorded actor
         deep-equals `{ kind: "user", userId: <the session user id> }`.
     11. `expect(revalidatePath).toHaveBeenCalledTimes(1)` for a partially successful call, and
         `toHaveBeenCalledTimes(0)` when zero ids succeeded.

    B. `bulkReassignOrganizationOwner`
      1. no session -> `not_authenticated`; no `runWithActor`, no dispatch.
      2. target user missing -> `{ success: false, error: "invalid_owner" }`; assert
         `updateRecordOwnerByType` NOT called AND `runWithActor` NOT called.
      3. target user exists but `status: "pending_verification"` -> `invalid_owner`, no dispatch.
         Drive this by having the mocked `users.findFirst` return `undefined` for the two-predicate
         query and assert the query itself carried BOTH predicates if the mock records its argument.
      4. target user exists but `deletedAt` set -> `invalid_owner`, no dispatch.
      5. target valid, row not owned by the session user -> `notPermitted`, no dispatch for that id.
      6. target valid, 9 owned / 3 not -> 9 succeeded / 3 `notPermitted`.
      7. `runWithActor` called once; `revalidatePath` called once after the loop.
      8. `updateRecordOwnerByType` receives `("organization", id, ownerId, sessionUserId)` in that
         exact argument order — the order types cannot catch, since all four are strings.

    C. `exportSelectedOrganizations`
      1. no session -> failure, `fetchFilteredData` NOT called.
      2. empty ids -> failure, not called.
      3. 101 ids -> failure, not called.
      4. valid call -> the object passed to `fetchFilteredData` deep-equals
         `{ entityType: "organization", format: "csv", includeCustomFields: true, filters: { ids } }`.
      5. filename shape and count-from-result, using a mock whose `count` differs from the input length.
      6. `fetchFilteredData` failure passes through unchanged.

    D. SOURCE GATE (comment-stripped)
      Read `readStrippedSource("src/app/organizations/actions.ts")`, slice out each of the three bulk
      declarations (from `export async function <name>` to the next top-level `\nexport `, asserting the
      anchor index is `> -1` with a named message BEFORE slicing — WR-13: `indexOf(x, -1)` silently
      behaves as `indexOf(x, 0)` and a helper handed a missing anchor widens to the enclosing block and
      stops detecting), then assert:
        - Positive markers first: each slice is non-empty and the two write slices contain `runWithActor`.
        - The export slice contains none of `ExportFilters`, `ExportOptions`, `ExportFormat`,
          `pipedrive`, `getExportData`, `role`.
        - Neither write slice contains `Promise.all`, `db.transaction`, `session.user.role`,
          `updateOrganizationMutation`, `auditLog`, or `sendDeal`.
        - Each write slice contains exactly one `runWithActor` and exactly one `revalidatePath`.
      Header-comment the gate as comment-blind, naming Phase 37's nine-collision lesson.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/organizations/bulk-actions.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/app/organizations/bulk-actions.test.ts` passes with at least 28 tests
    - `grep -c 'not.toHaveBeenCalled' src/app/organizations/bulk-actions.test.ts` is at least 10
    - `grep -c 'toHaveBeenCalledTimes(1)' src/app/organizations/bulk-actions.test.ts` is at least 3
    - The admin-no-bypass test exists and passes (case A.7); record its name in the SUMMARY
    - `grep -c 'readStrippedSource' src/app/organizations/bulk-actions.test.ts` is at least 1 and `grep -c 'readFileSync' ...` is 0
    - The declaration-slicing helper asserts its anchor index is `> -1` with a named message before slicing
    - Negative proof: temporarily add `&& session.user.role !== "admin"` to `bulkDeleteOrganizations`' predicate, run the suite, observe case A.7 FAIL, revert. Record the message
    - Negative proof: temporarily move `revalidatePath` inside the loop, run the suite, observe the call-count assertion FAIL, revert. Record the message
    - `npm run typecheck` reports 0 errors
  </acceptance_criteria>
  <done>A session-swapping suite proves every refusal happens before any dispatch call, that organizations have no admin bypass, that the loop continues past failures, that the actor scope and revalidation each happen exactly once, and that the export action's signature admits nothing but ids — with two negative directions demonstrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> server action | A server action is a POST endpoint; `ids: string[]` is an annotation, not a control |
| server action -> mutation layer | The mutation checks existence only, so authorization must be complete before the dispatch call |
| non-admin caller -> admin-gated full export | The scoped export must not be able to express "no filter" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure / Elevation of Privilege | `exportSelectedOrganizations` | mitigate | Signature is `(ids: string[])` and nothing else; `ExportOptions` is constructed server-side with literal `entityType`, `format` and `includeCustomFields`. Empty ids return an error rather than an unfiltered fetch. A comment-stripped source gate asserts the declaration mentions no `ExportFilters`/`ExportOptions`/`ExportFormat`/`getExportData`/`role` |
| T-38-02 | Tampering / Elevation of Privilege | the per-record loop | mitigate | Ownership is checked INSIDE the loop, before each dispatch call, using organizations' own verbatim predicate. Never a batch `UPDATE ... WHERE id IN (...)`, which would authorize once for many rows. Every denial case asserts the dispatch was NOT called |
| T-38-03 | Denial of Service | unbounded id list | mitigate | `BULK_MAX_IDS = 100` enforced server-side after dedupe and before the actor scope opens. Asserted by a unit test that also proves no dispatch and no revalidation occurred |
| T-38-04 | Repudiation | actor attribution | mitigate | `runWithActor({ kind: "user", userId: session.user.id }, ...)` built only from the session, never from a parameter, opened AFTER the session check, exactly once around the whole loop. The recording mock asserts both the call count and the actor identity |
| T-38-06 | Elevation of Privilege (ownership transfer to an inactive principal) | `bulkReassignOrganizationOwner` | mitigate | The target is validated ONCE before the loop against `isNull(deletedAt)` AND `eq(status, "approved")`. Handing records to a `rejected` or unverified user would otherwise succeed silently, so no per-record failure could report it |
| T-38-07 | Information Disclosure | per-record failure reporting | mitigate | Failures carry only a `BulkFailureReason` code. No `error` string from a mutation is forwarded; a dispatch failure collapses to `"unknown"` |
| T-38-09 | Repudiation | reassign path | mitigate | Routes through `updateRecordOwnerByType`, never `updateOrganizationMutation({ ownerId })`. A source gate asserts the write slices do not mention `updateOrganizationMutation` |
| T-38-08 | Denial of Service (outbound fan-out) | 100 webhook deliveries per click | accept | Bounded by `BULK_MAX_IDS`. The delivery queue and its retry/backoff already exist and are unchanged. N deletes genuinely are N events; the loop stays sequential and the bar's disabled+spinner state is what makes the latency acceptable |
| T-38-34 | Tampering (aborting transaction hides per-record failures) | loop control flow | mitigate | No `db.transaction` and no `Promise.all`, asserted absent by the source gate. Best-effort per record is structurally required by SC-3, and a unit test proves the loop continues past a mid-list failure |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase (38-RESEARCH § Package Legitimacy Audit: empty input set) |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/app/organizations/bulk-actions.test.ts` — green
- `./node_modules/.bin/vitest run src/lib/audit/no-mutation-coupling.test.ts` — green (no audit write added)
- `npm run typecheck` — 0 errors, no new `@ts-expect-error`; `npm run lint` — 0 errors
- Both negative proofs demonstrated and reverted, messages recorded
</verification>

<success_criteria>
- `bulkDeleteOrganizations`, `bulkReassignOrganizationOwner` and `exportSelectedOrganizations` exist with the documented signatures
- Every refusal (unauthenticated, over-cap, empty, invalid owner) happens before any dispatch call, proven by absence assertions
- Organizations' predicate is copied verbatim with no admin bypass, proven by an admin-non-owner test
- The loop is sequential, best-effort, and continues past failures; one `runWithActor`; one `revalidatePath` only when something succeeded
- The reassign target is validated once against approved-and-not-deleted
- The export action admits only ids and returns `organizations-selected-<count>-<date>.csv` with the count taken from the fetch result
- `createOrganization`, `updateOrganization` and `deleteOrganization` are unmodified
</success_criteria>

<output>
Create `.planning/phases/38-bulk-operations/38-11-SUMMARY.md` when done
</output>
