---
phase: 15-multi-user-collaboration
plan: 06
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/messages/en-US.json
  - src/messages/pt-BR.json
  - src/messages/es-ES.json
autonomous: true

must_haves:
  truths:
    - "en-US.json has assignee-related keys in deals and activities namespaces"
    - "en-US.json has a team namespace with keys for the /team page"
    - "pt-BR.json has matching keys with Brazilian Portuguese translations"
    - "es-ES.json has matching keys with Spanish translations"
    - "nav namespace has a team key in all three locale files"
    - "All three locale files have the same set of new keys (no missing keys)"
  artifacts:
    - path: "src/messages/en-US.json"
      provides: "English assignee and team translation keys"
      contains: "assignees"
    - path: "src/messages/pt-BR.json"
      provides: "Portuguese assignee and team translation keys"
      contains: "assignees"
    - path: "src/messages/es-ES.json"
      provides: "Spanish assignee and team translation keys"
      contains: "assignees"
  key_links:
    - from: "src/messages/pt-BR.json"
      to: "src/messages/en-US.json"
      via: "matching key structure"
      pattern: "assignees"
    - from: "src/messages/es-ES.json"
      to: "src/messages/en-US.json"
      via: "matching key structure"
      pattern: "team"
---

<objective>
Add translation strings for all assignee-related UI text and the /team page in all three supported locales (en-US, pt-BR, es-ES).

Purpose: The app uses next-intl throughout. All new UI labels must have translations in all three locales or the build will warn/fail on missing keys.
Output: Updated en-US.json, pt-BR.json, es-ES.json with new assignee and team keys.
</objective>

<execution_context>
@/home/pedro/.claude/get-shit-done/workflows/execute-plan.md
@/home/pedro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/messages/en-US.json
@src/messages/pt-BR.json
@src/messages/es-ES.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add assignee and team translation keys to all three locale files</name>
  <files>src/messages/en-US.json, src/messages/pt-BR.json, src/messages/es-ES.json</files>
  <action>
Read all three JSON files in full first. Then merge new keys into the existing objects without removing existing keys.

---

**Keys to add to the `"deals"` namespace:**
```json
"assignees": "Assignees",
"noAssignees": "No assignees",
"selectAssignees": "Select assignees",
"assignee": "Assignee",
"allAssignees": "All assignees",
"myDeals": "My Deals"
```

**Keys to add to the `"activities"` namespace:**
```json
"assignee": "Assignee",
"assignedTo": "Assigned to",
"noAssignee": "No assignee",
"allAssignees": "All assignees"
```

**Key to add to the `"nav"` namespace:**
```json
"team": "Team"
```

**New top-level `"team"` namespace (add at end of JSON object):**
```json
"team": {
  "team": "Team",
  "teamView": "See who is assigned to what across the team",
  "assignedDeals": "Assigned Deals",
  "upcomingActivities": "Upcoming Activities",
  "noUsers": "No team members found",
  "noDeals": "No deals assigned",
  "noActivities": "No upcoming activities"
}
```

---

**pt-BR translations** — same namespaces, add these keys:

`"deals"` namespace:
```json
"assignees": "Responsáveis",
"noAssignees": "Sem responsáveis",
"selectAssignees": "Selecionar responsáveis",
"assignee": "Responsável",
"allAssignees": "Todos os responsáveis",
"myDeals": "Meus Negócios"
```

`"activities"` namespace:
```json
"assignee": "Responsável",
"assignedTo": "Atribuído a",
"noAssignee": "Sem responsável",
"allAssignees": "Todos os responsáveis"
```

`"nav"` namespace:
```json
"team": "Equipe"
```

New top-level `"team"` namespace:
```json
"team": {
  "team": "Equipe",
  "teamView": "Veja quem está atribuído ao quê na equipe",
  "assignedDeals": "Negócios Atribuídos",
  "upcomingActivities": "Próximas Atividades",
  "noUsers": "Nenhum membro da equipe encontrado",
  "noDeals": "Nenhum negócio atribuído",
  "noActivities": "Nenhuma atividade próxima"
}
```

---

**es-ES translations** — same namespaces, add these keys:

`"deals"` namespace:
```json
"assignees": "Asignados",
"noAssignees": "Sin asignados",
"selectAssignees": "Seleccionar asignados",
"assignee": "Asignado",
"allAssignees": "Todos los asignados",
"myDeals": "Mis Negocios"
```

`"activities"` namespace:
```json
"assignee": "Asignado",
"assignedTo": "Asignado a",
"noAssignee": "Sin asignado",
"allAssignees": "Todos los asignados"
```

`"nav"` namespace:
```json
"team": "Equipo"
```

New top-level `"team"` namespace:
```json
"team": {
  "team": "Equipo",
  "teamView": "Ve quién está asignado a qué en el equipo",
  "assignedDeals": "Negocios Asignados",
  "upcomingActivities": "Actividades Próximas",
  "noUsers": "No se encontraron miembros del equipo",
  "noDeals": "Sin negocios asignados",
  "noActivities": "Sin actividades próximas"
}
```

---

**Important:** Merge these into the existing JSON objects by appending new keys at the end of each namespace object. Do NOT replace or delete existing keys.
  </action>
  <verify>
Run for each file:
```bash
node -e "JSON.parse(require('fs').readFileSync('src/messages/en-US.json','utf8')); console.log('en-US OK')"
node -e "JSON.parse(require('fs').readFileSync('src/messages/pt-BR.json','utf8')); console.log('pt-BR OK')"
node -e "JSON.parse(require('fs').readFileSync('src/messages/es-ES.json','utf8')); console.log('es-ES OK')"
```
Then verify key presence:
```bash
node -e "const f=JSON.parse(require('fs').readFileSync('src/messages/en-US.json','utf8')); console.log(Object.keys(f.team))"
```
  </verify>
  <done>All three locale files updated, valid JSON, team namespace present in all three, nav.team present in all three, deals/activities assignee keys present in all three</done>
</task>

</tasks>

<verification>
1. All three JSON files parse without error
2. `deals.assignees`, `deals.noAssignees`, `deals.allAssignees`, `deals.myDeals` present in all three files
3. `activities.assignee`, `activities.assignedTo`, `activities.noAssignee` present in all three files
4. `nav.team` present in all three files
5. `team` namespace with all 7 keys present in all three files
6. Key count matches across locales for the new additions
</verification>

<success_criteria>
- en-US.json, pt-BR.json, es-ES.json all have new assignee keys in deals and activities namespaces
- nav.team key in all three locales (used by NavHeader Team link)
- team namespace in all three locales (used by /team page)
- Translations are accurate (not machine-translated placeholders)
- JSON syntax valid in all three files
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-user-collaboration/15-06-SUMMARY.md`
</output>
