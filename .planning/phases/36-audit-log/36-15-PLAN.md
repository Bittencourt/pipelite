---
phase: 36-audit-log
plan: 15
type: tdd
wave: 3
depends_on: ["36-03"]
files_modified:
  - src/app/api/v1/audit/route.ts
  - src/app/api/v1/audit/__tests__/route.test.ts
autonomous: true
requirements: [AUDIT-03]
must_haves:
  truths:
    - "An admin API key can read the audit log over REST"
    - "A non-admin key is refused, and so is a key whose actor cannot be resolved"
    - "There is no way to write, amend or delete an audit entry over HTTP"
  artifacts:
    - path: "src/app/api/v1/audit/route.ts"
      provides: "GET only, wrapped in withApiAuth, gated on resolveActorRole === admin"
      exports: ["GET"]
    - path: "src/app/api/v1/audit/__tests__/route.test.ts"
      provides: "admin allow, non-admin deny, unresolvable-actor deny, pagination clamp, filter validation"
      contains: "forbidden"
  key_links:
    - from: "src/app/api/v1/audit/route.ts"
      to: "src/lib/notes/authorize.ts"
      via: "resolveActorRole, because ApiAuthContext carries no role"
      pattern: "resolveActorRole"
---

<objective>
Expose the audit log over REST as a read-only, admin-only `GET` — and expose nothing else.

Purpose: 36-CONTEXT locks this surface as read-only and admin-only. An audit log any API key can read
is an information-disclosure surface, and it must never be writable from outside the subscriber.

Output: `src/app/api/v1/audit/route.ts` and its test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-audit-log/36-CONTEXT.md
@.planning/phases/36-audit-log/36-PATTERNS.md
@.planning/phases/36-audit-log/36-03-SUMMARY.md

@src/app/api/v1/notes/[noteId]/route.ts
@src/lib/notes/authorize.ts
@src/lib/api/pagination.ts
@src/lib/api/response.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write the audit route test — allow, deny, and the shape of the deny</name>
  <files>src/app/api/v1/audit/__tests__/route.test.ts</files>
  <read_first>
    - `src/app/api/v1/notes/__tests__/route.test.ts` — the closest existing v1 route suite, including
      the `withApiAuth` auth-bypass mock and `importOriginal` selective mocking at `:36-40`.
    - `src/app/api/v1/notes/[noteId]/route.ts:26-61` — the `authorizeNoteMutation` doc comment
      explaining WHY the role is re-read from storage rather than trusted from the request (T-35-24),
      and why an unresolvable actor is denied rather than treated as a non-admin fallback (T-35-25).
    - `src/lib/api/pagination.ts:19-33` — `parsePagination`'s clamping behaviour.
    - `src/lib/api/errors.ts` — the `Problems.*` shapes.
  </read_first>
  <behavior>
    - Admin key: `resolveActorRole` resolves `{ role: "admin" }` → `200` with a list response whose
      items are snake_case.
    - Non-admin key: resolves `{ role: "user" }` → `Problems.forbidden()`, i.e. `403`. **Not 404** —
      the resource exists and hiding that fact buys nothing here.
    - Unresolvable actor: `resolveActorRole` returns `null` → `403`. It is never treated as a
      non-admin fallback and never as a default-allow.
    - Pagination: `?limit=99999` is clamped to `MAX_PAGE_SIZE`; `?offset=-5` clamps to `0`;
      absent params use the defaults. Assert against `parsePagination`'s contract rather than
      re-implementing the clamp in the route.
    - Filters: `?entity_type=deal` narrows; `?entity_type=nonsense` is rejected with a `400`-class
      problem rather than reaching a SQL predicate. `?entity_id=`, `?actor_kind=` and
      `?workflow_run_id=` behave the same way — validated with zod before composition.
    - Method surface — test name contains `read-only`: the module exports `GET` and NOTHING else.
      Assert by importing the route module and checking that `POST`, `PUT`, `PATCH` and `DELETE` are
      all `undefined`. A route file that later grows a mutating handler must break this test.
    - A thrown database error yields `Problems.internalError()` and a `console.error`, never a raw
      Postgres message in the body.
  </behavior>
  <action>
    Create `src/app/api/v1/audit/__tests__/route.test.ts`. Mock `@/db` and `@/lib/notes/authorize`
    (`resolveActorRole`). Mock `@/lib/api/auth` with the auth-bypass shape the existing v1 suites use
    — this suite is about what the handler does AFTER authentication; `withApiAuth` itself is tested
    in 36-05.

    Run red. Commit: `test(36-15): add failing admin-only audit REST cases`.
  </action>
  <acceptance_criteria>
    - The file exists with at least 9 `it(` blocks.
    - A test name matches `/read-only/`.
    - `grep -c "forbidden" src/app/api/v1/audit/__tests__/route.test.ts` is at least `2`.
    - `npx vitest run src/app/api/v1/audit` exits non-zero.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/v1/audit; test $? -ne 0</automated>
  </verify>
  <done>Allow, both deny paths, the pagination clamp, the filter validation and the read-only method surface each have a named failing test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement the read-only admin audit GET</name>
  <files>src/app/api/v1/audit/route.ts</files>
  <read_first>
    - `src/app/api/v1/notes/[noteId]/route.ts:69-78` and `:114-117` — the handler envelope to copy:
      `withApiAuth(request, async (req, context) =&gt; { try { ... } catch (error) { console.error("&lt;METHOD&gt; &lt;path&gt; failed:", error); return Problems.internalError() } })`, plus the early-return
      `{ ok: false, response }` authorize helper.
    - `src/lib/notes/authorize.ts:1-22` — its module comment states it is notes-specific by design,
      which is why the admin check here is written as a small local helper rather than by extending
      that module.
    - `src/lib/api/pagination.ts:19` and `src/lib/api/response.ts` — `parsePagination`,
      `listResponse`.
    - `src/lib/timeline/assemble.ts:33-41` — the zod-validate-before-composition precedent for any
      value that reaches a SQL predicate.
    - `src/db/schema/audit-log.ts` from 36-03 — the columns and the four indexes the filters should
      hit.
  </read_first>
  <action>
    Create `src/app/api/v1/audit/route.ts` exporting `GET` and nothing else.

    Authorization: a small local helper in this file that calls `resolveActorRole(context.userId)`
    and returns `{ ok: false, response: Problems.forbidden() }` when the actor is null OR when
    `actor.role !== "admin"`. Direct role comparison, not `isAuthorOrAdmin` — there is no author on
    an audit row. Do not extend `src/lib/notes/authorize.ts`; its own module comment says it is
    notes-specific by design.

    Query: `parsePagination(request)` for offset and limit; zod-validated optional filters for
    `entity_type` (the four CRM literals plus `import_session`), `entity_id`, `actor_kind` (the five
    kinds) and `workflow_run_id`. An invalid filter value returns a `Problems` 400-class response and
    never reaches a predicate. Order by `created_at DESC, id DESC`.

    Response: `listResponse` with snake_case items, matching every other `/api/v1` route.

    Do NOT export `POST`, `PUT`, `PATCH` or `DELETE`, now or ever. The audit log is never writable
    from outside the subscriber, and the retention pruner is the only deletion path. Write that as a
    module header comment so the constraint travels with the file.

    Do not bind a JS `Date` into any raw `sql` fragment. Prefer the drizzle builder throughout; if a
    raw fragment becomes necessary, bind `${iso}::text::timestamp` — a bare `::timestamp` lets
    Postgres resolve the parameter to OID 1114 and the driver re-serializes it through a `Date`,
    silently truncating microseconds.

    Run green. Commit: `feat(36-15): add read-only admin audit REST endpoint`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/app/api/v1/audit` — all cases pass.
    - `grep -cE "export async function (POST|PUT|PATCH|DELETE)" src/app/api/v1/audit/route.ts` returns `0`.
    - `grep -c "export async function GET" src/app/api/v1/audit/route.ts` returns `1`.
    - `grep -c "withApiAuth" src/app/api/v1/audit/route.ts` returns `1`.
    - `grep -c "resolveActorRole" src/app/api/v1/audit/route.ts` returns `1`.
    - `grep -c "Problems.forbidden" src/app/api/v1/audit/route.ts` is at least `1`;
      `grep -c "Problems.notFound" src/app/api/v1/audit/route.ts` returns `0`.
    - `grep -c "parsePagination" src/app/api/v1/audit/route.ts` returns `1`.
    - `npm run typecheck` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/v1 &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>`GET /api/v1/audit` returns the log to an admin key, refuses everyone else with a 403, and the module exports no mutating handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API key → the whole audit log | The broadest read surface in the phase: one request can retrieve former values across every CRM record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Information Disclosure | any API key reading the audit log | mitigate | Admin-only via `resolveActorRole`, re-read from storage because `ApiAuthContext` is `{userId, keyId}` with no role. An unresolvable actor is DENIED, never defaulted to non-admin and never to allow — the Phase 35 T-35-25 fail-closed pattern. |
| T-36-33 | Tampering | a mutating handler appearing on this route later | mitigate | The module exports only `GET`, asserted by a test that imports the module and checks `POST`/`PUT`/`PATCH`/`DELETE` are `undefined`, plus a grep gate. The constraint is written into the module header so it travels with the file. |
| T-36-06 | Tampering | SQL injection via a filter parameter | mitigate | Every filter is zod-validated against a closed literal set or bound through the drizzle builder before composition, following `assemble.ts:33-41`. |
| T-36-10 | Elevation of Privilege | skipping `withApiAuth` | mitigate | The handler is wrapped, inheriting authentication, rate limiting and — since 36-05 — the `api_key` actor scope. Grep-asserted at exactly one occurrence. |
| T-36-34 | Denial of Service | an unbounded page size | mitigate | `parsePagination` clamps to `MAX_PAGE_SIZE`; the route does not re-implement the clamp. |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added this phase. |
</threat_model>

<verification>
- `npx vitest run src/app/api/v1` — green, including the new suite
- `npm run typecheck` — exit 0
- Grep gates: exactly one exported handler, zero `Problems.notFound`, exactly one `withApiAuth`
</verification>

<success_criteria>
- `GET /api/v1/audit` returns a snake_case list to an admin key
- Non-admin and unresolvable actors both get `403`
- Filters are validated before reaching any predicate
- No mutating handler exists on the route
</success_criteria>

<output>
Create `.planning/phases/36-audit-log/36-15-SUMMARY.md` when done
</output>
