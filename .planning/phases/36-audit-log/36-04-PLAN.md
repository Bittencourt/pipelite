---
phase: 36-audit-log
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/en-US.json
  - src/messages/es-ES.json
  - src/messages/pt-BR.json
  - src/messages/locale-parity.test.ts
autonomous: true
requirements: [AUDIT-03, AUDIT-04]
must_haves:
  truths:
    - "Every string the four audit surfaces render exists in all three shipped locales"
    - "Adding an audit string to a component without adding its dot-path to the required list fails the build"
  artifacts:
    - path: "src/messages/en-US.json"
      provides: "the audit namespace: 77 keys, plus 2 admin.dashboard keys"
      contains: "\"audit\""
    - path: "src/messages/locale-parity.test.ts"
      provides: "REQUIRED_AUDIT_KEYS wired into the same five assertions as REQUIRED_NOTE_KEYS"
      contains: "REQUIRED_AUDIT_KEYS"
  key_links:
    - from: "src/messages/locale-parity.test.ts"
      to: "REQUIRED_AUDIT_KEYS"
      via: "the five existing assertions"
      pattern: "REQUIRED_AUDIT_KEYS"
---

<objective>
Land all 79 new copy strings — 77 under a new top-level `audit` namespace and 2 under the existing
`admin.dashboard` namespace — in `en-US`, `es-ES` and `pt-BR`, and extend the parity gate so a
future audit string cannot ship in one locale only.

Purpose: `locale-parity.test.ts` asserts whole-file key-set identity across the three locale files,
so a key added to `en-US` alone fails the build. Every downstream UI plan (36-13 entry, 36-14
retention, 36-16 run section, 36-19 toggle) renders these keys, so they must exist first.

Output: three locale files and one extended gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-audit-log/36-UI-SPEC.md
@.planning/phases/36-audit-log/36-PATTERNS.md

@src/messages/locale-parity.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the audit namespace to all three locale files</name>
  <files>src/messages/en-US.json, src/messages/es-ES.json, src/messages/pt-BR.json</files>
  <read_first>
    - `.planning/phases/36-audit-log/36-UI-SPEC.md` § Copywriting Contract → Full key inventory
      (en-US). It is the authoritative source: 4 actor kinds + 12 predicates + 10 values/disclosure
      + 20 field labels + 5 filter + 8 run + 18 retention = 77 keys under `audit.*`, plus
      `admin.dashboard.auditLog` and `admin.dashboard.auditLogDescription`.
    - `src/messages/en-US.json` — the existing `notes` namespace, for nesting depth, ICU plural
      syntax and the surrounding formatting convention.
    - `src/messages/es-ES.json` and `src/messages/pt-BR.json` — the existing translations of the
      `notes` namespace, for register and tone.
    - `.planning/phases/36-audit-log/36-UI-SPEC.md` § Copy rules for this phase — no generic CTA
      labels, no exclamation marks, every error names the failed action and the next step, audit copy
      is factual and never editorial.
  </read_first>
  <action>
    Add one new top-level `audit` object to each of the three files, plus two keys inside the
    existing `admin.dashboard` object.

    Copy the en-US values verbatim from the UI-SPEC tables. Do not paraphrase — the two ICU plural
    strings (`audit.value.files`, `audit.showMoreFields`, `audit.run.fieldCount`,
    `audit.filter.emptyHidden.body`) carry `{count, plural, one {...} other {...}}` and the parity
    gate asserts placeholder survival across locales.

    Translate to es-ES and pt-BR. Two things that are not optional:
    - The 12 `audit.entry.{action}.{entityType}` predicates exist as twelve separate strings
      precisely because Spanish and Portuguese inflect the demonstrative with the noun's gender
      ("este trato" vs "esta actividad"). Translate each one with the correct gendered
      demonstrative; do not collapse them to a placeholder.
    - The ICU plural arms must be translated inside the ICU syntax, keeping `{count, plural, ...}`
      and the `#` intact.

    `audit.filter.emptyHidden.body` quotes the filter's own label verbatim ("Turn on Show field
    changes to see them"). In each locale, the quoted fragment must match that locale's
    `audit.filter.label` exactly, or the instruction points at a control the user cannot find.

    Do NOT add any key not listed in the UI-SPEC inventory. Every key added here is asserted by
    Task 2 and rendered by a downstream plan; an extra key is dead weight the gate will then pin.
  </action>
  <acceptance_criteria>
    - `node -e "const m=require('./src/messages/en-US.json'); const c=(o)=>Object.values(o).reduce((n,v)=>n+(typeof v==='object'?c(v):1),0); console.log(c(m.audit))"` prints `77`.
    - The same command against `es-ES.json` and `pt-BR.json` also prints `77`.
    - `node -e "for(const l of ['en-US','es-ES','pt-BR']){const m=require('./src/messages/'+l+'.json'); if(!m.admin.dashboard.auditLog||!m.admin.dashboard.auditLogDescription) throw new Error(l)}"` exits `0`.
    - `grep -c "plural" src/messages/es-ES.json` and the pt-BR equivalent are each at least `4` more
      than before this task.
    - All three files parse as JSON.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/messages/locale-parity.test.ts</automated>
  </verify>
  <done>All 79 keys exist in all three locale files with matching key sets, and every ICU placeholder survives translation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire REQUIRED_AUDIT_KEYS into the five existing parity assertions</name>
  <files>src/messages/locale-parity.test.ts</files>
  <read_first>
    - `src/messages/locale-parity.test.ts` lines 20-60 (`REQUIRED_NOTE_KEYS` and the comment
      explaining why the list is checked in) and lines 120-160 (the five assertions that consume it:
      missing-keys, key-set identity, non-empty, untranslated-in-both, placeholder survival).
    - `.planning/phases/36-audit-log/36-PATTERNS.md` § Shared Patterns → i18n parity gate.
  </read_first>
  <action>
    Add an exported `REQUIRED_AUDIT_KEYS: string[]` sibling to `REQUIRED_NOTE_KEYS`, listing all 79
    dot-paths (77 `audit.*` plus the 2 `admin.dashboard.*`), grouped with a comment per group
    matching the UI-SPEC's section names so a reader can find a key by surface.

    Wire it into the SAME five assertions the notes list already drives. Prefer parameterising the
    existing assertions over `[...REQUIRED_NOTE_KEYS, ...REQUIRED_AUDIT_KEYS]` if the file already
    reads the list once; either way the audit keys must be subject to all five, not just the
    existence check.

    Carry the checked-in-list rationale into a comment on the new constant: adding an `audit.*`
    string to the UI means adding its dot-path here first — that is the point of the list existing.

    Do not weaken any existing assertion to accommodate the new keys. If an assertion fails, the copy
    is wrong, not the gate.
  </action>
  <acceptance_criteria>
    - `grep -c "REQUIRED_AUDIT_KEYS" src/messages/locale-parity.test.ts` is at least `6` (the
      declaration plus its five consumers).
    - `node -e "const {REQUIRED_AUDIT_KEYS}=require('ts-node')" ` is NOT required; instead assert:
      `grep -c '"audit\.' src/messages/locale-parity.test.ts` returns `77`.
    - `npx vitest run src/messages/locale-parity.test.ts` passes.
    - Temporarily deleting `audit.value.empty` from `es-ES.json` makes the suite fail; restore it.
      Record that this negative check was performed in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/messages/locale-parity.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The audit key list is checked in and subject to all five parity assertions, proven by a deliberate deletion going red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translated copy → rendered DOM | Copy is developer-authored and static; user-authored text (custom field names) is never translated and is handled in 36-13. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-15 | Tampering | ICU placeholder loss in translation | mitigate | The parity gate's placeholder-survival assertion covers the new keys, so a translation that drops `{count}` fails the build rather than rendering a literal brace to a user. |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added this phase. |
</threat_model>

<verification>
- `npx vitest run src/messages/locale-parity.test.ts` — green
- Deliberate deletion of one key from one locale goes red, then is restored
- `npm test` — no regression
</verification>

<success_criteria>
- 77 `audit.*` keys and 2 `admin.dashboard.*` keys exist identically-keyed in all three locales
- `REQUIRED_AUDIT_KEYS` is checked in and drives all five parity assertions
- The 12 entry predicates are separately translated with correct gendered demonstratives
</success_criteria>

<output>
Create `.planning/phases/36-audit-log/36-04-SUMMARY.md` when done
</output>
