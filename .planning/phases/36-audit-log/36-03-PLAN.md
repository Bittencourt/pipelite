---
phase: 36-audit-log
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/audit-log.ts
  - src/db/schema/app-settings.ts
  - src/db/schema/index.ts
  - src/db/schema/_relations.ts
  - drizzle/0014_*.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0014_snapshot.json
  - scripts/audit-log-checks.sql
autonomous: true
requirements: [AUDIT-01, AUDIT-04]
must_haves:
  truths:
    - "The audit_log and app_settings tables exist in the running database"
    - "All four audit_log indexes are present in pg_indexes and were emitted by generate, not hand-written"
    - "The prune scan uses a Bitmap Index Scan on the created_at index, not a Seq Scan"
    - "A fresh deployment prunes at 90 days without an admin ever visiting /admin/audit — the locked default is seeded, not assumed"
  artifacts:
    - path: "src/db/schema/audit-log.ts"
      provides: "append-only audit_log table, four indexes, AuditLogRow type"
      contains: "pgTable('audit_log'"
      min_lines: 45
    - path: "src/db/schema/app-settings.ts"
      provides: "key/value settings table with a JSONB value"
      contains: "pgTable('app_settings'"
    - path: "drizzle/meta/_journal.json"
      provides: "the 0014 migration entry"
      contains: "0014"
    - path: "scripts/audit-log-checks.sql"
      provides: "re-runnable psql evidence: index catalog list, the seeded retention row, and both EXPLAIN plans"
    - path: "drizzle/0014_*.sql"
      provides: "generated DDL plus ONE hand-added idempotent data seed: audit.retention_days = 90"
      contains: "ON CONFLICT"
  key_links:
    - from: "src/db/schema/audit-log.ts"
      to: "src/db/schema/index.ts"
      via: "barrel export"
      pattern: "export \\* from \"./audit-log\""
    - from: "src/db/schema/_relations.ts"
      to: "auditLog"
      via: "auditLogRelations (users, workflowRuns) — NO entity relation"
      pattern: "auditLogRelations"
---

<objective>
Create the two new tables this phase needs — `audit_log` (the table PROJECT.md singles out as the
disk risk) and `app_settings` (the key/value table Phases 40 and 42 will also want) — with every
index declared in the schema file, and apply the migration to the running database.

Purpose: nothing else in the phase can be verified until these tables exist. Build and typecheck
both pass WITHOUT the migration applied, so verification of every downstream plan is a false
positive until this one actually runs.

Output: two schema files, two one-line barrel/relations edits, migration `0014` (generated DDL plus
one hand-added idempotent data seed for the locked 90-day retention default), and a checked-in psql
evidence script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-audit-log/36-CONTEXT.md
@.planning/phases/36-audit-log/36-PATTERNS.md

@src/db/schema/deal-stage-history.ts
@src/db/schema/notes.ts
@src/db/schema/import-sessions.ts
@src/db/schema/index.ts
@src/db/schema/_relations.ts
</context>

<interfaces>
<!-- Contract downstream plans compile against: 36-08 (settings), 36-09 (linked-records),
     36-11 (subscriber), 36-15 (REST), 36-17 (timeline source), 36-18 (pruner). -->

export type AuditEntityType = EntityType | "import_session"

export const auditLog: pgTable "audit_log"
  id            text  PK  $defaultFn(crypto.randomUUID)
  entityType    text  notNull  $type&lt;AuditEntityType&gt;()  -- EntityType imported from ./custom-fields, NOT redeclared
  entityId      text  notNull                        -- polymorphic, NO foreign key
  action        text  notNull  $type&lt;AuditAction&gt;()
  changes       jsonb notNull  default({})           -- Record&lt;field, {from,to}&gt;
  actorKind     text  notNull  $type&lt;AuditActorKind&gt;()
  actorUserId   text  → users.id            (nullable)
  workflowRunId text  → workflowRuns.id     (nullable)
  importSessionId text → importSessions.id  (nullable)
  createdAt     timestamp defaultNow notNull
  -- NO updatedAt, NO deletedAt

export type AuditLogRow = InferSelectModel&lt;typeof auditLog&gt;

export const appSettings: pgTable "app_settings"
  key       text PK
  value     jsonb $type&lt;unknown&gt;() notNull
  updatedAt timestamp defaultNow notNull

export type AppSettingRow = InferSelectModel&lt;typeof appSettings&gt;

<!-- Seeded by migration 0014, consumed by 36-08's readRetentionDays and 36-18's pruner. -->
key   = "audit.retention_days"
value = 90        -- the CONTEXT-locked default, seeded as DATA and never as a code fallback
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Declare the audit_log and app_settings tables with all indexes in the schema files</name>
  <files>src/db/schema/audit-log.ts, src/db/schema/app-settings.ts</files>
  <read_first>
    - `src/db/schema/deal-stage-history.ts` — Analog A. Copy verbatim: `text` id with
      `$defaultFn(() =&gt; crypto.randomUUID())` (NOT `uuid`/`gen_random_uuid()`), `timestamp(...)`
      with **no `mode` option**, `index(...)` in the third `pgTable` argument with `.desc()` on the
      sort column, the `InferSelectModel` export at the bottom, and the explicit comment justifying
      the absence of `updatedAt`/`deletedAt`.
    - `src/db/schema/notes.ts` lines 11-46 — Analog B: the polymorphic `entityType` with
      `.$type&lt;EntityType&gt;()` imported from `./custom-fields`, the no-foreign-key `entityId` and
      the partial index form `index(...).on(...).where(sql\`...\`)`.
    - `src/db/schema/import-sessions.ts:10` and `src/db/schema/http-templates.ts:10` — the two JSONB
      column idioms (`.notNull().default({})` and `.$type&lt;...&gt;().notNull()`).
    - `drizzle/0013_parched_redwing.sql:27-28` — proof that a partial index emits intact from
      `generate`.
    - `.planning/phases/36-audit-log/36-RESEARCH.md` § Pitfall 4 — the measured numbers that make
      the `created_at` index mandatory rather than nice-to-have.
  </read_first>
  <action>
    Create `src/db/schema/audit-log.ts` with the shape in `&lt;interfaces&gt;` above.

    `entityType` MUST be `.$type&lt;AuditEntityType&gt;()`, where
    `export type AuditEntityType = EntityType | "import_session"` and `EntityType` is IMPORTED from
    `./custom-fields`. Do not declare a fifth copy of the four-literal union (D-01 from Phase 35) —
    widen it by union, do not restate it.

    Why the fifth literal exists: 36-CONTEXT § Post-Research Addendum locks one summary audit row per
    import session, written by the importer (36-12). That row is about a session, not about a single
    CRM record, so forcing it to claim `entity_type = 'deal'` with a session id in `entity_id` would
    be a lie the schema tells about itself. The timeline never selects it: `assertEntityType` in
    `src/lib/timeline/assemble.ts:33-41` validates against the four CRM literals BEFORE any fragment
    is composed, so an `import_session` row is unreachable from every record timeline by
    construction. Write that reasoning into the type's doc comment.

    `entityId` carries NO foreign key. Notes has the same shape and its comment explains that the
    parent-existence check in the mutation layer is the only defence. Here the comment must say the
    OPPOSITE and say it explicitly: the audit log has no parent-existence check and must not gain
    one, because an audit row for a deleted record must survive the record. Without that comment the
    absence reads as the notes bug repeated.

    Also declare in this file, and export:
    `export type AuditAction = "created" | "updated" | "deleted"` and re-export or re-declare
    `AuditActorKind` consistent with `src/lib/audit/actor-context.ts` (36-01). Prefer importing the
    type from `@/lib/audit/actor-context` if that does not create a schema→lib cycle; if it does,
    declare it here and have 36-01's module remain the single runtime source. State which you chose
    in the summary.

    Four indexes, ALL declared in the third `pgTable` argument:
    1. `audit_log_entity_idx` on `(entityType, entityId, createdAt.desc())` — read pattern 1, the
       timeline branch.
    2. `audit_log_workflow_run_idx` on `(workflowRunId)` **partial**, `.where(sql\`${table.workflowRunId} is not null\`)` — read pattern 2, the run→records list. Copy the partial form from
       `notes.ts:42-44`.
    3. `audit_log_created_at_idx` on `(createdAt)` — read pattern 3, the prune scan. Measured: with
       it a 5,000-row batch delete is 17.8 ms; without it, 395.7 ms and a full Seq Scan. This index
       is load-bearing, not defensive; say so in a comment with those two numbers.
    4. `audit_log_import_session_idx` on `(importSessionId)` **partial**, same `is not null` form —
       the import summary row's lookup key.

    The immutability comment: audit rows are immutable append-only facts, so there is no `updatedAt`
    and no `deletedAt`, and the ONLY permitted deletion is the retention pruner. Every other CRM
    table carries both columns, so the absence would otherwise read as an oversight.

    Create `src/db/schema/app-settings.ts`: `key` as `text('key').primaryKey()`, `value` as a
    `$type`-narrowed JSONB, `updatedAt` as a plain `timestamp` with `defaultNow().notNull()` (this
    table IS mutable — it is the one place in this phase where an `updatedAt` is correct). State in
    a header comment that this is a new table shape for this codebase, that no key/value table
    existed before, that `notification-preferences.ts` is the near-miss (typed boolean columns keyed
    by user, which is the shape 36-CONTEXT rejected), and that this phase introduces the table plus
    exactly one key.

    Do NOT write any index SQL by hand anywhere. Phase 33 D-06: a hand-written index in this repo was
    silently dropped by a later `generate` (0009→0010).
  </action>
  <acceptance_criteria>
    - `grep -c "index(" src/db/schema/audit-log.ts` returns `4`.
    - `grep -c "deletedAt\|updatedAt" src/db/schema/audit-log.ts` returns `0` outside comments:
      `grep -vE '^\s*(\*|//|/\*)' src/db/schema/audit-log.ts | grep -cE "deletedAt|updatedAt"` is `0`.
    - `grep -c "AuditEntityType" src/db/schema/audit-log.ts` is at least `3` (the type, its use on
      the column, and the doc comment reference).
    - `grep -c "import_session" src/db/schema/audit-log.ts` is at least `1`.
    - `grep -c "is not null" src/db/schema/audit-log.ts` returns `2` (the two partial indexes).
    - `npm run typecheck` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <done>Both tables and all four indexes are declared in TypeScript, no DDL is hand-written, and the deviation comments are present.</done>
</task>

<task type="auto">
  <name>Task 2: Export the two tables from the barrel and add auditLogRelations</name>
  <files>src/db/schema/index.ts, src/db/schema/_relations.ts</files>
  <read_first>
    - `src/db/schema/index.ts:24-25` — how `notes` and `deal-stage-history` were added.
    - `src/db/schema/_relations.ts:236-243` — the notes comment explaining why a polymorphic
      `entityId` gets NO Drizzle relation. It applies to `auditLog.entityId` word for word.
    - `src/db/schema/_relations.ts:245-262` — `dealStageHistoryRelations`, the shape to copy.
  </read_first>
  <action>
    Add two `export * from "./audit-log"` / `"./app-settings"` lines to `src/db/schema/index.ts`,
    matching the surrounding ordering convention.

    Add `auditLogRelations` to `_relations.ts` with exactly three `one(...)` relations:
    `actorUser` → `users`, `workflowRun` → `workflowRuns`, `importSession` → `importSessions`.

    Deliberately NO `entity` relation, and a comment saying so, mirroring the notes comment: the
    `entityId` is polymorphic across four tables, no Drizzle relation is expressible for it, and the
    parent is resolved in the query layer instead.

    `app_settings` gets no relations entry — it references nothing and nothing references it.
  </action>
  <acceptance_criteria>
    - `grep -c "audit-log\|app-settings" src/db/schema/index.ts` returns `2`.
    - `grep -c "auditLogRelations" src/db/schema/_relations.ts` returns `1`.
    - `grep -A20 "auditLogRelations" src/db/schema/_relations.ts | grep -c "one("` returns `3`.
    - `npm run typecheck` exits `0` and `npx vitest run src/db` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>`import { auditLog, appSettings } from "@/db/schema"` resolves, and the relations carry no polymorphic entity link.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Generate migration 0014, apply it in the container, and capture psql evidence</name>
  <files>drizzle/0014_*.sql, drizzle/meta/_journal.json, drizzle/meta/0014_snapshot.json, scripts/audit-log-checks.sql</files>
  <read_first>
    - `drizzle/0013_parched_redwing.sql` — the latest migration on disk. This phase's is `0014`.
    - `.planning/phases/35-notes-record-timeline/35-03-SUMMARY.md` lines 130-180 and 400-430 — the
      worktree-safe apply path and why `docker compose up -d --build` is unsafe from a worktree.
    - `docker-compose.yml` and `docker-entrypoint.sh` — the entrypoint runs `npx drizzle-kit migrate`
      on start, so the apply below makes the entrypoint's run a verified no-op.
    - `.planning/phases/36-audit-log/36-RESEARCH.md` § Pitfall 4 and § Pitfall 10.
    - `.planning/phases/36-audit-log/36-CONTEXT.md` § Retention and Pruning, first bullet — **"Default
      retention is 90 days, configurable."** This is a distinct locked decision and is NOT under
      § Claude's Discretion, which delegates only the batch size and the tick interval. Step 1b
      exists to implement it.
    - `.planning/phases/36-audit-log/36-08-PLAN.md` Task 2 — its acceptance criteria forbid a `?? 90`
      fallback in `settings.ts`. That prohibition and this seed are COMPLEMENTARY, not contradictory;
      Step 1b explains why.
  </read_first>
  <action>
    THIS TASK IS BLOCKING. It runs AFTER both schema files and the barrel/relations edits are
    committed, and BEFORE any downstream plan verifies against these tables.

    Step 1 — generate: `npx drizzle-kit generate`. NEVER `drizzle-kit push`. Inspect the emitted
    `drizzle/0014_*.sql` and confirm it contains two `CREATE TABLE` statements and four
    `CREATE INDEX` statements, including the two `WHERE ... IS NOT NULL` partial forms. Do not edit
    the emitted DDL — if an index is missing, fix the schema file and regenerate. Step 1b appends a
    DATA statement and is the ONLY permitted hand-edit to this file.

    Step 1b — seed the locked 90-day retention default. Append ONE statement to the END of
    `drizzle/0014_*.sql`, after all generated DDL:

    an `INSERT INTO "app_settings" ("key", "value")` of `'audit.retention_days'` with the JSONB value
    `90`, carrying `ON CONFLICT ("key") DO NOTHING`.

    Four things about this, each of which must be written into a SQL comment above the statement,
    because each is a decision a later reader would otherwise reverse:

    1. **Why it exists.** 36-CONTEXT locks a 90-day default — "short enough to bound growth on a
       deployment where nobody is watching disk". Without a seed, `readRetentionDays()` returns
       `null` on a fresh install, the pruner deletes nothing, and the table PROJECT.md singles out as
       the disk risk grows forever until an admin proactively discovers `/admin/audit` and types a
       number. That is precisely the scenario the default exists to prevent.
    2. **Why it is DATA and not a code fallback.** 36-08 forbids a `?? 90` fallback in `settings.ts`
       on purpose: a code-level default would mean a corrupted, tampered or deliberately cleared
       setting row silently RESUMES deleting rows at 90 days, which is the wrong failure direction
       for an audit log. A seeded row plus fail-closed parsing gives both properties — a sane
       out-of-box policy AND a read path that keeps data whenever it cannot be sure. The two are
       complementary and neither substitutes for the other. Do not "simplify" either away.
    3. **Why hand-editing this file does not violate Phase 33 D-06.** D-06 forbids hand-written INDEX
       DDL in migration SQL, because `drizzle-kit generate` owns the schema and silently dropped a
       hand-written index in this repo once (0009 to 0010). `generate` does not manage data rows at
       all, never emits or re-emits an `INSERT`, and applied migrations are append-only — so this
       statement cannot be clobbered by a later `generate`. The distinction is DDL versus data, and
       it is the same carve-out Phase 25 used for its trigger-array data migration. Do not
       generalise it: no `CREATE INDEX` is ever hand-written here.
    4. **Why `ON CONFLICT DO NOTHING` rather than an upsert.** The seed must be idempotent and must
       NEVER overwrite a value an admin has chosen. On this project's live database the migration
       runs once, but a replayed or re-applied migration must be a no-op against an operator's
       setting.

    Bind nothing — this is a literal one-row, one-time statement, not a parameterised query.

    Step 2 — apply, worktree-safe. `.env` is present in the main checkout but ABSENT from a
    worktree, so `docker compose up -d --build` would interpolate every `${VAR}` to the empty string
    and crash-loop the user's live application (35-03 Rule-3 deviation). Instead copy the artifacts
    into the already-running container and run the same binary there:

    - `docker cp drizzle/0014_&lt;name&gt;.sql pipelite-app-1:/app/drizzle/`
    - `docker cp drizzle/meta/_journal.json pipelite-app-1:/app/drizzle/meta/`
    - `docker cp drizzle/meta/0014_snapshot.json pipelite-app-1:/app/drizzle/meta/`
    - `docker exec pipelite-app-1 sh -c 'cd /app &amp;&amp; npx drizzle-kit migrate'`

    `docker` needs NO `sudo` — the user is in the `docker` group. Never embed a password in any
    command, commit or checked-in file.

    Step 3 — restart the app container (`docker compose -p pipelite restart app`) so the real
    `docker-entrypoint.sh` migrate path runs end to end and is observed as a no-op; capture the log
    lines.

    Step 4 — create `scripts/audit-log-checks.sql`, re-runnable and idempotent, with three parts:
    1. Index catalog assertion: `SELECT indexname, indexdef FROM pg_indexes WHERE tablename =
       'audit_log' ORDER BY indexname;` — must list all four.
    2. Column assertion: `information_schema.columns` for `audit_log`, proving there is no
       `updated_at` and no `deleted_at`.
    3. Retention seed assertion:
       `SELECT key, value FROM app_settings WHERE key = 'audit.retention_days';` — must return
       exactly one row with the value `90`. This is the standing detector for the locked default: if
       a future migration or a botched restore drops it, this script says so.
    4. Prune plan: `EXPLAIN (ANALYZE, BUFFERS) DELETE FROM audit_log WHERE ctid IN (SELECT ctid FROM
       audit_log WHERE created_at &lt; now() - make_interval(days =&gt; 90) LIMIT 5000);` wrapped in
       `BEGIN; ... ROLLBACK;` so the script deletes nothing.

    Run it: `docker compose -p pipelite exec -T postgres psql -U pipelite -d pipelite -f - &lt;
    scripts/audit-log-checks.sql` and paste the full output into the SUMMARY.

    The table is empty at this point and there is deliberately NO backfill: there is no before-state
    to reconstruct for the 188,629 existing records, so an empty table is the honest starting point.
    State that in the SUMMARY explicitly — a verifier opening an old record will see an empty change
    history and would otherwise read it as a bug. The at-scale prune plan (Bitmap Index Scan,
    ~18 ms per 5,000 rows) was measured on a 1M-row probe in 36-RESEARCH; on the empty table the plan
    node may differ and that is expected — what this script proves today is that the index exists and
    the statement is valid.
  </action>
  <acceptance_criteria>
    - `ls drizzle/0014_*.sql` returns exactly one file.
    - `grep -c "CREATE INDEX" drizzle/0014_*.sql` returns `4`.
    - `grep -c "IS NOT NULL" drizzle/0014_*.sql` returns `2`.
    - `grep -c "0014" drizzle/meta/_journal.json` is at least `1`.
    - `grep -c "ON CONFLICT" drizzle/0014_*.sql` returns `1` and
      `grep -c "audit.retention_days" drizzle/0014_*.sql` returns `1` — the seed is present and
      idempotent.
    - `grep -c "CREATE INDEX" drizzle/0014_*.sql` is STILL `4` after the hand-edit — Step 1b appended
      a data statement and touched no DDL.
    - `docker compose -p pipelite exec -T postgres psql -U pipelite -d pipelite -tAc "SELECT value FROM app_settings WHERE key='audit.retention_days'"` prints `90`.
    - **Idempotency proof, performed and recorded:** re-run the seed statement by hand against the
      container and confirm it reports `INSERT 0 0` with the value still `90`. Then set the value to
      `30`, re-run the seed, and confirm it is STILL `30` — the seed does not overwrite an
      operator's choice. Restore `90` afterwards. Record all three steps in the SUMMARY.
    - `docker compose -p pipelite exec -T postgres psql -U pipelite -d pipelite -c "\d audit_log"`
      lists the table and four indexes.
    - The psql run of `scripts/audit-log-checks.sql` returns four rows from part 1 and zero rows
      matching `updated_at` or `deleted_at` in part 2.
    - The full psql output is pasted verbatim into `36-03-SUMMARY.md`.
  </acceptance_criteria>
  <verify>
    <automated>docker compose -p pipelite exec -T postgres psql -U pipelite -d pipelite -tAc "SELECT count(*) FROM pg_indexes WHERE tablename='audit_log'" | grep -q '^4$'</automated>
  </verify>
  <done>Migration 0014 is applied to the running database, all four indexes exist in `pg_indexes`, `audit_log` has no `updated_at` and no `deleted_at`, `app_settings` carries the seeded 90-day retention value, the seed is proven idempotent and proven not to overwrite an operator value, and the evidence script is checked in with its output recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → durable storage | The audit table is the phase's integrity record; its immutability is a schema property, not a convention. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-12 | Tampering | `audit_log` rows | mitigate | No `updatedAt` and no `deletedAt` column exists, so there is no supported path to amend or soft-delete a row. The only permitted deletion is the retention pruner (36-18). Asserted by `scripts/audit-log-checks.sql` part 2 against `information_schema.columns`. |
| T-36-13 | Spoofing | `workflow_run_id` | mitigate | A real foreign key to `workflow_runs.id`, so a fabricated run id cannot be stored. The value is written only from the ALS store established inside the executor. |
| T-36-09 | Denial of Service | prune scan cost | mitigate | `audit_log_created_at_idx` declared in the schema file; without it the daily batch is 22× slower and seq-scans the whole table (measured 395.7 ms vs 17.8 ms). |
| T-36-43 | Denial of Service | an unbounded audit table on a deployment where nobody configures retention | mitigate | Migration `0014` seeds `audit.retention_days = 90` as data, so the pruner is active from first boot with no admin action. `ON CONFLICT DO NOTHING` makes the seed idempotent and prevents it overwriting an operator's chosen value; the seed's presence is a standing assertion in `scripts/audit-log-checks.sql` part 3. |
| T-36-44 | Tampering | a seeded default silently resuming deletion after the setting row is cleared or corrupted | mitigate | The default is data, never a code fallback: `readRetentionDays()` (36-08) has no `?? 90` and fails closed to `null` for a cleared, corrupted or out-of-range row, so tampering stops pruning rather than starting it. |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added this phase. |
</threat_model>

<verification>
- `npm run typecheck` and `npm test` green after the schema edits
- `\d audit_log` in the container lists four indexes
- `scripts/audit-log-checks.sql` runs clean and its output is in the SUMMARY
- `docker compose -p pipelite logs app` shows the entrypoint migrate as a no-op after restart
</verification>

<success_criteria>
- `audit_log` and `app_settings` exist in the running database
- `app_settings` carries `audit.retention_days = 90`, seeded as data by the migration, proven
  idempotent and proven not to overwrite an operator's chosen value
- All four indexes present, all emitted by `generate`, none hand-written
- No `updated_at` / `deleted_at` on `audit_log`
- The no-backfill decision is stated in the SUMMARY, not implied
</success_criteria>

<output>
Create `.planning/phases/36-audit-log/36-03-SUMMARY.md` when done
</output>
