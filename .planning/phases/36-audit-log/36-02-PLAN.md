---
phase: 36-audit-log
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/events/types.ts
  - src/lib/audit/diff.ts
  - src/lib/audit/diff.test.ts
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "A one-field edit produces a one-key change map, whichever route emitted it"
    - "A formula-derived value never appears as a change"
    - "A create records the initial state; a delete records a tombstone built from the before-row"
  artifacts:
    - path: "src/lib/audit/diff.ts"
      provides: "PURE buildChanges + normaliseEventData + IGNORED_COLUMNS; the only fully unit-testable logic in the phase"
      exports: ["buildChanges", "normaliseEventData", "IGNORED_COLUMNS"]
      min_lines: 80
    - path: "src/lib/audit/diff.test.ts"
      provides: "Native diff, formula exclusion, multi_select guard, key normalisation, create/delete cases"
      contains: "normalise"
    - path: "src/lib/events/types.ts"
      provides: "CrmEventPayload.previous — the before-row the subscriber cannot recover for itself"
      contains: "previous"
  key_links:
    - from: "src/lib/audit/diff.ts"
      to: "src/lib/formula-helpers.ts"
      via: "isFormulaWrapper import"
      pattern: "isFormulaWrapper"
---

<objective>
Build the pure diff: given a `CrmEventPayload` carrying a `previous` row, produce the
`field → { from, to }` map that becomes the audit row's `changes` JSONB — with the two payload
casings reconciled and formula-derived values excluded.

Purpose: the vitest suite mocks `@/db` entirely, so this module is the only part of the capture path
that can be tested properly. Everything hard about AUDIT-01 lives here on purpose.

Output: `src/lib/events/types.ts` gains `previous?:`; `src/lib/audit/diff.ts` + its test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-audit-log/36-CONTEXT.md
@.planning/phases/36-audit-log/36-PATTERNS.md

@src/lib/events/types.ts
@src/lib/api/serialize.ts
@src/lib/formula-helpers.ts
</context>

<interfaces>
<!-- Downstream contract. 36-11 (subscriber) is the only consumer of buildChanges. -->

export type AuditChangeMap = Record&lt;string, { from: unknown; to: unknown }&gt;

export function normaliseEventData(entity: CrmEntityType, obj: Record&lt;string, unknown&gt;): Record&lt;string, unknown&gt;
export function buildChanges(payload: CrmEventPayload): AuditChangeMap

<!-- Existing shape being widened, src/lib/events/types.ts:4-12 -->
export interface CrmEventPayload {
  entity: CrmEntityType
  entityId: string
  action: CrmAction
  data: Record&lt;string, unknown&gt;
  changedFields: string[] | null
  userId: string
  timestamp: string
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Widen CrmEventPayload with an optional previous row</name>
  <files>src/lib/events/types.ts</files>
  <read_first>
    - `src/lib/events/types.ts` in full — 34 lines. `DealStageChangedPayload` extends
      `CrmEventPayload` at `:14-18`, so it inherits the new field for free; the 13-entry
      `CrmEventMap` needs no change.
    - `.planning/phases/36-audit-log/36-CONTEXT.md` § Claude's Discretion, last bullet — the shared
      vs. narrower-type decision is delegated here.
  </read_first>
  <action>
    Add one optional field to `CrmEventPayload`:

    `previous?: Record&lt;string, unknown&gt;` — the entity row as it existed immediately BEFORE this
    write, in the same casing as `data`.

    Resolving the delegated decision: it goes on the shared `CrmEventPayload` as optional, not on a
    narrower update-only payload type. Reasons to record in the doc comment: (a) creates legitimately
    have no `previous`, and optional says that honestly; (b) deletes emit `data === { id }` at all
    seven delete sites, so `previous` is the ONLY source of state there and a narrow "update-only"
    type would exclude the case that needs it most; (c) a second payload interface would force a
    parallel edit to `DealStageChangedPayload` and the 13-entry `CrmEventMap`.

    The doc comment must also state that `previous` is supplied by the writer (which already holds
    the pre-read row) and is never reconstructed by a subscriber, because a subscriber fires after
    the write.

    No other file changes in this task. Adding an optional field breaks no existing emit site.
  </action>
  <acceptance_criteria>
    - `grep -c "previous?:" src/lib/events/types.ts` returns `1`.
    - `npm run typecheck` exits `0` with no other file edited.
    - `npx vitest run src/lib/events` passes unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run src/lib/events</automated>
  </verify>
  <done>`CrmEventPayload.previous` exists, is optional, and every existing emit site still compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED): Write diff.test.ts covering both casings, the formula gate and the tombstone</name>
  <files>src/lib/audit/diff.test.ts</files>
  <read_first>
    - `src/lib/api/serialize.ts` lines 23-107 — `toIsoString`, `serializeOrganization`,
      `serializePerson`, `serializeDeal`, `serializeActivity`. The key maps are derived from these.
    - `.planning/phases/36-audit-log/36-PATTERNS.md` § The five snake_case emit sites (verified,
      exhaustive) — **PATTERNS corrects RESEARCH here**: only `serializePerson` (`:48-63`) and
      `serializeDeal` (`:70-86`) ever reach a `crmBus.emit`. `serializeActivity` and
      `serializeOrganization` never do. Build TWO key maps, not four.
    - `src/lib/formula-helpers.ts` around `:144` — `isFormulaWrapper`, including its
      `!Array.isArray` guard, which is why a `multi_select` array is not mistaken for a wrapper.
    - `.planning/phases/36-audit-log/36-VALIDATION.md` — the five AUDIT-01 rows whose Automated
      Commands are `npx vitest run src/lib/audit/diff.test.ts` with `-t` selectors `formula`,
      `multi_select`, `normalise`, `create|delete`.
  </read_first>
  <behavior>
    - Native columns: a payload whose `previous.title` differs from `data.title` yields exactly
      `{ title: { from, to } }` and nothing else.
    - `IGNORED_COLUMNS`: `updatedAt`, `createdAt`, `id` and `position` never appear in the map even
      when they differ. (`updatedAt` differs on literally every write; `position` differs on every
      kanban reorder.)
    - Formula exclusion — test name contains `formula`: a `customFields` key whose value is
      `{ formula: true, value: 42 }` on EITHER side is dropped. Both directions: wrapped→wrapped
      with a different `value`, and unwrapped→wrapped (a definition flipped to formula-typed).
    - multi_select guard — test name contains `multi_select`: a `customFields` value that is an
      ARRAY is diffed normally and is not treated as a formula wrapper.
    - Key normalisation — test name contains `normalise`: a `person.updated` payload with
      camelCase `previous` (`firstName`, `organizationId`, `customFields`) and snake_case `data`
      (`first_name`, `organization_id`, `custom_fields`, plus the computed `full_name`) where only
      the email changed yields a **one-key** map. Assert the map has exactly one key and that
      `full_name` is absent, because it is computed rather than stored.
    - The same case for `deal.created` via `serializeDeal` keys.
    - `changedFields` is NEVER read as the diff's key source: a payload whose `changedFields` is
      `["firstName"]` while the actual difference is on `phone` still reports `phone`. (In the
      people update route the same payload literal disagrees with itself.)
    - Create — test name matches `create|delete`: `action: "created"` with no `previous` yields one
      entry per non-ignored key in `data`, each with `from: undefined`.
    - Delete: `action: "deleted"` with `data === { id }` and a full `previous` yields the tombstone
      built from `previous` — one entry per non-ignored key with `to: undefined`. This is the case
      that fails silently if the implementation diffs `data` first.
    - Empty update: identical `previous` and `data` yield `{}`.
    - Deep equality: two `customFields` objects with the same keys in a DIFFERENT insertion order
      compare equal. A `JSON.stringify` comparison would report every custom field as changed on
      every save; assert this explicitly so a later refactor to stringify goes red.
  </behavior>
  <action>
    Create `src/lib/audit/diff.test.ts`. Pure test, no `vi.mock` — the module under test imports only
    `isFormulaWrapper` and types.

    Build a small `payload(overrides)` helper returning a valid `CrmEventPayload` so each case reads
    as its own deviation rather than as a 9-field literal.

    Run it; confirm red because `./diff` does not exist. Commit RED:
    `test(36-02): add failing pure diff cases for casing, formulas and tombstones`.
  </action>
  <acceptance_criteria>
    - `src/lib/audit/diff.test.ts` exists with at least 11 `it(` blocks.
    - Test names exist matching each of `/formula/`, `/multi_select/`, `/normalise/`, `/create/`,
      `/delete/`.
    - `grep -c "vi.mock" src/lib/audit/diff.test.ts` returns `0`.
    - `npx vitest run src/lib/audit/diff.test.ts` exits non-zero.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/audit/diff.test.ts; test $? -ne 0</automated>
  </verify>
  <done>Every behaviour above has a named failing test and the file mocks nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): Implement the pure diff, the two key maps and the formula gate</name>
  <files>src/lib/audit/diff.ts</files>
  <read_first>
    - `src/lib/audit/diff.test.ts` from Task 2.
    - `.planning/phases/36-audit-log/36-RESEARCH.md` § Architecture Patterns → Pattern 3 — the
      reference implementation shape for `buildChanges`.
    - `src/lib/api/serialize.ts:48-63` and `:70-86` — the two serializers whose key names the maps
      invert.
    - `src/lib/formula-helpers.ts` module header — the posture this module copies: no database
      import, no bus import, no clock.
  </read_first>
  <action>
    Create `src/lib/audit/diff.ts` exporting `IGNORED_COLUMNS`, `normaliseEventData` and
    `buildChanges`.

    `normaliseEventData(entity, obj)`: for `entity === "person"` and `entity === "deal"`, map the
    serializer's snake_case key names back to the column names (`first_name`→`firstName`,
    `organization_id`→`organizationId`, `custom_fields`→`customFields`, `expected_close_date`→
    `expectedCloseDate`, `person_id`→`personId`, `stage_id`→`stageId`, `owner_id`→`ownerId`, and so
    on for every key the two serializers emit). Drop `full_name` entirely — it is computed in
    `serializePerson`, never stored, so it can only ever be noise. Keys already in column form pass
    through untouched, which is what makes it safe to apply to both sides of every payload.

    For `entity === "organization"` and `entity === "activity"` the function is the identity. Write a
    comment saying the other two serializers are deliberately absent because they never reach an
    emit site (36-PATTERNS § Corrections to RESEARCH), or a later reader will "fix" the omission.

    `toIsoString` turns a `Date` into a string on the snake_case sites only, so a date column can
    disagree in TYPE as well as key. `IGNORED_COLUMNS` covering `createdAt`/`updatedAt` closes most
    of it; for `expectedCloseDate`, `dueDate` and `completedAt` the normaliser must also coerce a
    `Date` to its ISO string so both sides compare in one representation.

    `IGNORED_COLUMNS` is a `Set` of `id`, `createdAt`, `updatedAt`, `position`.

    `buildChanges(payload)`: normalise both sides; walk the union of native keys skipping
    `IGNORED_COLUMNS` and `customFields`; then walk the union of `customFields` keys, skipping any
    key where `isFormulaWrapper` is true on EITHER side, emitting under the key
    `` `customFields.${key}` ``.

    Deep equality: use `isDeepStrictEqual` from `node:util`, or a small explicit recursive compare.
    Do NOT compare with `JSON.stringify` — JSONB round-trip key order is not stable and every save
    would report every custom field as changed.

    Delete handling: when `action === "deleted"`, `data` is `{ id }` at all seven delete sites, so
    the map is built from `previous` alone with `to: undefined`. Do not let the generic union walk
    produce a one-key `{id: …}` map for a delete.

    Run green. Commit GREEN: `feat(36-02): add pure audit diff with key normalisation and formula gate`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/audit/diff.test.ts` — all cases pass.
    - `grep -vE '^\s*(\*|//|/\*)' src/lib/audit/diff.ts | grep -cE '"@/db"|@/lib/events/bus'` returns `0`.
    - `grep -c "isFormulaWrapper" src/lib/audit/diff.ts` is at least `1`.
    - `grep -c "JSON.stringify" src/lib/audit/diff.ts` returns `0`.
    - `npm run typecheck` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/audit/diff.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>A one-field REST edit produces a one-key change map, formula values never appear, and a delete produces a tombstone from the before-row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event payload → stored `changes` blob | Values that reach durable storage; former values become data-at-rest with no expiry other than retention. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-08 | Denial of Service | `buildChanges` output size | mitigate | The formula gate drops the largest derived keys before they reach the blob; measured worst realistic case ~10 kB/row. No per-row cap in this phase (36-RESEARCH A10); the retention window bounds the aggregate. |
| T-36-11 | Information Disclosure | stored former values | accept | Former values of corrected fields become durable. The retention window is the only expiry and is the declared control (V8). Recorded here so it is a decision, not an accident. |
| T-36-14 | Information Disclosure | a CRM field holding a credential | accept | No CRM field is a designated secret today (workflow secrets are redacted by `serializeWorkflow` and workflows are out of scope). Re-check if a later phase adds one. |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added this phase. |
</threat_model>

<verification>
- `npx vitest run src/lib/audit/diff.test.ts` — all green
- `npm run typecheck` — exit 0
- `npx vitest run src/lib/events src/app/api/v1` — no regression from the optional payload field
</verification>

<success_criteria>
- `CrmEventPayload.previous` exists and is optional
- `buildChanges` yields a one-key map for a one-field snake_case REST edit
- Formula-wrapped values on either side are excluded; arrays are not
- A delete produces a tombstone built from `previous`
</success_criteria>

<output>
Create `.planning/phases/36-audit-log/36-02-SUMMARY.md` when done
</output>
