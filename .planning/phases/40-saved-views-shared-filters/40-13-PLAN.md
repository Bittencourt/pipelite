---
phase: 40-saved-views-shared-filters
plan: 13
type: execute
wave: 6
depends_on: ["40-01", "40-05", "40-10", "40-18"]
files_modified:
  - src/app/activities/page.tsx
  - src/app/activities/activities-client.tsx
  - src/app/activities/activity-filters.tsx
  - src/app/activities/actions.ts
  - src/app/activities/__tests__/get-activities-filters.test.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03]

must_haves:
  truths:
    - "A saved activities view carrying status=overdue actually narrows the list, not only the chip row"
    - "A saved activities view carrying a date range actually narrows the list"
    - "The bar renders only in the list tab, never in the calendar tab"
    - "Clearing the last filter chip or emptying the search box does not bounce the user into their default view"
    - "Changing a filter inside an open view keeps that view named in the URL"
  artifacts:
    - path: "src/app/activities/actions.ts"
      provides: "getActivities honours status and the date range as SQL predicates"
      contains: "isNotNull"
    - path: "src/app/activities/activities-client.tsx"
      provides: "the bar inside <TabsContent value=\"list\"> above <ActivityFilters>"
      contains: "SavedViewsBar"
  key_links:
    - from: "src/app/activities/activity-filters.tsx"
      to: "src/lib/views/url-params.ts"
      via: "withViewEscape on all THREE router.push call sites"
      pattern: "withViewEscape"
    - from: "src/app/activities/page.tsx"
      to: "src/app/activities/actions.ts"
      via: "status and dateFrom/dateTo threaded into getActivities"
      pattern: "dateFrom"
---

<objective>
Mount the bar in the `/activities` list tab, escape **three** navigations (not two), fix the search
input, and make `status` and the date range into real SQL predicates so a saved view on this surface
narrows what it claims to narrow.

Purpose: `/activities` is the richest filter surface and the one where CONTEXT nominated the wrong
host. It is also the surface where the export guard has a hole that neither CONTEXT nor UI-SPEC
checked: **`hasExportableFilter("activity", { status: "overdue" }) === true`, but the list applies no
`status` predicate for `pending`/`overdue` and no date range at all** — so a "filter" that narrows
nothing would authorize an export of all 79,022 live activities. Plan 40-07 fixed the export side;
this plan fixes the list side so the two agree.
Output: four edited files and one new test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-CONTEXT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md

@src/app/activities/page.tsx
@src/app/activities/activities-client.tsx
@src/app/activities/activity-filters.tsx
@src/app/activities/actions.ts
@src/lib/views/url-params.ts
</context>

<interfaces>
<!-- Read this session. Re-locate the lines rather than trusting the numbers. -->

`src/app/activities/activity-filters.tsx` — **THREE** list-route `router.push` sites, not two:
```
72:  setFilter:          router.push(`${pathname}?${params.toString()}`)   // last chip removed -> `?` = BARE
77:  clearAll:           router.push(pathname)                            // BARE
91:  handleSearchChange: router.push(`${pathname}?${params.toString()}`)   // search emptied, no other filter -> `?` = BARE
131: defaultValue={search}                                                // the stale input
126: <div className="flex items-center gap-2">                            // the row measured EXACTLY full at 241px
136: <Popover> …                                                          // measured NON-CONFORMING at 320x640
```
**40-UI-SPEC's escape-param table lists only sites 1 and 2 for this file. Site 3 (`handleSearchChange`)
was found during planning and is the same defect: emptying the search box when it was the only filter
produces `${pathname}?` with an empty query, which Next reads as no params, which the new redirect
guard recaptures.** The spec's "six call sites" is therefore an undercount; there are seven, and
plan 40-14's gate is written exhaustively rather than against that list.

`src/app/activities/activities-client.tsx`:
```
177:  handleLoadMore: router.push(`/activities?${sp.toString()}`)   // always carries page -> non-bare, escape anyway
253,258: <div className="h-2 w-2 rounded-full bg-green-500" /> and bg-amber-500   // K-2 ANTI-PATTERN, out of scope
265-275: <Tabs defaultValue="list"><TabsList> list | calendar
277-284: <TabsContent value="list"><div className="space-y-4"><ActivityFilters …/>   // <- mount ABOVE ActivityFilters
306:  router.push("/activities")                                     // no-results "Clear filters" -> BARE
```

`src/app/activities/page.tsx`:
```
51-60: searchParams: Promise<{ type?, owner?, assignee?, status?, dateFrom?, dateTo?, search?, page? }>
73-100: builds `filters` for getActivities — typeId, ownerId, assigneeId, completed, search, limit
        ** `status` maps ONLY when === "completed"; `dateFrom`/`dateTo` are NEVER passed **
215-222: activeFilters = { type, owner, assignee, status, dateFrom, dateTo } — for the CHIP ROW only
230-244: <ActivitiesClient … activeFilters hasMore search currentPage />
```

`getActivities` in `src/app/activities/actions.ts` — measured defects:
```
if (filters?.completed === true) {
  conditions.push(isNull(activities.deletedAt))   // a NO-OP duplicate, with a comment admitting it
}
…
limit: filters?.limit,
…
// then filters completion in JS, AFTER the limit was applied
if (filters?.completed !== undefined) filteredResults = result.filter(a => …)
```
So `?status=completed` with `limit: 51` returns fewer than 50 completed rows even when more exist,
and `?status=pending` / `?status=overdue` / any date range narrow nothing at all.

<!-- Measured in the live database this session. -->
activities **79,022** live · **4,165** pending · **4,151** overdue · 4 activity types

M-2, measured: the `/activities` filter row is **EXACTLY full at 241px** — search input 147 + 8px gap
+ "Filters" button 86 = 241. Zero slack in en-US.
M-5, measured: that row's `PopoverContent` renders **388px tall at `top: -41`** in a 640px viewport,
with `max-height: none` and `overflow-y: visible`, because `popover.tsx:33` never consumes
`--radix-popover-content-available-height` (which Radix computed as 347px). It is **already
non-conforming**, pre-existing, and out of scope — but it is why nothing in this phase goes inside it.
M-9, measured: the search `<Input>` does not re-sync when the `search` param changes.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: status and the date range become real predicates</name>
  <files>src/app/activities/actions.ts, src/app/activities/page.tsx, src/app/activities/__tests__/get-activities-filters.test.ts</files>
  <behavior>
    Write `src/app/activities/__tests__/get-activities-filters.test.ts` first. It is a parsed-source
    gate over `getActivities`'s extracted function body (`readStrippedSource` + brace matching), not a
    db test — there is no jsdom here and mocking Drizzle's builder proves nothing:

      - the body contains a `filters?.status` guard, and references `isNotNull` and `completedAt`
      - the body contains `filters?.dateFrom` and `filters?.dateTo` guards referencing `dueDate`
      - the body contains **zero** occurrences of `.filter(` applied to the query result — the
        post-fetch JS completion filter is gone. Assert on the extracted body; put in the failure
        message that a post-fetch filter combined with `limit` returns fewer rows than exist.
      - the body contains **zero** `conditions.push(isNull(activities.deletedAt))` beyond the first
        (the no-op duplicate is gone). Count occurrences in the extracted body and assert exactly 1.

    Then a live-database assertion if the `*.db.test.ts` harness is usable (`npm run test:db` runs a
    separate vitest project against `pipelite_dedup_test`, provisioned from a dump by
    `scripts/dedup-db-test-setup.sh`; `drizzle-kit migrate` cannot build a fresh database — an early
    migration alters `import_sessions` before any migration creates it). If it is usable, add
    `src/app/activities/__tests__/get-activities-filters.db.test.ts` asserting
    `status: "overdue"` returns a count strictly between 0 and the total. If it is not usable within a
    reasonable effort, say so in the summary and record a manual `psql` count instead — do NOT weaken
    the parsed gate to compensate.

    NEGATIVE PROOF, TO BE RUN: delete the `filters?.dateFrom` guard, confirm exactly one failure
    naming `dateFrom`, restore. Record the failure text.
  </behavior>
  <action>
    In `getActivities` (`src/app/activities/actions.ts`), widen the filter parameter to
    `{ …existing, status?: string, dateFrom?: string, dateTo?: string }` and:

      - DELETE the `if (filters?.completed === true) conditions.push(isNull(activities.deletedAt))`
        no-op and the post-fetch `filteredResults` block. Replace with a real `status` predicate:
        `"completed"` → `isNotNull(activities.completedAt)`;
        `"pending"` → `isNull(activities.completedAt)`;
        `"overdue"` → `and(isNull(activities.completedAt), lt(activities.dueDate, new Date()))`;
        anything else → no predicate. Keep `completed?: boolean` in the signature and map it to the
        same predicate, so no existing caller breaks — check for other callers first
        (`grep` for `getActivities(` across `src/`) and record what you find.
      - add `dateFrom` → `gte(activities.dueDate, new Date(filters.dateFrom))` and `dateTo` →
        `lte(activities.dueDate, new Date(filters.dateTo))`, mirroring `fetchActivities` in
        `src/lib/export/formatters.ts` so the list and the export agree.
      - import `isNotNull` and `lt` from `drizzle-orm` if absent.

    In `src/app/activities/page.tsx`, thread the three params through:
    `if (params.status) filters.status = params.status`, and the same for `dateFrom` / `dateTo`.
    **Leave `activeFilters` exactly as it is** — it feeds the chip row and already reads all six.

    WRITE THE JUSTIFICATION, because Decision 1 says this phase adds NO new filters and a reader will
    and should ask:

    > This is not a new filter. `status`, `dateFrom` and `dateTo` are already in the URL contract
    > (`page.tsx:51-60`), already written by `activity-filters.tsx`, and already rendered as removable
    > chips. They simply never reached the query. Leaving them broken makes criterion 1 false on this
    > surface — a saved view would restore a chip that filters nothing — which is the same class of
    > defect as the stale search input that CONTEXT amendment A4 and UI-SPEC B-6 already ruled in
    > scope. It also leaves a live export hole: `hasExportableFilter("activity", { status: "overdue" })`
    > is `true`, so an ineffective filter would authorize an export of all 79,022 live activities,
    > which is exactly what the E-2 guard exists to prevent. And after plan 40-07 the export DOES
    > filter by date and status, so leaving the list alone would make the export narrower than the
    > list it claims to match.

    Also fix the `limit` interaction while you are here: with real predicates the `limit` is applied
    to already-narrowed rows, so `hasMore` becomes correct. Note that in the summary.
  </action>
  <verify>
    <automated>npx vitest run src/app/activities/ && npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>RED recorded, then green. All other `getActivities(` callers were enumerated and none broke. The negative proof was RUN and recorded. Live counts recorded (expected ~4,151 overdue of 79,022).</done>
</task>

<task type="auto">
  <name>Task 2: The default-view redirect and the bar props</name>
  <files>src/app/activities/page.tsx</files>
  <action>
    After `params = await searchParams` and after the `auth()` gate, before the `Promise.all`:
    ```
    if (Object.keys(params).length === 0) {
      const target = await resolveDefaultViewRedirect("activity", session.user)
      if (target) redirect(`/activities${target}`)
    }
    ```
    Same U-2 comment as the other three pages. `redirect()` throws; keep it out of any try/catch, and
    import it if absent.

    Add `resolveSavedViewsBarProps({ entityType: "activity", viewer: session.user, rawSearchParams:
    params })` as an additional entry in the existing six-read `Promise.all`, and pass the result to
    `<ActivitiesClient>` as one `viewsBar` prop. Extend that component's props interface.

    Do NOT touch: the two separate `users` queries (the comment at 123-131 explains why widening
    either would silently drop options from two existing controls), `retentionDays`'s un-defaulted
    pass-through, or `activeFilters`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>The redirect fires only on a params-free URL; the bar props ride in the existing `Promise.all`. Both `users` queries unchanged. typecheck 0, lint 0 errors.</done>
</task>

<task type="auto">
  <name>Task 3: Mount the bar, escape four navigations, fix the input</name>
  <files>src/app/activities/activities-client.tsx, src/app/activities/activity-filters.tsx</files>
  <action>
    **PRECONDITION CHANGED BY PLAN 40-18 — read before editing the writers.** The URL now carries
    `?view=<id>` naming the open view, and `withViewEscape` PRESERVES it whenever a saveable filter
    survives. Consequences for this plan, and they are all "do nothing extra":

      - **No `view=` writer is added here.** These files change filters, never the selection. Only the
        bar mints or clears a selection.
      - **No call-site shape change either.** Both writers in this file already build their
        `URLSearchParams` from `searchParams.toString()`, so `view` is already in the clone and
        `withViewEscape` carries it through untouched. That is the whole reason the preservation rule
        lives inside the helper rather than being threaded as a prop through six files.
      - **Do not add a `selectedViewId` prop to these components.** Plan 40-11 does pass one to the two
        `data-table.tsx` files, because those build their query strings from PROPS and have no
        `searchParams` to preserve from. This file does, so it needs nothing. The asymmetry is between
        where the two groups get their params, not a gap.
      - Clearing the last filter still lands on `?view=none`: no filter survives, so the escape wins and
        the selection goes with it (U-2 — a selection with no filters is not a state).

    (a) MOUNT, in `activities-client.tsx`, **inside `<TabsContent value="list">`, as the first child of
    its `<div className="space-y-4">`, above `<ActivityFilters>`**:
    ```
    <SavedViewsBar {...viewsBar} />
    ```
    Write the placement rationale, all three parts:
      - **Rule P-1 — only the `list` tab.** The `calendar` tab reads no filter params, so a view picker
        there would offer to restore filters the surface cannot apply. Mounting it outside
        `<TabsContent>` would put it in both.
      - **Not inside `activity-filters.tsx`, despite CONTEXT nominating it as "the richest filter
        toolbar and the natural host".** That nomination is declined on measured grounds (amendment
        A3): its row is EXACTLY full at 241px (M-2) and its `PopoverContent` renders 388px into a 347px
        slot with 41px clipped off the top of the viewport (M-5). Mounting the phase's primary
        affordance inside a container that already clips content off screen would reproduce F-39-07 on
        purpose.
      - Its own row; not sticky, not fixed (K-8) — `bulk-action-bar.tsx` already owns one fixed bar on
        this page.

    (b) `activities-client.tsx`'s `bg-green-500` / `bg-amber-500` stat dots at 253 and 258 are the K-2
    anti-pattern reference **in this phase's own edit path**. Do not copy them into the bar, and do not
    fix them here — they are pre-existing and out of scope. Say both halves in a comment so a reviewer
    knows the omission is deliberate.

    (c) ESCAPE, in `activities-client.tsx`, both list-route pushes:
      - `handleLoadMore` (177): it always sets `page`, so it is non-bare today — route it through
        `withViewEscape("activity", sp)` anyway, so the file has one rule rather than two and plan
        40-14's exhaustive gate does not need a second exemption.
      - the no-results "Clear filters" button (306): `router.push("/activities")` is BARE and is the
        clearest hijack in the app — a user pressing "Clear filters" would be dropped straight back
        into their default view's filters. Replace with
        `router.push(\`/activities?${withViewEscape("activity", new URLSearchParams())}\`)` →
        `?view=none`.

    (d) ESCAPE, in `activity-filters.tsx`, **all THREE** `router.push` sites — `setFilter` (72),
    `clearAll` (77) and `handleSearchChange` (91):
    ```
    router.push(`${pathname}?${withViewEscape("activity", params)}`)
    ```
    `clearAll` becomes `router.push(\`${pathname}?${withViewEscape("activity", new URLSearchParams())}\`)`.

    **Site 3 is not in UI-SPEC's table and must still be escaped.** Write why in a comment: emptying
    the search box when it was the last filter produces `${pathname}?` with an empty query, Next reads
    that as no params, and the new redirect guard recaptures it — identical in effect to `clearAll`.
    The spec's "six call sites" is an undercount by one; there are seven across the four surfaces.

    (e) `key={search}` on the search `<Input>` at 131, keeping `defaultValue={search}` (B-6). Same
    reason as the other two surfaces: `defaultValue` is ignored after mount and app-router navigation
    re-renders without remounting; the fix is `key`, **not** a controlled input, because this is a
    300ms-debounced writer and a controlled value fed from the URL fights its own debounce on every
    keystroke.

    (f) `activity-filters.tsx`'s one hardcoded literal (`placeholder="Search activities..."`) stays
    exactly as it is. Do not translate it, do not add another.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run && npm test</automated>
  </verify>
  <done>The bar renders only inside the list tab. Four navigations escaped (2 in `activities-client.tsx`, 3 in `activity-filters.tsx` — five total in this task). `key={search}` present. No new English literal; the two stat dots untouched. typecheck 0, lint 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `?status=` / `?dateFrom=` → SQL | three params that reached only a chip row now reach a WHERE clause over 79,022 rows |
| filter clear → the redirect guard | four navigations on this surface can produce a bare or empty query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-58 | Elevation of privilege | `hasExportableFilter("activity", …)` | mitigate | making `status` and the date range real predicates is what stops an ineffective filter authorizing an export of all 79,022 activities. Plan 40-07 added the export-side predicates; without this plan the two disagree and the guard is satisfiable by a control that narrows nothing |
| T-40-59 | Tampering | `new Date(filters.dateFrom)` | mitigate | the value is validated by plan 40-05's `validateStoredFilters` (`/^\d{4}-\d{2}-\d{2}$/` + `Date.parse`) before it reaches a view; and `pickFilterParams` bounds its length. An `Invalid Date` in a `gte()` is a 500 on a route with no `error.tsx` (M-14) |
| T-40-60 | Tampering | the three `activity-filters.tsx` pushes | mitigate | all three routed through `withViewEscape`, including the one UI-SPEC's table omits |
| T-40-61 | Information disclosure | the no-results "Clear filters" button | mitigate | escaped to `?view=none`, so a user trying to leave a filter is not silently returned to their default view's filters |
| T-40-62 | Denial of service (functional) | the `/activities` filter `Popover` | accept | measured non-conforming at 320x640 (M-5, 388px at `top: -41`). Pre-existing, recorded in BACKLOG.md, and out of scope: fixing `popover.tsx` touches every popover in the app and needs its own reachability suite. This phase's obligation is only to not ADD to it, which O-2 discharges by putting nothing in a `Popover` |
| T-40-63 | Repudiation | the removed post-fetch completion filter | mitigate | replacing it with a SQL predicate also fixes a silent under-count: `limit` was applied BEFORE the JS filter, so `?status=completed` returned fewer rows than exist |
| T-40-SC | Tampering | npm installs | mitigate | this plan installs nothing |
</threat_model>

<verification>
- `npx vitest run src/app/activities/` green; full `npm test` green
- `npm run typecheck` 0, `npm run lint` 0 errors
- exactly 3 `withViewEscape` call sites in `activity-filters.tsx`, exactly 2 in `activities-client.tsx`
- `git diff` shows the two `bg-*-500` stat dots and the `placeholder=` literal unchanged
- the `dateFrom` negative proof was RUN and recorded
- live counts recorded for `status=overdue` before and after
</verification>

<success_criteria>
- A saved activities view carrying `status` or a date range narrows the list, not only the chip row
- The list side and the export side apply the same predicates for the same keys
- The bar renders in the list tab only
- All three of this file's navigations escape, including the one the spec's table omitted
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-13-SUMMARY.md` when done. Record every
`getActivities(` caller you found, the negative-proof text, the before/after live counts, and the final
line numbers of all five escaped navigations so plan 40-14's gate is written against reality.
</output>
