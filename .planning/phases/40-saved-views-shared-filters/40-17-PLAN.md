---
phase: 40-saved-views-shared-filters
plan: 17
type: execute
wave: 9
depends_on: ["40-16"]
files_modified:
  - .planning/BACKLOG.md
autonomous: false
requirements: [VIEW-01, VIEW-02, VIEW-03]

must_haves:
  truths:
    - "Nothing this phase renders is a light-mode island, and none of it carries text-primary as a text colour"
    - "A human has driven all four surfaces in a real browser and confirmed the four success criteria"
    - "Everything this phase measured and declined is written down rather than rediscovered later"
  artifacts:
    - path: ".planning/BACKLOG.md"
      provides: "the Phase 40 findings, including the locale-parity figure correction"
      contains: "Found in Phase 40"
  key_links:
    - from: ".planning/BACKLOG.md"
      to: "src/messages/locale-parity.test.ts"
      via: "the corrected pinned key-set sizes, read from the file rather than from a document"
      pattern: "REQUIRED_DEDUP_KEYS"
---

<objective>
The dark-mode pass, the backlog entry for everything this phase measured and declined, and a human in
front of the four surfaces.

Purpose: **a passing source gate is how an unreachable feature ships.** Plan 39-14's gates were green
while the organization duplicate warning could not fire from any surface, and only a human driving a
browser found it. Criterion 2 is a claim about what a different person sees; criteria 1, 3 and 4 are
claims about what happens when somebody presses something. Waves 1-8 established that the code is
shaped correctly. This plan establishes that it works.
Output: a dark-mode measurement record, a BACKLOG.md entry, and an approved checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-CONTEXT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md
@.planning/BACKLOG.md
</context>

<interfaces>
<!-- Verified this session. -->

Dark mode has been reachable since Phase 45: `ThemeProvider` is mounted in `src/app/layout.tsx`
(`attribute="class"`, `defaultTheme="system"`, `enableSystem`), `UserMenu` carries a three-value
`DropdownMenuRadioGroup`, and dark is confirmed by `documentElement.className` containing `dark`.
Phase 45's lesson: check for a light-mode ISLAND, not for a dark page — a screenshot glance misses one.

F-39-06: `text-primary` links measure `lab(90.952)` against a `lab(98.26)` body in dark mode. This
phase renders **zero `text-primary` text** by design; the pass confirms the ABSENCE rather than
inferring it. The only permitted `text-primary` is the `Loader2`, identified by also carrying
`animate-spin`.

**MEASURED DISCREPANCY — the correction this plan must record.** `40-UI-SPEC.md` M-13 and
`40-CONTEXT.md` A5 both state figures they claim to have parsed from
`src/messages/locale-parity.test.ts`. Parsed from that file during planning, they are wrong:

| Constant | Documents claim | Actually in the file |
|---|---|---|
| `REQUIRED_DEDUP_KEYS` | 83 | **80** |
| `REQUIRED_AUDIT_KEYS` | 88 | **86** |
| `REQUIRED_BULK_KEYS` | 47 | **46** |
| `REQUIRED_TRASH_KEYS` | 66 | **63** |

The file also carries `it("the checked-in dedup contract still lists exactly 80 keys")` with
`expect(REQUIRED_DEDUP_KEYS).toHaveLength(80)` — so a future plan that copies the documents and
"corrects" 80 to 83 breaks the suite. `ICU_PLURAL_KEYS` = 11 and `IDENTICAL_TRANSLATION_ALLOWED` = 3
were both correct. Re-read the file before writing the entry, and record what you find rather than
what this table says.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: V-40-10 — the dark-mode pass, measured not judged</name>
  <files>.planning/BACKLOG.md</files>
  <action>
    Drive a real Chromium in dark mode against the rebuilt container at http://localhost:3001, reusing
    the existing storageState so no password is handled. A temporary capture spec is fine; delete it
    afterwards and confirm `e2e/` is back to exactly the specs the phase created (Phase 45 did this and
    it is the right shape).

    Assert `documentElement.className` contains `dark` **before** measuring anything.

    Surfaces to visit, and what to record at each — `getComputedStyle` values, never impressions:
      1. the bar on `/organizations` with a `Modified` badge showing (navigate to a saved view's URL,
         then change one filter). Record the badge's foreground and background `lab()` values so a later
         phase has a number rather than an opinion.
      2. the open picker with a selected item and both group labels visible. Record the selected item's
         background and the `DropdownMenuLabel` foreground.
      3. the save dialog with an inline `views.save.nameTaken` error showing (submit a duplicate name).
         Record the error text's colour and confirm it resolves from `--destructive`.
      4. the manage dialog with one read-only row and one editable row. Record the
         `views.manage.readOnly` line's colour.
      5. the delete `AlertDialog`. Confirm the action button's colour comes from `--destructive` and is
         none of `text-red-600` / `bg-green-500` / `bg-amber-500` (K-2).
      6. the `views.degraded` notice. Confirm it is muted body text and that no element with
         `data-slot="alert"` exists on the page (C-40-4).

    Then the F-39-06 non-dependency check: enumerate every element inside the bar and both dialogs and
    assert **none** carries `text-primary` as a text colour, excluding any element that also carries
    `animate-spin`. Report the count of elements examined — an assertion over an empty set is vacuous,
    and this is exactly the shape of the Phase 39 assertion that was `false` forever by construction.

    Record every value in the summary. "Looks fine" is not a result.
  </action>
  <verify>
    <automated>ls e2e/*.spec.ts | sort && docker compose ps --format '{{.Service}} {{.State}}' | grep -q "app running"</automated>
  </verify>
  <done>Every observation is a recorded `getComputedStyle` value. The `text-primary` absence was asserted over a non-empty element set whose size is recorded. The temporary capture spec is deleted and `e2e/` contains exactly the phase's specs.</done>
</task>

<task type="auto">
  <name>Task 2: Write down what this phase measured and declined</name>
  <files>.planning/BACKLOG.md</files>
  <action>
    Append a `## Found in Phase 40 (Saved Views & Shared Filters)` section to `.planning/BACKLOG.md`,
    following the existing entries' shape: what it is, where it was found, what is already known, so a
    later phase does not rediscover it.

    Entries, each with its evidence:

    1. **The locale-parity pinned figures in `40-UI-SPEC.md` M-13 and `40-CONTEXT.md` A5 are wrong.**
       Re-read `src/messages/locale-parity.test.ts` and record the actual lengths of
       `REQUIRED_DEDUP_KEYS`, `REQUIRED_AUDIT_KEYS`, `REQUIRED_BULK_KEYS` and `REQUIRED_TRASH_KEYS`
       (measured at planning time as 80 / 86 / 46 / 63 against documented 83 / 88 / 47 / 66). Note the
       existing `toHaveLength(80)` test, and note that both documents claim to have parsed the file.
       The process lesson to record beside it: **verify a file contains what an assertion assumes** —
       the same lesson BACKLOG.md already records for a stale `import-wizard.tsx` reference that
       propagated into three documents while proving nothing.

    2. **`deal-filters.tsx` has roughly 20 hardcoded English literals and calls no `useTranslations`
       at all**, and `kanban-board.tsx` has five ("Pipeline:", "Select pipeline", "Add Deal",
       "No results match your filters", "Clear filters"). Named in UI-SPEC § Out of scope and
       deliberately declined by this phase, whose edits to both files are one-expression-scale. Needs a
       dedicated copy pass. Also note `organizations/data-table.tsx` and `people/data-table.tsx` carry
       their search placeholders and Add-buttons in English, and `activity-filters.tsx` carries one.

    3. **`/activities` accepted three filter params that narrowed nothing** — `status=pending`,
       `status=overdue` and any `dateFrom`/`dateTo` — while rendering removable chips for all of them,
       and `status=completed` was applied by a post-fetch JS filter AFTER `limit`, so it under-counted.
       Closed by plan 40-13 because leaving it made criterion 1 false and left the export guard
       satisfiable by a control that filters nothing. Record it as CLOSED with its cause, so nobody
       reintroduces the post-fetch filter shape.

    4. **UI-SPEC's escape-param table undercounts the call sites.** It lists six; there are seven bare
       or can-be-bare sites (the seventh is `activity-filters.tsx`'s `handleSearchChange`, which
       produces `${pathname}?` when the search box was the last filter) and seventeen list-route
       navigations in total across the six files. Plan 40-14's gate covers all seventeen. Record the
       final table from that plan's summary.

    5. **`popover.tsx` reachability** — confirm the existing entry carries M-5's `/activities` figures
       (Radix computed 347px available; the popover rendered 388px at `top: -41`) and M-6's `/deals`
       7px clearance. Add whichever is missing.

    6. **A soft-deleted user's private views are permanently unreachable by anyone** (CONTEXT A6,
       V-6). Six such users exist. Accepted — a saved view is a filter set with no record content —
       but it is a row that outlives every actor who can act on it. Record it so it is not discovered
       as a bug.

    7. **Criterion 4's deliberate `/deals` narrowing.** A deals view carrying only `pipeline` is
       saveable but not exportable, by design (E-2). Record it as a documented product limitation with
       its reason (25,195 deals scoped only by a board selector is the unbounded export
       38-CONTEXT.md:110-116 forbids, and Decision 2 replaced that admin gate with a guard), so a
       future reader does not "fix" it by widening `hasExportableFilter`.

    8. Anything else the sixteen prior summaries recorded as measured-and-declined. Read them.
  </action>
  <verify>
    <automated>grep -q "Found in Phase 40" .planning/BACKLOG.md && grep -q "REQUIRED_DEDUP_KEYS" .planning/BACKLOG.md</automated>
  </verify>
  <done>Eight entries written, each with its evidence. The locale figures were re-read from the file, not copied from this plan. Every prior summary was read for declined findings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the four success criteria</name>
  <files>none — this task changes nothing</files>
  <action>
    Present the verification script below to the user and STOP. Do not proceed, do not self-certify,
    and do not substitute an agent's browser run for the human's — plan 40-15 and 40-16 already did the
    agent-driven half. This checkpoint exists because 39-14's source gates were green while the feature
    could not fire from any surface, and a human found it.

    Before presenting, confirm the container is running the rebuilt image
    (`docker compose ps` shows `app running`, and the views bar is visible on `/organizations`), so the
    user is not asked to verify stale JavaScript. If a second account's credentials are needed for
    criterion 2 and none is available to the user, say so up front rather than letting them discover it
    at step 7 — the seeded e2e member exists and its password is in `E2E_MEMBER_PASSWORD`.
  </action>
  <what-built>
    Saved views on all four list pages: a `SavedViewsBar` with a picker and a save resolver, a
    save/update dialog, a manage dialog with share/default/delete, a per-user default that redirects,
    and a CSV export of what a view matches. Plus the `?view=none` escape threaded through thirteen
    navigations, the search-input resync on three surfaces, real `status`/date predicates on
    `/activities`, and the deleted-pipeline fallback on `/deals`.
  </what-built>
  <how-to-verify>
    The app is at **http://localhost:3001** and the image has been rebuilt. Sign in as yourself.

    **Criterion 1 — save and reopen.**
    1. Go to `/activities`. Set at least two filters (a type and a status, or a date range).
    2. The views bar should now show a "Save view" button instead of the sentence
       "Filter this list to save it as a view." Press it.
    3. Name it, leave it private, press Save. Expect a toast naming the view.
    4. Navigate away (click `/deals`), then come back to `/activities`. Open the picker and select the
       view. **Check three things: the list narrowed, the address bar carries the filters, and every
       filter chip AND the search box show what the URL says.**
    5. Change one filter. The picker trigger should gain a `Modified` badge and the button should read
       "Save changes".
    Repeat 1-4 on `/organizations` using the search box. **Expect it to feel thin — a view there stores
    one string, and that is Decision 1 accepted openly, not a defect.**

    **Criterion 2 — shared vs private.** Needs two accounts.
    6. As yourself, create one SHARED view and one PRIVATE view on `/organizations`.
    7. Sign in as the other user (a second browser profile or a private window). Open
       `/organizations`, open the picker, and open Manage views.
       - the shared view should appear under "Shared with me", with your name or email beside it
       - **the private view should appear NOWHERE — not in the picker, not in the manage dialog.**
         This holds even if the other account is an admin, and that is deliberate.
    8. As that other user, set your shared view as their default (the switch in Manage views), then
       open `/organizations` with a clean URL. They should land on your view's filters.
    9. As that other user, confirm they CANNOT rename, unshare or delete your view — the row should
       say "Only … or an admin can change this view."

    **Criterion 3 — default view.**
    10. Set one of your own views as default. Type `localhost:3001/activities` with nothing after it.
        You should land on the view's filters, **and the address bar should show them** — not a bare
        URL secretly filtering.
    11. Now open the picker and choose "All records". You should end on `?view=none` and see an
        unfiltered list. **Then clear the search box, and press "Clear filters" on a no-results list —
        you must NOT be bounced back into your default view.** This is the single most likely place for
        something to feel possessed; please try to break it.

    **Criterion 4 — export.**
    12. With a filtered `/activities` list, open the picker and choose "Export these results (CSV)".
        The bar should show "Preparing your file…" and a CSV should download. Open it and confirm the
        rows match what the list showed.
    13. On `/deals`, select ONLY a pipeline (no stage, no owner, no dates). The export item should be
        **disabled**, with the reason beside it: "Choose at least one filter first. An unfiltered
        export isn't allowed." **This is deliberate and narrows criterion 4** — a pipeline is a board
        selector, and 25,195 deals scoped only by board is the unbounded export Phase 38 forbids. Add a
        stage or an owner and the item should become available.

    **Mobile, if you can.** Narrow the window to about 320px wide (or use a phone). On each of the four
    pages: open the picker, scroll to its last item, open the save dialog and confirm the **Save button
    is reachable and pressable**, open Manage views and confirm you can reach the last row's Delete.
    Press Escape from each. This is the check a green automated gate has certified before and been
    wrong about.

    **Dark mode.** Switch to dark in the user menu and look at the bar with a `Modified` badge, both
    dialogs, and the delete confirmation. Nothing should be a light-mode island; nothing should be
    unreadable.
  </how-to-verify>
  <verify>
    <human-check>The user types "approved", or describes what they saw.</human-check>
  </verify>
  <done>The user has responded. Their words are recorded verbatim in the summary. Any finding becomes either a fix in this phase or a recorded gap — never a silent pass.</done>
  <resume-signal>Type "approved", or describe what you saw. Anything that felt wrong — even
  "the wording confused me" — is worth writing down; it is cheaper now than after the milestone.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a green suite → a shipped feature | 39-14's gates passed while the feature could not fire from any surface |
| a document's claim → a file's contents | two phase-40 artifacts state locale figures they claim to have measured, and all four are wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-79 | Repudiation | dark mode | mitigate | colour values recorded via `getComputedStyle` rather than judged by eye, and the `text-primary` ABSENCE asserted over an element set whose size is reported — an assertion over an empty set is vacuous, which is the shape of the Phase 39 assertion that was `false` forever |
| T-40-81 | Repudiation | the whole phase | mitigate | a human drives all four surfaces and both accounts. No agent run and no source gate substitutes for it, and the checkpoint is `gate="blocking"` |
| T-40-82 | Tampering | future plans copying these documents | mitigate | the locale-figure correction is written into BACKLOG.md with the real numbers and with the name of the existing `toHaveLength(80)` test, so a future "correction" to 83 is anticipated rather than discovered |
| T-40-83 | Information disclosure | the checkpoint script | mitigate | the second account is the seeded e2e member and its password lives in `E2E_MEMBER_PASSWORD`. **Never write a password into a plan, a summary, a commit or a command line** — this repo has leaked one to a public remote once |
| T-40-84 | Spoofing | stale JavaScript | mitigate | the container is confirmed running the rebuilt image and the bar confirmed visible before the user is asked to verify anything |
| T-40-SC | Tampering | npm installs | mitigate | this plan installs nothing |
</threat_model>

<verification>
- dark-mode colour values recorded for all six surfaces; `text-primary` absence asserted over a
  non-empty, counted element set
- the temporary capture spec deleted; `e2e/` contains exactly the phase's specs
- BACKLOG.md carries eight Phase 40 entries including the locale-figure correction
- the human checkpoint is APPROVED, or every finding is recorded as a gap for `--gaps` closure
</verification>

<success_criteria>
- All four ROADMAP success criteria observed by a human in a real browser, with criterion 4's
  deliberate `/deals` narrowing understood rather than reported as a bug
- Nothing this phase renders is a light-mode island and none of it carries `text-primary` text
- Everything measured and declined is written down
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-17-SUMMARY.md` when done. Record the human's
verbatim response, every dark-mode colour value with the element count examined, and any gap surfaced.
</output>
