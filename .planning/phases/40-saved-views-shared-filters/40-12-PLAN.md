---
phase: 40-saved-views-shared-filters
plan: 12
type: execute
wave: 6
depends_on: ["40-01", "40-05", "40-10", "40-18"]
files_modified:
  - src/app/deals/page.tsx
  - src/app/deals/kanban-board.tsx
  - src/app/deals/deal-filters.tsx
autonomous: true
requirements: [VIEW-01, VIEW-02]

must_haves:
  truths:
    - "A saved deals view pointing at a deleted pipeline renders the default board, not the pipeline-not-found page"
    - "The degraded notice appears when a stored pipeline, stage or owner no longer exists, and the board still renders"
    - "Clearing all deal filters with no pipeline set does not bounce the user into their default view"
    - "The views bar renders on /deals even when only one pipeline exists"
    - "Changing a filter inside an open view keeps that view named in the URL"
  artifacts:
    - path: "src/app/deals/page.tsx"
      provides: "the pipeline fallback, the default-view redirect, the eight bar props"
      contains: "resolveSavedViewsBarProps"
    - path: "src/app/deals/kanban-board.tsx"
      provides: "the bar between the pipeline row and <DealFilters>"
      contains: "SavedViewsBar"
  key_links:
    - from: "src/app/deals/deal-filters.tsx"
      to: "src/lib/views/url-params.ts"
      via: "withViewEscape on both router.replace call sites"
      pattern: "withViewEscape"
    - from: "src/app/deals/page.tsx"
      to: "allPipelines"
      via: "an unknown ?pipeline= falls back to the default board instead of pipelineNotFound"
      pattern: "isDefault"
---

<objective>
Mount the bar on `/deals` between the pipeline row and the filter row, make a saved view's dead
pipeline fall back to the default board, and escape both of `deal-filters.tsx`'s navigations.

Purpose: Decision 4 requires a deals view to carry its `pipeline`, because `pipeline` selects which
kanban board renders and a deals view without it is not reproducible. The consequence CONTEXT states
explicitly: a view pointing at a deleted pipeline must fall back to the default board rather than the
`pipelineNotFound` page it renders today (`deals/page.tsx:87`). With 11 live pipelines, that is a
reachable case, not a hypothetical.
Output: three edited files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-CONTEXT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md

@src/app/deals/page.tsx
@src/app/deals/kanban-board.tsx
@src/app/deals/deal-filters.tsx
@src/lib/views/resolve.ts
@src/lib/views/url-params.ts
</context>

<interfaces>
<!-- Read this session. Re-locate the lines rather than trusting the numbers. -->

`src/app/deals/page.tsx`:
```
30-37:  searchParams: Promise<{ pipeline?, stage?, owner?, assignee?, dateFrom?, dateTo? }>
39-45:  auth() gate
48-59:  allPipelines = findMany(where isNull(deletedAt), orderBy isDefault DESC, name)
62-73:  if (allPipelines.length === 0) -> the noPipelines empty state
76-78:  const selectedPipeline = params.pipeline
            ? allPipelines.find(p => p.id === params.pipeline)      // <- undefined for a dead id
            : allPipelines.find(p => p.isDefault) || allPipelines[0]
80-91:  if (!selectedPipeline) -> the t('pipelineNotFound') page       // <- Decision 4 target
105-119: filterConditions — stage, owner, assignee (a deal_assignees subquery), dateFrom/dateTo
```

`src/app/deals/kanban-board.tsx`:
```
448:  router.push(`${pathname}?pipeline=${pipelineId}`)                 // pipeline select — ALREADY SAFE
490:  <div /> in place of the pipeline cluster when pipelines.length <= 1
492-497: the "Add Deal" primary-filled button
501-507: <Suspense fallback={null}><DealFilters …/></Suspense>          // <- mount the bar ABOVE this
528:  router.replace(`${pathname}?pipeline=${selectedPipelineId}`)      // no-results clear — ALREADY SAFE
```

`src/app/deals/deal-filters.tsx` — two list-route `router.replace` sites, both can produce a bare query:
```
56:  setFilter:  router.replace(`${pathname}?${params.toString()}`)     // last chip removed, no pipeline -> BARE
67:  clearAll:   router.replace(`${pathname}?${params.toString()}`)     // keeps pipeline, but bare when none set
```

M-3, measured: the `/deals` pipeline row is **EXACTLY full at 241px** — "Pipeline:" cluster 118 +
8px gap + "Add Deal" 115 = 241. Zero slack.
M-6, measured: the `/deals` filter `Popover` is 388px of content clearing the viewport by only **7px**.
M-12, measured: **11 live pipelines**, 73 stages. Live deals: **25,195**.

`deal-filters.tsx` carries roughly **20 hardcoded English literals** ("Filters", "Stage", "Owner",
"All stages", "Clear all", "Stage: ", "From: ") and calls no `useTranslations` at all.
`kanban-board.tsx` carries "Pipeline:", "Select pipeline", "Add Deal", "No results match your
filters", "Clear filters".

From plan 40-05: `resolveSavedViewsBarProps`, `resolveDefaultViewRedirect`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The pipeline fallback, the redirect, and the bar props</name>
  <files>src/app/deals/page.tsx</files>
  <action>
    (a) THE DECISION-4 FALLBACK. Replace the ternary at 76-78 with an explicit three-step resolution
    that records whether the requested pipeline was honoured:

    ```
    const requestedPipeline = params.pipeline
      ? allPipelines.find((p) => p.id === params.pipeline)
      : undefined
    const pipelineWasDropped = Boolean(params.pipeline) && requestedPipeline === undefined
    const selectedPipeline =
      requestedPipeline ?? allPipelines.find((p) => p.isDefault) ?? allPipelines[0]
    ```

    Keep the `if (!selectedPipeline)` / `pipelineNotFound` branch. It is now unreachable while
    `allPipelines.length > 0` (guarded above), and that is the point — say so in the comment, and say
    that it is retained for TypeScript narrowing and for the day the guard above changes. Do not
    delete a branch to make a diff look tidier.

    Write the Decision-4 rationale: `pipeline` selects which board renders, so a deals view must carry
    it; but a stored id whose pipeline has since been deleted must land the user on the default board,
    not on a dead-end page. With 11 live pipelines this is reachable, and a "pipeline not found" page
    would make criterion 1 look like a crash.

    (b) THE DEFAULT-VIEW REDIRECT, after `params = await searchParams` and after the `auth()` gate,
    before `allPipelines` is read:
    ```
    if (Object.keys(params).length === 0) {
      const target = await resolveDefaultViewRedirect("deal", session.user)
      if (target) redirect(`/deals${target}`)
    }
    ```
    Same U-2 comment as the other three pages: the guard is "no params at all", `view=none` is a param
    so the escape is never hijacked, and the resolver returns `null` for an all-dropped filter set so
    the redirect cannot loop. `redirect()` throws — keep it out of any try/catch.

    (c) THE BAR PROPS. `resolveSavedViewsBarProps({ entityType: "deal", viewer: session.user,
    rawSearchParams: params })`. Add `"pipeline"` to the resolver's dropped-key set when
    `pipelineWasDropped` is true — the resolver validates the SELECTED view's stored filters, and this
    page has information the resolver does not: whether the URL's own pipeline resolved. Merge the two
    sources so `views.degraded` fires for both cases (V-11 covers a deleted owner, a deleted stage and
    a deleted pipeline in one sentence, deliberately).

    Pass the result to `<KanbanBoard>` as one `viewsBar` prop.

    Do NOT touch: the `auth()` gate, the `noPipelines` empty state, `filterConditions`, the
    `deal_assignees` subquery, or `retentionDays`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>An unknown `?pipeline=` resolves to the default board and contributes `"pipeline"` to `droppedFilterKeys`. The `pipelineNotFound` branch still exists with a comment saying why. typecheck 0, lint 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the bar and escape both filter navigations</name>
  <files>src/app/deals/kanban-board.tsx, src/app/deals/deal-filters.tsx</files>
  <action>
    **PRECONDITION CHANGED BY PLAN 40-18 — read before editing the writers.** The URL now carries
    `?view=<id>` naming the open view, and `withViewEscape` PRESERVES it whenever a saveable filter
    survives. Consequences for this plan, and they are all "do nothing extra":

      - **No `view=` writer is added here.** These files change filters, never the selection. Only the
        bar mints or clears a selection.
      - **No call-site shape change either.** Both writers in this file already build their
        `URLSearchParams` from `searchParams.toString()`, so `view` is already in the clone and
        `withViewEscape` carries it through untouched. That is the whole reason the preservation rule
        lives inside the helper rather than being threaded as a prop through six files.
      - **Do not add a `selectedViewId` prop to these components.** Plan 40-11 does pass one to the two
        `data-table.tsx` files, because those build their query strings from PROPS and have no
        `searchParams` to preserve from. This file does, so it needs nothing. The asymmetry is between
        where the two groups get their params, not a gap.
      - Clearing the last filter still lands on `?view=none`: no filter survives, so the escape wins and
        the selection goes with it (U-2 — a selection with no filters is not a state).

    (a) MOUNT, in `kanban-board.tsx`, on **its own row between the pipeline row and the
    `<Suspense><DealFilters/></Suspense>` block**:
    ```
    <SavedViewsBar {...viewsBar} />
    ```
    Write the placement rationale, all three parts:
      - **Not inside `deal-filters.tsx`**: a deals view carries `pipeline` (Decision 4) and the
        pipeline control lives in the row ABOVE the filters. A bar that can change the pipeline must
        sit above both things it changes.
      - **Not merged into the pipeline row**: that row is measured EXACTLY full at 241px (M-3), with
        zero slack, before pt-BR or es-ES lengthens either label.
      - **Rule P-2 — the bar renders even when only one pipeline exists.** `kanban-board.tsx` renders
        `<div />` in place of the pipeline cluster when `pipelines.length <= 1`; the bar has no such
        condition, because its content does not depend on the pipeline count. Do not copy that guard.
    The bar is not sticky and not fixed (K-8).

    (b) **DO NOT TOUCH** `kanban-board.tsx`'s two navigations at 448 and 528. Both carry
    `?pipeline=…`, `pipeline` is a saveable key, so `withViewEscape` would append nothing and the
    expressions are already escape-safe. UI-SPEC lists them as unchanged specifically so a plan does
    not "fix" them. Plan 40-14's call-site gate carries both as named exemptions matched by exact
    expression text — **changing them, even harmlessly, turns that gate RED.** Leave them byte-identical.

    (c) ESCAPE BOTH `deal-filters.tsx` navigations. Both are `router.replace` (keep `replace`, not
    `push` — filter changes should not stack history entries, which is the existing behaviour):
    ```
    router.replace(`${pathname}?${withViewEscape("deal", params)}`)
    ```
    at `setFilter` and at `clearAll`. `params` is already a `URLSearchParams` built from
    `searchParams.toString()` at both sites, so the helper receives what it expects and the diff is
    one expression each.

    Why both, not just `clearAll`: `clearAll` deletes the five filter keys and KEEPS `pipeline`, so it
    is bare only when no pipeline param was set — which is the common case, because the page defaults
    the pipeline without putting it in the URL. And `setFilter(key, null)` removing the last remaining
    chip produces the same bare query by the same route. Both would be recaptured by the new redirect
    guard.

    (d) `deal-filters.tsx`'s ~20 hardcoded English literals and its total absence of `useTranslations`
    are **explicitly out of scope**. Do not translate them, do not add one, and do not add a
    `useTranslations` call. This file's edit is two expressions; translating a 250-line filter
    component in the same diff would make the URL change unreviewable. The debt is real, is named in
    UI-SPEC § Out of scope, and belongs to a dedicated copy pass.

    (e) `kanban-board.tsx`'s five hardcoded literals are also out of scope, for the same reason.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run && git diff --unified=0 src/app/deals/kanban-board.tsx | grep -c "router.replace\|router.push" | grep -qx 0</automated>
  </verify>
  <done>The bar is mounted between the pipeline row and `<DealFilters>`, with no `pipelines.length` guard. Both `deal-filters.tsx` navigations go through `withViewEscape`. `git diff` on `kanban-board.tsx` touches ZERO `router.` lines. No new English literal in either file. typecheck 0, lint 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `?pipeline=` → which board renders | an arbitrary id decides which stage set and which 25,195-row scope is queried |
| filter clear → the redirect guard | two `router.replace` sites can produce a bare query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-53 | Denial of service (functional) | an unknown `?pipeline=` | mitigate | falls back to the default board rather than rendering `pipelineNotFound`. There is no `error.tsx` above this route (M-14), so a dead end here is the whole page |
| T-40-54 | Information disclosure | the fallback | accept | falling back means a user who saved a view against a pipeline they could see now sees the default board's deals instead. Per-record visibility is unchanged and `views.degraded` says the view was partly ignored; a saved view is a filter set, not a grant |
| T-40-55 | Tampering | `deal-filters.tsx` clearAll / setFilter | mitigate | both routed through `withViewEscape`, so clearing the last filter with no pipeline set lands on `?view=none` and is not recaptured by the default-view redirect |
| T-40-56 | Tampering | `kanban-board.tsx`'s two navigations | accept (unchanged) | both already carry a saveable `pipeline` key and are escape-safe. Left byte-identical; plan 40-14's gate matches their exact expression text as named exemptions |
| T-40-57 | Denial of service | the `stage` filter after a pipeline fallback | mitigate | plan 40-05's validator drops a `stage` that does not belong to the surviving pipeline, so the board never queries a stage id from a different board |
| T-40-SC | Tampering | npm installs | mitigate | this plan installs nothing |
</threat_model>

<verification>
- `npm run typecheck` 0, `npm run lint` 0 errors, full `npm test` green
- `git diff src/app/deals/kanban-board.tsx` contains zero changed `router.` lines
- `deal-filters.tsx` has exactly two `withViewEscape` call sites and zero new literals
- an unknown `?pipeline=` was exercised manually against the running container and rendered a board
  (record which pipeline it fell back to)
</verification>

<success_criteria>
- A deals view whose pipeline was deleted renders the default board plus `views.degraded`
- The bar sits above both the pipeline row's sibling and the filter row, and renders with one pipeline
- Both `deal-filters.tsx` navigations escape; both `kanban-board.tsx` navigations are untouched
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-12-SUMMARY.md` when done. Record the final
line numbers of every `router.push`/`router.replace` in all three files, and the exact expression text
of `kanban-board.tsx`'s two untouched calls, so plan 40-14's exemption list is written against reality.
</output>
