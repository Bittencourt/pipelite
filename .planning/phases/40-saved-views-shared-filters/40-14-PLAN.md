---
phase: 40-saved-views-shared-filters
plan: 14
type: execute
wave: 7
depends_on: ["40-11", "40-12", "40-13", "40-18"]
files_modified:
  - src/lib/views/__tests__/view-escape-call-sites.test.ts
  - src/components/views/__tests__/responsive-classes.test.ts
autonomous: true
requirements: [VIEW-01]

must_haves:
  truths:
    - "Every list-route navigation in the six client files is either escaped or a named, exact-matched exemption"
    - "The bar is the only file that mints a view selection, and it never hand-builds one"
    - "Adding a new bare router.push to any of those six files fails the suite"
    - "Deleting the comment that explains the rule does NOT satisfy the rule"
    - "No component this phase adds can place two views horizontally adjacent at any viewport"
  artifacts:
    - path: "src/lib/views/__tests__/view-escape-call-sites.test.ts"
      provides: "V-40-4 — the call-site gate over every router.push/replace argument expression"
      contains: "callArguments"
    - path: "src/components/views/__tests__/responsive-classes.test.ts"
      provides: "R-4 / R-3 parsed-class gate over the three new components"
  key_links:
    - from: "src/lib/views/__tests__/view-escape-call-sites.test.ts"
      to: "src/components/custom-fields/__tests__/source-scan"
      via: "readStrippedSource + callArguments — parsed arguments, not a token grep"
      pattern: "callArguments"
---

<objective>
V-40-4: a call-site gate that extracts **every** `router.push(...)` / `router.replace(...)` argument
expression from the six client files this phase edits and proves each one is either derived from
`withViewEscape` or a named exemption matched by exact expression text. Plus the R-4 responsive-class
gate over the three new components.

Purpose: `withViewEscape` and `?view=none` are load-bearing, and the failure mode is silent — a user
presses "Clear filters" and lands back inside the filters they were leaving. **This gate must not be a
token grep.** Raw-token grep criteria were hit FIVE times in Phase 39: the comment explaining a rule
trips the rule's own grep, and deleting the comment also passes, which is the wrong fix. An argument
expression cannot be satisfied by a comment.
Output: two source gates, each with a RUN negative proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md
@.planning/BACKLOG.md

Read the three integration summaries FIRST — they record the final line numbers and the exact
expression text of every navigation, which is what this gate is written against:
@.planning/phases/40-saved-views-shared-filters/40-11-SUMMARY.md
@.planning/phases/40-saved-views-shared-filters/40-12-SUMMARY.md
@.planning/phases/40-saved-views-shared-filters/40-13-SUMMARY.md

@src/components/custom-fields/__tests__/source-scan.ts
@src/app/organizations/__tests__/toolbar-wiring.test.ts
</context>

<interfaces>
<!-- Verified this session. These are the tools; do not write new ones. -->
```ts
// src/components/custom-fields/__tests__/source-scan.ts
export function stripComments(source: string): string
export function readStrippedSource(path: string): string
/** Argument text of every `${callee}(...)` call, string-aware brace matching so a `)` inside a
 *  string literal cannot close the list early. Source is expected comment-stripped. */
export function callArguments(source: string, callee: string): string[]
```
`callArguments` treats a backtick as a quote, so a template literal is returned whole — including any
`${withViewEscape(...)}` inside it. That is exactly the shape this gate asserts on.

<!-- The complete navigation inventory, read from source this session. Re-derive it from the three
     summaries and from the files themselves; this table is the planning-time baseline. -->

| # | file | enclosing function | disposition |
|---|---|---|---|
| 1 | `organizations/data-table.tsx` | `handleSearchChange` (non-empty) | ESCAPED |
| 2 | `organizations/data-table.tsx` | `handleSearchChange` (empty) | ESCAPED |
| 3 | `organizations/data-table.tsx` | `onOpen` row-open | **EXEMPT — detail-route navigation** |
| 4 | `organizations/data-table.tsx` | Load More | ESCAPED |
| 5 | `people/data-table.tsx` | `handleSearchChange` (non-empty) | ESCAPED |
| 6 | `people/data-table.tsx` | `handleSearchChange` (empty) | ESCAPED |
| 7 | `people/data-table.tsx` | `onOpen` row-open | **EXEMPT — detail-route navigation** |
| 8 | `people/data-table.tsx` | Load More | ESCAPED |
| 9 | `activities/activity-filters.tsx` | `setFilter` | ESCAPED |
| 10 | `activities/activity-filters.tsx` | `clearAll` | ESCAPED |
| 11 | `activities/activity-filters.tsx` | `handleSearchChange` | ESCAPED — **not in UI-SPEC's table; found during planning** |
| 12 | `activities/activities-client.tsx` | `handleLoadMore` | ESCAPED |
| 13 | `activities/activities-client.tsx` | no-results "Clear filters" | ESCAPED |
| 14 | `deals/deal-filters.tsx` | `setFilter` | ESCAPED |
| 15 | `deals/deal-filters.tsx` | `clearAll` | ESCAPED |
| 16 | `deals/kanban-board.tsx` | pipeline select | **EXEMPT — already carries `?pipeline=`, a saveable key** |
| 17 | `deals/kanban-board.tsx` | no-results clear | **EXEMPT — same reason** |

Seventeen call sites, thirteen escaped, four exempt. **UI-SPEC's "six call sites" counts only the ones
that are bare TODAY; it is an undercount of the sites the gate must cover, by one bare site (#11) and
by every non-bare list-route site.** Write the gate over all seventeen.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: V-40-4 — the call-site gate</name>
  <files>src/lib/views/__tests__/view-escape-call-sites.test.ts</files>
  <action>
    **THE CENSUS AFTER PLAN 40-18 — read this before counting anything.** 40-18 added `?view=<id>` and a
    second serialiser, `withViewSelection`. Three consequences, and the first is the one that matters:

      1. **The six-file census is UNCHANGED at 17 / 13 escaped / 4 exempt.** None of the six files
         selects a view; they change filters, searches and pages, and they get `view` preservation from
         `withViewEscape` itself. No row moves, no exemption is added, and the four exact-text
         exemptions (two detail-route row-opens, two already-safe `kanban-board.tsx` calls) are
         untouched. Verify this against the three integration summaries rather than trusting it.
      2. **The classification rule widens by one alternative:** an argument expression is ESCAPED when it
         derives from `withViewEscape(` **or** `withViewSelection(`. Today no expression in the six uses
         the latter — assert that too, as an exact count of zero, so a `view=` writer appearing in a
         filter toolbar is a failure rather than a silent pass. A filter toolbar that mints a selection
         would let a filter change *reassign* which view is open.
      3. **A SEVENTH FILE joins the gate: `src/components/views/saved-views-bar.tsx`.** It is the only
         place a selection is minted, so an unescaped or hand-built push there is exactly the defect
         this gate exists for, and it is the one file where `withViewSelection` must appear. Give it its
         own rows with the same count-then-classify treatment: every `router.push` argument derives from
         one of the two helpers, no expression contains both, and **no expression contains a literal
         `view=`**. Plan 40-10's own wiring gate asserts this too; the duplication is deliberate, because
         that gate is scoped to one component while this one is the phase's single census of every
         navigation — and 40-10's gate would not notice a new writer added to a seventh file later.

    So the totals this gate asserts become: **six files at 17 / 13 / 4, plus the bar's own rows.** Report
    the bar's count as measured rather than predicted; 40-10's summary records it.

    Create the gate. Structure it as a data table in the test file, one row per call site:

    ```ts
    interface NavSite {
      file: string
      /** The enclosing function or JSX handler, used only in failure messages. */
      where: string
      /** "escaped" | "exempt" */
      rule: "escaped" | "exempt"
      /** For "exempt" only: the EXACT argument expression text, whitespace-normalised.
       *  An exemption that stops matching exactly is a failure, not a pass. */
      exactArgument?: string
      /** For "exempt" only: why. Rendered in the failure message. */
      reason?: string
    }
    ```

    For each of the six files:
      1. `const source = readStrippedSource(file)` — comments gone FIRST, so the prose in the
         component explaining the rule cannot satisfy anything below. This is the whole point.
      2. `const args = [...callArguments(source, "router.push"), ...callArguments(source, "router.replace")]`
      3. Assert `args.length` equals the number of table rows for that file, with both counts and the
         extracted expressions in the failure message. **This is what catches a NEW call site**: an
         eighteenth navigation added later is in neither the escaped nor the exempt set, and the count
         assertion fails before anything else does.
      4. For every extracted argument, classify it:
         - it MATCHES the table's `exactArgument` for some `exempt` row (compare after collapsing
           whitespace) → exempt, and that row is marked consumed
         - else it must contain `withViewEscape(` **or** `withViewSelection(` as a substring of the
           ARGUMENT EXPRESSION (not of the file) → escaped
         - else FAIL, printing the file, the offending expression verbatim, and this sentence:
           "A list-route navigation that does not pass through withViewEscape lands on a bare or
           empty query. The default-view redirect guard reads that as 'no params' and sends the user
           straight back into their default view — so pressing Clear filters would return them to the
           filters they were leaving."
      5. Assert every `exempt` row was consumed exactly once. An exemption whose expression was
         edited, moved or removed fails here rather than silently widening the allowance.

    Then two cross-file assertions:
      - across the six files the total escaped count is 13 and the total exempt count is 4, both
        asserted with the actual numbers in the message, so the gate reports drift rather than absorbing
        it
      - across the six files, `withViewSelection(` appears in **zero** extracted arguments: a filter
        toolbar that minted a selection would let a filter change reassign which view is open
      - in `saved-views-bar.tsx`, `withViewSelection(` appears at least once and no extracted argument
        contains a literal `view=`
      - `withViewEscape` is imported from `@/lib/views/url-params` in all six files (assert on the
         stripped source's import statements, extracted by matching `import` up to the `from` clause —
         not a bare substring search, because a comment mentioning the path is stripped anyway but an
         unused import would still be a lie)

    Write a header saying what this test is NOT: it does not measure, it does not visit a browser, and
    it does not prove the escape WORKS — the pure-function proof is plan 40-01's V-40-6 and the
    behavioural proof is plan 40-15's. It proves only that no list-route navigation in these six files
    bypasses the helper.

    **NEGATIVE PROOFS, TO BE RUN, THREE of them, each naming what it must fail:**
      (a) revert one escaped site to a bare literal (e.g. `router.push("/organizations")` in
          `organizations/data-table.tsx`) → the classification step must FAIL for that expression.
          Record the message. Restore.
      (b) **THE ANTI-GREP PROOF.** Add a comment line to that same file containing the literal text
          `withViewEscape("organization", params)` while the bare push from (a) is still in place →
          the gate must be **STILL RED**. This is the proof that the gate reads argument expressions
          and not tokens; it is the exact check Phase 39 lacked five times. Record the message, then
          remove the comment and restore the push.
      (c) add an eighteenth `router.push("/people?foo=1")` to `people/data-table.tsx` → the COUNT
          assertion for that file must fail before the classification does. Record the message. Remove.
      (d) replace the bar's `withViewSelection(` with a hand-built template containing `view=${view.id}`
          → the bar's no-literal-`view=` assertion must fail. This is the shape that would pass a
          `withViewEscape`-only gate while bypassing every refusal `withViewSelection` makes (no
          selection over an unfiltered list, no junk id). Record the message. Restore.

    If any of the three does not behave as described, the gate is wrong — fix the gate, and say so.
    Do not adjust the expectation to match a gate that does not discriminate.
  </action>
  <verify>
    <automated>npx vitest run src/lib/views/__tests__/view-escape-call-sites.test.ts && npm test</automated>
  </verify>
  <done>Green with 13 escaped and 4 exempt across the six files, plus the bar's own rows measured and recorded. All four negative proofs RUN, all four messages recorded, and proof (b) confirmed STILL RED with the comment present. Full `npm test` green.</done>
</task>

<task type="auto">
  <name>Task 2: R-4 / R-3 — the responsive-class gate</name>
  <files>src/components/views/__tests__/responsive-classes.test.ts</files>
  <action>
    Create one gate covering the three new components (`saved-views-bar.tsx`,
    `save-view-dialog.tsx`, `manage-views-dialog.tsx`) for the rules that are cross-file rather than
    per-component. Per-component clamps and variants are already gated by plans 40-08/09/10; do not
    duplicate them — assert here only what those three gates cannot see individually.

    Over `readStrippedSource` of each file, extract every `className="…"` and every
    `className={...}` literal string (a small extractor; reuse 40-08's tag walker rather than adding
    a fourth brace matcher to the repo — BACKLOG.md records two that should already be consolidated):

      1. R-4 — **no `grid-cols-2` (or any `grid-cols-N`, N ≥ 2) without an `sm:` prefix** in any of
         the three files. "Nothing renders two views horizontally adjacent at any viewport" is the
         rule; at 241px two columns give 120px each.
      2. R-4 — no `flex` row containing two view blocks without `flex-wrap`. Approximate it as: every
         className containing `flex` and NOT containing `flex-col` also contains either `flex-wrap`
         or `shrink-0`/`min-w-0` on that same element. Report the offending className verbatim so a
         false positive is obvious rather than mysterious, and if a legitimate exception appears, add
         it to an explicit exemption list IN THE TEST with a reason — never by loosening the rule.
      3. R-3 — `min-w-0` appears on the bar's outermost row, on the picker trigger, and on each
         manage-dialog row's name cluster. Assert per extracted element, not per file. A flex item's
         default `min-width: auto` is the mechanism behind every overflow Phase 45 measured.
      4. Responsive contract — the three files introduce **no breakpoint other than `sm:`**. Assert
         zero `md:`, `lg:`, `xl:`, `2xl:` prefixes. This phase declares one breakpoint and adds no
         media query; a stray `md:` would be a second, undocumented layout.
      5. Cross-file absence sweep, in all three files at once: zero `Popover`, zero
         `DropdownMenuSubContent`, zero `ProgressBar`, zero `sticky`, zero `fixed`, zero
         `leading-none`, zero of `bg-green-500` / `bg-amber-500` / `text-red-600` / `text-green-600` /
         `text-amber-500`, and zero font size outside `text-xs` / `text-sm` / `text-lg`.
      6. `src/components/ui/` is UNTOUCHED by this phase: assert `git diff --name-only` (or a stored
         checksum of the directory listing) reports no file under `src/components/ui/`. Every clamp
         O-1 requires is a per-call-site className, never a primitive edit — `dialog.tsx` is shared by
         roughly sixteen dialogs. If a `git`-based assertion is awkward inside vitest, assert instead
         that none of the three component files imports anything from `src/components/ui/` that does
         not already exist, and record the `git diff --stat src/components/ui/` output manually.

    **NEGATIVE PROOF, TO BE RUN:** add `className="grid grid-cols-2"` to a wrapper in
    `manage-views-dialog.tsx` → assertion 1 must turn RED naming that file and that className. Restore.
    Record the message.
  </action>
  <verify>
    <automated>npx vitest run src/components/views/ && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <done>All six assertion groups green. The negative proof was RUN and recorded. `git diff --stat src/components/ui/` output is in the summary and is empty. Full `npm test` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future edit → the escape rule | the rule is invisible in a diff; only a gate makes it survive |
| a gate's own comment → the gate | the Phase 39 trap: prose describing a rule satisfying the rule's grep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-64 | Tampering | a new unescaped navigation | mitigate | the per-file COUNT assertion fails for any call site not in the table, so an eighteenth navigation cannot be added silently. Classification then names the offender |
| T-40-65 | Tampering | the gate itself | mitigate | `readStrippedSource` removes comments before anything is read, and every assertion is on an extracted ARGUMENT expression. Negative proof (b) confirms a comment containing the exact helper call keeps the gate RED — the one check that distinguishes this gate from the five token greps Phase 39 shipped |
| T-40-66 | Tampering | an exemption widening | mitigate | exemptions match exact, whitespace-normalised argument text and must each be consumed exactly once. A "fixed" `kanban-board.tsx` expression breaks the gate, which is why plan 40-12 was told to leave those two byte-identical |
| T-40-67 | Denial of service (functional) | a stray `md:` or `grid-cols-2` | mitigate | assertions 1 and 4 catch a second layout that only appears at a width nobody measured |
| T-40-68 | Tampering | `src/components/ui/` | mitigate | asserted untouched. A clamp added to `dialog.tsx` would change roughly sixteen dialogs at once, which is a different change with a different blast radius |
| T-40-SC | Tampering | npm installs | mitigate | this plan installs nothing and adds no test library |
</threat_model>

<verification>
- `npx vitest run src/lib/views/ src/components/views/` green; full `npm test` green
- `npm run typecheck` 0, `npm run lint` 0 errors
- all five negative proofs RUN, all five messages recorded, and (b) confirmed STILL RED
- no third/fourth brace matcher added to the repo (say which existing one you reused)
- `git diff --stat src/components/ui/` empty
</verification>

<success_criteria>
- Every list-route navigation in the six edited files is escaped or an exact-matched named exemption
- The bar is the only file minting a selection, and it does so through `withViewSelection` and never a
  hand-built `view=`
- Adding a bare push, or a comment naming the helper, both leave the gate RED
- The three new components declare one breakpoint and place no two views side by side
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-14-SUMMARY.md` when done. Record the final
seventeen-row table as implemented (it may differ from the planning baseline — the summaries and the
files win), all four negative-proof messages, and which brace matcher you reused.
</output>
