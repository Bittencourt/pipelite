---
phase: 40-saved-views-shared-filters
plan: 11
type: execute
wave: 5
depends_on: ["40-01", "40-05", "40-10"]
files_modified:
  - src/app/organizations/page.tsx
  - src/app/organizations/data-table.tsx
  - src/app/people/page.tsx
  - src/app/people/data-table.tsx
autonomous: true
requirements: [VIEW-01, VIEW-02]

must_haves:
  truths:
    - "Opening /organizations with no params lands on the user's default view, with the filters in the address bar"
    - "Emptying the search box does NOT bounce the user back into their default view"
    - "Applying a saved view whose search is 'acme' leaves the search box reading 'acme'"
    - "Selecting All records clears the filter AND clears the search box"
  artifacts:
    - path: "src/app/organizations/page.tsx"
      provides: "the default-view redirect and the eight bar props"
      contains: "resolveDefaultViewRedirect"
    - path: "src/app/organizations/data-table.tsx"
      provides: "the bar on its own row, key={search} on the Input, both pushes escaped"
      contains: "withViewEscape"
  key_links:
    - from: "src/app/organizations/page.tsx"
      to: "src/lib/views/resolve.ts"
      via: "resolveSavedViewsBarProps + resolveDefaultViewRedirect before the list query"
      pattern: "resolveSavedViewsBarProps"
    - from: "src/app/organizations/data-table.tsx"
      to: "src/lib/views/url-params.ts"
      via: "withViewEscape wrapping every list-route router.push"
      pattern: "withViewEscape"
---

<objective>
Mount the bar on `/organizations` and `/people`, apply the default-view redirect, escape every
list-route navigation, and fix the search input that does not re-sync.

Purpose: two of the four surfaces, and the two where **criterion 1 is measurably broken today**. M-9:
the search `<Input>` uses `defaultValue={search}`, which is ignored after mount, and app-router
navigation re-renders without remounting — so applying a saved view filters the list correctly while
the box still shows the old text. That is criterion 1 failing visibly, not polish.
Output: four edited files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-CONTEXT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md

@src/app/organizations/page.tsx
@src/app/organizations/data-table.tsx
@src/app/people/page.tsx
@src/app/people/data-table.tsx
@src/lib/views/resolve.ts
@src/lib/views/url-params.ts
@src/components/views/saved-views-bar.tsx
</context>

<interfaces>
<!-- Read this session. Exact line numbers as of planning; re-locate rather than trusting them. -->

`src/app/organizations/data-table.tsx` — three list-route `router.push` sites and one detail-page one:
```
291:  router.push(`/organizations?search=${encodeURIComponent(value)}&page=1`)   // handleSearchChange, non-empty
293:  router.push("/organizations")                                              // handleSearchChange, EMPTY -> BARE
302:  onOpen: (org) => router.push(`/organizations/${org.id}`)                   // DETAIL NAV — leave alone
458:  router.push(`/organizations?search=${encodeURIComponent(search)}&page=${currentPage + 1}`)  // Load More
348:  defaultValue={search}                                                      // the stale input
343:  <div className="flex flex-wrap items-center justify-between gap-2">        // the existing toolbar row
```
`src/app/people/data-table.tsx` is the same shape at `161`, `163`, `245`, `400`, `290`.

`src/app/organizations/page.tsx`:
- `searchParams: Promise<{ search?: string; page?: string }>`
- `auth()` gate at 78-82, `isAdmin` at 90, `params = await searchParams` at 92
- a four-read `Promise.all` at 126-139 (`getOrganizations`, `readTrashRetentionDays`, an owners
  query, `readOrgIdentityInputFields`)
- renders `<DataTable columns data hasMore search currentPage retentionDays bulkOwners
  identityFieldNames isAdmin />` at 171-181

M-4, measured: the `/organizations` and `/people` toolbars **already wrap to two rows at 320px** with
their three current controls — orgs: search cluster 50 + "Find duplicates" 133 on row 1,
"Add Organization" 171 on row 2, total height 80px. A fourth and fifth control on that row produces
four rows of ungrouped buttons.

M-9, measured: typed "acme" on `/organizations` → URL `?search=acme&page=1`; pressed Back → URL
returned to `/organizations` with **the input still reading "acme"**.

F-39-08: `data-table-keyboard.tsx` is mounted on BOTH these pages.

From plan 40-05:
```ts
resolveSavedViewsBarProps({ entityType, viewer, rawSearchParams }): Promise<SavedViewsBarProps>
resolveDefaultViewRedirect(entityType, viewer): Promise<string | null>   // "?k=v&…" or null
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The default-view redirect and the bar props, on both pages</name>
  <files>src/app/organizations/page.tsx, src/app/people/page.tsx</files>
  <action>
    In each page, immediately after `const params = await searchParams` and BEFORE any list query:

    ```
    if (Object.keys(params).length === 0) {
      const target = await resolveDefaultViewRedirect("organization", session.user)
      if (target) redirect(`/organizations${target}`)
    }
    ```

    Write the U-2 rationale inline, because every part of it is load-bearing:
      - the guard is **"no params at all"**, and `view=none` IS a param, so the escape URL is never
        hijacked and the redirect cannot loop
      - `resolveDefaultViewRedirect` returns `null` when the default's validated filter set is empty,
        which is the second half of the loop guard: its target always carries at least one whitelisted
        key
      - it is a **redirect, not a rewrite** (U-4), so after it the URL carries the view's params and
        the picker resolves the selection from the URL like any other visit. There is deliberately no
        second code path for "arrived by default".
      - `?search=` (an empty value) is one param, so a deliberate bare-filter visit is not hijacked

    `redirect()` throws, so it must sit outside any try/catch — Next's `NEXT_REDIRECT` must propagate.
    `redirect` is already imported in `organizations/page.tsx`; check `people/page.tsx`.

    Then add `resolveSavedViewsBarProps({ entityType: "organization", viewer: session.user,
    rawSearchParams: params })` as a fifth entry in the existing `Promise.all`. Do **not** add a
    separate `await` above it: these pages already do four reads in one round trip and the comment
    block at 96-125 explains why that matters.

    Pass the resolved object to `<DataTable>` as a single `viewsBar` prop rather than eight loose
    props. Extend the `DataTableProps` interface accordingly (task 2 owns the component).

    Do NOT touch: the `auth()` gate, the `isAdmin` visibility-only comment, `retentionDays` (it must
    stay un-defaulted — a numeric fallback would have the delete dialog promise a window nobody is
    enforcing), `bulkOwners`' two predicates, or `identityFieldNames`' degrade-to-`[]`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <done>Both pages redirect only on a params-free URL, resolve the bar props inside the existing `Promise.all`, and pass one `viewsBar` prop. typecheck 0, lint 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the bar, escape the pushes, fix the input</name>
  <files>src/app/organizations/data-table.tsx, src/app/people/data-table.tsx</files>
  <action>
    THREE edits per file, and no fourth.

    (1) MOUNT THE BAR ON ITS OWN ROW, immediately ABOVE the existing
    `<div className="flex flex-wrap items-center justify-between gap-2">` toolbar:
    ```
    <SavedViewsBar {...viewsBar} />
    ```
    **Its own row, never merged into that toolbar** (R-40-2c). Write the measurement in the comment:
    that toolbar already wraps to two rows at 320px with three controls (M-4), and a fourth and fifth
    make four rows while mixing "which slice of the list am I seeing" with "search and create", which
    are different questions. The bar is not sticky and not fixed (K-8) — `bulk-action-bar.tsx` already
    owns one fixed bar on this page and D-45-02 is an open UAT item about a fixed bar occluding
    content.

    (2) `key={search}` ON THE SEARCH `<Input>` (B-6), keeping `defaultValue={search}`:
    ```
    <Input key={search} placeholder="Search organizations..." defaultValue={search} … />
    ```
    Write the reason inline, in full, because the tempting alternative is wrong: `defaultValue` is
    ignored after mount and app-router navigation re-renders without remounting, so applying a view
    that stores `search=acme` filters the list and leaves the box showing whatever was there before,
    and selecting `views.allRecords` clears the filter and leaves "acme" visible. **The fix is `key`,
    NOT conversion to a controlled input:** these inputs are 300ms-debounced writers, and a controlled
    value fed from the URL fights its own debounce on every keystroke. `key` remounts only when the
    URL actually changes, which is exactly the event that must reset the box. Measured: M-9.

    The existing `placeholder="Search organizations..."` / `"Search people..."` English literals are
    **out of scope** and stay exactly as they are. Do not translate them, and do not add a new literal.

    (3) ESCAPE EVERY LIST-ROUTE NAVIGATION. Route each of the three list-route `router.push` argument
    expressions through `withViewEscape`. Build a `URLSearchParams` rather than concatenating, so the
    helper receives what it expects:

    ```
    const push = (params: URLSearchParams) =>
      router.push(`/organizations?${withViewEscape("organization", params)}`)
    ```
    - `handleSearchChange`, non-empty: `params.set("search", value); params.set("page", "1")`
    - `handleSearchChange`, empty: `push(new URLSearchParams())` → `/organizations?view=none`.
      **This is the site that matters.** Today it pushes the bare `"/organizations"`, which the new
      redirect guard would read as "no params" and bounce straight into the user's default view — so
      a user clearing their search box would find themselves back inside a filter they were trying to
      leave.
    - Load More: `params.set("search", search); params.set("page", String(currentPage + 1))` — note
      `search` may be `""` here, which the helper correctly treats as no filter, so the result carries
      `page` and `view=none`. Harmless and correct: page 2 of an unfiltered list is not hijacked.

    **LEAVE the detail-page navigation alone** (`onOpen: (org) => router.push(\`/organizations/${org.id}\`)`).
    It targets a record route, not the list route, and wrapping it would be nonsense. Plan 40-14's
    call-site gate carries it as a named exemption with that reason — do not "fix" it, and do not
    change its expression text, because the gate matches it exactly.

    Do NOT touch anything else in these two files: not the keyboard hook wiring, not the bulk bar,
    not the `isAdmin` conditional, not the `min-w-0 flex-1` search cluster classes (they are the other
    half of the 320px fix Phase 45 paid for and `toolbar-wiring.test.ts` asserts them).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run src/app/organizations/ src/app/people/ && npm test</automated>
  </verify>
  <done>Both files: bar on its own row above the toolbar, `key={search}` on the Input, all three list-route pushes routed through `withViewEscape`, detail-page push untouched. `src/app/organizations/__tests__/toolbar-wiring.test.ts` still green. typecheck 0, lint 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bare URL → redirect | a redirect computed from stored data, taken before any list query runs |
| client navigation → the redirect guard | six existing controls navigate to bare paths and each would be hijacked without the escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-49 | Denial of service | the default-view redirect | mitigate | the guard is "no params at all" and `view=none` is a param, so the escape URL is never hijacked; and `resolveDefaultViewRedirect` returns `null` for an all-dropped filter set, so the redirect target always carries a whitelisted key. Both halves are required for the loop to be impossible |
| T-40-50 | Tampering | `handleSearchChange`'s empty branch | mitigate | routed through `withViewEscape`, so clearing the search box lands on `?view=none` instead of a bare path the redirect would recapture |
| T-40-51 | Information disclosure | `resolveSavedViewsBarProps` | mitigate | called with `session.user` after the `auth()` gate; the visibility predicate lives in plan 40-05 and no view is filtered client-side |
| T-40-52 | Tampering | `retentionDays` | accept (unchanged) | left un-defaulted exactly as it is. A numeric fallback added here would make the delete dialog promise a retention window the pruner is not enforcing (T-38-10) |
| T-40-SC | Tampering | npm installs | mitigate | this plan installs nothing |
</threat_model>

<verification>
- `npm run typecheck` 0, `npm run lint` 0 errors, full `npm test` green
- `src/app/organizations/__tests__/toolbar-wiring.test.ts` still green (the `flex-wrap` / `min-w-0`
  assertions are unaffected because the bar is a SIBLING of that row, not a child)
- exactly three `withViewEscape` call sites per data-table, and the detail-page push has none
- `git diff` shows no change to the `placeholder=` literals
</verification>

<success_criteria>
- Criterion 3 works on both surfaces: a params-free visit lands on the default view with the filters
  visible in the address bar
- Criterion 1's visible half works: the search box shows what the URL says, in both directions
- Emptying the search box escapes rather than being recaptured
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-11-SUMMARY.md` when done. Record the final
line numbers of every changed `router.push` so plan 40-14's gate can be written against reality.
</output>
