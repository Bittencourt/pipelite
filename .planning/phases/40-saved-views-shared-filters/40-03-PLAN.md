---
phase: 40-saved-views-shared-filters
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/en-US.json
  - src/messages/pt-BR.json
  - src/messages/es-ES.json
  - src/messages/locale-parity.test.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03]

must_haves:
  truths:
    - "Every one of the 61 views.* keys exists, non-blank, in all three locales"
    - "A 62nd views.* string added without a contract entry fails the suite"
    - "The four pre-existing pinned key sets are unchanged"
  artifacts:
    - path: "src/messages/en-US.json"
      provides: "61 views.* keys"
      contains: "\"views\""
    - path: "src/messages/locale-parity.test.ts"
      provides: "REQUIRED_VIEWS_KEYS exact-set contract, ICU_PLURAL_KEYS 11 -> 12"
      contains: "REQUIRED_VIEWS_KEYS"
  key_links:
    - from: "src/messages/locale-parity.test.ts"
      to: "REQUIRED_VIEWS_KEYS"
      via: "wired into missingIn / blankIn / untranslatedInBoth / placeholderDrift"
      pattern: "placeholderDrift\\(REQUIRED_VIEWS_KEYS\\)"
---

<objective>
Add the phase's entire copy surface — exactly 61 keys under `views.*` — to all three locale files,
and pin them with a new exact-set contract so a 62nd string cannot ship untranslated.

Purpose: K-1 is inherited and non-negotiable, and every UI plan in waves 3-5 consumes these keys.
Doing it first means no UI plan ever has a reason to inline an English literal.
Output: 61 × 3 keys, `REQUIRED_VIEWS_KEYS`, `ICU_PLURAL_KEYS` at 12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md

**The copy is already written.** UI-SPEC § New message keys gives all 61 keys with en-US, pt-BR and
es-ES values in five tables (18 + 22 + 13 + 5 + 3). Transcribe them; do not re-translate, do not
improve, do not shorten. Every value in this plan is a copy-paste from that document.

@src/messages/locale-parity.test.ts
</context>

<interfaces>
<!-- MEASURED THIS SESSION by parsing the actual file. These numbers differ from the ones
     40-UI-SPEC.md M-13 and 40-CONTEXT.md A5 quote, and THE FILE WINS. -->

| Constant | Value IN THE FILE right now | What UI-SPEC M-13 / CONTEXT A5 claim |
|---|---|---|
| `REQUIRED_NOTE_KEYS` | 32 | — |
| `REQUIRED_AUDIT_KEYS` | **86** | 88 |
| `REQUIRED_TRASH_KEYS` | **63** | 66 |
| `REQUIRED_BULK_KEYS` | **46** | 47 |
| `REQUIRED_SHELL_KEYS` | 16 | — |
| `REQUIRED_DEDUP_KEYS` | **80** | 83 |
| `ICU_PLURAL_KEYS` | 11 | 11 |
| `IDENTICAL_TRANSLATION_ALLOWED` | 3 | 3 |

There is also an existing test at `locale-parity.test.ts:860` literally named
`it("the checked-in dedup contract still lists exactly 80 keys")` with
`expect(REQUIRED_DEDUP_KEYS).toHaveLength(80)`. **A plan that "corrects" 80 to 83 breaks the suite.**

The rule this plan follows is therefore *unchanged-ness*, not a literal number: re-read the file,
record what you find, and assert equality against what is there. If your number and the file
disagree, the file wins and you record the discrepancy — do not force the file to match a document.

`views` currently has **0** keys in all three locale files (verified). `bulk.exported`,
`bulk.error.exportFailed`, `common.cancel` and `common.close` all exist (verified) and are REUSED.

The contract wiring pattern to copy, from the same file:
```
const DEDUP_NAMESPACE = "dedup"
const dedupKeys = keysMatching(inNamespace(DEDUP_NAMESPACE))
...
const dedupContract = [...REQUIRED_DEDUP_KEYS].sort()
for (const locale of LOCALES) {
  expect.soft([...dedupKeys[locale]].sort(), `${DEDUP_NAMESPACE} key set in ${locale}.json diverges from the checked-in contract`).toEqual(dedupContract)
}
```
plus `missingIn`, `blankIn`, `untranslatedInBoth`, `placeholderDrift` — four shared assertion bodies
that each take a `required` array.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The contract, written before the keys exist</name>
  <files>src/messages/locale-parity.test.ts</files>
  <behavior>
    Edit the test file FIRST, so it goes RED against the current (empty) `views` namespace. Expect
    roughly 6 failing assertions naming missing keys, and record that count.

    Add:
      - `export const REQUIRED_VIEWS_KEYS: string[] = [ … 61 dot-paths … ]`, grouped by the five
        UI-SPEC sub-tables with a comment per group giving its count (18 / 22 / 13 / 5 / 3) and the
        sum. Follow `REQUIRED_DEDUP_KEYS`'s formatting exactly.
      - `const VIEWS_NAMESPACE = "views"` and
        `const viewsKeys = keysMatching(inNamespace(VIEWS_NAMESPACE))`. The whole contract lives under
        one namespace with no extras array, so the exact-set comparison is TOTAL — say so in a
        comment, and say that this is unlike `audit`/`trash`/`shell`, which need extras arrays.
      - `it("the checked-in views contract still lists exactly 61 keys")` asserting BOTH
        `toHaveLength(61)` and `new Set(REQUIRED_VIEWS_KEYS).size === 61` (the duplicate guard the
        dedup test already carries).
      - `REQUIRED_VIEWS_KEYS` added to all four shared assertion bodies:
        `missingIn`, `blankIn`, `untranslatedInBoth`, `placeholderDrift`, each as its own
        `expect.soft(...)` line so a broken contract reports alongside the others in one run.
      - the exact-set comparison loop for `viewsKeys` against `[...REQUIRED_VIEWS_KEYS].sort()`.
      - `views.manage.filterCount` appended to `ICU_PLURAL_KEYS` (11 → 12), and update the surrounding
        comment's count.
      - `it("the four pre-existing pinned key sets are unchanged by phase 40")` asserting
        `REQUIRED_AUDIT_KEYS`, `REQUIRED_TRASH_KEYS`, `REQUIRED_BULK_KEYS` and `REQUIRED_DEDUP_KEYS`
        each have the length you READ FROM THE FILE at the start of this task (86 / 63 / 46 / 80 as
        measured, but re-read and use what is there). The comment above it must say: **this phase
        reuses `bulk.exported` and `bulk.error.exportFailed` verbatim, so if you find yourself
        editing `REQUIRED_BULK_KEYS`, you have added a key you were told to reuse.**

    Do NOT add anything to `IDENTICAL_TRANSLATION_ALLOWED`. `untranslatedInBoth` flags a key only
    when pt-BR AND es-ES are both byte-identical to en-US, and none of the 61 are. The near-misses
    (`views.modified` = "Modificada" in both non-English locales, `views.manage.filterCount`,
    `views.export.disabledReason`) are identical TO EACH OTHER, which that function does not test.
    **RUN the gate to confirm this rather than reasoning about it** (L-5) — Phase 39 gave the same
    instruction and it was correct to.

    Do NOT add a placeholder-occurrence-count assertion. `dedup.merge.confirmBody` has one because it
    repeats `{loser}`; L-4 states that **no `views.*` key repeats a placeholder**, so the analogue
    would be vacuous. Write that sentence in a comment so a checker does not ask for it.
  </behavior>
  <action>
    Read `src/messages/locale-parity.test.ts` in full once and note the actual lengths of the four
    pinned arrays before editing. Make the edits above. Run the suite and record the RED output.
  </action>
  <verify>
    <automated>npx vitest run src/messages/locale-parity.test.ts 2>&1 | tail -40</automated>
  </verify>
  <done>The suite is RED with failures naming missing `views.*` keys, and the RED count is recorded. The four pre-existing lengths were read from the file, not copied from a document, and any discrepancy with UI-SPEC M-13 is written into the summary.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe the 61 keys into all three locales</name>
  <files>src/messages/en-US.json, src/messages/pt-BR.json, src/messages/es-ES.json</files>
  <action>
    Add a `"views"` object to each of the three files. Transcribe the five UI-SPEC tables verbatim:

      - `views.*` (18): picker.label, allRecords, modified, groupMine, groupShared, badgeShared,
        badgePrivate, badgeDefault, ownedBy, ownerUnavailable, emptyMenu, saveNew, saveChanges,
        manageAction, exportAction, exporting, needsFilter, degraded
      - `views.save.*` (22)
      - `views.manage.*` (13)
      - `views.delete.*` (5)
      - `views.export.*` (3)

    Nesting: `views.picker.label` means `{"views": {"picker": {"label": …}}}`. `views.save.titleNew`
    means `{"views": {"save": {"titleNew": …}}}`. The flattener in the parity test joins with `.`, so
    the nesting must produce those exact dot-paths.

    Placeholders — reproduce the braces EXACTLY, including the curly typographic quotes `“ ”` where
    the spec uses them:
      `views.ownedBy` `{owner}` · `views.save.nameTaken` `{name}` · `views.save.targetUpdate` `{name}`
      · `views.save.targetNewOnly` `{name}` + `{owner}` · `views.save.created` `{name}`
      · `views.save.updated` `{name}` · `views.manage.readOnly` `{owner}` · `views.delete.body` `{name}`
      · `views.delete.success` `{name}` · `views.export.tooMany` `{max}`

    `views.manage.filterCount` is the only ICU plural:
      en-US `{count, plural, one {# filter} other {# filters}}`
      pt-BR `{count, plural, one {# filtro} other {# filtros}}`
      es-ES `{count, plural, one {# filtro} other {# filtros}}`
    `one`/`other` only in all three — correct for pt-BR and es-ES.

    Place the `"views"` object in the same relative position in all three files (alphabetically among
    the top-level namespaces if the files are ordered that way; otherwise adjacent to `"trash"`), so
    the three diffs are reviewable side by side. Then run the gate; it must be GREEN including the
    whole-file identical-key-set test at the bottom of the parity suite.
  </action>
  <verify>
    <automated>npx vitest run src/messages/locale-parity.test.ts && npm run typecheck</automated>
  </verify>
  <done>All parity tests green. `python3 -c` count of `views.*` leaves is exactly 61 in each of the three files. `untranslatedInBoth` was RUN and reported no `views.*` key. `REQUIRED_BULK_KEYS` length is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translation file → rendered HTML | an ICU string with a broken placeholder renders a raw token or throws at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-11 | Information disclosure | `views.save.privateHelp` | mitigate | the sentence "Only you can see this view. Nobody else, including admins." is the ONLY place the user learns Decision 3's departure from the app's `owner \|\| role === "admin"` idiom. A mistranslation here is a false security promise, so all three locales are transcribed from the spec rather than machine-translated |
| T-40-12 | Tampering | `ICU_PLURAL_KEYS` | mitigate | `placeholderDrift`'s regex is `/\{[a-zA-Z0-9_]+\}/g` and cannot see `{count, plural, …}`; the dedicated `icuPluralReport` assertion is the only defence, so `views.manage.filterCount` must be added to that list or its plural wrapper can be silently lost in translation |
| T-40-SC | Tampering | npm installs | mitigate | this plan installs nothing |
</threat_model>

<verification>
- `npx vitest run src/messages/` fully green
- exactly 61 `views.*` leaves in each locale file
- `REQUIRED_AUDIT_KEYS` / `TRASH` / `BULK` / `DEDUP` lengths unchanged from what was read at task 1
- `ICU_PLURAL_KEYS` length 12
- `IDENTICAL_TRANSLATION_ALLOWED` length still 3
</verification>

<success_criteria>
- A 62nd `views` string added without a `REQUIRED_VIEWS_KEYS` entry fails the suite (prove it: add one
  temporarily, confirm RED, remove it — a negative proof that is RUN, not reasoned about)
- No key in this phase duplicates `bulk.exported`, `bulk.error.exportFailed`, `common.cancel` or
  `common.close`
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-03-SUMMARY.md` when done. Record the RED
count, the four pinned lengths as read from the file, the negative-proof result, and the M-13
discrepancy.
</output>
