---
phase: 40-saved-views-shared-filters
plan: 08
type: execute
wave: 3
depends_on: ["40-01", "40-03", "40-06"]
files_modified:
  - src/components/views/save-view-dialog.tsx
  - src/components/views/__tests__/save-view-dialog-wiring.test.ts
autonomous: true
requirements: [VIEW-01, VIEW-02]

must_haves:
  truths:
    - "The save dialog's submit button is reachable inside a 640px-tall viewport"
    - "A user who cannot overwrite someone else's shared view is told whose it is, not shown a missing option"
    - "A duplicate name is refused next to the input and the typed name survives"
    - "Enter inside the name input submits; Enter on the submit button does not navigate the list behind the dialog"
  artifacts:
    - path: "src/components/views/save-view-dialog.tsx"
      provides: "the save/update dialog, S-1 through S-15"
      contains: "max-h-[calc(100dvh-2rem)]"
    - path: "src/components/views/__tests__/save-view-dialog-wiring.test.ts"
      provides: "the O-1 clamp gate and the T-1 leading-tight gate, scoped to extracted elements"
  key_links:
    - from: "src/components/views/save-view-dialog.tsx"
      to: "src/lib/views/actions.ts"
      via: "createView / updateView called inside a form onSubmit"
      pattern: "createView|updateView"
---

<objective>
The save / update dialog: one name field, two checkboxes, an optional target radio group, and the
phase's only primary-filled button — inside a `DialogContent` that declares its own height clamp
because the primitive declares none.

Purpose: this is where a filter set becomes a named object, so it is criterion 1's write half. It is
also the single most likely place for F-39-07 to recur: `DialogContent` has **no `max-h-*` and no
`overflow-y-*`** and the `/organizations` create dialog already measures 586px in a 640px viewport —
about 54px of headroom (M-8).
Output: `src/components/views/save-view-dialog.tsx` plus a scoped source gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md
@.planning/BACKLOG.md

@src/components/ui/dialog.tsx
@src/components/ui/radio-group.tsx
@src/components/ui/checkbox.tsx
@src/components/bulk/bulk-dialogs.tsx
@src/lib/views/types.ts
@src/lib/views/actions.ts
</context>

<interfaces>
<!-- Measured / read this session. Do not re-derive. -->

| Fact | Evidence |
|---|---|
| `DialogContent` has **no `max-h-*` and no `overflow-y-*`**; `max-height: none`, `overflow-y: visible`, `position: fixed`, `body { overflow: hidden }` | direct `getComputedStyle` read (M-8) |
| the `/organizations` create dialog already measures **586px in a 640px viewport** → ~54px headroom | M-8 |
| `DialogContent` inherits `max-w-[calc(100%-2rem)]` → **288px at a 320px viewport**, and `sm:max-w-lg` | `dialog.tsx`, M-8/R-6 |
| `DialogTitle` ships `text-lg leading-none font-semibold` — `leading-none` on a wrapping title is a Phase 39 forbidden pattern | `dialog.tsx:149` |
| `DialogDescription` ships `text-muted-foreground text-sm` | `dialog.tsx:162` |
| `DialogFooter` is `flex flex-col-reverse gap-2 sm:flex-row sm:justify-end` — at 320px it stacks and `col-reverse` puts the LAST DOM child visually FIRST, so the primary submit lands at the TOP of the stacked footer | `dialog.tsx:127` |
| es-ES `views.save.titleUpdate` = "Guardar los cambios de esta vista" — 33 chars, wraps inside 288px | UI-SPEC T-1 |
| `data-table-keyboard.tsx` registers `useHotkeys("enter", …, { preventDefault: true })` with **no ref**; `isFormFocused` exempts INPUT/TEXTAREA/SELECT/contenteditable but **not BUTTON**. Mounted on `/organizations` and `/people`. | F-39-08, BACKLOG.md |
| `radio-group.tsx` exists (added by Phase 39). `checkbox.tsx`, `switch.tsx`, `label.tsx`, `input.tsx` all exist. | directory listing |

Usable content width at a 320px viewport is **241px** (M-1); the dialog's own inner width is 288px
minus its padding.

From plan 40-06:
```ts
createView({ entityType, name, filters, isShared, makeDefault })
updateView({ id, name, filters, isShared, makeDefault })
// both -> { success: true, id?, name } | { success: false, error: "name_taken" | "name_required"
//                                                   | "no_filters" | "forbidden" | "failed" }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The dialog</name>
  <files>src/components/views/save-view-dialog.tsx</files>
  <behavior>
    Written before the component: the wiring gate in task 2 asserts, on the EXTRACTED
    `<DialogContent>` opening tag, that its `className` contains both `max-h-[calc(100dvh-2rem)]` and
    `overflow-y-auto`. Write that test first and watch it fail because the file does not exist.
  </behavior>
  <action>
    Create `src/components/views/save-view-dialog.tsx`, `"use client"`.

    Props: `{ open, onOpenChange, entityType, filters, selectedView: SavedViewSummary | null,
    canUpdateSelected: boolean }`. `filters` is the already-picked map from the bar.

    **O-1, MANDATORY:** `<DialogContent className="max-h-[calc(100dvh-2rem)] overflow-y-auto sm:max-w-lg">`.
    Both classes, at this call site, never as a `dialog.tsx` edit — that primitive is shared by
    roughly sixteen dialogs and retuning it is a different change. Write the reason inline: the
    primitive declares neither (M-8), the headroom is ~54px, and F-39-07 is a mobile dead-end that
    shipped past a GREEN horizontal-overflow gate. `dvh` not `vh`, because a mobile URL bar changes
    `vh` and F-39-07 is a mobile defect. Do **not** override `max-w-[calc(100%-2rem)]` (R-6).

    Structure:
      - `DialogTitle className="leading-tight"` (T-1, per call site) resolving ONE label at a time:
        `views.save.titleNew` when creating, `views.save.titleUpdate` when the target group is present
      - `DialogDescription` = `views.save.description`
      - a real `<form onSubmit={…}>` wrapping the fields (O-4: the name input is exempted by
        `isFormFocused`, so Enter INSIDE it submits the form safely; that is the whole reason this is a
        form and not a click handler)
      - `space-y-4` between field groups, `space-y-2` inside one (the spacing scale)

      - TARGET `RadioGroup` (S-3) — rendered only when `selectedView && canUpdateSelected`.
        Legend `views.save.targetLegend`; options `views.save.targetUpdate` with `{name}` and
        `views.save.targetNew`. **Default selection `targetUpdate`** — "I opened my view, tweaked it,
        and pressed save" means update far more often than fork.
      - When `selectedView && !canUpdateSelected` (S-4): NO radio group, and
        `views.save.targetNewOnly` with `{name}` and `{owner}` in its place at
        `text-xs text-muted-foreground`. `{owner}` comes from `selectedView.ownerLabel`, and when that
        is `null` use `views.ownerUnavailable` as the substitution — **two of three live users have
        `name = NULL` and six are soft-deleted, so both branches are live here.** A refusal that names
        neither the reason nor the person is how a user concludes the feature is broken.

      - NAME field (S-5): `Label` + `Input`, `views.save.nameLabel` / `views.save.namePlaceholder`.
        Pre-filled with `selectedView.name` when the target is `targetUpdate`, empty otherwise.
        **Never pre-filled with a generated name.** `autoFocus` — it is the surface's focal point and
        the only control with no correct default.
      - INLINE ERROR slot beneath the input, `text-xs text-destructive`, holding
        `views.save.nameRequired` or `views.save.nameTaken` with `{name}`. **The submit is NOT
        disabled on an empty name** (S-7, P-7): a disabled control with no adjacent reason is
        forbidden; the inline message on submit is discoverable and announced. The error survives
        while the user retypes — clear it on the next SUBMIT, not on every keystroke.

      - SHARED (S-8): `Checkbox` + `Label` `views.save.sharedLabel`. Helper line beneath resolving on
        the checkbox state: `views.save.sharedHelp` when checked, `views.save.privateHelp` when not.
        `Checkbox` and not `Switch`, because the change takes effect on submit and a `Switch` in this
        app implies immediate effect. Default: `selectedView?.isShared ?? false`.
      - DEFAULT (S-10): `Checkbox` + `Label` `views.save.defaultLabel`, helper
        `views.save.defaultHelp`. The helper carries **no `{entity}` placeholder** — one sentence for
        all four surfaces, deliberately, because interpolating the entity noun into pt-BR and es-ES
        needs gender agreement on the article and would turn one key into twelve. Default:
        `selectedView?.isDefaultForViewer ?? false`.

      - `DialogFooter` (S-11/S-12): Cancel `common.cancel` `variant="outline"` FIRST in the DOM,
        submit LAST. `col-reverse` therefore puts the submit at the top of the stacked 320px footer,
        which is the primitive's behaviour and is NOT overridden — plan 40-15 asserts it is
        trial-clickable in that arrangement. Submit is `type="submit"`, the default (primary-filled)
        variant, reading `views.save.submit` → `views.save.submitting` with a
        `Loader2 className="text-primary animate-spin"` while in flight. It is the **only**
        primary-filled control this phase adds anywhere; write that in a comment so nobody adds a
        second. Pass no close-label override (K-4).

      - SUBMIT handler: `startTransition` around the action call. In-flight state is set inside the
        transition callback, **never in an effect body** — `react-hooks/set-state-in-effect` is an
        ERROR in this repo (K-7).
        Success (S-13): `toast.success(views.save.created | views.save.updated)` with `{name}` and
        `onOpenChange(false)`. The action's `revalidatePath` re-renders the bar; do not refetch.
        `name_taken` → set the inline error, keep the dialog open, keep every field.
        `name_required` → the same slot.
        `forbidden` → `toast.error(views.save.failed)` and keep it open.
        Anything else (S-14) → `toast.error(views.save.failed)`, dialog stays open, fields intact:
        the user's next act is retry, so nothing is discarded.

    FORBIDDEN in this file, and each is checkable:
      - any `Popover` or `DropdownMenuSubContent` (O-2)
      - `text-primary` as a TEXT colour (F-39-06 non-dependency); the only `text-primary` permitted is
        on the `Loader2`
      - `leading-none` anywhere (T-1)
      - `bg-green-500` / `bg-amber-500` / `text-red-600` / `text-green-600` / `text-amber-500` (K-2) —
        `activities-client.tsx:253,258` has two of these and is the anti-pattern reference, not a
        template
      - any font size outside `text-xs` / `text-sm` / `text-lg`
      - any `grid-cols-2` without an `sm:` prefix (R-4)
      - any hardcoded user-visible English string. Every one comes from the catalog.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run src/components/views/</automated>
  </verify>
  <done>typecheck 0, lint 0 errors. The clamp gate written in task 2 goes from RED to GREEN. `views.save.*` usage count in the file matches the 22 keys minus the ones this dialog does not own (`noFilters` belongs to the server, `submitting`/`submit` both used).</done>
</task>

<task type="auto">
  <name>Task 2: The scoped source gate</name>
  <files>src/components/views/__tests__/save-view-dialog-wiring.test.ts</files>
  <action>
    Create the gate following `src/app/organizations/__tests__/toolbar-wiring.test.ts` in shape: a
    header saying what the test is NOT (it measures nothing; the real 320px measurement is plan
    40-15's), `readStrippedSource` so the prose above cannot satisfy anything below, and **every
    assertion scoped to an extracted element rather than made against the whole file.**

    Reuse `readStrippedSource` and `callArguments` from
    `@/components/custom-fields/__tests__/source-scan`. Write a small tag extractor (or reuse
    `toolbar-wiring.test.ts`'s brace/tag-depth walker in shape — do NOT add a third brace matcher to
    the repo; BACKLOG.md already records two that should be consolidated. Import or copy the shape,
    and say which you did and why).

    Assertions, each naming its rule and its file:
      1. O-1 — the extracted `<DialogContent` opening tag's `className` contains BOTH
         `max-h-[calc(100dvh-2rem)]` and `overflow-y-auto`. **Scoped to the opening tag**, because a
         file-wide search would be satisfied by the comment explaining the rule — the K-9 trap that
         was hit five times in Phase 39.
      2. R-6 — that same opening tag does NOT contain `max-w-[` other than inside `sm:max-w-lg`
         (the inherited `max-w-[calc(100%-2rem)]` must not be overridden).
      3. T-1 — every extracted `<DialogTitle` opening tag's className contains `leading-tight`, and
         the stripped source contains zero occurrences of `leading-none`.
      4. S-12 — inside the extracted `<DialogFooter>` region, the Cancel button appears at a LOWER
         index than the submit button. That is what makes `col-reverse` put the submit on top at
         320px. Assert by index comparison, with both indices in the failure message.
      5. O-2 — the stripped source contains zero `Popover` and zero `DropdownMenuSubContent`.
      6. K-2 — zero `bg-green-500`, `bg-amber-500`, `text-red-600`, `text-green-600`,
         `text-amber-500`.
      7. F-39-06 — the only `text-primary` occurrence is on the same JSX element as
         `animate-spin`. Extract the tag; do not count occurrences file-wide.
      8. R-4 — zero `grid-cols-2` that is not preceded by `sm:`.
      9. S-7 — the extracted submit `<Button` opening tag contains no `disabled={` binding that
         references the name state. Assert the tag's text does not match `/disabled=\{[^}]*name/`,
         with S-7 and P-7 named in the message.

    **NEGATIVE PROOF, TO BE RUN, for the one assertion that matters most:** delete
    `max-h-[calc(100dvh-2rem)]` from the `DialogContent` className, confirm assertion 1 turns RED and
    that nothing else changes, restore it. Record the failure text. Then, separately, add a comment
    line to the component containing the literal string `max-h-[calc(100dvh-2rem)]` and confirm
    assertion 1 is **still RED** — that is the proof the gate is scoped and not a token grep. Record
    both observations. This second half is the exact check Phase 39 lacked five times.
  </action>
  <verify>
    <automated>npx vitest run src/components/views/__tests__/save-view-dialog-wiring.test.ts && npm test</automated>
  </verify>
  <done>All nine assertions green. Both halves of the negative proof were RUN and their outputs recorded — including the comment-only probe staying RED. Full `npm test` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog form → server action | the submitted name and filter map are attacker-authored; plan 40-06 re-derives both |
| keyboard event → the page behind the modal | `data-table-keyboard.tsx`'s ref-less `useHotkeys("enter")` fires inside a portalled dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-35 | Denial of service (functional) | `DialogContent` height | mitigate | `max-h-[calc(100dvh-2rem)] overflow-y-auto` at the call site. Without it the submit can leave the 640px viewport with nothing to scroll — F-39-07 verbatim, which shipped past a green gate |
| T-40-36 | Tampering | Enter on the focused submit | accept + work around | F-39-08 is app-wide and touches six surfaces; fixing it is out of scope. The fields live in a real `<form>` so Enter in the name input submits (the input IS exempted by `isFormFocused`), and plan 40-15's V-40-11 asserts Enter on the submit does not change the URL. Working within a known defect is not inheriting it silently |
| T-40-37 | Information disclosure | `views.save.privateHelp` | mitigate | the checkbox's unchecked helper states Decision 3 in words the user can act on. A user who cannot read "Nobody else, including admins" cannot rely on it |
| T-40-38 | Spoofing | `{owner}` in `views.save.targetNewOnly` | mitigate | resolved from `selectedView.ownerLabel` (server-computed, `name \|\| email`, `null` → `views.ownerUnavailable`), never from a client-side lookup and never a uuid |
| T-40-SC | Tampering | npm installs | mitigate | **do not run `shadcn add`** — it silently bumps `radix-ui` `^1.4.3` → `^1.6.7` and rewrites ~1184 lines of `package-lock.json`. Every primitive this dialog needs already exists (`dialog`, `radio-group`, `checkbox`, `label`, `input`, `button`, `sonner`). If one is genuinely missing, STOP and report; vendor by hand with the unified `radix-ui` import (K-5) |
</threat_model>

<verification>
- `npm run typecheck` 0, `npm run lint` 0 errors, full `npm test` green
- all nine wiring assertions green, each scoped to an extracted element
- both halves of the clamp negative proof RUN and recorded
- `git diff src/components/ui/` is empty — the clamps are call-site classNames, never primitive edits
- `git diff package-lock.json` is empty
</verification>

<success_criteria>
- The dialog declares its own fits-or-scrolls clamp, asserted on the opening tag
- The `!canUpdateSelected` branch renders a sentence naming the view and its owner, not a gap
- The submit is never disabled on an empty name
- Zero hardcoded user-visible strings
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-08-SUMMARY.md` when done. Record the RED
count, both negative-proof outputs, and whether you imported or copied the tag extractor.
</output>
