---
phase: 40-saved-views-shared-filters
plan: 16
type: execute
wave: 9
depends_on: ["40-15"]
files_modified:
  - e2e/saved-views-visibility-member.spec.ts
  - e2e/saved-views-visibility-admin.spec.ts
  - e2e/saved-views-degraded.spec.ts
autonomous: true
requirements: [VIEW-02]

must_haves:
  truths:
    - "An admin's private view is absent from a member's picker AND from the member's manage dialog"
    - "A member's private view is absent from the admin's picker AND manage dialog"
    - "A shared view created by the admin appears for the member, and the member can set it as their own default"
    - "A view whose stored owner or pipeline no longer exists renders a 200 with the list intact and a muted notice"
  artifacts:
    - path: "e2e/saved-views-visibility-member.spec.ts"
      provides: "V-40-8, the member direction — an admin's private view is invisible to a member"
      contains: "E2E_MEMBER_EMAIL"
    - path: "e2e/saved-views-visibility-admin.spec.ts"
      provides: "V-40-8, the DEPARTING direction — a member's private view is invisible to an admin"
    - path: "e2e/saved-views-degraded.spec.ts"
      provides: "V-40-9 — the degraded-read gate"
  key_links:
    - from: "e2e/saved-views-visibility-member.spec.ts"
      to: "playwright.config.ts"
      via: "the chromium-member project's storageState"
      pattern: "member"
---

<objective>
Close criterion 2 with a two-session proof, and close the degraded-read contract with a
200-plus-notice proof.

Purpose: **a passing source gate is how an unreachable feature ships.** Plan 39-14's gates passed while
the organization duplicate warning could not fire from any surface, and it took a human driving a
browser to find it. Criterion 2 is the one criterion no source gate can establish — it is a claim about
what a DIFFERENT user sees — and criterion 3's landing behaviour is a claim about what happens when
somebody opens a page.
Output: two e2e specs. The dark-mode pass, the backlog entry and the human checkpoint are plan 40-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-CONTEXT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md
@.planning/BACKLOG.md

@e2e/views-fixtures.ts
@e2e/seed-member.ts
@e2e/saved-views-320.spec.ts
@playwright.config.ts
</context>

<interfaces>
<!-- From plan 40-04. -->
`e2e/seed-member.ts` → `E2E_MEMBER_EMAIL`, `seedE2eMember`. `e2e/.auth/member.json` is the member
storageState, mounted by the `chromium-member` project whose `testMatch` is `/.*-member\.spec\.ts/` —
**so the visibility spec's filename MUST end in `-member.spec.ts`** or it will run under the admin
session and prove the opposite of what it claims.

`e2e/views-fixtures.ts` → `VIEWS_FIXTURE_PREFIX`, `openDb`, `userIdByEmail`, `insertViewFixture`,
`setDefaultFixture`, `purgeViewFixtures`.

<!-- Live data facts that make each branch reachable. Measured this session. -->
| Fact | Value |
|---|---|
| active users | 3 (2 admins, 1 member) |
| active users with `name = NULL` | 2 |
| soft-deleted users | **6** — so the `views.ownerUnavailable` branch is live |
| live pipelines | 11 — so a deleted-pipeline fixture is realistic |
| live activities | 79,022 |

`/deals` currently renders `t('pipelineNotFound')` for an unknown `?pipeline=` (`page.tsx:87`). Plan
40-12 replaced that path with a fallback to the default board. This spec proves the fallback.

Dark mode is reachable since Phase 45 (`ThemeProvider` in `src/app/layout.tsx`, a three-value
`DropdownMenuRadioGroup` in `UserMenu`, `documentElement.className === "dark"`).
F-39-06: `text-primary` links measure `lab(90.952)` against a `lab(98.26)` body in dark mode. This
phase renders **zero `text-primary` text** by design, and the dark-mode pass should confirm the
ABSENCE rather than infer it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: V-40-8 — the private-visibility gate, both directions</name>
  <files>e2e/saved-views-visibility-member.spec.ts</files>
  <action>
    Create `e2e/saved-views-visibility-member.spec.ts` — the filename suffix is load-bearing
    (`chromium-member`'s `testMatch`). Add an assertion in `beforeAll` that the running session IS the
    member: navigate to `/admin/audit` and assert the pathname is NOT `/admin/audit`. **Without that,
    a mis-mounted storageState would run this whole spec as an admin and every "absent" assertion
    would pass for the wrong reason.**

    Fixtures, seeded directly in the database in `beforeAll` (purge first, per the 45-08 rule):
      - `ADMIN_PRIVATE` — owned by `E2E_ADMIN_EMAIL`, `isShared: false`, `entityType: "organization"`,
        filters `{ search: "adminprivate" }`
      - `ADMIN_SHARED` — owned by `E2E_ADMIN_EMAIL`, `isShared: true`, filters `{ search: "adminshared" }`
      - `MEMBER_PRIVATE` — owned by `E2E_MEMBER_EMAIL`, `isShared: false`, filters `{ search: "memberprivate" }`
    All three names prefixed with `VIEWS_FIXTURE_PREFIX`.

    Assertions as the MEMBER, on `/organizations`:
      1. anchor on the locale-dependent `h1` first
      2. open the picker: `ADMIN_SHARED`'s name is visible under `views.groupShared`
      3. **`ADMIN_PRIVATE`'s name is ABSENT from the open menu** — `expect(locator).toHaveCount(0)`
      4. `MEMBER_PRIVATE` is visible under `views.groupMine`
      5. open the manage dialog: `ADMIN_PRIVATE` is **absent there too**. Both surfaces, because a view
         hidden from the picker but listed in the manage dialog is still disclosed.
      6. **ANTI-VACUITY, and it is essential:** assert the member's menu is not simply empty — assert
         `ADMIN_SHARED` and `MEMBER_PRIVATE` are both present in the same open menu as the absent
         `ADMIN_PRIVATE`. A broken query returning zero rows would satisfy assertions 3 and 5 and prove
         nothing. Name this in the test title.
      7. G-7 — toggle the default `Switch` on `ADMIN_SHARED`'s manage row and assert it commits
         (`views.manage.saved` toast). Then reload `/organizations` with no params and assert the URL
         redirected to a query carrying BOTH `search=adminshared` **and** `view=<ADMIN_SHARED's id>`.
         **Do not assert the exact string `?search=adminshared`** — plan 40-18 made
         `resolveDefaultViewRedirect` name its view, so the default landing now carries the selection
         too. Assert the two params by parsing the URL, not by comparing it. Then assert the picker
         trigger names the view rather than reading `views.allRecords`, which is what the `view=` param
         buys and is the half a filters-only assertion would miss.
         **This is criterion 3 across an ownership boundary**, and it is the payoff CONTEXT names for
         sharing at all.
      8. assert the member's manage row for `ADMIN_SHARED` shows `views.manage.readOnly` and has NO
         share switch and NO delete button — visibility only; plan 40-06 is the authorization.

    Then a SECOND spec, `e2e/saved-views-visibility-admin.spec.ts` — a NEW file, so it runs under the
    default `chromium` project with the ADMIN storageState. Do not add it to
    `e2e/saved-views-320.spec.ts`: that file belongs to plan 40-15 and this phase keeps one owner per
    file. Its `beforeAll` seeds the same three fixtures (share the fixture-name constants by exporting
    them from the member spec, or put them in `e2e/views-fixtures.ts` — say which) and asserts the
    session IS an admin (`/admin/audit` renders), the mirror of the member spec's check.
      9. as the ADMIN, open the picker on `/organizations` and assert **`MEMBER_PRIVATE` is absent**,
         and absent from the admin's manage dialog too.
      **This is the direction that departs**: the app's established idiom is
      `owner || role === "admin"` (`src/app/deals/actions.ts:83`, locked for Trash in
      37-CONTEXT.md:31), and Decision 3 deliberately breaks it because "private" that an admin can
      read is not private. An admin who can see a member's private view means `listVisibleViews` grew
      an admin branch. Put that sentence in the failure message.
      Include the same anti-vacuity companion: `ADMIN_PRIVATE` and `ADMIN_SHARED` must both be present
      in that same open menu.

    `purgeViewFixtures` in `afterAll`. Budget `retries: 1` awareness: if the member setup flakes, the
    whole project fails — record any flake rather than re-running until green.
  </action>
  <verify>
    <automated>npx playwright test --project=chromium-member 2>&1 | tail -30 && npx playwright test e2e/saved-views-visibility-admin.spec.ts --project=chromium 2>&1 | tail -30</automated>
  </verify>
  <done>Both directions green, both with their anti-vacuity companions. The member-session assertion in `beforeAll` confirmed the session is not an admin. Zero fixture rows remain.</done>
</task>

<task type="auto">
  <name>Task 2: V-40-9 — the degraded-read gate</name>
  <files>e2e/saved-views-degraded.spec.ts</files>
  <action>
    Create `e2e/saved-views-degraded.spec.ts` (admin session, `chromium` project).

    Fixtures — seed views whose stored ids are dead, using ids that provably do not exist rather than
    deleting live rows (**never delete a live pipeline or user from this database**):
      - `DEAD_OWNER` on `entityType: "activity"`, filters `{ owner: <a soft-deleted user's real id>,
        type: <a real activity type id> }`. Read a soft-deleted user's id from the database — there are
        six — so the fixture exercises the real branch rather than a random uuid.
      - `DEAD_PIPELINE` on `entityType: "deal"`, filters `{ pipeline: <a random uuid>, owner: <a real
        active user id> }`
      - `DEAD_STAGE` on `entityType: "deal"`, filters `{ pipeline: <a real pipeline id>,
        stage: <a stage id belonging to a DIFFERENT pipeline> }` — the cross-pipeline stage case plan
        40-05 validates. With 11 pipelines and 73 stages this is easy to construct.

    For each, select the view from the picker and assert:
      1. the response is **200** and the `h1` is visible (assert the response status via
         `page.goto`'s return value on the redirected URL, and the heading for the render)
      2. the list rendered — at least one row, or the surface's legitimate empty state. Assert
         whichever applies and say which; "the page did not crash" is not the same claim as "the list
         rendered", and Phase 39's lesson is that only the second one matters.
      3. `views.degraded` is visible — one muted `text-xs` line beneath the bar
      4. **no `Alert`**: assert `page.locator('[data-slot="alert"]')` has count 0, and specifically no
         destructive-variant alert. C-40-4: a view whose owner or pipeline no longer exists still
         rendered a list; nothing failed, nothing is unsafe, and a red panel over a working list
         teaches the user that red means nothing.
      5. for `DEAD_PIPELINE` on `/deals`: the default board rendered, and the text of
         `deals.pipelineNotFound` is **NOT** present. Read that string from the message catalog.
         This is Decision 4's whole point.
      6. **ANTI-VACUITY:** also select a fully VALID view on each surface and assert `views.degraded`
         has count 0. A component that always rendered the notice would pass assertions 3 above and be
         a permanent false alarm.

    Also assert the no-500 posture explicitly: there is **no `error.tsx` anywhere under `src/app`**
    (M-14), so a throw here is a blank page rather than a caught error. Verify that claim in this task
    (`find src/app -name error.tsx` → 0 results) and record it — an assertion should not assume a fact
    about the tree it has not checked.
  </action>
  <verify>
    <automated>test $(find src/app -name error.tsx | wc -l) -eq 0 && npx playwright test e2e/saved-views-degraded.spec.ts --project=chromium 2>&1 | tail -30</automated>
  </verify>
  <done>All three degraded fixtures render 200 with the list intact, the muted notice, and no Alert. The `/deals` case rendered the default board and never the `pipelineNotFound` text. The anti-vacuity valid-view check passes. `find src/app -name error.tsx` returned 0 and is recorded. Zero fixture rows remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| one user's session → another user's private data | the whole of criterion 2; no source gate can establish it |
| a green suite → a shipped feature | 39-14's gates passed while the feature could not fire from any surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-75 | Information disclosure | private views | mitigate | proved absent in BOTH directions and on BOTH surfaces (picker and manage dialog), with an anti-vacuity companion so an empty query cannot pass. The admin direction is the one that departs from the app's `owner \|\| role === "admin"` idiom, so it is the one that must be asserted |
| T-40-76 | Spoofing | the member storageState | mitigate | the spec's `beforeAll` asserts the running session is refused at `/admin/audit`. A mis-mounted state would otherwise make every absence assertion pass as an admin looking at their own views |
| T-40-77 | Denial of service (functional) | a degraded read | mitigate | 200 + list rendered + muted notice + no Alert, on three separate degradation kinds. There is no `error.tsx` under `src/app` (checked in task 2, not assumed), so a throw is a blank page |
| T-40-78 | Elevation of privilege | the member's manage row | mitigate | asserted to show no share switch and no delete button, AND plan 40-06 refuses the mutation server-side. The UI assertion is the reachability half; the action test is the control half |
| T-40-80 | Denial of service (data loss) | fixtures | mitigate | no live pipeline or user is ever deleted; dead-id fixtures use a real soft-deleted user id and random uuids. Prefix-scoped purge in `beforeAll` and `afterAll` |
| T-40-SC | Tampering | npm installs | mitigate | this plan installs nothing |
</threat_model>

<verification>
- `npx playwright test` green across all three projects, run twice
- V-40-8 green in both directions with both anti-vacuity companions
- V-40-9 green on all three degradation kinds plus the valid-view companion
- `find src/app -name error.tsx` → 0, recorded
- zero fixture rows remain; production counts unchanged (46054 / 38348 / 25195 / 79022)
</verification>

<success_criteria>
- Criterion 2 proved in the direction that departs from the app's existing idiom, with an
  anti-vacuity companion in each direction
- Every degradation renders a working list and a sentence, never a red panel and never a 500
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-16-SUMMARY.md` when done. Record both
anti-vacuity results, the soft-deleted user id you used, and the `find src/app -name error.tsx` output.
</output>
