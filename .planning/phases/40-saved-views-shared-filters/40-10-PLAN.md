---
phase: 40-saved-views-shared-filters
plan: 10
type: execute
wave: 4
depends_on: ["40-01", "40-03", "40-05", "40-06", "40-07", "40-08", "40-09"]
files_modified:
  - src/components/views/saved-views-bar.tsx
  - src/components/views/__tests__/saved-views-bar-wiring.test.ts
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03]

must_haves:
  truths:
    - "The bar derives nothing: every piece of its state arrives as a prop"
    - "Selecting a view navigates to that view's stored filters and to page 1"
    - "Selecting All records navigates to ?view=none and is not bounced back into the default"
    - "On an unfiltered list the save slot is a sentence, not a greyed button"
    - "An export that cannot be authorized is a disabled menu item with its reason beside it"
  artifacts:
    - path: "src/components/views/saved-views-bar.tsx"
      provides: "SavedViewsBar — one component, four mounts (B-1..B-6, V-1..V-11, E-1..E-4)"
      contains: "withViewEscape"
    - path: "src/components/views/__tests__/saved-views-bar-wiring.test.ts"
      provides: "V-40-5 — the parsed-interface gate plus the derives-nothing gate"
  key_links:
    - from: "src/components/views/saved-views-bar.tsx"
      to: "src/lib/views/url-params.ts"
      via: "withViewEscape on every navigation this component performs"
      pattern: "withViewEscape"
    - from: "src/components/views/saved-views-bar.tsx"
      to: "src/lib/views/export-action.ts"
      via: "exportViewResults inside a startTransition, then the bulk-action-bar download idiom"
      pattern: "exportViewResults"
---

<objective>
The one component this phase adds: a two-slot, non-sticky row holding a `DropdownMenu` picker and a
label-resolving save control, with the export in-flight state living in the bar rather than in the
menu.

Purpose: it is the phase's primary affordance and its only new component. Everything it must not do
is measured: it must not live in a `Popover` (the `/activities` filter popover already renders 388px
into a 347px slot with 41px clipped off the top of the viewport), it must not be sticky or fixed
(`bulk-action-bar.tsx` already owns one fixed bar on all four of these pages and D-45-02 is an open
UAT item about exactly that), and it must not derive `isModified` on the client.
Output: `src/components/views/saved-views-bar.tsx` plus the V-40-5 gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md
@.planning/BACKLOG.md

@src/components/ui/dropdown-menu.tsx
@src/components/bulk/bulk-action-bar.tsx
@src/app/activities/activity-filters.tsx
@src/lib/views/types.ts
@src/lib/views/url-params.ts
@src/lib/views/export-action.ts
</context>

<interfaces>
<!-- Measured / read this session. Do not re-derive any of it. -->

| Fact | Evidence |
|---|---|
| `DropdownMenuContent` IS height-safe by construction: `max-h-(--radix-dropdown-menu-content-available-height)` + `overflow-y-auto`. Pass no class that removes either. | `dropdown-menu.tsx:45` (M-7) |
| `DropdownMenuSubContent` is `overflow-hidden` with **no clamp** and opens sideways into a 241px budget — forbidden (O-2) | `dropdown-menu.tsx:233` (M-7) |
| `PopoverContent` never consumes `--radix-popover-content-available-height`; the `/activities` filter popover renders **388px at `top: -41`** in a 640px viewport — forbidden (O-2) | `popover.tsx:33` (M-5) |
| `DropdownMenuRadioGroup` and `DropdownMenuRadioItem` are exported | `dropdown-menu.tsx:111,122` |
| `DropdownMenuItem`'s default inset is `px-2 py-1.5` — 6px vertical, too tight for a two-line item. Override the vertical half to `py-2`; keep `px-2` so a view item and `Manage views` share one left edge. | `dropdown-menu.tsx:66` |
| a two-control bar wraps to two rows at 241px in all three locales: 200px trigger + 8px gap + 139px "Guardar cambios" = 347 > 241. **There is no trigger width at which both fit with the Spanish label.** Accepted, not fought. | M-10 |
| the Blob/ObjectURL download idiom, including the `revokeObjectURL`, is at `bulk-action-bar.tsx:73-83`. Copy in SHAPE; do not write a fourth downloader. | file read |
| `bulk.exported` = `{count, plural, one {# record exported.} other {# records exported.}}` and `bulk.error.exportFailed` exist and are REUSED VERBATIM. `REQUIRED_BULK_KEYS` stays at its current length. | `en-US.json` |
| `activity-filters.tsx` wraps in `<Suspense fallback={<Button variant="outline" size="sm">…}>` — the precedent for this component's fallback | `activity-filters.tsx:356` |
| `react-hooks/set-state-in-effect` is an **ERROR** in this repo | eslint config |

From plan 40-01:
```ts
// src/lib/views/types.ts — DECLARED THERE, re-exported here
export interface SavedViewsBarProps {
  entityType: ViewEntityType
  views: SavedViewSummary[]
  selectedViewId: string | null
  isModified: boolean
  droppedFilterKeys: string[]
  canSave: boolean
  canExport: boolean
  canUpdateSelected: boolean
}
```
```ts
withViewEscape(entityType, params: URLSearchParams): string   // no leading "?"
filtersToSearchParams(entityType, filters): URLSearchParams
VIEW_ESCAPE_KEY, VIEW_ESCAPE_VALUE
```
From plan 40-07: `exportViewResults({ entityType, filters })`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The picker and the save resolver</name>
  <files>src/components/views/saved-views-bar.tsx</files>
  <behavior>
    The task-3 gate is written first. Its two headline assertions: the exported `SavedViewsBarProps`
    names exactly the eight B-2 properties, and the component body contains **no** comparison of
    `useSearchParams()` output against a `filters` object.
  </behavior>
  <action>
    Create `src/components/views/saved-views-bar.tsx`, `"use client"`.

    Re-export the props type: `export type { SavedViewsBarProps } from "@/lib/views/types"`. The
    interface is DECLARED in `src/lib/views/types.ts` (plan 40-01) so the server resolver can return it
    without a client module entering a server import graph, and so there is ONE declaration of the
    eight props rather than two that can drift. V-40-5's parsed gate reads that file; note it here.

    **B-2 — THE BAR DERIVES NOTHING.** Write the three reasons in the header, because they are the
    reason this is a rule and not a preference:
      1. No loading state, no flash. All four hosts are server components that already
         `await searchParams`, so the views arrive with the first paint. A client-fetched picker would
         flash "All records" over the user's actual default view on every navigation. **There is
         therefore no loading state anywhere in this component, and that absence is a decision.**
      2. `isModified` and `droppedFilterKeys` both manifest as "the URL differs from the stored blob",
         and only the server knows WHICH: a key the user changed versus a key the read-side validator
         dropped because its target no longer exists. A client comparing the two would label every
         degraded view "Modified" and invite the user to save the damage.
      3. It is gateable, and task 3 gates it.

    **B-3** — wrap the exported component in `<Suspense>` (it calls `useSearchParams`, `usePathname`
    and `useRouter`), following `activity-filters.tsx:356`. The fallback is the picker trigger in its
    disabled shape reading `views.allRecords`. Export the wrapper as `SavedViewsBar` and keep the
    inner as a module-private `SavedViewsBarInner`.

    **B-4 / R-40-2f** — the row is `className="flex flex-wrap items-center gap-2 min-w-0"`. **Not
    sticky, not fixed, no spacer.** `bulk-action-bar.tsx` already owns one fixed bar on all four of
    these pages and D-45-02 is a live open UAT item about a fixed bar occluding content; a second one
    is not available to this phase. `flex-wrap` is load-bearing, not decoration — the row wraps to two
    at 241px in all three locales (M-10) and that is accepted.

    SLOT 1 — the picker (`DropdownMenu`):
      - Trigger (V-1): `Button variant="outline"` with `min-w-0 max-w-[200px]`, a `ChevronDown` at
        `shrink-0`, `aria-label` (or an sr-only span) of `views.picker.label`, and a visible label of
        the selected view's `name` with `truncate`, or `views.allRecords` when nothing is selected.
        When `isModified`, a `Badge variant="secondary" shrink-0` reading `views.modified` follows the
        name. `min-w-0` on the trigger, `truncate` on the name, `shrink-0` on the badge — R-40-2b, and
        `shrink-0` on the badge is what guarantees the STATE survives when the NAME does not.
        `max-w-[200px]` is measured (M-10): wider and the trigger alone eats the 241px row before the
        badge; narrower and an ordinary name shows four characters.
      - `DropdownMenuContent`: pass **no** className that removes `max-h-(--radix-…)` or
        `overflow-y-auto`. It is height-safe by construction and that is the only reason the menu may
        be unbounded.
      - Selection is a `DropdownMenuRadioGroup` of `DropdownMenuRadioItem`s (V-2), value
        `selectedViewId ?? VIEW_ESCAPE_VALUE`. `role="menuitemradio"` + `aria-checked` exposes the
        selection to assistive tech instead of drawing it; the `Check` indicator the block ships is a
        shape, not a colour, and is additive.
      - Menu anatomy top to bottom (V-3), exactly this order:
        (1) `views.allRecords` as the first radio item, always present, always first
        (2) `DropdownMenuSeparator`
        (3) `DropdownMenuLabel` = `views.groupMine`, then `views.filter(v => v.isOwnedByViewer)`
            alphabetically
        (4) `DropdownMenuSeparator` + `DropdownMenuLabel` = `views.groupShared`, then
            `views.filter(v => !v.isOwnedByViewer)` alphabetically. **This group contains only shared
            views owned by someone else and never a private view, for any viewer including an admin**
            (V-6) — and it is guaranteed by the server's SQL predicate (plan 40-05), not by this
            filter. State that: the client filter is a grouping, not a control.
        (5) `DropdownMenuSeparator`
        (6) `views.saveNew` (present whenever `canSave`) — so the capability stays reachable even when
            slot 2 renders nothing
        (7) `views.exportAction`
        (8) `views.manageAction`
        **The default view is NOT floated to the top** — it carries the word `views.badgeDefault`
        instead, because a list that reorders itself under the user's finger is a misclick generator.
      - Each view item is TWO STACKED LINES (V-4, R-40-2d), `py-2` overriding the primitive's
        `py-1.5`: line 1 the name at `text-sm` with `truncate`; line 2 the state words at
        `text-xs text-muted-foreground` joined by ` · ` — visibility, then default, then owner.
        **Line 2 is never truncated.** Never one horizontal row of name + badges: that cannot survive
        241px, and truncating it would delete the state words.
      - Owner attribution (V-5): `views.ownedBy` with `{owner}` = `view.ownerLabel`, and
        `views.ownerUnavailable` when `ownerLabel` is `null`. **Both branches are live in this
        deployment** — two of three active users have `name = NULL` and six users are soft-deleted.
        Never a blank, never a uuid.
      - No cap, no paging (V-7): the primitive clamps and scrolls.
      - `views.emptyMenu` as a non-interactive `DropdownMenuLabel` between the separators when there
        are zero visible views (V-8). `views.allRecords`, `views.saveNew` and `views.manageAction`
        stay present — the menu is never a dead end.
      - Selecting a view (V-9): build `filtersToSearchParams(entityType, view.filters)` — **from the
        view's filters only**, so every param outside the whitelist is dropped including `page`; a
        view lands you on page 1 — then `router.push(\`${pathname}?${withViewEscape(entityType,
        params)}\`)`. Selecting `views.allRecords` pushes
        `\`${pathname}?${withViewEscape(entityType, new URLSearchParams())}\`` → `?view=none`.
      - The menu closes on selection for every item except… nothing (V-10 + E-4): `onSelect` is NOT
        prevented anywhere in this component, including on the export item.

    SLOT 2 — the resolver (B-5), ONE control and ONE label at a time, in this precedence:
      | condition | renders |
      |---|---|
      | export in flight | `Loader2 className="text-primary animate-spin"` + `views.exporting`, muted, non-interactive — overrides everything below |
      | `!canSave` | **`views.needsFilter`** as `text-xs text-muted-foreground`. **NOT a disabled button** — 39-UI-SPEC P-7 forbids a bare greyed control with no explanation, a sentence in the control's place IS the explanation, and it costs the same 36px row. On an unfiltered list the bar has nothing else to say, so this is the bar's empty state rather than noise. |
      | `canSave && !selectedViewId` | `Button variant="outline"` reading `views.saveNew` |
      | `canSave && selectedViewId && isModified && canUpdateSelected` | `Button variant="outline"` reading `views.saveChanges` |
      | `canSave && selectedViewId && isModified && !canUpdateSelected` | `Button variant="outline"` reading `views.saveNew` (the dialog explains via `views.save.targetNewOnly`) |
      | `canSave && selectedViewId && !isModified` | **nothing** |

    ACCENT BUDGET (§ Color): slot 2's control is `variant="outline"`, the trigger is
    `variant="outline"`, `Manage views` and `Export these results` are menu items. **The bar spends
    ZERO primary-filled buttons** — all four host pages already spend their one on
    "Add Organization" / "Add Person" / "Add Deal" / "Add Activity". The only `text-primary` in this
    file is on the `Loader2`.

    V-11 — the degraded notice: when `droppedFilterKeys.length > 0`, one
    `text-xs text-muted-foreground` line **beneath** the bar reading `views.degraded`. Never inside
    the menu, never an `Alert`, never destructive (C-40-4): a view whose owner or pipeline id no longer
    exists still rendered a list; nothing failed, nothing is unsafe, and a red panel over a working
    list teaches the user that red means nothing. There is no `--warning` token and this phase does not
    add one. One sentence covers all three degradations deliberately — naming which key was dropped
    would need a per-key label resolver, a second field-label map that 39-UI-SPEC M-4 refused, and the
    repair is identical in all three cases.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <done>typecheck 0, lint 0 errors. All 18 `views.*` bar keys referenced. Zero `Popover`, zero `DropdownMenuSubContent`, zero `sticky`, zero `fixed` in the file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Export, save and manage wiring</name>
  <files>src/components/views/saved-views-bar.tsx</files>
  <behavior>
    Gate assertions written with this task (task 3 hosts them): the extracted `onSelect` handler for
    the export item calls `startTransition`, the file contains a `URL.revokeObjectURL` call, and the
    file contains zero `useEffect` bodies that call a `set`-prefixed function (K-7).
  </behavior>
  <action>
    EXPORT (E-1..E-6):
      - The affordance is `views.exportAction` in the picker menu, **not a toolbar button** — all four
        toolbars are at or past their width budget (M-2 `/activities` and M-3 `/deals` are EXACTLY full
        at 241px; M-4 `/organizations` and `/people` already wrap to two rows) and the menu is the one
        height-safe container on these pages.
      - When `!canExport`, render the item `disabled` with `views.export.disabledReason` as a second
        muted line (E-3). **This is the one place in the phase where a disabled control is correct**,
        because a menu is a vertical list with room for the reason adjacent rather than hidden in a
        tooltip.
      - On select (E-4): do NOT prevent the default — the menu closes. Set the in-flight state
        **inside a `startTransition` callback, never in an effect body** (K-7). Slot 2 then resolves to
        `Loader2` + `views.exporting`. A menu held open for a 50k-row `SELECT` is dismissible by any
        outside click, which would strand the user with no feedback; the bar is in the page and nothing
        can dismiss it. Nothing else is disabled — the user may keep working.
      - **No progress bar** (E-5): `fetchFilteredData` is one query, not a job, and `ProgressBar` has
        no `role="progressbar"` / `aria-valuenow` (F-39-04), so a fake bar would add an a11y defect in
        service of a lie.
      - Call `exportViewResults({ entityType, filters: <the picked filters from useSearchParams> })`.
        On `{ success: true }`, download via the `bulk-action-bar.tsx:73-83` idiom — Blob,
        `createObjectURL`, an anchor appended and clicked, then **`URL.revokeObjectURL`**. Copy in
        SHAPE; an object URL keeps its blob alive for the document's lifetime, so a user exporting
        repeatedly accumulates every CSV until they navigate away. Do not write a fourth downloader:
        if the idiom can be lifted into a shared helper without touching `bulk-action-bar.tsx`'s
        behaviour, do that and say so; otherwise copy it and say that.
      - The filename comes from the server and is **never translated** (E-6).
      - Toasts: success reuses `bulk.exported` with `{count}` (E-7 — zero new keys for the happy path,
        and `REQUIRED_BULK_KEYS` must not change; if you find yourself editing it you have added a key
        you were told to reuse). `error: "too_many"` → `views.export.tooMany` with `{max}`.
        `error: "refused"` → `views.export.refused`. Anything else → `bulk.error.exportFailed` (E-8).
        `bulk.error.tooMany` is deliberately NOT reused — its copy is about a selection of ids and
        would read as nonsense here.

    SAVE: slot 2's button and the menu's `views.saveNew` both open `<SaveViewDialog>` (plan 40-08),
    passing `entityType`, the picked `filters`, `selectedView` (looked up from `views` by
    `selectedViewId`) and `canUpdateSelected`. Hold `open` in local state.

    MANAGE: `views.manageAction` opens `<ManageViewsDialog>` (plan 40-09), passing `entityType` and
    `views`.

    Both dialogs render as siblings of the bar row, inside the same fragment. The bar does not remount
    them per view.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npx vitest run src/components/views/</automated>
  </verify>
  <done>typecheck 0, lint 0 errors. `URL.revokeObjectURL` present. Zero `useEffect` calling a setter. `REQUIRED_BULK_KEYS` unchanged (`git diff src/messages/locale-parity.test.ts` shows no bulk edit).</done>
</task>

<task type="auto">
  <name>Task 3: V-40-5 — the parsed-interface and derives-nothing gate</name>
  <files>src/components/views/__tests__/saved-views-bar-wiring.test.ts</files>
  <action>
    Create the gate. `readStrippedSource`, extracted-element scoping, and a header stating that this
    test measures nothing (plan 40-15 measures) and that its two headline assertions are V-40-5's.

    1. **THE PARSED-INTERFACE ASSERTION.** Read `src/lib/views/types.ts` (the file that actually
       declares `SavedViewsBarProps` — verify it does before asserting, and fail with a clear message
       if it does not; a stale assertion naming the wrong file propagated into three Phase 39
       documents while proving nothing). Extract the `export interface SavedViewsBarProps { … }` body
       by brace matching, parse out the property names, and assert the set equals **exactly**:
       `entityType, views, selectedViewId, isModified, droppedFilterKeys, canSave, canExport,
       canUpdateSelected` — no more, no fewer. Assert the count is 8 with the actual list in the
       failure message. A ninth prop means somebody moved a derivation to the client; a missing one
       means the server stopped computing something.

    2. **THE DERIVES-NOTHING ASSERTION.** In the stripped source of `saved-views-bar.tsx`, assert
       there is **no comparison of `useSearchParams()` output against a `filters` object.** Implement
       it as a structural check, not a token grep:
       - extract the argument text of every `pickFilterParams(`, `filtersToSearchParams(` and
         `withViewEscape(` call with `callArguments`
       - assert no expression anywhere in the file matches
         `/(searchParams|urlFilters)[^;\n]*(===|!==|JSON\.stringify)[^;\n]*(\.filters|view\.filters)/`
         nor its mirror image
       - assert the identifiers `isModified` and `droppedFilterKeys` appear **only** as reads from the
         props object — i.e. there is no `const isModified =` and no `const droppedFilterKeys =`
         declaration in the file. That single assertion is the sharpest form of B-2: the moment
         somebody computes either locally, it fails.
       Put B-2 reason 2 in the failure message: a client comparing the URL to the blob labels every
       degraded view "Modified" and invites the user to save the damage.

    3. O-2 — zero `Popover`, zero `PopoverContent`, zero `DropdownMenuSubContent`, zero
       `DropdownMenuSub`.
    4. K-8 / R-40-2f — zero `sticky`, zero `fixed` in any className in the file.
    5. R-40-2a — the extracted outermost bar `<div` opening tag's className contains `flex`,
       `flex-wrap`, `items-center`, `gap-2` and `min-w-0`. Scoped to the tag: a file-wide check is
       satisfied by any nested cluster.
    6. R-40-2b — the extracted trigger `<Button` opening tag contains `min-w-0` and `max-w-[200px]`;
       the extracted `Badge` tag contains `shrink-0`; the name element carries `truncate`.
    7. M-7 — the extracted `<DropdownMenuContent` opening tag contains no `max-h-` and no
       `overflow-` className of its own. It is height-safe by construction and a local override would
       be the one way to break that.
    8. R-40-2d — every extracted `<DropdownMenuRadioItem` for a VIEW carries `py-2`.
    9. Accent budget — the file contains zero `variant="default"` on a `Button`, and its only
       `text-primary` is on the same JSX element as `animate-spin`.
    10. E-4/K-7 — the extracted export `onSelect` handler body contains `startTransition`, and there
        is no `useEffect` in the file whose body calls a `set`-prefixed identifier.
    11. E-6 — `URL.revokeObjectURL` appears exactly once.
    12. K-2, T-1, R-4 — the same absence assertions as plans 40-08/09.
    13. Zero hardcoded user-visible English: assert every JSX text node in the file is either `{t(` /
        `{tBulk(` interpolation or whitespace/punctuation. Extract text nodes between `>` and `<`.

    **NEGATIVE PROOFS, TO BE RUN, three of them, each naming what it must fail:**
      (a) add a ninth property to `SavedViewsBarProps` → assertion 1 RED with the property named
      (b) add `const isModified = urlFilters.stage === selectedView?.filters.stage` to the component
          → assertion 2 RED. This is the exact defect B-2 exists to prevent, so this proof is the one
          that makes the gate worth having.
      (c) add `className="max-h-96"` to `DropdownMenuContent` → assertion 7 RED
    Restore after each; record all three failure texts. Phase 39 shipped a gate that stayed green with
    its defect present because unrelated code satisfied the assertion, and another that was `false`
    forever by construction. Do not add a third.
  </action>
  <verify>
    <automated>npx vitest run src/components/views/__tests__/saved-views-bar-wiring.test.ts && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <done>All 13 assertions green. All three negative proofs RUN and their failure texts recorded. The gate reads `src/lib/views/types.ts` and verified that file declares the interface before asserting on it. Full `npm test` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client props → the rendered picker | the props come from a server resolver; the client may not re-derive them |
| menu selection → navigation | every URL this component builds must pass through `withViewEscape` |
| export selection → a CSV in the user's downloads | the action authorizes; the bar only asks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-44 | Information disclosure | the shared-views group | mitigate | the group is a client-side GROUPING of an already-scoped list; the SQL predicate in plan 40-05 is the control. A private view never reaches the RSC payload, so it cannot be read out of the DOM or the flight data |
| T-40-45 | Tampering | every navigation | mitigate | `withViewEscape` on every `router.push` in this file, asserted at the call sites by plan 40-14's V-40-4 gate |
| T-40-46 | Tampering | client-side `isModified` | mitigate | assertion 2 of the gate forbids a local declaration of `isModified` or `droppedFilterKeys`, so a degraded view can never be mislabelled "Modified" and offered for overwrite |
| T-40-47 | Denial of service | the export in-flight state | mitigate | set inside `startTransition`, never in an effect body (`react-hooks/set-state-in-effect` is an ERROR here); and `URL.revokeObjectURL` releases each blob, so repeated exports do not accumulate in memory |
| T-40-48 | Denial of service (functional) | the picker menu height | mitigate | `DropdownMenuContent` is height-safe by construction and the gate forbids a local `max-h-`/`overflow-` override. Nothing in this phase lives in a `Popover`, which is measured non-conforming at 320x640 |
| T-40-SC | Tampering | npm installs | mitigate | `dropdown-menu`, `badge`, `button`, `sonner` all exist. **No `cmdk`** — the picker is a `DropdownMenu`, not a `Command` (N-2). Do not run `shadcn add` (N-1) |
</threat_model>

<verification>
- `npm run typecheck` 0, `npm run lint` 0 errors, full `npm test` green
- all three V-40-5 negative proofs RUN and recorded
- `git diff src/messages/locale-parity.test.ts` shows no `REQUIRED_BULK_KEYS` edit
- `git diff src/components/ui/` empty; `git diff package-lock.json` empty
</verification>

<success_criteria>
- `SavedViewsBarProps` has exactly eight properties and a gate proves it
- Nothing named `isModified` or `droppedFilterKeys` is computed inside the component
- Selecting `views.allRecords` produces `?view=none`
- The bar spends zero primary-filled buttons and renders no `text-primary` text
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-10-SUMMARY.md` when done. Record all three
negative-proof outputs and whether the download idiom was lifted into a helper or copied.
</output>
