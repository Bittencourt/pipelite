---
phase: 40-saved-views-shared-filters
plan: 04
type: execute
wave: 2
depends_on: ["40-02"]
files_modified:
  - e2e/seed-member.ts
  - e2e/member.setup.ts
  - e2e/views-fixtures.ts
  - playwright.config.ts
autonomous: true
requirements: [VIEW-02]

must_haves:
  truths:
    - "A second Playwright storageState exists holding a non-admin, approved member session"
    - "A spec can create saved views for either account and purge exactly its own fixtures"
    - "The flaky setup project retries once instead of failing the whole suite"
  artifacts:
    - path: "e2e/seed-member.ts"
      provides: "idempotent member seed, loopback-only guard"
      exports: ["E2E_MEMBER_EMAIL", "seedE2eMember"]
    - path: "e2e/views-fixtures.ts"
      provides: "prefix-scoped saved_views create/purge helpers"
      exports: ["VIEWS_FIXTURE_PREFIX", "openDb", "purgeViewFixtures", "insertViewFixture", "setDefaultFixture"]
    - path: "e2e/.auth/member.json"
      provides: "the member storageState"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/.auth/member.json"
      via: "a chromium-member project with its own storageState"
      pattern: "member.json"
---

<objective>
Extend the Playwright harness with the one thing V-40-8 cannot be written without: a second
authenticated context that is **not** an admin. Plus a fixture module both later e2e plans use to
create and hard-delete saved views without touching a human's data.

Purpose: criterion 2 ("a private view stays invisible to everyone else") is only provable with two
sessions, and Decision 3 departs from the app's `owner || role === "admin"` idiom specifically in the
admin direction — so the second session must be a member AND the first must be an admin, and the
proof must run in both directions.
Output: a member seed, a second storageState, a fixture helper, and `retries: 1` on setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/40-saved-views-shared-filters/40-UI-SPEC.md
@.planning/BACKLOG.md

The two files to copy in shape, and the config to extend:
@e2e/seed-admin.ts
@e2e/auth.setup.ts
@playwright.config.ts
@e2e/merge-screen-320.spec.ts
</context>

<interfaces>
<!-- Verified this session. -->

`e2e/seed-admin.ts` exports `E2E_ADMIN_EMAIL = "pipelite-e2e@local.test"` and
`seedE2eAdmin(): Promise<{email, password}>`. It reads `E2E_DATABASE_URL` (must be
`localhost`/`127.0.0.1` — a hard guard, do not weaken it) and `E2E_ADMIN_PASSWORD` (no fallback, by
design). It sets `role`, `status: "approved"`, `emailVerified`, `deletedAt: null` explicitly because
all three default to values that fail login.

`playwright.config.ts` today: `fullyParallel: false`, `launchOptions: { ignoreDefaultArgs:
["--hide-scrollbars"] }` (DO NOT REMOVE — it is what makes clientWidth 305), no `webServer` block
(the app runs in Docker only), and two projects: `setup` (`testMatch: /.*\.setup\.ts/`) and
`chromium` (`dependencies: ["setup"]`, `storageState: "e2e/.auth/admin.json"`,
`viewport: {width: 320, height: 640}`).

`e2e/merge-screen-320.spec.ts` is the fixture precedent (the 45-08 rule): a `FIXTURE_PREFIX`
constant, an `openDb()` using `postgres` against `E2E_DATABASE_URL`, `purgeFixture(sql)` called from
BOTH `beforeAll` (so a crashed previous run is reclaimed) and `afterAll`, and hard deletes scoped by
that prefix.

Live user set, measured: 3 active (2 admins, 1 member; two of the three have `name = NULL`),
6 soft-deleted.

Tables created by plan 40-02: `saved_views` (id, owner_id, entity_type, name, filters jsonb,
is_shared, created_at, updated_at) and `saved_view_defaults` (user_id, entity_type, view_id,
updated_at) with PK (user_id, entity_type).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Seed a member account and mint its storageState</name>
  <files>e2e/seed-member.ts, e2e/member.setup.ts, playwright.config.ts</files>
  <action>
    Create `e2e/seed-member.ts` as a near-copy of `e2e/seed-admin.ts`, with these differences and no
    others:
      - `export const E2E_MEMBER_EMAIL = "pipelite-e2e-member@local.test"`
      - `role: "member"`, `status: "approved"`, `emailVerified: new Date()`, `deletedAt: null`
      - the password comes from `E2E_MEMBER_PASSWORD`, and there is **no fallback and no reuse of
        `E2E_ADMIN_PASSWORD`** — a literal default here would be a committed credential, and sharing
        the admin's password would make the two accounts indistinguishable in a leak
      - keep the loopback-only `E2E_DATABASE_URL` hostname guard verbatim. It is what makes it
        impossible to plant a standing account on a shared database.

    **The spec (V-40-8) says "the live member account". Decline that and say so in the file header:**
    we do not have that human's password, and borrowing a human account violates the 45-08
    fixtures rule. A dedicated seeded member proves exactly the same thing — a non-admin session —
    and can be reasoned about.

    Create `e2e/member.setup.ts` as a near-copy of `e2e/auth.setup.ts`:
      - logs in through the real form at `/login` using `#email`, `#password` and the
        `Sign In` button (all hardcoded English in `(auth)/login/page.tsx`, so locale-independent)
      - waits for navigation away from `/login`
      - ANTI-VACUITY, inverted from the admin's: navigate to `/admin/audit` and assert the session is
        **redirected away** — `expect(new URL(page.url()).pathname).not.toBe("/admin/audit")`. A
        member landing on an admin page means the seed set the wrong role, and a storageState that
        "works" but is secretly an admin would make V-40-8 pass vacuously in the one direction that
        matters most. Also assert a member-reachable page renders (`/organizations` h1 visible), so a
        failed login is not mistaken for a successful redirect.
      - writes `e2e/.auth/member.json`

    Extend `playwright.config.ts`:
      - the existing `setup` project keeps `testMatch: /.*\.setup\.ts/`, which already matches the new
        file. Add `retries: 1` to that project — BACKLOG.md records `auth.setup.ts` timing out on
        `waitForURL` in 2 of 8 invocations, and one flaky setup now fails two dependent projects.
      - add a third project `chromium-member` with `dependencies: ["setup"]`,
        `storageState: "e2e/.auth/member.json"`, the same `viewport: {width: 320, height: 640}`, and
        `testMatch` narrowed to the specs that need it — `/.*-member\.spec\.ts/`. Narrowing by
        filename rather than by directory keeps every existing spec running exactly once; a
        `chromium-member` project with no `testMatch` would run the whole suite twice.
      - do NOT touch `launchOptions`, `fullyParallel`, `use.baseURL`, or the absence of a `webServer`
        block. Do NOT add anything to `.github/workflows/ci.yml` — `npm test` and CI stay clear of
        Playwright, as 45-02 decided.

    Confirm `/e2e/.auth/` is still gitignored BEFORE running anything (it holds a live 7-day session
    cookie). `git check-ignore -v e2e/.auth/member.json` must report the ignore rule.
  </action>
  <verify>
    <automated>git check-ignore -q e2e/.auth/member.json && npx playwright test --project=setup 2>&1 | tail -20 && test -s e2e/.auth/member.json</automated>
  </verify>
  <done>Both setup specs pass; `e2e/.auth/member.json` exists and is gitignored. The member's `/admin/audit` redirect was asserted and observed. Run the setup project twice to prove the seed is idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: The saved-view fixture module</name>
  <files>e2e/views-fixtures.ts</files>
  <action>
    Create `e2e/views-fixtures.ts` following `e2e/merge-screen-320.spec.ts`'s fixture discipline.
    Export:

      - `VIEWS_FIXTURE_PREFIX = "[e2e] View"` — every fixture name starts with it, and every delete
        is scoped by `name LIKE '[e2e] View%'`. No unqualified `DELETE FROM saved_views`, ever.
      - `openDb()` — `postgres(process.env.E2E_DATABASE_URL)`, with the same loopback hostname guard
        as `seed-admin.ts`. This module writes rows into a shared 46,054-organization database.
      - `userIdByEmail(sql, email): Promise<string>` — resolves the seeded admin / member ids.
      - `insertViewFixture(sql, { ownerEmail, entityType, name, filters, isShared }): Promise<string>`
        returning the new id. `name` is asserted to start with `VIEWS_FIXTURE_PREFIX` and the function
        THROWS if it does not — that assertion is what makes the prefix-scoped purge exact rather
        than hopeful.
      - `setDefaultFixture(sql, { userEmail, entityType, viewId })` — upsert into
        `saved_view_defaults` on the (user_id, entity_type) primary key.
      - `purgeViewFixtures(sql)` — deletes `saved_view_defaults` rows whose `view_id` is a prefixed
        view (the FK cascades, but delete explicitly so the helper also cleans a default that was set
        against a NON-fixture view during a test), then deletes the prefixed `saved_views` rows.
        Returns the two delete counts so a spec can log what it reclaimed.

    Then write `e2e/views-fixtures.spec.ts`... **no.** Do not add a spec file here; a fixture module
    with no consumer would run in the default `chromium` project and its assertions would duplicate
    plan 40-15's. Instead prove the module from the command line in this task and record the output:
      1. insert 3 fixtures (2 admin-owned, 1 member-owned; one shared, two private)
      2. set a member default pointing at the admin's SHARED view — the G-7 cross-owner case
      3. `purgeViewFixtures` → assert it returns (1, 3) and that
         `SELECT count(*) FROM saved_views WHERE name LIKE '[e2e] View%'` is 0
      4. assert `SELECT count(*) FROM saved_views` is back to its pre-test value, and that
         organizations is still 46054 and activities still 79022

    ANTI-VACUITY for the purge: before step 3, also insert ONE row named `zz-not-a-fixture-40-04`
    directly, confirm `purgeViewFixtures` does **not** delete it, then delete it by exact name
    yourself. A purge that deletes everything would pass step 3 and 4 and be a data-loss weapon; only
    this probe distinguishes them.
  </action>
  <verify>
    <automated>npx tsc --noEmit e2e/views-fixtures.ts --moduleResolution bundler --module esnext --target es2022 --skipLibCheck 2>&1 | head -5; docker compose exec -T postgres psql -U pipelite -d pipelite -tAc "select count(*) from saved_views where name like '[e2e] View%'" | grep -qx 0</automated>
  </verify>
  <done>All four command-line steps plus the anti-vacuity probe were RUN and their outputs are in the summary. Zero fixture rows remain. `npm run typecheck` 0 and `npm run lint` 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → shared dev database | this plan writes a password-bearing user row and saved-view rows into the same database that holds 46,054 real organizations |
| storageState file → filesystem | `e2e/.auth/member.json` is a live session cookie with a 7-day maxAge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-13 | Elevation of privilege | `seed-member.ts` | mitigate | the loopback-only `E2E_DATABASE_URL` hostname guard is copied verbatim from `seed-admin.ts`; a standing password-bearing account on a shared or production database would be a backdoor |
| T-40-14 | Information disclosure | `e2e/.auth/member.json` | mitigate | `git check-ignore` is asserted BEFORE the file is written, per 45-02's V-2. Never commit it, never upload it as a CI artifact |
| T-40-15 | Spoofing | the member storageState | mitigate | `member.setup.ts` asserts the session is REFUSED at `/admin/audit`. Without that inverted anti-vacuity check, a mis-seeded admin session would make V-40-8's admin-direction proof pass while proving nothing |
| T-40-16 | Denial of service (data loss) | `purgeViewFixtures` | mitigate | every delete is scoped by the `[e2e] View` name prefix, `insertViewFixture` throws on an unprefixed name, and an explicit non-fixture row probe proves the purge is selective |
| T-40-SC | Tampering | npm installs | mitigate | `@playwright/test` and `postgres` are already dependencies. This plan installs nothing |
</threat_model>

<verification>
- both setup specs green, run twice (idempotence)
- `e2e/.auth/member.json` exists, is non-empty, is gitignored
- the member session is redirected away from `/admin/audit` (observed, not assumed)
- `purgeViewFixtures` leaves the non-fixture probe row alone
- `.github/workflows/ci.yml` contains zero occurrences of `playwright` (unchanged)
</verification>

<success_criteria>
- V-40-8 in plan 40-16 can be written using only these exports and needs no new harness work
- `retries: 1` is on the setup project only
- Every existing spec still runs exactly once
</success_criteria>

<output>
Create `.planning/phases/40-saved-views-shared-filters/40-04-SUMMARY.md` when done. Record the
`/admin/audit` redirect observation, the purge counts, and the anti-vacuity probe result.
</output>
