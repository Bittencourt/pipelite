---
phase: 35-notes-record-timeline
plan: 12
type: execute
wave: 6
depends_on: ["35-11"]
files_modified:
  - src/components/timeline/activity-entry.tsx
  - src/components/timeline/stage-change-entry.tsx
  - src/components/timeline/timeline-entry.tsx
  - src/components/timeline/empty-timeline.tsx
autonomous: true
requirements: [NOTE-02]

must_haves:
  truths:
    - "An activity appears in a deal timeline with its type icon, title, and due or completed state"
    - "A stage change appears in a deal timeline naming the actor and both stages"
    - "One switch maps every entry kind to its renderer, so adding Phase 36's audit kind is a one-branch change"
    - "An empty timeline shows the correct copy for a deal versus for an organization, person or activity"
  artifacts:
    - path: "src/components/timeline/activity-entry.tsx"
      provides: "activity row renderer"
    - path: "src/components/timeline/stage-change-entry.tsx"
      provides: "stage-change row renderer"
    - path: "src/components/timeline/timeline-entry.tsx"
      provides: "discriminated-union switch over entry kinds"
      contains: "kind"
    - path: "src/components/timeline/empty-timeline.tsx"
      provides: "variant-aware empty state"
  key_links:
    - from: "src/components/timeline/timeline-entry.tsx"
      to: "src/components/timeline/note-entry.tsx"
      via: "switch branch on kind note"
      pattern: "NoteEntry"
    - from: "src/components/timeline/stage-change-entry.tsx"
      to: "src/app/deals/[id]/page.tsx stageColors"
      via: "reusing the existing pastel stage colour map"
      pattern: "bg-slate-100|stageColors"
---

<objective>
Render the two non-note entry kinds, wire the discriminated-union switch that maps any entry to its
renderer, and build the variant-aware empty state.

Purpose: SC-2 requires ONE chronological feed that interleaves notes, activities and stage changes.
That only reads as one list if all three kinds share the same row skeleton, which is what this plan
enforces.
Output: activity-entry.tsx, stage-change-entry.tsx, timeline-entry.tsx and empty-timeline.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/35-notes-record-timeline/35-UI-SPEC.md
@.planning/phases/35-notes-record-timeline/35-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plan 35-13 renders TimelineEntryRow and EmptyTimeline. -->

activity-entry.tsx ('use client'):
  export function ActivityEntry(props: { entry: ActivityTimelineEntry }): JSX.Element

stage-change-entry.tsx ('use client'):
  export function StageChangeEntry(props: { entry: StageChangeTimelineEntry }): JSX.Element

timeline-entry.tsx ('use client'):
  export function TimelineEntryRow(props: {
    entry: TimelineEntry
    canManage: boolean
    onUpdated: (entry: NoteTimelineEntry) => void
    onDeleted: (noteId: string) => void
  }): JSX.Element | null
  // Exhaustive switch on entry.kind. The default branch must be an exhaustiveness check
  // (assign to never) so adding a kind in Phase 36 is a compile error until it is handled.

empty-timeline.tsx ('use client'):
  export function EmptyTimeline(props: { variant: 'full' | 'notesOnly' }): JSX.Element
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Activity and stage-change row renderers</name>
  <files>src/components/timeline/activity-entry.tsx, src/components/timeline/stage-change-entry.tsx</files>
  <read_first>
    - src/components/timeline/note-entry.tsx (plan 35-11, the row skeleton all three kinds must share: w-8 shrink-0 rail, flex-1 min-w-0 content column, line 1 with flex flex-wrap items-center gap-2, line 2 as the body)
    - src/lib/timeline/types.ts (plan 35-05, the ActivityTimelineEntry and StageChangeTimelineEntry shapes)
    - src/app/activities/activity-list.tsx lines 88-103 (the existing activity type icon map and the colorMap for Call, Meeting, Task and Email; reuse it as-is, add zero new hues)
    - src/app/deals/[id]/page.tsx lines 80-89 (the existing stageColors pastel map for slate, blue, emerald, amber, rose, violet, cyan and orange; reuse as-is)
    - src/components/ui/relative-time.tsx (the timestamp component with its hydration guard)
    - src/components/ui/badge.tsx
    - src/messages/en-US.json (plan 35-02, the keys notes.entry.stageChanged, notes.entry.activityDue, notes.entry.activityCompleted, notes.unknownAuthor)
  </read_first>
  <action>
Create src/components/timeline/activity-entry.tsx and
src/components/timeline/stage-change-entry.tsx, both with the 'use client' directive first.

Both files reuse the exact row skeleton from note-entry.tsx so the three kinds read as one list.

ActivityEntry:
- Rail: a size-8 rounded-full element with the bg-muted fill holding the lucide icon for the entry
  type at h-4 w-4 text-muted-foreground. Map the type name through the existing icon map from
  activity-list.tsx (Phone for Call, Users for Meeting, CheckSquare for Task, Mail for Email) with a
  sensible fallback for an unknown type name. The rail icon is aria-hidden.
- Line 1: the activity title at text-sm font-semibold leading-tight, then the time element wrapping
  RelativeTime for occurredAt at text-xs text-muted-foreground.
- Line 2: when completedAt is set, render notes.entry.activityCompleted; otherwise render
  notes.entry.activityDue interpolating the formatted due date. Render the existing activity type
  pastel badge from the colorMap. Add zero new hues; those maps are light-mode-only in the current
  codebase and this phase reuses them as-is rather than adding new instances of that problem.
- The activity title links to the activity detail route using text-primary hover:underline, which is
  the link treatment already used on the detail pages and one of the three sanctioned uses of the
  primary colour in this card.
- No Edit or Delete controls. Only notes are manageable from the timeline.

StageChangeEntry:
- Rail: the same size-8 rounded-full bg-muted circle holding an ArrowRight icon at h-4 w-4
  text-muted-foreground, aria-hidden.
- Line 1: the actor name at text-sm font-semibold leading-tight, falling back to notes.unknownAuthor
  when actor is null, then the time element wrapping RelativeTime.
- Line 2: notes.entry.stageChanged interpolating from and to. Render the from and to stage names as
  badges coloured through the existing stageColors map keyed by the entry's fromStageColor and
  toStageColor. When fromStageName is null (the deal was created into a stage) render only the
  destination badge and keep the sentence grammatical.
- No Edit or Delete controls.

Neither file may contain dangerouslySetInnerHTML, and neither may introduce a new colour token or a
hardcoded hex.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>head -1 src/components/timeline/activity-entry.tsx | grep -q "use client" &amp;&amp; head -1 src/components/timeline/stage-change-entry.tsx | grep -q "use client" &amp;&amp; echo CLIENT_DIRECTIVES_OK</automated>
    <automated>test "$(grep -cE '#[0-9a-fA-F]{6}|bg-white|text-black' src/components/timeline/activity-entry.tsx src/components/timeline/stage-change-entry.tsx | grep -v ':0' | wc -l)" -eq 0 &amp;&amp; echo NO_HARDCODED_COLOR</automated>
    <automated>npx vitest run "src/app/admin/fields/[entityType]/__tests__/rsc-boundary.test.tsx"</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Both files start with the use client directive.
    - Both use the same rail plus content column structure as note-entry.tsx: a w-8 shrink-0 rail and a flex-1 min-w-0 content column.
    - Rail icons carry aria-hidden.
    - ActivityEntry renders notes.entry.activityCompleted when completedAt is set and notes.entry.activityDue otherwise, and never both.
    - StageChangeEntry renders only the destination badge when fromStageName is null.
    - Neither file contains a hardcoded hex colour, bg-white, or text-black.
    - Neither file renders an Edit or Delete control.
    - Every user-facing string resolves through useTranslations with the notes namespace.
    - The rsc-boundary gate passes; npm run typecheck and npm run lint exit 0.
  </acceptance_criteria>
  <done>Activities and stage changes render in the same visual skeleton as notes, reusing the two existing colour maps and adding no new hues.</done>
</task>

<task type="auto">
  <name>Task 2: Entry switch and variant-aware empty state</name>
  <files>src/components/timeline/timeline-entry.tsx, src/components/timeline/empty-timeline.tsx</files>
  <read_first>
    - src/components/timeline/note-entry.tsx, src/components/timeline/activity-entry.tsx, src/components/timeline/stage-change-entry.tsx (the three renderers being dispatched to)
    - src/lib/timeline/types.ts (plan 35-05, the TimelineEntry discriminated union on kind)
    - src/app/activities/activities-client.tsx lines 159-167 (the existing empty-state block shape)
    - src/messages/en-US.json (plan 35-02, the keys notes.empty.heading, notes.empty.body, notes.emptyNotes.heading, notes.emptyNotes.body)
  </read_first>
  <action>
Create src/components/timeline/timeline-entry.tsx with the 'use client' directive first.

It exports TimelineEntryRow, a switch over entry.kind:
- 'note' renders NoteEntry, forwarding canManage, onUpdated and onDeleted.
- 'activity' renders ActivityEntry.
- 'stage_change' renders StageChangeEntry.
- The default branch assigns the narrowed entry to a variable typed never, so that adding the
  'audit' kind in Phase 36 is a COMPILE ERROR here until it is handled, rather than a silently
  dropped row. Return null after the exhaustiveness assignment.

Keep this file thin. It is a dispatcher, not a layout owner; every visual decision lives in the
three renderers.

Create src/components/timeline/empty-timeline.tsx with the 'use client' directive first.

It exports EmptyTimeline taking a variant of 'full' or 'notesOnly':
- 'full' (deals, which get notes plus activities plus stage changes) renders notes.empty.heading and
  notes.empty.body.
- 'notesOnly' (organizations, people and activities, which get the notes source alone) renders
  notes.emptyNotes.heading and notes.emptyNotes.body.
- The heading uses the Label typography (text-sm font-semibold leading-tight) and the body uses
  text-sm text-muted-foreground. Centre it inside the card content with vertical padding.
- Both bodies end by pointing at the composer that is already on screen, which is why the composer
  must remain visible above the empty state rather than being hidden when there are no entries.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>head -1 src/components/timeline/timeline-entry.tsx | grep -q "use client" &amp;&amp; head -1 src/components/timeline/empty-timeline.tsx | grep -q "use client" &amp;&amp; echo CLIENT_DIRECTIVES_OK</automated>
    <automated>grep -q "never" src/components/timeline/timeline-entry.tsx &amp;&amp; grep -q "NoteEntry" src/components/timeline/timeline-entry.tsx &amp;&amp; grep -q "ActivityEntry" src/components/timeline/timeline-entry.tsx &amp;&amp; grep -q "StageChangeEntry" src/components/timeline/timeline-entry.tsx &amp;&amp; echo SWITCH_COMPLETE</automated>
    <automated>grep -q "emptyNotes" src/components/timeline/empty-timeline.tsx &amp;&amp; grep -q "empty.heading" src/components/timeline/empty-timeline.tsx &amp;&amp; echo BOTH_EMPTY_VARIANTS</automated>
    <automated>npx vitest run "src/app/admin/fields/[entityType]/__tests__/rsc-boundary.test.tsx"</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Both files start with the use client directive.
    - timeline-entry.tsx dispatches all three kinds and its default branch performs a never-typed exhaustiveness assignment.
    - timeline-entry.tsx contains no Tailwind layout classes beyond what is needed to return the child renderer; it owns no visual decisions.
    - empty-timeline.tsx renders the full variant copy for deals and the notesOnly variant copy for the other three entity types, sourced from the notes namespace.
    - Neither file contains a hardcoded English string.
    - The rsc-boundary gate passes and the full npm test suite is green.
    - npm run typecheck and npm run lint exit 0.
  </acceptance_criteria>
  <done>Any TimelineEntry can be rendered by one call, adding a fourth kind is a compile error until handled, and both empty variants exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored activity and stage data to the DOM | Activity titles and stage names are user-authored text rendered as React children |
| RSC boundary | These are client components rendered from a client parent; the Phase 44 gate still applies to every new .tsx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering | stored XSS via an activity title or stage name | mitigate | Rendered as React text children only; no dangerouslySetInnerHTML in either renderer, and no markdown or HTML parsing anywhere in the phase |
| T-35-32 | Information Disclosure | a Phase 36 audit entry silently dropped or mis-rendered by an unhandled union branch | mitigate | The switch default performs a never-typed exhaustiveness assignment, so a new kind fails the typecheck rather than disappearing at runtime |
| T-35-30 | Denial of Service | a silently broken RSC boundary | mitigate | The class-wide asChild gate is run after both tasks |
| T-35-SC | Tampering | npm installs | accept | Zero packages installed; the lucide icons and the two colour maps are already in the repo |
</threat_model>

<verification>
- The rsc-boundary gate passes.
- npm run typecheck, npm run lint and npm test are green.
- All four files start with the use client directive and contain no hardcoded English strings or colours.
</verification>

<success_criteria>
- All three entry kinds render in one shared row skeleton so the feed reads as a single list.
- The dispatcher is exhaustive and compile-checked.
- Both empty-state variants exist with the approved copy.
</success_criteria>

<output>
Create `.planning/phases/35-notes-record-timeline/35-12-SUMMARY.md` when done.
</output>
