---
phase: 35-notes-record-timeline
plan: 06
type: tdd
wave: 2
depends_on: ["35-01"]
files_modified:
  - src/lib/events/subscribers/stage-history.ts
  - src/lib/events/subscribers/stage-history.test.ts
  - instrumentation.ts
autonomous: true
requirements: [NOTE-02]

must_haves:
  truths:
    - "Every deal.stage_changed event persists exactly one deal_stage_history row"
    - "All four existing emit sites reach the subscriber without being modified"
    - "Registering the subscriber twice attaches only one listener"
    - "A failing insert is logged, never silently swallowed, and never rejects inside crmBus.emit"
  artifacts:
    - path: "src/lib/events/subscribers/stage-history.ts"
      provides: "registerStageHistorySubscriber + _resetForTesting"
      exports: ["registerStageHistorySubscriber", "_resetForTesting"]
    - path: "src/lib/events/subscribers/stage-history.test.ts"
      provides: "NOTE-02 subscriber coverage mirroring webhook.test.ts"
      min_lines: 70
    - path: "instrumentation.ts"
      provides: "registration inside the NEXT_RUNTIME === nodejs guard"
      contains: "registerStageHistorySubscriber"
  key_links:
    - from: "instrumentation.ts"
      to: "src/lib/events/subscribers/stage-history.ts"
      via: "dynamic await import inside register()"
      pattern: "subscribers/stage-history"
    - from: "src/lib/events/subscribers/stage-history.ts"
      to: "deal_stage_history"
      via: "db.insert(dealStageHistory)"
      pattern: "db\\.insert\\(dealStageHistory\\)"
---

<objective>
Give stage changes a real persisted source so SC-2's "interleaving … stage changes" is met inside
this phase instead of being deferred to Phase 36's audit log.

Purpose (TDD): the handler's mapping from `DealStageChangedPayload` to a `deal_stage_history` row is
pure `expect(insert).toHaveBeenCalledWith(...)` logic, and the double-register guard is exactly the
kind of invariant a test locks in permanently. The one thing tests CANNOT prove is that the
subscriber is registered in the Docker build — this repo has already shipped that exact bug — so a
browser verification is carried in plan 35-15.
Output: `src/lib/events/subscribers/stage-history.ts`, its test, and the `instrumentation.ts` wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-notes-record-timeline/35-CONTEXT.md
@.planning/phases/35-notes-record-timeline/35-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plan 35-08 reads the table this subscriber fills. -->

src/lib/events/subscribers/stage-history.ts exports:
  export function registerStageHistorySubscriber(): void
  export function _resetForTesting(): void

Payload → row mapping (the payload type already exists in src/lib/events/types.ts):
  DealStageChangedPayload { entity: "deal", entityId, oldStageId, newStageId, userId, ... }
    → dealStageHistory { dealId: payload.entityId,
                         fromStageId: payload.oldStageId ?? null,
                         toStageId: payload.newStageId,
                         changedBy: payload.userId ?? null }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the stage-history subscriber test</name>
  <files>src/lib/events/subscribers/stage-history.test.ts</files>
  <read_first>
    - src/lib/events/subscribers/webhook.test.ts (THE scaffold: mock-before-import ordering at lines 1-19, `beforeEach(() => { vi.clearAllMocks(); _resetForTesting() })`, and the `"does not double-register on repeated calls"` test at lines 73-89 — copy that test's shape verbatim, it is the one that proves the `registered` guard)
    - src/lib/events/subscribers/workflow-trigger.ts (the implementation shape to be tested — the mandatory `.catch(err => console.error("[tag]", err))` that `webhook.ts` omits)
    - src/lib/events/types.ts (the `DealStageChangedPayload` interface at lines 14-18 and the `CrmEventMap` entry at line 24)
    - src/lib/events/bus.ts (the globalThis-pinned synchronous EventEmitter singleton — `emit` cannot await)
    - src/lib/mutations/organizations.test.ts (the `vi.mock("@/db")` factory + typed handle shape to reuse for the insert assertion)
    - src/db/schema/deal-stage-history.ts (created in plan 35-01 — the exact column names asserted)
  </read_first>
  <action>
Create `src/lib/events/subscribers/stage-history.test.ts`.

Mock `@/db` with `{ db: { insert: vi.fn() } }` using the `organizations.test.ts` factory shape, and
place the mock BEFORE the subscriber import (vitest hoisting). Import `crmBus` from `../bus`.
`beforeEach(() => { vi.clearAllMocks(); _resetForTesting() })`.

Tests:
- `"inserts one deal_stage_history row on deal.stage_changed"` — register, emit a
  `DealStageChangedPayload` with `entityId: 'deal-1'`, `oldStageId: 'stage-a'`,
  `newStageId: 'stage-b'`, `userId: 'user-1'`; assert `db.insert` called once and the object passed
  to `.values()` equals `{ dealId: 'deal-1', fromStageId: 'stage-a', toStageId: 'stage-b', changedBy: 'user-1' }`.
- `"maps a missing oldStageId to a null fromStageId"` — omit/null `oldStageId`; assert
  `fromStageId` is `null`, not `undefined` and not `''`.
- `"maps a missing userId to a null changedBy"`.
- `"does not double-register on repeated calls"` — call `registerStageHistorySubscriber()` twice,
  emit once, assert `db.insert` called exactly once.
- `"ignores other CRM events"` — emit `deal.updated` and `activity.created`; assert `db.insert` was
  never called.
- `"logs and does not reject when the insert fails"` — make the insert chain reject; spy on
  `console.error`; emit; await a microtask flush (`await Promise.resolve()` / `await new Promise(r => setImmediate(r))`);
  assert `console.error` was called with a `[stage-history]` tag and that no unhandled rejection
  escaped. The bus is a synchronous `EventEmitter` — `emit()` cannot await — so the `.catch()` is
  the only thing standing between a failed insert and a lost row with no trace.
- Add a file-header comment noting that `_resetForTesting()` calls
  `crmBus.removeAllListeners("deal.stage_changed")`, which ALSO removes the webhook and
  workflow-trigger listeners for that event. That is already true of the two existing
  `_resetForTesting` helpers so it is consistent, but a test that resets this one and then asserts
  on webhook delivery will get a confusing result.

Run. It MUST fail on the missing `./stage-history` module. Commit as
`test(35-06): add failing tests for the stage-history subscriber`.
  </action>
  <behavior>
    - `emit('deal.stage_changed', {entityId:'d1', oldStageId:'s1', newStageId:'s2', userId:'u1', ...})` → `db.insert(dealStageHistory).values({dealId:'d1', fromStageId:'s1', toStageId:'s2', changedBy:'u1'})`.
    - `oldStageId` absent → `fromStageId: null`.
    - Two `register` calls + one emit → exactly one insert.
    - `emit('deal.updated', ...)` → zero inserts.
    - Insert rejects → `console.error` called with `[stage-history]`; no unhandled rejection.
  </behavior>
  <verify>
    <automated>npx vitest run src/lib/events/subscribers/stage-history.test.ts; test $? -ne 0 &amp;&amp; echo RED_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/events/subscribers/stage-history.test.ts` exists with `it` titles including `double-register`, `null fromStageId`, and `logs and does not reject`.
    - `npx vitest run src/lib/events/subscribers/stage-history.test.ts` exits non-zero on a missing `./stage-history` module.
    - The test file mocks `@/db` before importing the subscriber.
    - A commit exists matching `test(35-06):`.
  </acceptance_criteria>
  <done>The subscriber's mapping, idempotency and failure-visibility contract is written and failing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the subscriber and register it in instrumentation.ts</name>
  <files>src/lib/events/subscribers/stage-history.ts, instrumentation.ts</files>
  <read_first>
    - src/lib/events/subscribers/stage-history.test.ts (from task 1 — the contract)
    - src/lib/events/subscribers/workflow-trigger.ts (the whole 36-line file — copy the module-scope `let registered`, the early return, the `.catch(err => console.error("[tag]", err))`, and the exported `_resetForTesting`; replace the `ALL_EVENTS` loop with a single `crmBus.on("deal.stage_changed", ...)`)
    - instrumentation.ts (the `NEXT_RUNTIME === "nodejs"` guard and the two existing `await import` subscriber registrations at lines 12-16 — APPEND, do not reorder)
    - src/lib/mutations/deals.ts (the three `deal.stage_changed` emit sites: the update path, `updateDealStageMutation`, and `reorderDealsMutation` — read them to confirm the payload fields, but DO NOT modify this file)
    - src/app/api/v1/deals/[id]/route.ts lines 341-352 (the fourth emit site, which builds `stageChangedPayload` explicitly — again read only, do not modify)
    - Dockerfile lines 22-41 (the explicit `cp` that copies `instrumentation.js` into the standalone output, because Next.js standalone tracing omits it — the copy is guarded with `|| true` and therefore fails open)
  </read_first>
  <action>
Create `src/lib/events/subscribers/stage-history.ts` implementing the `<interfaces>` contract.

Shape, copied from `workflow-trigger.ts`:
- module-scope `let registered = false`
- `export function registerStageHistorySubscriber(): void` with `if (registered) return` first
- one `crmBus.on("deal.stage_changed", (payload: DealStageChangedPayload) => { ... })`
- inside the handler, `db.insert(dealStageHistory).values({...}).catch((err) => console.error("[stage-history]", err))`
  — fire-and-forget. The bus is a synchronous `EventEmitter`; `emit` cannot await, so do NOT make
  the handler `async` and do NOT await the insert. The `.catch` is mandatory: without it a
  rejection is an unhandled promise and the row is lost with no trace.
- set `registered = true` at the end
- `export function _resetForTesting(): void` that calls
  `crmBus.removeAllListeners("deal.stage_changed")` when registered, then sets `registered = false`.

Then edit `instrumentation.ts`: inside the existing `NEXT_RUNTIME === "nodejs"` guard, immediately
after the `registerWorkflowTriggerSubscriber()` call, add
`const { registerStageHistorySubscriber } = await import("@/lib/events/subscribers/stage-history")`
followed by `registerStageHistorySubscriber()`. Append only — do not reorder the existing
registrations, and do not touch the processors.

Do NOT modify any of the four emit sites. A bus subscriber catches all four for free; that is the
entire argument for the bus over an inline insert. Record in the SUMMARY that all four were
inspected and left unchanged: `src/lib/mutations/deals.ts` (three sites — update,
`updateDealStageMutation`, `reorderDealsMutation`) and
`src/app/api/v1/deals/[id]/route.ts` (one site).

Also record in the SUMMARY the accepted secondary race: `emit` returns before the insert resolves,
so a stage drag immediately followed by a timeline read may not yet show the row. In practice the
client round trip dwarfs a single INSERT. Accept it; do NOT "fix" it by making the bus async.

Run until green. Commit as `feat(35-06): persist deal stage changes via a crmBus subscriber`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/events/subscribers/stage-history.test.ts</automated>
    <automated>npx vitest run src/lib/events/subscribers/webhook.test.ts src/lib/events/subscribers/workflow-trigger.test.ts 2>/dev/null || npx vitest run src/lib/events</automated>
    <automated>grep -q "subscribers/stage-history" instrumentation.ts &amp;&amp; grep -q "registerStageHistorySubscriber()" instrumentation.ts &amp;&amp; echo INSTRUMENTATION_WIRED</automated>
    <automated>test "$(git diff --name-only HEAD~1 -- src/lib/mutations/deals.ts 'src/app/api/v1/deals/[id]/route.ts' | wc -l)" -eq 0 &amp;&amp; echo EMIT_SITES_UNTOUCHED</automated>
    <automated>npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/events/subscribers/stage-history.test.ts` exits 0 with every test passing.
    - The existing `src/lib/events/` suite still passes (no listener leakage between subscribers).
    - `src/lib/events/subscribers/stage-history.ts` contains `.catch(` and the literal `[stage-history]`.
    - `src/lib/events/subscribers/stage-history.ts` contains no `async (payload` and no `await db.insert`.
    - `instrumentation.ts` contains `registerStageHistorySubscriber()` inside the `NEXT_RUNTIME === "nodejs"` block, positioned after `registerWorkflowTriggerSubscriber()`.
    - `git diff` shows zero changes to `src/lib/mutations/deals.ts` and `src/app/api/v1/deals/[id]/route.ts`.
    - `npm run typecheck` and `npm run lint` exit 0.
    - Commits matching `test(35-06):` then `feat(35-06):` both exist.
  </acceptance_criteria>
  <done>Stage changes are persisted from all four emit sites through one subscriber, and the registration is wired into the Node runtime bootstrap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crmBus event payload → database insert | The payload originates from authenticated server-side deal mutations and the authenticated v1 route; it is not attacker-shaped, but a bad mapping silently corrupts history |
| build output → runtime registration | `instrumentation.js` reaches the standalone Docker image only via an explicit `cp` guarded with `\|\| true` (fails open) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-21 | Repudiation | stage changes silently not recorded because the subscriber never registered in the Docker build | mitigate | This repo has already shipped exactly this bug (STATE.md 2026-08-08: standalone build omitted `instrumentation.js`, all four processors dead in production). A passing unit test proves the handler works, NOT that it is registered — so plan 35-15 carries a mandatory browser verification: drag a deal to a new stage, reload, confirm a stage-change entry appears |
| T-35-22 | Repudiation | a failed insert vanishing with no trace | mitigate | `.catch(err => console.error("[stage-history]", err))`, asserted by the "logs and does not reject" test. Without it the rejection is an unhandled promise |
| T-35-23 | Tampering | duplicate history rows from double registration (hot reload, repeated `register()`) | mitigate | module-scope `registered` guard, asserted by the double-register test |
| T-35-SC | Tampering | npm installs | accept | Zero packages installed |
</threat_model>

<verification>
- `npx vitest run src/lib/events/subscribers/stage-history.test.ts` green.
- Existing `src/lib/events/` suite green.
- `instrumentation.ts` registers the subscriber inside the nodejs guard.
- Emit sites untouched.
</verification>

<success_criteria>
- RED precedes GREEN.
- One subscriber captures all four `deal.stage_changed` emit sites without modifying any of them.
- The insert is fire-and-forget with a mandatory `.catch`.
- The Docker-registration risk is explicitly carried forward to plan 35-15 rather than assumed away.
</success_criteria>

<output>
Create `.planning/phases/35-notes-record-timeline/35-06-SUMMARY.md` when done, with RED / GREEN /
REFACTOR sections, the commit list, the four inspected emit sites, and the accepted emit/read race.
</output>
