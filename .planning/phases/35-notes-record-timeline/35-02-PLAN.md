---
phase: 35-notes-record-timeline
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/messages/locale-parity.test.ts
  - src/messages/en-US.json
  - src/messages/es-ES.json
  - src/messages/pt-BR.json
autonomous: true
requirements: [NOTE-01, NOTE-02]

must_haves:
  truths:
    - "All 30 notes.* copy keys exist in en-US.json, es-ES.json and pt-BR.json"
    - "A checked-in test fails if any of the three locale files loses a notes.* key the others have"
  artifacts:
    - path: "src/messages/locale-parity.test.ts"
      provides: "the i18n drift gate (no such gate exists in the repo today)"
      contains: "REQUIRED_NOTE_KEYS"
    - path: "src/messages/en-US.json"
      provides: "the notes namespace, 30 keys"
      contains: "\"notes\""
    - path: "src/messages/es-ES.json"
      provides: "the notes namespace, 30 keys"
      contains: "\"notes\""
    - path: "src/messages/pt-BR.json"
      provides: "the notes namespace, 30 keys"
      contains: "\"notes\""
  key_links:
    - from: "src/messages/locale-parity.test.ts"
      to: "src/messages/*.json"
      via: "direct JSON import / readFileSync"
      pattern: "en-US.json"
---

<objective>
Add the new `notes` copy namespace to all three locale files and, first, build the gate that keeps
them from drifting. 30 new keys across 3 files is exactly the drift risk this repo has never had a
guard for.

Purpose (TDD): the gate is written before the keys exist, so the RED failure literally proves the
gate detects a missing key. A gate written after the keys would be green on arrival and prove
nothing.
Output: `src/messages/locale-parity.test.ts` plus the `notes` namespace in all three locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/35-notes-record-timeline/35-CONTEXT.md
@.planning/phases/35-notes-record-timeline/35-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the locale parity gate before any key exists</name>
  <files>src/messages/locale-parity.test.ts</files>
  <read_first>
    - src/messages/en-US.json (existing namespace layout: shortcuts, home, dashboard, common, nav, auth, organizations, people, deals, activities, settings, admin, import, errors, time, team, emails — and the nesting style used by `admin.webhooks.*`)
    - src/messages/es-ES.json
    - src/messages/pt-BR.json
    - vitest.config.ts (the base project includes `src/**/*.{test,spec}.*`; a test placed in `src/messages/` is picked up automatically — no config change needed)
    - src/lib/mutations/organizations.test.ts (repo test conventions: `describe`/`it`, `beforeEach(() => vi.clearAllMocks())`, no globals config)
  </read_first>
  <action>
Create `src/messages/locale-parity.test.ts`.

Structure:
- Import `describe, it, expect` from `vitest`, and read the three locale files. Prefer static
  `import enUS from "./en-US.json"` style if `resolveJsonModule` is on; otherwise use
  `readFileSync(new URL("./en-US.json", import.meta.url), "utf8")` + `JSON.parse`. Check
  `tsconfig.json` before choosing, and pick whichever typechecks.
- Export a module-level constant `REQUIRED_NOTE_KEYS: string[]` containing exactly these 30
  dot-paths (this list is the copy contract from 35-UI-SPEC.md § Copywriting Contract):
  `notes.timeline`, `notes.composerPlaceholder`, `notes.addNote`, `notes.adding`,
  `notes.empty.heading`, `notes.empty.body`, `notes.emptyNotes.heading`, `notes.emptyNotes.body`,
  `notes.error.saveFailed`, `notes.error.loadMoreFailed`, `notes.error.editFailed`,
  `notes.error.deleteFailed`, `notes.error.notPermitted`, `notes.edited`, `notes.migrated`,
  `notes.migratedTooltip`, `notes.unknownAuthor`, `notes.loadMore`, `notes.loadingMore`,
  `notes.editNote`, `notes.deleteNote`, `notes.saveEdit`, `notes.cancelEdit`,
  `notes.entry.stageChanged`, `notes.entry.activityDue`, `notes.entry.activityCompleted`,
  `notes.announceAdded`, `notes.deleteDialog.title`, `notes.deleteDialog.description`,
  `notes.deleteDialog.cancel`.
- Write a `flattenKeys(obj, prefix)` helper that walks nested objects and returns the sorted array
  of leaf dot-paths.
- Test 1 (`"every required notes.* key exists in every locale"`): for each of the three locales,
  assert `REQUIRED_NOTE_KEYS.filter(k => !flat.includes(k))` equals `[]`. Report the locale name in
  the assertion message so a failure names the offending file.
- Test 2 (`"the notes namespace has identical key sets across all three locales"`): compute the
  `notes.`-prefixed leaf sets for each locale and assert all three are deeply equal.
- Test 3 (`"every notes.* value is a non-empty string"`): assert no key resolves to `""` or to a
  value that is byte-identical to the en-US string in es-ES AND pt-BR simultaneously ONLY for keys
  that are genuinely translatable — skip that last clause if it produces false positives on
  proper nouns; the non-empty assertion is the hard requirement.
- Before finalising: run a one-off check of whether the three files ALREADY have identical global
  key sets. If they do, add Test 4 asserting whole-file parity across all three locales. If they do
  NOT (pre-existing drift), do NOT add Test 4 — scope the gate to the `notes` namespace, and put a
  comment at the top of the test file recording the measured pre-existing drift count and why the
  global assertion is deferred. Record that number in the plan SUMMARY either way.

This is the RED gate. Run it now; it MUST fail because no locale file contains a `notes` namespace
yet. If it passes, the test is wrong — investigate before proceeding.

Commit as `test(35-02): add failing locale parity gate for the notes namespace`.
  </action>
  <behavior>
    - Given all three locale files lack a `notes` namespace, the "every required notes.* key exists" test FAILS listing 30 missing keys per locale.
    - Given en-US has all 30 keys and es-ES has 29, the parity test FAILS naming the missing key.
    - Given all three have all 30 keys, all tests PASS.
  </behavior>
  <verify>
    <automated>npx vitest run src/messages/locale-parity.test.ts; test $? -ne 0 &amp;&amp; echo RED_CONFIRMED</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/messages/locale-parity.test.ts` exists and exports/declares `REQUIRED_NOTE_KEYS` with exactly 30 entries (assert with `grep -c "notes\." src/messages/locale-parity.test.ts` ≥ 30, or by reading the array length).
    - `npx vitest run src/messages/locale-parity.test.ts` exits NON-ZERO at this point, and the failure output names missing `notes.*` keys — not a syntax, import, or module-resolution error.
    - `npm run typecheck` exits 0 (the test file itself compiles).
    - A commit exists matching `test(35-02):`.
  </acceptance_criteria>
  <done>The gate exists and fails for the right reason: 30 keys are missing from three files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add the notes namespace to all three locale files</name>
  <files>src/messages/en-US.json, src/messages/es-ES.json, src/messages/pt-BR.json</files>
  <read_first>
    - src/messages/locale-parity.test.ts (created in task 1 — the key list is the contract)
    - src/messages/en-US.json (insert the new top-level `notes` namespace following the existing formatting: 2-space indent, alphabetical placement is NOT used, so append near the other entity namespaces)
    - src/messages/es-ES.json
    - src/messages/pt-BR.json
    - .planning/phases/35-notes-record-timeline/35-UI-SPEC.md § Copywriting Contract (the source of truth for every en-US string, including the copy rules: no generic CTA labels, no exclamation marks, every error names the failed action and the next step)
  </read_first>
  <action>
Add a top-level `notes` namespace to all three files in `src/messages/`. Use the exact en-US copy
below (it is the approved UI-SPEC contract — do not paraphrase it), and the es-ES / pt-BR copy below.
Interpolation placeholders `{from}`, `{to}`, `{date}` must survive translation verbatim.

| key | en-US | es-ES | pt-BR |
|-----|-------|-------|-------|
| timeline | Timeline | Cronología | Linha do tempo |
| composerPlaceholder | Write a note… | Escribe una nota… | Escreva uma nota… |
| addNote | Add note | Añadir nota | Adicionar nota |
| adding | Adding… | Añadiendo… | Adicionando… |
| empty.heading | Nothing has happened yet | Todavía no ha pasado nada | Nada aconteceu ainda |
| empty.body | Notes, activities and stage changes for this deal will appear here. Write the first note above. | Las notas, actividades y cambios de etapa de este trato aparecerán aquí. Escribe la primera nota arriba. | Notas, atividades e mudanças de etapa deste negócio aparecerão aqui. Escreva a primeira nota acima. |
| emptyNotes.heading | No notes yet | Aún no hay notas | Ainda não há notas |
| emptyNotes.body | Nobody has written about this record yet. Write the first note above. | Nadie ha escrito sobre este registro todavía. Escribe la primera nota arriba. | Ninguém escreveu sobre este registro ainda. Escreva a primeira nota acima. |
| error.saveFailed | Your note wasn't saved. The text is still in the box — check your connection and try again. | Tu nota no se guardó. El texto sigue en el cuadro: revisa tu conexión e inténtalo de nuevo. | Sua nota não foi salva. O texto continua na caixa — verifique sua conexão e tente novamente. |
| error.loadMoreFailed | Couldn't load older entries. Try again. | No se pudieron cargar entradas anteriores. Inténtalo de nuevo. | Não foi possível carregar entradas anteriores. Tente novamente. |
| error.editFailed | Your changes weren't saved. Try again. | Tus cambios no se guardaron. Inténtalo de nuevo. | Suas alterações não foram salvas. Tente novamente. |
| error.deleteFailed | The note wasn't deleted. Try again. | La nota no se eliminó. Inténtalo de nuevo. | A nota não foi excluída. Tente novamente. |
| error.notPermitted | Only the author or an admin can change this note. | Solo el autor o un administrador puede modificar esta nota. | Somente o autor ou um administrador pode alterar esta nota. |
| edited | edited | editada | editada |
| migrated | Migrated | Migrada | Migrada |
| migratedTooltip | Imported from this record's old notes field | Importada del antiguo campo de notas de este registro | Importada do antigo campo de notas deste registro |
| unknownAuthor | Unknown | Desconocido | Desconhecido |
| loadMore | Load more | Cargar más | Carregar mais |
| loadingMore | Loading… | Cargando… | Carregando… |
| editNote | Edit note | Editar nota | Editar nota |
| deleteNote | Delete note | Eliminar nota | Excluir nota |
| saveEdit | Save note | Guardar nota | Salvar nota |
| cancelEdit | Cancel edit | Cancelar edición | Cancelar edição |
| entry.stageChanged | moved this deal from {from} to {to} | movió este trato de {from} a {to} | moveu este negócio de {from} para {to} |
| entry.activityDue | Due {date} | Vence {date} | Vence {date} |
| entry.activityCompleted | Completed | Completada | Concluída |
| announceAdded | Note added | Nota añadida | Nota adicionada |
| deleteDialog.title | Delete note | Eliminar nota | Excluir nota |
| deleteDialog.description | This removes the note from the record's timeline. You can't undo this here. | Esto elimina la nota de la cronología del registro. No puedes deshacerlo aquí. | Isso remove a nota da linha do tempo do registro. Você não pode desfazer isso aqui. |
| deleteDialog.cancel | Keep note | Conservar nota | Manter nota |

Nesting: `empty`, `emptyNotes`, `error`, `entry`, and `deleteDialog` are nested objects under
`notes`; the rest are flat string values. That is one level deeper than most namespaces but matches
the existing `admin.webhooks.*` convention, so it is in-convention (D-16).

Preserve each file's existing indentation and key ordering conventions. Do not reformat unrelated
parts of the JSON. Do not touch any existing namespace.

Run the gate. It MUST now pass. Commit as
`feat(35-02): add the notes copy namespace to all three locales`.
  </action>
  <behavior>
    - After this task, `locale-parity.test.ts` passes all tests.
    - Deleting any single `notes.*` key from any single locale file makes the gate fail again.
  </behavior>
  <verify>
    <automated>npx vitest run src/messages/locale-parity.test.ts</automated>
    <automated>node -e "for (const l of ['en-US','es-ES','pt-BR']) { const j = require('./src/messages/'+l+'.json'); if (!j.notes) { console.error('MISSING notes namespace in '+l); process.exit(1) } } console.log('NOTES_NAMESPACE_OK')"</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/messages/locale-parity.test.ts` exits 0 with all tests passing.
    - Each of the three JSON files parses (`node -e "require('./src/messages/es-ES.json')"` exits 0).
    - `notes.entry.stageChanged` in every locale contains both the literal `{from}` and the literal `{to}`.
    - `notes.entry.activityDue` in every locale contains the literal `{date}`.
    - `notes.deleteDialog.cancel` in en-US is exactly `Keep note` — NOT `Cancel` (UI-SPEC copy rule: no generic CTA labels).
    - `notes.addNote` in en-US is exactly `Add note`.
    - No `notes.*` value in any locale contains an exclamation mark.
    - `npm run typecheck` and `npm run lint` both exit 0.
    - A commit exists matching `feat(35-02):`.
  </acceptance_criteria>
  <done>All 30 keys exist in all three locales, the gate is green, and every UI plan downstream can call `t('addNote')` without adding a key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy files → rendered UI | Locale strings are rendered as React text children; next-intl escapes them. No boundary crossing occurs here beyond that |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-10 | Information Disclosure | error copy strings | mitigate | `notes.error.*` strings name the failed action and the next step and carry no server, database or stack detail; raw Postgres errors go to `console.error` only (enforced in plans 35-04, 35-09, 35-10) |
| T-35-13 | Spoofing | copy that over-promises a capability the product lacks | mitigate | `notes.deleteDialog.description` deliberately does not promise restoration — soft delete exists but trash/restore is Phase 37 and no restore surface ships here |
| T-35-SC | Tampering | npm installs | accept | Zero packages installed in this plan or this phase |
</threat_model>

<verification>
- `npx vitest run src/messages/locale-parity.test.ts` green.
- Two commits present: `test(35-02):` then `feat(35-02):`.
- `npm run lint` and `npm run typecheck` green.
</verification>

<success_criteria>
- The RED commit precedes the GREEN commit and the RED run genuinely failed on missing keys.
- 30 `notes.*` keys exist in all three locale files with the exact approved en-US copy.
- The repo now has a locale drift gate it did not have before.
</success_criteria>

<output>
Create `.planning/phases/35-notes-record-timeline/35-02-SUMMARY.md` when done, including the
measured pre-existing global locale drift count from task 1.
</output>
