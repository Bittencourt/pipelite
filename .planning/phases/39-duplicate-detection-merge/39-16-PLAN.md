---
phase: 39-duplicate-detection-merge
plan: 16
type: execute
wave: 6
depends_on: ["39-04", "39-08"]
files_modified:
  - src/lib/dedup/import-flags.ts
  - src/lib/dedup/import-flags.test.ts
  - src/components/dedup/import-duplicate-notice.tsx
  - src/app/import/steps/confirm-step.tsx
  - src/app/admin/import/pipedrive-api/steps/progress-step.tsx
  - src/app/organizations/data-table.tsx
  - src/app/people/data-table.tsx
  - src/app/organizations/__tests__/toolbar-wiring.test.ts
autonomous: true
requirements: [DEDUP-01]
must_haves:
  truths:
    - "After an import finishes, the summary says how many imported records look like duplicates and links to the review page"
    - "The importer never stops to ask about a duplicate"
    - "A user can reach the duplicates page from the organizations and people list pages"
    - "Adding that button does not push either toolbar off a 320px screen"
  artifacts:
    - path: "src/components/dedup/import-duplicate-notice.tsx"
      provides: "I-1 through I-5: one shared notice on both completion summaries"
    - path: "src/lib/dedup/import-flags.ts"
      provides: "the flagged-row count for a finished import"
      exports: ["countFlaggedImportedRecords"]
  key_links:
    - from: "src/app/organizations/data-table.tsx"
      to: "/duplicates?type=organizations"
      via: "a variant=outline Find duplicates button in a now-wrapping toolbar"
      pattern: "dedup.findDuplicates"
---

<objective>
Close the last third of the phase goal — "duplicates entering through the importer are caught" —
without making the importer interactive, and give the two list pages a way in.

Purpose: DEDUP-01. The locked decision is explicit: **the importer gets a report, not a prompt.** An
import of thousands of rows cannot stop for each match, so matched rows are flagged in the summary
and the user runs a scan afterwards.
Output: the flagged count, the shared notice on both completion summaries, and the two toolbar entry
points.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-duplicate-detection-merge/39-UI-SPEC.md
@.planning/phases/39-duplicate-detection-merge/39-CONTEXT.md
</context>

<key_facts>
- **I-1:** ONE shared component on BOTH completion summaries —
  `src/app/import/steps/confirm-step.tsx` (the CSV importer's "Import Complete" branch, around line
  198) and `src/app/admin/import/pipedrive-api/steps/progress-step.tsx` (the Pipedrive `isCompleted`
  branch). These are the only two places an import ends. One component, same props, **one added line
  each**.
- **I-2:** `Alert variant="default"` with a `Copy` lucide icon. `AlertTitle` is `dedup.import.flagged`
  with `{count}`, `AlertDescription` is `dedup.import.flaggedBody`, then a `variant="outline"` link
  `dedup.import.review` to `/duplicates?type=...`.
- **I-3:** NOT `destructive`. Flagged rows were imported successfully; the import did not fail.
- **I-4:** the notice never lists individual rows. An import of thousands can flag hundreds; the count
  plus a link is the whole report. This is the direct UI consequence of the locked
  importer-is-not-interactive decision.
- **I-5:** rendered only when `count > 0`.
- **`progress-step.tsx`'s `text-green-600` / `text-orange-500` and its eleven hardcoded English
  literals are PRE-EXISTING and explicitly out of scope.** Do not copy them, do not fix them here,
  and do not let the one added line grow into a cleanup.
- **L-10 and R-5:** a `variant="outline"` "Find duplicates" button on the `/organizations` and
  `/people` toolbars, navigating to `/duplicates?type=...`. **Not** a new global-nav link: Phase 45
  measured the header's 320px budget at 190px of 241 usable and every nav link is `hidden md:flex`, so
  a seventh link is real risk against a route used a few times a year. **The toolbar row must become
  `flex flex-wrap items-center justify-between gap-2` with `min-w-0 flex-1` on the search cluster** —
  it is `flex items-center justify-between` today (`organizations/data-table.tsx:303`) and adding a
  third control to a non-wrapping row is exactly the defect Phase 45 spent a rebuild fixing on
  `/deals` and `/activities` (measured 412 and 356/425/430 against a 305px client width).
- **R-4:** a flex item's default `min-width: auto` is the mechanism behind every overflow Phase 45
  measured. `min-w-0` is not decoration.
- `/duplicates` is admin-only. The toolbar button is visible to everyone; a non-admin who clicks it is
  redirected by `layout.tsx`. **Planner decision: render the button for admins only**, using the same
  visibility-only pattern `/trash` uses (`isAdmin` for VISIBILITY, never as the authorization) — a
  control that always redirects is worse than no control. State that the visibility is cosmetic and
  the layout gate is the authority.
- `src/components/ui/table.tsx:11` already carries `relative w-full overflow-x-auto`, so the tables
  themselves are 320px-safe. The toolbar above them is not.
- **45-11's measurement is the reason this plan exists as its own plan:** a Radix Checkbox renders a
  `position: absolute` bubble input whose `offsetParent` resolves to `<body>`, so an
  `overflow-x-auto` row containing one must ALSO be `relative` or the hidden inputs escape the clip
  and widen the document. Keep that in mind when touching a toolbar that sits above a checkbox table.
</key_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The flagged-row count</name>
  <files>src/lib/dedup/import-flags.ts, src/lib/dedup/import-flags.test.ts</files>
  <read_first>
    - `src/app/import/actions.ts` — the CSV importer's completion path. **Determine what identifier it
      has for the records it just created** (a list of ids in memory, an import session id, or
      neither) and record the finding.
    - `src/lib/import/pipedrive-api-import-actions.ts` around lines 296-330 — the Pipedrive importer
      runs inside `runWithActor({ kind: "import", userId, importSessionId })`, so its created records
      carry `audit_log.import_session_id`.
    - `src/db/schema/audit-log.ts` — `importSessionId`, `entityType`, `entityId`, `action`.
    - `src/lib/dedup/matching.ts` (plan 39-08) — `findCertainMatches`.
    - `src/lib/trash/queries.ts` — the fail-closed read posture.
  </read_first>
  <behavior>
    - Test 1: `countFlaggedImportedRecords({ entityType, importSessionId })` resolves the created
      record ids from `audit_log` where `import_session_id` matches, `action = 'created'` and
      `entity_type` matches, then counts how many have at least one certain match.
    - Test 2: the same function accepts `{ entityType, recordIds }` directly, for an importer that
      has the ids in hand and no session row.
    - Test 3: a record whose only match is ITSELF is not counted — assert with a fixture where the
      created record would match itself by name and identity value. This is the `excludeId` path in
      `findCertainMatches` and without it every imported record is flagged.
    - Test 4: the count is capped and the work is bounded — assert the implementation processes ids in
      batches rather than issuing one query per record, and that a session with zero created records
      returns 0 with no per-record query at all.
    - Test 5: a rejecting query returns 0 and logs, never throws. A failed count must not break an
      import summary that is otherwise reporting a successful import.
    - Test 6: organizations with no configured identity field return 0 — the certain tier does not
      exist, so nothing can be flagged, and this must cost nothing.
  </behavior>
  <action>
    Create `src/lib/dedup/import-flags.ts` exporting
    `countFlaggedImportedRecords(input: { entityType: MergeableEntityType } & ({ importSessionId: string } | { recordIds: string[] })): Promise<number>`.

    **Read both importers first and record in the summary which identifier each one can supply.** If
    the CSV importer has neither a session id nor a usable id list, say so plainly and render the
    notice on the Pipedrive summary only, recording the gap as a deliberate, stated limitation rather
    than shipping a notice whose count is always zero. Do not invent a tracking mechanism inside the
    importer — 39-CONTEXT scopes the importer to "reporting matched rows".

    Resolve ids, then evaluate matches in batches (a single query per batch, using the same
    `norm_name` / `norm_email` equality predicates `findCertainMatches` uses, with `excludeId`
    semantics applied so a record never matches itself). Bound the total work and comment the bound.

    Fail closed: a rejection logs `[dedup-import-flags]` with identifiers only and returns 0.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/lib/dedup/import-flags.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/lib/dedup/import-flags.test.ts` exits 0 with at least 6
      assertions passing.
    - Negative proof, RUN: remove the self-exclusion, re-run, confirm Test 3 fails **by name**;
      restore it.
    - `grep -c "throw" src/lib/dedup/import-flags.ts` = 0.
    - The finding about each importer's available identifier is recorded in the summary, with the file
      and line that establishes it.
    - `npm run typecheck` = 0 errors; `npm run lint` = 0 errors.
  </acceptance_criteria>
  <done>A finished import can be summarised with a real flagged-row count, bounded, self-exclusion-correct and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: The shared notice, on both completion summaries</name>
  <files>src/components/dedup/import-duplicate-notice.tsx, src/app/import/steps/confirm-step.tsx, src/app/admin/import/pipedrive-api/steps/progress-step.tsx</files>
  <read_first>
    - `src/app/import/steps/confirm-step.tsx` — the "Import Complete" branch around line 198.
    - `src/app/admin/import/pipedrive-api/steps/progress-step.tsx` — the `isCompleted` branch, and
      lines 85-110 and 151 so you can SEE the `text-green-600` / `text-orange-500` /
      `text-2xl font-bold` presentation you are not copying.
    - `src/components/ui/alert.tsx` — the two variants.
    - `src/lib/dedup/import-flags.ts` (task 1).
  </read_first>
  <action>
    Create `src/components/dedup/import-duplicate-notice.tsx` taking `{ count: number; entityType }`.
    Return `null` when `count <= 0` (I-5). Otherwise render `Alert variant="default"` with a `Copy`
    lucide icon, `AlertTitle` = `dedup.import.flagged` with `{count}` (an ICU plural), and
    `AlertDescription` = `dedup.import.flaggedBody`, then a `variant="outline"` `Link` reading
    `dedup.import.review` pointing at `/duplicates?type=<entityType>s`.

    It never lists rows (I-4) and it is never `destructive` (I-3). Add a one-line comment for each,
    naming the reason, because both are the kind of thing a later reader "improves".

    Add **one line each** to the two completion branches. Do not restructure either file, do not
    translate their pre-existing English, and do not touch their colour classes — all of that is
    explicitly out of scope and touching it makes this diff unreviewable.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat src/app/import/steps/confirm-step.tsx src/app/admin/import/pipedrive-api/steps/progress-step.tsx`
      shows a SMALL insertion count and **zero deletions** in both files. Quote the numbers in the
      summary.
    - `grep -c "text-green-600\|text-orange-500" src/components/dedup/import-duplicate-notice.tsx` = 0.
    - `grep -c "variant=\"destructive\"" src/components/dedup/import-duplicate-notice.tsx` = 0.
    - Both `dedup.import.flagged` and `dedup.import.flaggedBody` and `dedup.import.review` appear in
      the component; none is hardcoded.
    - `grep -c "count <= 0\|count === 0\|!count" src/components/dedup/import-duplicate-notice.tsx` ≥ 1
      — the I-5 guard exists.
    - `npm run typecheck` = 0 errors; `npm run lint` = 0 errors; `npm run test` green.
  </acceptance_criteria>
  <done>Both importers report flagged rows with one added line each, and neither importer's pre-existing presentation moved.</done>
</task>

<task type="auto">
  <name>Task 3: The two list-page entry points, and the wrapping toolbars</name>
  <files>src/app/organizations/data-table.tsx, src/app/people/data-table.tsx, src/app/organizations/__tests__/toolbar-wiring.test.ts</files>
  <read_first>
    - `src/app/organizations/data-table.tsx` around line 303 — the toolbar row, currently
      `flex items-center justify-between` with no `flex-wrap`.
    - `src/app/people/data-table.tsx` — the twin toolbar.
    - STATE.md's Phase 45 entries on the non-wrapping page toolbars (measured 412 and 356/425/430
      against a 305px client width) and on the Radix Checkbox bubble input escaping an
      `overflow-x-auto` row whose container is not `relative`.
    - `src/app/trash/page.tsx` — the `isAdmin` VISIBILITY-ONLY comment.
    - `src/components/custom-fields/__tests__/source-scan.ts` — `readStrippedSource`.
  </read_first>
  <action>
    In both data tables, change the toolbar row to
    `flex flex-wrap items-center justify-between gap-2` and add `min-w-0 flex-1` to the search
    cluster (R-4/R-5). Comment the change with the Phase 45 measurement that motivates it, so a later
    reader does not tidy the classes away.

    Add a `variant="outline"` `Button asChild` wrapping a `Link` to `/duplicates?type=organizations`
    (respectively `?type=people`), labelled `dedup.findDuplicates`, rendered **only when the viewer is
    an admin**. Pass the admin flag down as a prop from the server component that renders the table
    rather than calling `auth()` in a client file, and comment that the visibility is COSMETIC —
    `src/app/duplicates/layout.tsx` is the authority, and a control that always redirects is worse
    than no control.

    Create `src/app/organizations/__tests__/toolbar-wiring.test.ts` with `readStrippedSource`,
    covering both data tables:
    - The toolbar row's class string contains `flex-wrap` AND `gap-2`. Extract the row's opening tag
      and assert on it, not on the file.
    - `min-w-0` appears on the search cluster, within the same extracted region.
    - `dedup.findDuplicates` appears exactly once per file, and the `href` appears exactly once per
      file with the right `type` value — a second copy is how a duplicated control silently drifts
      (the 45-09 counting precedent).
    - The button is inside an admin conditional — extract the conditional and assert the button is
      within it.
    - Anti-vacuity: assert each toolbar region is non-empty and still contains its search input, so
      the class assertions cannot pass on a deleted row.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/app/organizations/__tests__/toolbar-wiring.test.ts && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/app/organizations/__tests__/toolbar-wiring.test.ts` exits 0.
    - Negative proof, RUN: remove `flex-wrap` from one toolbar, re-run, confirm the scoped assertion
      fails **by name** naming that file; restore it.
    - Negative proof, RUN: add a second `dedup.findDuplicates` button to one file, re-run, confirm the
      exactly-once assertion fails by name; remove it.
    - `grep -c "auth()" src/app/organizations/data-table.tsx src/app/people/data-table.tsx` = 0 — the
      admin flag arrives as a prop.
    - `npm run typecheck` = 0 errors; `npm run lint` = 0 errors; `npm run test` (both projects) green.
    - No Docker rebuild is paid here. The 320px measurement of both toolbars is plan 39-17's, and this
      plan's summary must say so rather than claiming the layout is verified.
  </acceptance_criteria>
  <done>Both list pages offer the entry point to admins, both toolbars wrap, and the layout claim is deferred to the plan that can actually measure it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import session -> flagged count | `importSessionId` identifies records created by an import |
| browser -> /duplicates via the toolbar link | a non-admin can click the link |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Elevation of privilege | the "Find duplicates" toolbar button | mitigate | The button is rendered for admins only, and that visibility is COSMETIC and commented as such — `src/app/duplicates/layout.tsx` is the authority and redirects any non-admin who reaches the route by URL. |
| T-39-05 | Information disclosure | the flagged count | accept | The count reveals only how many just-imported records look like duplicates, to the user who ran the import, over records the list pages already show them. Individual rows are never listed (I-4). |
| T-39-38 | Denial of service | counting matches for a large import | mitigate | Ids are resolved once and evaluated in batches, never one query per record; a session with zero created records issues no per-record query; and a rejection returns 0 rather than failing the import summary. |
| T-39-10 | Information disclosure | `[dedup-import-flags]` logs | mitigate | Identifiers and counts only; never a record name or value. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package. 39-RESEARCH section Package Legitimacy Audit: 55 OK, 1 SUS (`vitest`, incumbent, dismissed), 0 SLOP. No install checkpoint required. |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/lib/dedup src/app/organizations` green.
- `git diff --stat` on the two importer files shows zero deletions.
- `npm run typecheck` = 0 errors, `npm run lint` = 0 errors, `npm run test` (both projects) green.
</verification>

<success_criteria>
- DEDUP-01's importer half: a finished import reports how many rows look like duplicates and links to
  the review page, without ever interrupting the import.
- Both list pages offer an admin entry point and both toolbars wrap.
- Three RUN negative proofs are recorded in the summary, and the layout claim is explicitly deferred
  to plan 39-17.
</success_criteria>

<output>
Create `.planning/phases/39-duplicate-detection-merge/39-16-SUMMARY.md` when done.
</output>
