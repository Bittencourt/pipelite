---
phase: 39-duplicate-detection-merge
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/en-US.json
  - src/messages/pt-BR.json
  - src/messages/es-ES.json
  - src/messages/locale-parity.test.ts
autonomous: true
requirements: [DEDUP-01, DEDUP-02, DEDUP-03]
must_haves:
  truths:
    - "Every string this phase will render exists in all three locales before any component is written"
    - "A 78th dedup string added later without a contract entry fails the test run"
    - "The ten new ICU plural keys are structurally checked, not merely present"
  artifacts:
    - path: "src/messages/en-US.json"
      provides: "77 dedup.* leaves and 4 new audit.entry.* leaves"
      contains: "\"dedup\""
    - path: "src/messages/locale-parity.test.ts"
      provides: "REQUIRED_DEDUP_KEYS exact-set contract and the extended audit contract"
      contains: "REQUIRED_DEDUP_KEYS"
  key_links:
    - from: "src/messages/locale-parity.test.ts"
      to: "src/messages/{en-US,pt-BR,es-ES}.json"
      via: "an exact-set comparison of the whole dedup namespace, the REQUIRED_BULK_KEYS shape"
      pattern: "REQUIRED_DEDUP_KEYS"
---

<objective>
Land the entire message catalog for the phase in one commit, in all three locales, with an exact-set
contract that fails in both directions.

Purpose: this is wave 1 deliberately. Every component plan downstream (39-11, 39-13, 39-14, 39-15,
39-16, 39-12) consumes these keys, and Phase 45 established that a component written against a key
that does not exist yet renders a dot-path to the user. Doing the catalog first also means the copy
is reviewed as copy, once, rather than as a footnote to six diffs.
Output: 77 `dedup.*` leaves, 4 new `audit.entry.*` leaves, and the parity contract that polices them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-duplicate-detection-merge/39-UI-SPEC.md
@.planning/phases/39-duplicate-detection-merge/39-CONTEXT.md
</context>

<key_facts>
- **39-UI-SPEC section Copywriting Contract holds the full three-locale table for 69 keys.** Copy it
  verbatim. Do not re-translate, do not improve the wording, do not reflow a sentence.
- **Two planner-decided deltas to that approved catalog**, both forced by 39-CONTEXT's Post-Research
  Decisions, which superseded a locked rule after the UI-SPEC was written:
  1. `dedup.reason.nameDomain` is **renamed** to `dedup.reason.nameIdentity` and re-worded. The
     original copy said "Same name and website domain", but `website` is NULL on all 46,054
     organizations, so the rule it describes can never fire. The organization certain tier is now
     "same normalized name and the same value in a configured identity custom field". Key count
     unchanged.
     - en-US: `Same name and the same identifying field`
     - pt-BR: `Mesmo nome e mesmo campo de identificacao` (with the correct accents: identificação)
     - es-ES: `Mismo nombre y mismo campo de identificacion` (with the correct accents: identificación)
  2. **Eight new `dedup.identity.*` keys** for the admin control that names those identity fields.
     39-CONTEXT locks "the organization identity key is ADMIN-CONFIGURABLE"; the UI-SPEC has no
     surface for it, so plan 39-11 adds a small settings card on `/duplicates` (already admin-only).
     The eight keys are listed in task 1 in all three locales.
  Total: **77 `dedup.*` keys**, plus **4 `audit.*` keys**.
- **L-2:** `REQUIRED_AUDIT_KEYS` is already an exact-set contract, so adding keys without extending
  the list fails and extending the list without adding the keys also fails. **Read the constant and
  count it; do not trust any number written in a planning document** (39-RESEARCH A7 records that the
  size is reported inconsistently across documents — a raw grep this session found 79 audit lines).
- **L-3:** `placeholderDrift()`'s regex is `/\{[a-zA-Z0-9_]+\}/g` and cannot see
  `{count, plural, ...}`. The dedicated `icuPluralReport` assertion (locale-parity.test.ts around
  line 738) is the only defence for the ten new plural keys.
- **L-4:** `IDENTICAL_TRANSLATION_ALLOWED` should need no additions, but this must be **run, not
  reasoned about**. `untranslatedInBoth()` flags a key only when BOTH pt-BR and es-ES are
  byte-identical to en-US. `dedup.merge.movesNotes` is identical between pt and es (`# nota`), which
  the function does not test.
- **L-5:** `placeholderDrift()` compares a sorted joined set. `dedup.merge.confirmBody` carries
  `{loser}` twice and `{survivor}` once in en-US. If the helper deduplicates, a translation with one
  `{loser}` slips through — check the helper's actual behaviour and add an occurrence-count
  assertion for `confirmBody` if it does.
- Existing contracts to copy in shape: `REQUIRED_BULK_KEYS` (line ~306) is the newest and the one
  whose namespace is entirely self-contained, exactly like `dedup`. Use the **total** form with no
  `*_EXTRA_KEYS` sibling. Per-contract assertions use `expect.soft` so every broken contract names
  itself in one run (45-05 recorded that a hard `expect` stops at the earliest-listed failure).
</key_facts>

<tasks>

<task type="auto">
  <name>Task 1: Add 77 dedup keys and 4 audit keys to all three locale files</name>
  <files>src/messages/en-US.json, src/messages/pt-BR.json, src/messages/es-ES.json</files>
  <read_first>
    - `.planning/phases/39-duplicate-detection-merge/39-UI-SPEC.md` section "New message keys" — the
      full three-locale table. This is the source; transcribe it.
    - `src/messages/en-US.json` — the existing `audit.entry.*` group, to place the four additions
      beside their siblings, and the existing top-level namespace ordering.
    - `src/messages/pt-BR.json` and `src/messages/es-ES.json` — for the vocabulary already in use
      (organization -> empresa/empresa, person -> pessoa/persona, deal -> negocio/oferta,
      Trash -> Lixeira/Papelera).
  </read_first>
  <action>
    Add one new top-level `dedup` namespace to each of the three locale files, holding the six groups
    from the UI-SPEC (`warning` 4, `reason` 4, `scan` 13, `review` 17, `merge` 27, `import` 3) plus
    the leaf `findDuplicates`, transcribed verbatim from the UI-SPEC's tables — with the
    `nameDomain` -> `nameIdentity` rename and re-wording from this plan's key_facts applied.

    Add the seventh group, `dedup.identity`, with these eight keys:

    | Key | en-US | pt-BR | es-ES |
    |---|---|---|---|
    | `title` | Organization identity fields | Campos de identificacao da empresa | Campos de identificacion de la empresa |
    | `help` | A create-time duplicate warning needs a field that identifies a company uniquely, such as a tax number. Choose up to two custom fields, checked in order. Until one is chosen, new organizations are not checked. | Um aviso de duplicado na criacao precisa de um campo que identifique a empresa de forma unica, como o CNPJ. Escolha ate dois campos personalizados, verificados nesta ordem. Ate escolher um, novas empresas nao sao verificadas. | Un aviso de duplicado al crear necesita un campo que identifique a la empresa de forma unica, como el NIF. Elige hasta dos campos personalizados, comprobados en ese orden. Hasta que elijas uno, las nuevas empresas no se comprueban. |
    | `primaryLabel` | First identity field | Primeiro campo de identificacao | Primer campo de identificacion |
    | `secondaryLabel` | Second identity field (optional) | Segundo campo de identificacao (opcional) | Segundo campo de identificacion (opcional) |
    | `none` | None | Nenhum | Ninguno |
    | `save` | Save identity fields | Salvar campos de identificacao | Guardar campos de identificacion |
    | `saved` | Identity fields saved. | Campos de identificacao salvos. | Campos de identificacion guardados. |
    | `saveFailed` | The identity fields weren't saved. Nothing was changed — try again. | Os campos de identificacao nao foram salvos. Nada foi alterado — tente novamente. | Los campos de identificacion no se guardaron. No se modifico nada — intentalo de nuevo. |

    **Restore every Portuguese and Spanish accent** when writing the JSON — the table above is
    written without them only because this plan file is plain ASCII. `identificacao` is
    `identificação`, `unica` is `única`, `ate` is `até`, `nao` is `não`, `identificacion` is
    `identificación`, `comprobados` is unchanged, `modifico` is `modificó`, `intentalo` is
    `inténtalo`. The same applies to every string transcribed from the UI-SPEC, which does carry its
    accents — copy those characters exactly as the UI-SPEC has them.

    Add the four `audit.entry.*` keys from the UI-SPEC's `audit.*` table:
    `merged.organization`, `merged.person`, `mergedNoFieldChanges`, `mergedChildren`. Place
    `merged` as a sibling of the existing `created` / `updated` / `deleted` predicate groups.
    `audit.entry.merged` has exactly TWO children — `organization` and `person` — because deals and
    activities are out of scope for deduplication.

    Keep the three files structurally identical: same key order, same nesting. Do not reformat
    unrelated regions of the files.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/messages/locale-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A node one-liner counting the leaves under `dedup` in each file returns exactly **77** for all
      three locales, and the three key sets are identical.
    - The leaves under `audit.entry.merged` number exactly 2 in each locale.
    - `grep -c "nameDomain" src/messages/*.json` = 0.
    - `grep -c "website domain\|dominio do site\|dominio del sitio" src/messages/*.json` = 0 — the
      superseded copy is gone, not merely unused.
    - Every one of the ten ICU plural keys parses: `dedup.warning.title`, `dedup.review.pairsFound`,
      `dedup.merge.conflictsLegend`, `dedup.merge.filledLegend`, `dedup.merge.identical`,
      `dedup.merge.movesDeals`, `dedup.merge.movesNotes`, `dedup.merge.movesPeople`,
      `dedup.import.flagged`, `audit.entry.mergedChildren` — each contains `{count, plural,` and both
      an `one {` and an `other {` arm in all three locales.
    - `npm run typecheck` = 0 errors; `npm run lint` = 0 errors.
  </acceptance_criteria>
  <done>All 81 new leaves exist in all three locales with identical structure, and the superseded domain copy is absent from every file.</done>
</task>

<task type="auto">
  <name>Task 2: The REQUIRED_DEDUP_KEYS exact-set contract and the extended gates</name>
  <files>src/messages/locale-parity.test.ts</files>
  <read_first>
    - `src/messages/locale-parity.test.ts` — read it in ONE pass, extracting: `REQUIRED_AUDIT_KEYS`
      (line ~73) and its current length; `REQUIRED_BULK_KEYS` (line ~306) and the exact-set
      assertion for it (lines ~686-696); the five helper functions `missingIn`, `blankIn`,
      `untranslatedInBoth`, `placeholderDrift`, `icuPluralReport` (lines ~567-640); the namespace
      constants (lines ~503-506); and `expectIdenticalKeySets` (line ~641). Note the five assertion
      blocks at lines ~651, ~659, ~709, ~719, ~730 that each need one added line.
    - `src/messages/en-US.json` (task 1).
  </read_first>
  <action>
    Add `export const REQUIRED_DEDUP_KEYS: string[]` listing all 77 `dedup.*` dot-paths, grouped with
    a comment per group carrying its count (`// Create-time warning — 4`, `// Match reasons — 4`,
    `// Scan — 13`, `// Review list — 17`, `// Merge screen — 27`, `// Import notice — 3`,
    `// Identity fields — 8`, `// Entry point — 1`). The per-group counts are load-bearing: they are
    how a reader sees at a glance that a group lost a key. Copy the doc-comment posture of
    `REQUIRED_BULK_KEYS`, including its statement that the namespace is entirely self-contained and
    therefore needs no `*_EXTRA_KEYS` sibling.

    Add `const DEDUP_NAMESPACE = "dedup"` beside the four existing namespace constants, and a
    `dedupKeys` scoped-key computation mirroring `bulkKeys`.

    Extend the four existing `merged`-relevant `REQUIRED_AUDIT_KEYS` entries: append
    `audit.entry.merged.organization`, `audit.entry.merged.person`,
    `audit.entry.mergedNoFieldChanges`, `audit.entry.mergedChildren` to the array, placed beside
    their sibling entries and covered by the group comment they join. **Read the array's current
    length first and assert the new length is exactly that plus 4** in the summary — do not carry a
    number from a planning document.

    Add one line to each of the five existing assertion blocks:
    `expect.soft(missingIn(REQUIRED_DEDUP_KEYS)).toEqual(emptyPerLocale)`,
    `expectIdenticalKeySets(dedupKeys, DEDUP_NAMESPACE)`,
    `expect.soft(blankIn(REQUIRED_DEDUP_KEYS)).toEqual(emptyPerLocale)`,
    `expect.soft(untranslatedInBoth(REQUIRED_DEDUP_KEYS)).toEqual([])`,
    `expect.soft(placeholderDrift(REQUIRED_DEDUP_KEYS)).toEqual({})`.

    Add the exact-set assertion in the `REQUIRED_BULK_KEYS` shape: sort a copy of
    `REQUIRED_DEDUP_KEYS`, and for each locale `expect.soft(dedupKeys[locale], "<message naming the
    locale>").toEqual(dedupContract)`. The message must name the locale file, so a failure identifies
    which file diverged.

    Extend the dedicated ICU-plural assertion to all ten new plural keys listed in task 1's
    acceptance criteria, following the `icuPluralReport("bulk.failures.retryHintPartial")` idiom at
    line ~744.

    **Read `placeholderDrift`'s implementation** and determine whether it deduplicates the
    placeholder set. If it does, add a separate assertion that `dedup.merge.confirmBody` contains
    `{loser}` exactly twice and `{survivor}` exactly once in all three locales. If it does not,
    record that finding in the summary and add no extra assertion.

    Do not add anything to `IDENTICAL_TRANSLATION_ALLOWED` unless the run demands it. If the run does
    demand it, add the specific key with a recorded reason, following the three-product-noun
    precedent — never a blanket exemption.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run src/messages/locale-parity.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` exits 0.
    - `REQUIRED_DEDUP_KEYS.length === 77`, asserted inside the test file itself so the count cannot
      drift silently.
    - Negative proof, RUN: add a throwaway `"zzz": "x"` leaf to `dedup` in `en-US.json` only, re-run,
      confirm the run reports BOTH the exact-set divergence for en-US AND the missing-key failure for
      pt-BR/es-ES in the SAME run (this is what `expect.soft` buys); remove the leaf.
    - Negative proof, RUN: delete one entry from `REQUIRED_DEDUP_KEYS`, re-run, confirm the exact-set
      assertion fails naming the surplus shipped key; restore it.
    - Negative proof, RUN: break one ICU plural arm in `pt-BR.json` (remove the `other {` arm),
      re-run, confirm the dedicated ICU assertion fails naming that key — proving `placeholderDrift`
      alone would not have caught it; restore it.
    - `grep -c "IDENTICAL_TRANSLATION_ALLOWED" src/messages/locale-parity.test.ts` is unchanged from
      its pre-plan value unless a run demanded an addition, in which case the added key carries a
      one-line recorded reason.
  </acceptance_criteria>
  <done>The dedup namespace is under a total exact-set contract in both directions, the four audit additions are covered by the pre-existing exact-set contract, and the ICU-plural blind spot is closed by a run negative proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translator/author -> rendered UI | catalog strings are interpolated into pages with user-supplied `{name}`, `{loser}`, `{survivor}` values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-17 | Information disclosure | `dedup.merge.confirmBody`, `dedup.merge.success`, `audit.entry.merged.*` | accept | These interpolate record NAMES the acting user is already authorized to see (the merge screen and the timeline both require reading both records first). next-intl escapes interpolated values as React text children; no key uses rich-text or HTML. |
| T-39-18 | Tampering | a missing or malformed key rendering a dot-path | mitigate | The exact-set contract fails in both directions, and the dedicated ICU assertion covers the structure `placeholderDrift` cannot see. Three negative proofs are RUN, not reasoned about. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no package. 39-RESEARCH section Package Legitimacy Audit: 55 OK, 1 SUS (`vitest`, incumbent, dismissed), 0 SLOP. No install checkpoint required. |
</threat_model>

<verification>
- `./node_modules/.bin/vitest run src/messages/locale-parity.test.ts` green.
- `npm run test` (both projects) green at plan end.
- `npm run typecheck` = 0 errors, `npm run lint` = 0 errors.
</verification>

<success_criteria>
- All 77 `dedup.*` and 4 `audit.*` keys ship in three locales in one commit.
- The contract fails when a key is added without a contract entry, and when a contract entry exists
  without a key — both proven by RUN negative proofs.
- The two deltas from the approved UI-SPEC catalog (the `nameIdentity` rename and the eight identity
  keys) are recorded in the summary with their reasons.
</success_criteria>

<output>
Create `.planning/phases/39-duplicate-detection-merge/39-04-SUMMARY.md` when done.
</output>
