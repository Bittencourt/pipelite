"use client"

// Stub -- will be implemented in Task 2
export function ConditionConfig({ nodeId, config }: { nodeId: string; config: Record<string, unknown> }) {
  return <div className="p-4">Condition config placeholder</div>
}
