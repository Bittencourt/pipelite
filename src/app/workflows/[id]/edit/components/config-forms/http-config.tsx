"use client"

// Stub -- will be implemented in Task 2
export function HttpConfig({ nodeId, config }: { nodeId: string; config: Record<string, unknown> }) {
  return <div className="p-4">HTTP config placeholder</div>
}
