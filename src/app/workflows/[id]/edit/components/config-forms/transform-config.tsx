"use client"

// Stub -- will be implemented in Task 3
export function TransformConfig({ nodeId, config }: { nodeId: string; config: Record<string, unknown> }) {
  return <div className="p-4">Transform config placeholder</div>
}
