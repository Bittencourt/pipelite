"use client"

// Stub -- will be implemented in Task 2
export function CrmConfig({ nodeId, config }: { nodeId: string; config: Record<string, unknown> }) {
  return <div className="p-4">CRM config placeholder</div>
}
