"use client"

// Stub component - will be replaced with full implementation in Plan 03
export function PersonDialog(props: {
  open: boolean
  onOpenChange: (open: boolean) => void
  person?: unknown | null
  organizations: { id: string; name: string }[]
  onSuccess: () => void
}) {
  return null
}
